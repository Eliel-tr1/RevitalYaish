# HANDOFF · שלב 2 — מודל הנתונים (שדות)

> **עודכן 03/08/2026 — סקירת 26 שיחות הפרויקט.**

| # | מה שונה | מקור |
|---|---|---|
| 1 | באנר הקדימות עודכן: המסמך עדיין גובר על התיעוד הקודם, אך **סקירת 03/08/2026 גוברת עליו** בכל סתירה | סקירת 26 שיחות |
| 2 | ארבע קריאות ה"מסמך זה מיושן" תוקנו במקור — מסומן ליד כל אחת מה שכבר הוחל | chat_02, chat_08 |
| 3 | ארבעת השדות שנוצרו ב-26/07 **אומתו כחיים** בחשבון | סקירת 03/08/2026 |
| 4 | נוספה טבלת מפות שדות ל-7 אובייקטים שאומתו מול ה-API החי | chat_07 |
| 5 | נוספה רשימת שדות ירושה להסתרה באובייקט `1` לקוח | chat_07 |
| 6 | נוספו שתי הרצועות העליונות המוצמדות (תהליך מכירה, תהליך ליווי) | chat_07 |
| 7 | נוספו שינויי 27/07 באובייקט `1059`: ערך picklist `5` + `pcfScheduledSendAt` | chat_13 |
| 8 | נוסף סייג קדימות: המסמך **אינו מכסה** את `1060`; הסכימה החיה גוברת | chat_14 |
| 9 | נוסף `pcfwhatspgrouprelated` על `1007` (lookup לקבוצת WhatsApp) | chat_19 |
| 10 | נוספה סתירת `pcfGroupid` מול `pcfGreenApiId` ב-`1068` — ⚠️ סתירה פתוחה | chat_20, chat_22 |
| 11 | נוסף סדר העבודה המחייב לשכבת השדות (7 תחנות) | chat_25 |
| 12 | נוספה נקודת ההתחלה: `1` לקוחות — 131 שדות / 2 צהובים / 44 ב"בדיקה" | chat_25 |
| 13 | נוספה מתודולוגיית 4 סוגי הממצא + מקרא חוברת v2 | chat_25 |
| 14 | נוספו תוצאות אודיט 26/07: אפס שדות חובה חסרים, 4 שדות שיקוף חסרים | chat_22 |
| 15 | נוספה טבלת ספירות אודיט מול המיפוי (8 אובייקטים) | chat_22 |
| 16 | נוספו 11 שדות החובה של `1051` + סיכון `ownerid` בכשל יצירה | chat_22 |
| 17 | פריט 2 (מחיקת `pcfsystemfield102`) — **עדיין ממתין** לאישור פר-שדה מאליאל; נוסף כלל D5 | chat_18 |
| 18 | פריט 1 (קיבולת קבוצות בוסט) — **עדיין חסום** אצל רויטל | סקירת 03/08/2026 |
| 19 | נוספה ⚠️ סתירה פתוחה: 22 מול 74 מחזורי בוסט (`1007`) — בעלים מייק | chat_03, chat_08 |
| 20 | נוספו 8 זוגות השדות הכפולים; `1068` ו-`1059` סומנו 🔴 | chat_22 |
| 21 | סעיף 3ד (היפוך `pcfCreatedByPersonOrSystem`) אושר ואומת מול 6 אובייקטים | chat_22 |

---

## ‼️ קדימות המסמך — עודכן 03/08/2026

> ⚠️ **מסמך זה גובר על כל תיעוד קודם בפרויקט בכל מקרה של סתירה — למעט סקירת 03/08/2026.**
> **סדר הקדימות התקף:** סקירת 03/08/2026 (26 שיחות) ← **מסמך זה** ← 00–09 ← כל השאר.
> כלומר: היכן שסקירת 03/08/2026 סותרת את המסמך הזה — **הסקירה גוברת**.

**מצב ההערות המקוריות של המסמך (נכון ל-03/08/2026):**

| הערה מקורית | מצב היום |
|---|---|
| `03_LIVE_FIREBERRY.md` — ספירות שדות מיושנות (`6` = 41, `1069` = 21) | ✅ **הוחל במקור.** אין עוד צורך לקרוא את ההערה מכאן. שים לב: `03` עודכן מאז גם בספירות נוספות — ראה טבלת האודיט בפרק 3ה |
| `06_N8N_WORKFLOW_SPECS.md` — ביטול ערך `4` ב-`pcfSignatureStatus` | ✅ **הוחל במקור.** המפרט מצטט כיום `1`=ממתין לחתימה |
| `02_TRANSCRIPT_DECISIONS.md` — אותה הערה לגבי ערך `4` | ✅ **הוחל במקור** |
| `05_GAP_CHECKLIST.md` — "חסר הערות מנטורית + קישור הזמנה" | ✅ **הוחל במקור** — השורה נסגרה |
| — | ⚠️ **חדש:** המסמך **אינו מכסה את אובייקט `1060` (קטלוג תפריט)**. הסכימה שאומתה מהחשבון החי (`fields related (35).csv` + צילומי מסך, chat_14) **גוברת** על כל הגדרה אחרת של `1060`, כולל מסמך זה |

**תאריך:** 26/07/2026 · **חשבון:** רויטל יעיש (Fireberry, חי) · **ביצוע:** ידני ב-UI
**מקור:** ClickUp `86cawzg0p` · תתי-משימות 2.4–2.7
**סטטוס בסוף הסשן:** 2.4 ✅ סגורה · 2.5 🟡 סכמה מוכנה, נתונים ריקים · 2.6 נסגרה בהחלטת מייק · 2.7 פתוחה (דרישה מתמשכת)

---

## 1. מה בוצע בפועל — change-log

| אובייקט | system_name | פעולה | תוצאה |
|---|---|---|---|
| `6` פגישה | `pcfArrivalConfirmation` | יצירת picklist "סטטוס אישור הגעה" — `1` אישרה · `2` לא אישרה · `3` לא ענתה | ✅ נוצר, אומת (34→35 שדות), על המסך |
| `1051` תהליך ליווי | `pcfContinuationStatus` | יצירת picklist "סטטוס המשך" — `1` בוצעה שיחה · `2` פולו-אפ · `3` בחרה לא להמשיך · `4` בחרה להמשיך | ✅ נוצר, על המסך |
| `1069` טופס שקילה | `pcfMentorNotes` | יצירת שדה טקסט ארוך "הערות מנטורית" | ✅ נוצר, על המסך |
| `1069` טופס שקילה | `pcfcrmorderid` | יצירת lookup → `13` הזמנה, תווית "הזמנה - מקושר" | ✅ נוצר, אומת (16→18 שדות), על המסך |
| `4` תהליך מכירה | `pcfSignatureStatus` | **שינוי תווית** ערך `1`: "נשלח" ← "ממתין לחתימה" | ✅ בוצע. **לא נוסף ערך `4`** |

> ✅ **אימות 03/08/2026:** ארבעת השדות שנוצרו (`pcfArrivalConfirmation`, `pcfContinuationStatus`, `pcfMentorNotes`, `pcfcrmorderid`) **מאושרים כחיים בחשבון**. אין רגרסיה, אין כפילות שנוצרה בעקבותיהם.

**לאף שדה לא הוגדרה ברירת מחדל.** כולם אופציונליים, לא חובה, לא לקריאה בלבד.

### 1א. שינויים שנוספו אחרי כתיבת המסמך (27/07/2026)

| אובייקט | פריט | פעולה | מקור |
|---|---|---|---|
| `1059` הקצאת תפריט | `pcfMenuStatus` | נוסף ערך picklist **`5` = "מוכן לשליחה"** | chat_13 |
| `1059` הקצאת תפריט | `pcfScheduledSendAt` | נוצר שדה חדש מסוג **dateTime** | chat_13 |
| `1007` מחזור בוסט | `pcfwhatspgrouprelated` | נוסף שדה "קבוצת בוסט WhatsApp - מקושר" (lookup) — ככל הנראה זהו ה-lookup שמייק טען שקיים ב-chat_15 | chat_19 |

### מיקום על המסכים (לייאאוט)
| אובייקט | מקטע | מיקום |
|---|---|---|
| `6` | פרטי פעילות | מתחת ל"סטטוס פגישה" |
| `1051` | שימור ובקרה | סוף המקטע |
| `1069` | פרטי שקילה | "הערות מנטורית" מתחת ל"הרגשה כללית"; "הזמנה - מקושר" בסוף |

### 1ב. רצועות עליונות מוצמדות — מפרט לייאאוט מחייב (chat_07)

| אובייקט | רצועה עליונה מוצמדת |
|---|---|
| `4` תהליך מכירה | `statuscode` · `pcfdayspast` · `pcfFollowUpTime` · `ownerid` — **בלי** `pcfLeadLevel` (בוטל) |
| `1051` תהליך ליווי | `pcfStatus` · `pcfCurrentProgram` · `pcfCurrentWeek` · `pcfContinuationStatus` · `ownerid` |

---

## 2. החלטות ארכיטקטוניות שהתקבלו

**א. `pcfSignatureStatus` — רילייבל במקום ערך רביעי.**
האפיון ביקש ערך "ממתין לחתימה". הערך הקיים `1`=נשלח מתאר **בדיוק** את אותו מצב (מסמך יצא, טרם נחתם). הוספת ערך `4` הייתה יוצרת שני ערכים למצב אחד, ומחייבת כל תרחיש Make לבדוק "1 או 4".
**ההכרעה:** שינוי תווית של `1`. אפס שינוי ב-Make (ממשיך לכתוב `1`), אפס דאטה היסטורית שנשברת, פעולה הפיכה.
**ערכים נוכחיים:** `1`=ממתין לחתימה · `2`=נחתם · `3`=בוטל.

**ב. `statuscode=7` על אובייקט 6 — נשאר, לא נכתב אליו.**
ב-`statuscode` (סטטוס פגישה) כבר קיימים `6`=לא הופיע/ה ו-`7`=אישר/ה הגעה. זה מערבב אישור מראש עם הגעה בפועל — בדיוק מה שהאפיון מזהיר ממנו.
**ההכרעה:** הערך נשאר (יש בו דאטה היסטורית), אבל **אף אוטומציה לא כותבת אליו יותר.** כל כתיבה של אישור הגעה עוברת ל-`pcfArrivalConfirmation` בלבד.

**ג. הפרדת הצירים — אישור הגעה מול צפייה בזום.**
הובהר על ידי מייק: "סטטוס אישור הגעה" מתייחס ל**פגישות עם רויטל**, לא לזום. הסטטוס הרטרואקטיבי של הזום כבר קיים בנפרד — `pcfWhatchedZoom` על `1051` (`1`=ראתה · `2`=ראתה הקלטה · `3`=לא ראתה). שני הצירים מופרדים נכון.

**ד. `pcfcrmorderid` על 1069 — שם זהה למקבילו על 1051.**
נבחר במכוון לעקביות מיפוי ב-Make. שמות שדה הם פר-אובייקט, אין התנגשות.

**ה. סדר עבודה מחייב לשכבת השדות (נקבע chat_25).**
`1` לקוחות → `4` תהליכי מכירה → `1051` תהליכי ליווי → `14` מוצרים → `1007` מחזור → הפיננסי (`13` / `1039` / `1018` / `1035`) → התפעולי.
לא מדלגים ולא מקדימים אובייקט. כל אובייקט נסגר לפני שעוברים לבא אחריו.

**ו. מתודולוגיה קבועה פר-אובייקט (chat_25).**
קלוד מביא לכל אובייקט **4 סוגי ממצא**:
1. שדות צהובים — קיימים בחשבון וללא החלטת אפיון
2. כפילויות ושדות מתים
3. lookups מפוקפקים
4. ערכי picklist שלא מתיישבים עם המודל

מייק מכריע → קלוד מייצר **רשימת PUT-ים + change-log בעברית לאליאל**. אין ביצוע לפני הכרעה.

**ז. מקרא חוברת האפיון v2 (chat_25).**
- **שורה צהובה** = קיים בחשבון ולא אופיין מעולם
- **שורה אפורה נטויה** = שיקוף או שדה ליצירה, לא פיזי
- **שורה רגילה** = החלטת האפיון המקורית נשמרה במלואה, כולל עמודה A

---

## 3. ממצאים שסותרים את התיעוד הקיים

**א. אין 4 שדות כפולים ב-`1059` (הקצאת תפריט).**
נבדקו כל 23 השדות — כל שם מערכת וכל תווית ייחודיים. ההפרש מול התיעוד הישן (26) הוא שלושה שדות מערכת שהייצוא לא מציג (`customobject1059id`, `deletedon`, `deletedby`).
זוגות שנראים כפולים אך אינם: `pcfDietitianPaid` (סטטוס) מול `pcfDietitianPaidDate` (תאריך) — שניהם נחוצים.
**הטענה ב-ClickUp שגויה.** אם מישהו זוכר שדות ספציפיים — שיציין בשמם.
⚠️ **עדכון 03/08/2026:** האודיט הרוחבי (chat_22) כן מצא ב-`1059` זוג כפול אחר — `pcfSentDate` מול `pcfMenuMsgSent`. ראה פרק 3ו.

**ב. `pcfsystemfield102` — כפילות בת שבוע, בלי דאטה.**
רצף הזמנים: `pcfsystemfield102` נוצר 20/07 16:38, עודכן 17:01 (סומן "לא בשימוש"); `pcfCurrentProgram` נוצר 17:02 — דקה אחריו. מישהו יצר, הבין שהשם שגוי, יצר מחדש.
**אובייקט `1051` מכיל רשומה אחת בלבד** ("הליווי של מיכאל אוקס" — רשומת בדיקה). אין דאטה בסיכון.
⚠️ `pcfsystemfield100` (מרחק מהיעד) ו-`101` (נוסחת RMR) הם שדות **תקינים** — לא לגעת.
🔒 **סטטוס 03/08/2026: המחיקה טרם בוצעה ועדיין ממתינה לאישור פר-שדה מאליאל.** בנוסף נאכף כלל פרויקט **D5** (chat_18): **מחיקות ע"י סוכן אוטונומי אסורות לחלוטין** — גם כשהמשימה מבקשת ניקוי. מחיקה ידנית בלבד, אחרי אישור פר-שדה.

**ג. `pcfAvailableSlots` הוא שדה נוסחה.**
לא היה בתיעוד. הוא מחשב את עצמו מ-`pcfCapacity` ו-`pcfCurrentCount` — כלומר תלוי בהם, ואם `pcfCurrentCount` לא מתוחזק, גם הוא שגוי.

**ד. הפוך ב-1069:** `pcfCreatedByPersonOrSystem` על `1069` הוא `1`=מערכת · `2`=אנושי, בעוד על `6` ו-`1051` זה `1`=אנושי · `2`=מערכת. **מלכודת לכל תרחיש Make שכותב לשני האובייקטים.** לא תוקן (שינוי דאטה, לא סכמה).
✅ **אומת ב-03/08/2026 והורחב (chat_22):** ההיפוך תקף ל-`1069` **בלבד**. ב-`1051`, `1007`, `14`, `6`, `1059`, `1068` — `1`=אנושי / `2`=מערכת.
⚠️ **פרקטית:** על `1069` יש לכתוב **`1`** כדי לסמן "מערכת". כתיבת `2` תסמן כל שקילה אוטומטית כידנית.

**ה. תוצאות אודיט השדות הרוחבי (26/07/2026, chat_22) — התוצאה המחייבת.**
- **אפס שדות חובה חסרים ב-10 האובייקטים שנבדקו.**
- **4 שדות חסרים בסך הכול, כולם שדות שיקוף לא-חובה** שסומנו "בדיקה": `pcfEventId` על רישום שיווקי (`1002`) + 3 שיקופים מ-Opportunity על הרשמה לאירוע (`1030`).

**ספירות האודיט מול המיפוי:**

| אובייקט | במיפוי | בחשבון | פער |
|---|---|---|---|
| `1` לקוח | 129 | 131 | +2 |
| `4` תהליך מכירה | 123 | 130 | +7 |
| `14` מוצר | 35 | 37 | +2 |
| `1051` תהליך ליווי | 31 | 57 | +26 |
| `1039` תשלום | 42 | 42 | 0 |
| `1018` תיעוד תשלום | 31 | 31 | 0 |
| `1035` הוראת קבע | 29 | 29 | 0 |
| `1032` אירוע | 34 | 34 | 0 |

**ו. 8 זוגות שדות כפולים ברמת החשבון (chat_22).**

| אובייקט | הזוג | חומרה |
|---|---|---|
| `1068` | `pcfGroupid` / `pcfGreenApiId` | 🔴 — לאיזה מהם GreenAPI מתחבר בפועל |
| `1068` | `pcfCurrentCount` / `pcfGroupNumber` | 🟡 |
| `1051` | `pcfRMR` / `pcfsystemfield101` | 🟡 |
| `1051` | `pcfLastWeighDate` / `pcfWeighingDate` | 🟡 |
| `1051` | `pcfCurrentWeight` / `pcfWeeklyWeight` | 🟡 |
| `1051` | `pcfCurrentProgram` / `pcfsystemfield102` | 🟡 — ראה פרק 3ב |
| `1059` | `pcfSentDate` / `pcfMenuMsgSent` | 🔴 — דגל "נשלח" מתפצל ⇒ סכנת כפל שליחה |
| `1007` | `pcfRegistration`/`pcfAvailability`/`pcfQuota` מול `pcfBoostCount`/`pcfOpertuintyCount` | 🟡 |

**ז. ⚠️ סתירה פתוחה — `pcfGroupid` מול `pcfGreenApiId` ב-`1068`.**
- **עמדה א' (chat_20):** `pcfGreenApiId` הוא הקנוני — אישור מייק ("הראשון").
- **עמדה ב' (chat_22):** מייק אמר "לא יודע — צריך לבדוק"; מפרט **WF-03 מפנה ל-`pcfGroupid`**.
**בעלים:** מייק. **בדיקת הכרעה (5 דקות):** לפתוח 2–3 מתוך 8 רשומות `1068` ולראות באיזה שדה יושב הפורמט `1203630xxxxx@g.us`.
בנוסף — קיימת סתירה נלווית סביב קיום שדה lookup `1007`→`1068`. **דרך האימות:** `GET /metadata/records/1007/fields`. הערה: `pcfwhatspgrouprelated` שנוסף לאחרונה (chat_19) הוא ככל הנראה אותו lookup.

**ח. ⚠️ סתירה פתוחה — ספירת מחזורי הבוסט (`1007`).**
- **עמדה א' — 22 מחזורים.** מקור: ספירת מלאי chat_08 (27–28/07/2026) + chat_16 + chat_25 (36 שדות / 22 רשומות).
- **עמדה ב' — 74 מחזורים.** מקור: אימות ישיר מול ה-API החי, chat_03 (**02/08/2026**), עם הקביעה שכולם מסתיימים ביום ראשון.
שתי הספירות רצו מול **אותו חשבון** בהפרש של שבוע.
**בעלים: מייק.** **חובה:** ריצת ספירה אחת מכריעה (`GET` על אובייקט `1007`) **לפני** שמצטטים מספר במסמך כלשהו או בונים עליו אוטומציה. עד אז — אין להסתמך על אף אחד משני המספרים.

---

## 4. מצב 2.5 — קבוצות WhatsApp (`1068`)

**הסכמה מלאה ומאומתת:** `pcfCapacity` (Number) · `pcfCurrentCount` (Number) · `pcfAvailableSlots` (Number, **נוסחה**) · `pcfGroupNumber` (Number) · `pcfGroupPhoto` (Url). `pcfActualGroup` על `1051` מצביע נכון ל-1068.

**הנתונים לא מוכנים:**
- 8 קבוצות קיימות ("להתאהב בעצמך מחדש 1–8"), כולן בסטטוס פעילה
- **קיבולת ריקה ב-6 מתוך 8.** לשתיים יש 999 ו-99 — מספרי בדיקה
- **מספר לקוחות רשומים = 0** בכל הקבוצות למעט אחת
- **מדריכת הקבוצה = "ויטרו ראשי" בכולן** — אין שיוך מנטורית אמיתי, בניגוד ליחס החד-חד-ערכי שבאפיון

**מסקנה:** הסכמה לא החוסם. אוטומציית "קבוצה מלאה" ושיוך אוטומטי לפי מקומות פנויים ירוצו על אפסים.

🔒 **סטטוס 03/08/2026: מספרי הקיבולת עדיין חסרים.** רויטל טרם מסרה את המספר "כמה לקוחות בקבוצה = מלא". ללא זה `pcfCapacity` נשאר ריק/מזויף, `pcfAvailableSlots` (נוסחה) מחזיר ערך חסר משמעות, ואוטומציית התפוסה לא ניתנת להפעלה.
⚠️ ראה גם פרק 3ז — הכפילות `pcfGroupid`/`pcfGreenApiId` וזוג `pcfCurrentCount`/`pcfGroupNumber` חייבים הכרעה לפני שמחברים את GreenAPI לאובייקט הזה.

---

## 4א. מפות שדות מאומתות מול ה-API החי — 7 אובייקטים (chat_07)

אומת מול החשבון החי, כולל חלוקה למקטעים ורצועה עליונה מוצמדת לכל אובייקט:

| # | אובייקט | קוד | מצב המפה |
|---|---|---|---|
| 1 | תהליך מכירה | `4` | ✅ מפה מלאה + רצועה עליונה (ראה 1ב) |
| 2 | תהליך ליווי | `1051` | ✅ מפה מלאה + רצועה עליונה (ראה 1ב) |
| 3 | מחזור בוסט | `1007` | ✅ מפה מלאה (36 שדות) |
| 4 | הקצאת תפריט | `1059` | ✅ מפה מלאה (23–26 שדות, ראה 3א) |
| 5 | קטלוג תפריט | `1060` | ⚠️ הסכימה החיה (chat_14) גוברת על מסמך זה |
| 6 | קבוצת WhatsApp | `1068` | ✅ מפה מלאה (21 שדות) |
| 7 | לקוח | `1` | ✅ מפה מלאה (131 שדות) — ראה 4ב |

### 4ב. אובייקט `1` לקוח — נקודת ההתחלה של שכבת השדות (chat_25)

- **131 שדות** בחשבון
- **2 שדות צהובים** — קיימים ולא אופיינו מעולם
- **44 שדות בסטטוס "בדיקה"** — דורשים הכרעה

**שדות ירושה להסתרה (עדיפות גבוהה):**
`accountratingcode` · `industrycode` · `lostreason` · **כל שדות ה-UTM** · `pcfQuestionNumber*` / `pcfQuestionSelect*` הגנריים.
הערכת קלוד: *"הניקוי כאן שווה יותר מבכל אובייקט אחר."*
⚠️ **פתוח:** רשימת "מותר להסתיר" **פר-שדה** לא הופקה — קלוד הציע, **מייק לא אישר**. ראה פריט 7 בטבלת הפתוחים.

### 4ג. אובייקט `1051` — 11 שדות חובה, סיכון תפעולי (chat_22)

מתוך 11 שדות החובה, **6 שהאוטומציה חייבת לספק בזמן היצירה**:
`name` · `pcfStatus` · `pcfaccountid` · `pcfProduct` · `ownerid` · `pcfStartDate`

⚠️ **סיכון:** `ownerid` (מדריכה) הוא **שדה חובה**, אבל המנטורית נבחרת ידנית ע"י נציג המכירות ⇒ **נציג ששכח לבחור → יצירת תהליך הליווי תיכשל.**
`pcfEstimatedEndDate` — **אינו** חובה.

---

## 5. פתוח — מי חייב מה

| # | פריט | אצל מי | חוסם | סטטוס 03/08/2026 |
|---|---|---|---|---|
| 1 | כמה לקוחות בקבוצה = מלא? (הערך ל-`pcfCapacity`) | **רויטל** | אוטומציית תפוסה 90%, שיוך אוטומטי | 🔴 **עדיין פתוח** — המספרים לא נמסרו |
| 2 | אישור מחיקת `pcfsystemfield102` | **אליאל** | ניקוי בלבד, לא חוסם | 🔴 **עדיין פתוח** — ממתין לאישור פר-שדה |
| 3 | שיוך מנטורית אמיתית ל-8 הקבוצות | רויטל / עודד | שיוך אוטומטי | פתוח |
| 4 | אוטומציה שמתחזקת את `pcfCurrentCount` | שלב 6 | כל מנגנון התפוסה | פתוח, תלוי בפריט 1 |
| 5 | היכן חי ה-change-log המצטבר | **מייק** | תיעוד לאליאל | פתוח |
| 6 | 2.6 סעיפים C3+C4 — נסגרו בהחלטה, לא בוצעו | — | לא קריטי | סגור בהחלטה |
| 7 | רשימת "מותר להסתיר" פר-שדה לאובייקט `1` | **מייק** | ניקוי אובייקט לקוח | 🆕 נוסף 03/08/2026 |
| 8 | ⚠️ **סתירה פתוחה** — 22 מול 74 מחזורי בוסט (`1007`) | **מייק** | כל ציטוט מספרי ואוטומציה על `1007` | 🆕 ראה פרק 3ח |
| 9 | ⚠️ **סתירה פתוחה** — `pcfGroupid` מול `pcfGreenApiId` (`1068`) | **מייק** | WF-03, חיבור GreenAPI | 🆕 ראה פרק 3ז |

**נוסח לאליאל בפריט 2:**
> `pcfsystemfield102` על תהליך ליווי — כפילות של `pcfCurrentProgram`, נוצרה בטעות ב-20/07 והוחלפה דקה אחריה. אובייקט תהליכי ליווי מכיל רשומת בדיקה אחת בלבד, אין דאטה בסיכון. מבקש אישור למחיקה.

---

## 6. גוצ'ות תפעוליות שנלמדו היום

### Fireberry UI
- **החיפוש ב-Object Studio לא אמין.** חיפוש `pcfSignatureStatus` ו-`Signature` שניהם החזירו "לא נמצאו רשומות" — למרות שהשדה קיים. **הדרך שעובדת:** ניקוי החיפוש → מיון לפי "שם מערכת" → גלילה ידנית.
- **נתיב עריכת לייאאוט:** מתוך **רשומה** (לא מ-Object Studio) ← ⋮ ← **עריכת עמוד** ← לחיצה על כותרת המקטע ← "+ הוספת שדה" בפאנל שמאל ← **שמור** (כפתור ירוק למעלה).
- **בהוספת lookup לעמוד:** לחיצה על החץ `‹` נכנסת לשדות של האובייקט המקושר (לשליפת מידע), לא מוסיפה את השדה. השדה עצמו נוסף בלחיצה על השורה.
- **"תיבת טקסט"** (אייקון כתום) = טקסט ארוך / TextArea. **"טקסט"** (אייקון כחול Aa) = שורה אחת. השמות מטעים.
- **יצירת lookup ב-UI בטוחה יותר מ-API:** בוחרים את האובייקט כסוג השדה, אין `relatedObjectType` לנחש.
- שם המערכת נשמר בדיוק כפי שהוקלד — פיירברי לא הוסיף תחילית `pcf` אוטומטית.
- ייצוא CSV של שדות אובייקט לא כולל `<obj>id`, `deletedon`, `deletedby` — הפרש קבוע של 3 מול ספירת התיעוד.

### Claude Code
- **הרצת הפרומפט נכשלה ולא הפיקה כלום.** שגיאת "Remote Control disconnected" — הסשן בטרמינל הפסיק להגיב.
- נמצאו **13 תהליכי `claude.exe` יתומים** בלי חלון. נוקו ב-`taskkill /F /IM claude.exe`.
- **תיקיית העבודה:** `C:\Users\b2b\revital-yaish` (Windows). קיימים בה `CLAUDE.md`, `README.md`, `context\`, `docs\`.
- **אף אחד מקבצי הפלט לא נוצר** — לא `2.4_creation_log.md`, לא `2.5_capacity_verification.md`, לא `2.6_dry_run_report.md`.
- ⚠️ **עבודה משני מחשבים במקביל** (`docs\second-computer-setup.md`). קבצי פלט נכתבים מקומית → צריך תיקייה משותפת לפני ההרצה הבאה, אחרת לא יודעים מה רץ ואיפה.

---

## 7. מה הלאה

**מיידי:** לסגור את פריטים 1–2 מטבלת הפתוחים (רויטל, אליאל) — **שניהם עדיין פתוחים נכון ל-03/08/2026.**
**מיידי (נוסף):** להכריע את שתי הסתירות הפתוחות — ספירת `1007` (פריט 8) ו-`pcfGroupid`/`pcfGreenApiId` (פריט 9). שתיהן בבעלות מייק, שתיהן בדיקה של דקות.
**התחנה הבאה בשכבת השדות:** אובייקט `1` לקוחות — 131 שדות, 2 צהובים, 44 ב"בדיקה" (פרק 4ב), לפי סדר העבודה בפרק 2ה.
**שלב 6:** אוטומציית תחזוקת `pcfCurrentCount` + התראת 90% תפוסה. **תלוי בפריט 1.**
**לפני הרצת Claude Code הבאה:** להחליט על תיקיית פלט משותפת, ולהריץ עם המחשב מחובר לחשמל ובלי שינה אוטומטית.
**נדחה לסוף הפרויקט:** 2.6 סעיף C4 — סקירת קבוצת "לא בשימוש" על פני כל האובייקטים.

---

## 8. הפרומפט ל-Claude Code — עדיין תקף

הקובץ `PROMPT_CLAUDE_CODE_שלב2_שדות.md` נשאר רלוונטי **חלקית בלבד**: משימה A (יצירה) בוצעה ידנית, ומשימות B ו-C בוצעו/נסגרו. אם מריצים אותו שוב — הוא ידלג על מה שקיים ולא ייצור כפילויות, אבל אין בו עוד ערך אמיתי לשלב 2. **הערך שלו הוא כתבנית לשלבים הבאים:** מבנה שערי האישור, איסור המחיקה, ונוהל היצירה פר-שדה עם קריאה חוזרת.
⚠️ **תוספת 03/08/2026:** כל תבנית עתידית חייבת לכלול את כלל **D5** — סוכן אוטונומי **לעולם לא מוחק** שדה, ערך picklist או רשומה. הפקת רשימת PUT-ים + change-log לאליאל בלבד; הביצוע ידני.
