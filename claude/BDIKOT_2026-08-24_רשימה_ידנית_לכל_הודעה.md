# רשימת בדיקות ידנית · הודעה אחר הודעה

**עדכון 24.08.2026** · 16 הודעות · מצב בדיקה עדיין דלוק, רק `0533435240` מקבל

## שרשרת הטריגרים של הודעות הוולקאם

```
תשלום נכנס
  ↓
WF-20 · ראוטר תשלומים (אשראי GROW)   ·   WF-27 · תשלום שאינו אשראי
  ↓                                        ↓
        WF-22 · ראוטר תהליכים מקצועיים
  ↓
WF-23 בוסט   ·   WF-24 ליווי   ·   WF-25 תוכנית המשך
```

**⚠️ שינוי סטטוס של תהליך מכירה אינו טריגר.** רק קליטת תשלום מפעילה את השרשרת. זה מתקן את מה שנכתב בגרסה הקודמת של המסמך.

## איך מריצים תהליך מתוזמן ידנית

לפתוח את התהליך ב-n8n, ללחוץ על הנוד `⚙️ הגדרות`, לשנות `STAGE_OVERRIDE` ו-`DATE_OVERRIDE`, לסגור, וללחוץ **Test workflow**. הנוד האחרון הוא דוח ריצה: כמה נשלחו, כמה נחסמו ולמה.

## A · רכישה ואונבורדינג

### `welcome_boost_v2` · ברוכה הבאה לבוסט — WF-20/27 ← WF-22 ← WF-23
**הפעלה:** תשלום אמיתי דרך GROW על מוצר סדנת בוסט (`grow-payment-roital`), או טופס התשלום הידני (`manual-payment-roital`). **אין קיצור דרך דרך פיירברי.**
**בוואטסאפ:** שם פרטי נכון · תאריך תחילת המחזור מופיע ולא `__` · קישור הקבוצה נכון · קובץ המדריך מצורף פעם אחת בלבד.
**בפיירברי:** נוצר תהליך ליווי ומקושר למחזור.

### `welcome_livuy` · ברוכה הבאה לליווי — WF-20/27 ← WF-22 ← WF-24
**הפעלה:** אותה שרשרת. קליטת תשלום על מוצר תוכנית ליווי 12 שבועות.
**בוואטסאפ:** שם פרטי · קישור קבוצה · **שם המנטורית מופיע ולא ריק** (תקלה ידועה מהעבר) · הכפתור פותח את דף האונבורדינג.

### `welcome_continue` · תוכנית המשך — 🔴 אין תהליך ששולח

## B · הבוסט והוובינר

### `boost_webinar_invite_v2` · הזמנה, שלישי — WF-30
**הפעלה:** `STAGE_OVERRIDE=invite`, `DATE_OVERRIDE=` תאריך התחלת המחזור +2.
**בוואטסאפ:** כתוב **מחר בערב** ולא הערב (תיקון שבוצע) · שם פרטי נכון · אין קישור בגוף, הוא בקבוצה.
**בדוח:** נשלחו = מספר המשתתפות · הרצה שנייה מציגה נחסמו ככפילות.

### `boost_webinar_morning` · תזכורת בוקר — WF-30
**הפעלה:** `STAGE_OVERRIDE=morning`, `DATE_OVERRIDE=` התחלה +3.
**בוואטסאפ:** 🎯 **כתוב "הערב ב-20:00" ולא שעה ריקה.** אם ריק, השדה `webinar_time` לא נכתב.

### `boost_webinar_live` · מתחילים עכשיו — WF-30
**הפעלה:** `STAGE_OVERRIDE=live`, `DATE_OVERRIDE=` התחלה +3.

### `boost_webinar_followup` · פולואפ — WF-30 ואז WF-31
**הפעלה:** `STAGE_OVERRIDE=followup`, `DATE_OVERRIDE=` התחלה +4. **ואז ללחוץ על הכפתור בוואטסאפ.**
**בפיירברי אחרי הלחיצה:** 🎯 נפתחת משימה "לתאם פגישה עם רויטל" לביצוע היום · משויכת לנציג של תהליך המכירה ולא לויטרו ראשי · מקושרת ללקוחה ולתהליך המכירה · לחיצה שנייה לא פותחת משימה נוספת.

### `boost_final_form` · שאלון סיום בוסט — 🔴 אין תהליך ששולח

## C · פגישות מכירה

### `meeting_invite` · זימון — WF-32
**הפעלה:** לפתוח פגישה בעוד 3 ימים. חובה: תאריך ושעה, לקוחה מקושרת, אחראי, סטטוס עתידה להתקיים.
**🔴 כרגע לא נמסר.** מטא חוסמת בגלל פרמטר כפתור בפלואו `Client_Notification_new_meeting`. לתקן קודם.
**בפיירברי:** מתמלא `הודעת זום נשלחה` · הרצה שנייה לא שולחת שוב.

### `meeting_reminder_day_v2` · יום לפני — WF-32 ואז WF-33
**הפעלה:** להעביר את הפגישה למחר, `HOUR_DAY=` השעה הנוכחית, Test workflow. **ואז ללחוץ על הכפתורים.**
**בוואטסאפ:** "מחר ב-שעה" עם השעה הנכונה · שני כפתורים.
**בפיירברי:** מתמלא `תזכורת יום לפני` · 🎯 **אני אגיע!** → אישור הגעה = אישרה, סטטוס = אישרה הגעה · 🎯 **צריכה להזיז** → אישור הגעה = לא אישרה, **הסטטוס חוזר ל-עתידה להתקיים**, נפתחת משימת תיאום מחדש · לחיצה כפולה = משימה אחת.

### `meeting_reminder_today` · ביום הפגישה — WF-32
**הפעלה:** להעביר את הפגישה להיום בשעה מאוחרת, **לנקות את אישור ההגעה**, `HOUR_TODAY=` השעה הנוכחית.
**🎯 הבדיקה הכי חשובה:** למלא אישור הגעה = אישרה ולהריץ. **ההודעה לא אמורה להישלח.** לנקות ולהריץ שוב: כן נשלחת. הרצה שלישית: נחסמת ככפילות.

### `meeting_reminder_30m` · 30 דקות — WF-32
**הפעלה:** להעביר את הפגישה לעוד כ-32 דקות.
**בפיירברי:** מתמלא `תזכורת 30 דקות לפני` · הרצה שנייה לא שולחת שוב.

## D · מהלך הליווי

### `weekly_weighin` · חמישי בבוקר — WF-34
**הפעלה:** `STAGE_OVERRIDE=weigh`. מומלץ קודם עם `DRY_RUN=true`.
**בוואטסאפ:** שם פרטי · הכפתור פותח את הטופס ב-Tally.
**בפיירברי:** ליוויים פעילים תואם למספר בסטטוס ליווי בתהליך · 🎯 **למלא את הטופס בפועל** ולוודא שנוצרה רשומת שקילה מקושרת ללקוחה ולתהליך הליווי · הרצה שנייה נחסמת ככפילות.

### `weekly_weighin_fu1` · חמישי בערב — WF-34
**הפעלה:** `STAGE_OVERRIDE=fu1`.
🎯 אחרי מילוי הטופס, להריץ שוב: **הלקוחה יוצאת מהרשימה**, בדוח "כבר מילאו".

### `weekly_weighin_fu2` · ראשון בבוקר — WF-34
**הפעלה:** `STAGE_OVERRIDE=fu2`. פותח ב"שבוע טוב".

## E · פולואפ

### `boost_no_convert_followup` — 🔴 אין תהליך ששולח

## 🔴 שלוש תבניות מאושרות בלי אוטומציה

סרקתי את כל 115 התהליכים ב-n8n. ל-`welcome_continue`, `boost_final_form` ו-`boost_no_convert_followup` אין שום תהליך שמפעיל אותן. התבניות מאושרות והפלואוז קיימים, חסר רק מי מפעיל ומתי.

## עדכון WF-07 · הטופס נכנס גם לתהליך המכירה

לבקשת מייק, WF-07 כותב עכשיו **גם לתהליך המכירה של הבוסט**, בנוסף לתהליך הליווי.

| שדה באובייקט 4 | ערך |
|---|---|
| `pcfFormFilled` | כן (1) |
| `pcfFormLink` | הקישור לטופס מ-Tally |
| `pcfQuestionNumber3` | משקל נוכחי |
| `pcfQuestionNumber2` | משקל יעד |
| הערה | כל התשובות, אותו פורמט כמו בתהליך הליווי |

**זיהוי תהליך המכירה:** לפי המוצר `סדנה בוסט` (`2f73403a-20de-47a7-b0cb-df4761b45f21`), ולא לפי `1051.pcfSale`. בבדיקה הראשונה הקישור מתהליך הליווי הצביע על מכירת תוכנית ההמשך והנתונים נכתבו למקום הלא נכון. תוקן ואומת.

**צמתים שנוספו:** `ב1` עד `ב7`, אחרי `הערה בלוג הליווי`. גיבוי של הגרסה הקודמת נשמר לפני השינוי.

## איפוס לקוחת הבדיקה · 24.08.2026

הורץ **WF-28 · איפוס לקוחת בדיקה** (`oby2q9e48LLFUS4U`, וובהוק `reset-test-customer`) עם `{"phone":"0533435240","confirm":"RESET","dryRun":false}`.

**45 רשומות נמחקו:** משימה 17 · מסמך 8 · תיעוד תשלום 6 · תהליך מכירה 4 · תשלום 3 · טופס שקילה 2 · לקוח 1 · תהליך ליווי 1 · פגישה 1 · הקצאת תפריט 1 · רישום שיווקי 1.

**אימות:** הרצה יבשה מראש, וסריקה עצמאית של כל 60 סוגי האובייקטים דרך המטא-דאטה. שתי השיטות החזירו בדיוק 45. אחרי המחיקה: 0 רשומות ב-11 אובייקטים ובחיפוש לפי טלפון ולפי שם.

**מה לא נמחק במודע:** הערות (אובייקט 7). פיירברי מחזירה 500 על סינון הערות לפי `objectid`, מתועד בקוד של WF-28. ההערות נשארות יתומות ולא מופיעות בשום מסך.

**המנוי בקלאודצ'אט נשאר** (`f201689u860230363`) כדי לא לאבד את ההסכמה לקבלת הודעות, אבל **כל 10 שדות המנוי אופסו לערך `-`**, כולל שלושת סימוני האנטי-כפילות `wf_webinar_last`, `wf_meeting_today_last`, `wf_weighin_last`. בלי זה חסמי הכפילות היו עוצרים את הבדיקות הבאות.
