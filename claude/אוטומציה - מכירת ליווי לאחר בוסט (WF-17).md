# WF-17 · מכירת ליווי לאחר בוסט — תיעוד תהליך מלא

> **נבנה:** 02.08.2026 · **סטטוס:** כבוי · `DRY_RUN = true` · לא נגע באף רשומה בחשבון
> **n8n:** `e30cNECSoBNJDQcf` → https://n8n.srv964196.hstgr.cloud/workflow/e30cNECSoBNJDQcf
> **תלוי־תאום עם:** `WF16DeadPath0001` (מסלול מת) — עודכן באותו סשן

---

## 1. הבעיה העסקית

לקוחה מסיימת בוסט ולא קונה מסלול. היום אין רשומה שאומרת "צריך למכור לה עכשיו",
אין תאריך פולואפ ואין תור עבודה. המעקב תלוי בזיכרון של נציג. הן נעלמות.

בנוסף — **יחס ההמרה בוסט←מסלול לא נמדד בכלל**, כי אין רשומה שמייצגת את הניסיון למכור.

## 2. מה הפלואו עושה

כל **יום רביעי 20:00** (אחרי הוובינר, לפני סוף המחזור) — סורק את מחזור הבוסט
שמסתיים ביום ראשון הקרוב, ולכל לקוחה ששילמה ועדיין לא נכנסה לליווי פותח
**תהליך מכירה חדש** על מסלול 12 שבועות עם פולואפ למחרת.

**התוצאה:** ביום חמישי בבוקר לנציג יש רשימת עבודה מוכנה במקום חלל ריק.

### ⚠️ מה זה **לא** עושה — לקרוא לפני שמסיקים מסקנות

**הפלואו לא שולח שום הודעה.** לא וואטסאפ, לא SMS, לא מייל, לא מתאם פגישות.

`תאריך לפולואפ` (`pcfFollowUpTime`) הוא **שדה תאריך ברשומה בלבד**. הוא מייצר
**תור עבודה לנציג** — לא פנייה ללקוחה. אם קראת "פולואפ" והבנת "נשלחת הודעה" — לא.

**שכבת ההודעות מקלאודצ'אט טרם אופיינה ולא נבנתה.** ראה §7.

---

## 3. ארבעת שערי הסינון

| # | שער | תנאי | הערה |
|---|---|---|---|
| 1 | מחזור | `1007.pcfEndDate` = היום+4 **וגם** `pcfStatus ≠ התבטל` | אומת: כל 74 המחזורים מסתיימים ביום ראשון |
| 2 | תשלום | `4.pcfCourse` = המחזור **וגם** `pcfProduct` = סדנה בוסט **וגם** `statuscode` = עסקה נסגרה בהצלחה | זה שער התשלום |
| 3 | ליווי | לא קיימת `1051` של הלקוחה עם `pcfCurrentProgram ≠ בוסט` | פוסל פרימיום · מתקדמים · המשך |
| 4 | כפילות | לא קיים `4` עם `pcfParentSale` = עסקת הבוסט | הגנת ריצה כפולה |

> **כלל־על:** כל כשל טכני בשער מייצר `skip` מפורש, **לא** יצירה.
> ספק אף פעם לא מייצר רשומה.

---

## 4. הרשומה שנוצרת · Opportunity (4)

| שדה | ערך |
|---|---|
| `name` | `מכירת ליווי לאחר בוסט - {accountname}` |
| `accountid` | מעסקת הבוסט |
| `ownerid` | מעסקת הבוסט · fallback `FALLBACK_OWNER_ID` |
| `statuscode` | `2` — ליד חדש |
| `pcfProduct` | `d9e842a7-f284-4596-bb61-4d2a350eebb5` — תוכנית ליווי 12 שבועות |
| `pcfCourse` | מחזור הבוסט — **נשמר** |
| `pcfWhatsAppToRef` | קבוצת הוואטסאפ — **נשמרת** (רק אם קיימת) |
| `pcfParentSale` | מזהה עסקת הבוסט |
| `pcfFollowUpTime` | מחר (חמישי) 10:00 |
| `pcfFormFilled` | `1` רק אם בעסקת הבוסט "כן" |
| `pcfFormLink` | מועתק מעסקת הבוסט (רק אם קיים) |
| `pcfQuestionNumber4` | 3601.69 · לפני מע"מ |
| `pcfsystemfield106` | 4249.99 · כולל מע"מ |
| `pcfCreatedByPersonOrSystem` | `2` — מערכת |
| נוסף | טלפון · מייל · משפך · משקל התחלתי · משקל יעד — אם קיימים |

---

## 5. Config — כל הכוונון במקום אחד

`DRY_RUN` · `TZ=Asia/Jerusalem` · `COHORT_END_OFFSET_DAYS=4` · `FOLLOWUP_OFFSET_DAYS=1` ·
`FOLLOWUP_HOUR=10` · `BOOST_PRODUCT_ID` · `TARGET_PRODUCT_ID` · `TARGET_PRICE_EXVAT` ·
`TARGET_PRICE_INCVAT` · `WON_STATUS_LABEL` · `NEW_STATUS_CODE=2` · `FALLBACK_OWNER_ID` · `NAME_PREFIX`

**להעלאה לאוויר:** `DRY_RUN → false`, ואז הפעלה. שום דבר אחר.

---

## 6. תיאום עם WF-16 · מסלול מת

**הבעיה שזוהתה:** WF-16 מסמן ליווי כ"סיום בוסט — לא המירה" 3 ימים אחרי סוף המחזור.
WF-17 פותח תהליך מכירה לאותן לקוחות בדיוק. בלי תיאום — לקוחה מסומנת "מתה"
בזמן שיש לה תהליך מכירה חי.

**מה בוצע (02.08.2026) ב-`WF16DeadPath0001`:**
1. `שליפת עסקאות הלקוחה` — נוסף `pcfParentSale` לרשימת ה-`fields`
2. `הכרעה` — נוסף שער לפני `mark_dead`: לקוחה עם תהליך מכירה **פתוח** שמצביע
   על עסקת בוסט (תוצר WF-17) → `skip` עם סיבה מפורשת

**המעגל נסגר:** מי שהמירה נכנסת למסלול · מי שלא — מסומנת אחרי 3 ימים ·
מי שבתהליך מכירה פעיל — לא נוגעים בה.

---

## 7. 🔜 חסר — שכבת ההודעות מקלאודצ'אט

**לא בסקופ שנבנה. דורש אפיון נפרד לפני בנייה.**

הפלואו מייצר את הרשומה ואת נקודת הזמן. **הפנייה ללקוחה עצמה לא קיימת.**

| # | שאלה פתוחה | הערה |
|---|---|---|
| 1 | מי שולח — CloudChat (הבוט של טל) או GreenAPI? | לבוט יש כבר שיחה פתוחה מול הלקוחה מרכישת הבוסט |
| 2 | הטריגר — יצירת התהליך ברביעי, או סריקה יומית של `תאריך לפולואפ = היום`? | דפוס "poll + sent-flag" מהפלייבוק |
| 3 | הנוסח ומי מחזיק אותו | אובייקט תבניות `1040` — **לא hardcoded** |
| 4 | 🔴 **שדה דגל "נשלח"** על תהליך מכירה (4) | **לא קיים היום. חייב להיווצר לפני בנייה** — בלעדיו הודעות כפולות |
| 5 | ההודעה יוצאת לכולן או רק למי שמילאה שאלון? | משפיע על הנוסח |
| 6 | יחס לוובינר — ההודעה נשלחת לפני או אחרי? | הפלואו רץ ברביעי 20:00, בערך בזמן הוובינר |

> **החלטה שנדרשת ממייק לפני בנייה:** אם ההודעה יוצאת אוטומטית — מי אחראי
> על הנוסח, ומה קורה ללקוחה שכבר בשיחה פעילה עם נציג?

---

## 8. אוטומציות פנימיות בפיירברי — ההכרעה: **אפס**

**לא נדרשת ולא מומלצת אף אוטומציה פנימית.** שלוש סיבות:

1. **טריגר** — אוטומציה פנימית מופעלת מאירוע על רשומה. התהליך הזה מופעל
   מ**תאריך סיום של מחזור** — אין רשומה שמשתנה. פיירברי לא יודעת לעשות את זה.
2. **הפניות בין אובייקטים** — כל ארבעת השערים דורשים שאילתה על אובייקט אחר
   (1007 → 4 → 1051 → 4). פיירברי **לא תומכת בהפניות בין אובייקטים**. חוסם מוחלט.
3. **מגבלת 50** — החשבון קרוב למיצוי. לא שורפים מכסה על משהו שרץ טוב חיצונית.

> ההחלטה הארכיטקטונית של הפרויקט נשמרת: **לוגיקה מורכבת רצה ב-n8n. פיירברי מחזיקה דאטה ומסכים.**

---

## 9. מה כן נדרש בפיירברי — ידני ב-UI

| # | פעולה | למה |
|---|---|---|
| 1 | **תצוגה: "מכירות ליווי לאחר בוסט"** — סינון `תהליך מכירה - אב ≠ ריק` + `סטטוס = ליד חדש`, ממוין לפי `תאריך לפולואפ` | 🔴 **הכי חשוב.** פלואו שיוצר רשומות שאף אחד לא רואה שווה לאפס |
| 2 | `תהליך מכירה - אב` ללייאאוט של תהליך מכירה | משימה **C4** הפתוחה. השדה תקין ב-API אבל לא על המסך |
| 3 | `תאריך לפולואפ` על המסך | מייצר את תור העבודה של חמישי |
| 4 | תיקון `בוסט · אוגוסט 02` | תאריך התחלה 09.08 במקום 02.08 · כפילות עם מחזור מבוטל |
| 5 | מדד לדשבורד מכירות: יחס המרה בוסט←מסלול | אפשרי רק עכשיו, כשיש רשומה שמייצגת את הניסיון |

---

## 10. תלויות שחוסמות היום

| # | מה חסר | מי אמור למלא | סטטוס |
|---|---|---|---|
| 1 | `statuscode = עסקה נסגרה בהצלחה` על עסקת בוסט | ענף התשלום של Grow | ❄ **מוקפא** |
| 2 | `pcfCourse` על עסקת הבוסט | אותו ענף | ❄ **מוקפא** — 2 מתוך 176 בלבד |
| 3 | `pcfFormLink` + `pcfFormFilled` על **תהליך המכירה** | 🔴 **אף אחד** | ראה §10 |

עד שאלה נפתחים — **הפלואו יחזיר אפס. זה תקין, לא תקלה.**

---

## 11. 🔴 פער פתוח — השאלון

`WF07BoostForm01` (פעיל היום) כותב את השאלון של Tally **לתהליך הליווי (1051) בלבד**:
`pcfFormLink` · `pcfFormQA` · `pcfReadinessScore` · `pcfWhatchedZoom`.

**הוא לא כותב לתהליך המכירה.** ההחלטה בסשן הזה הייתה שמקור האמת ל-WF-17
הוא תהליך מכירת הבוסט → **השדה יישאר ריק תמיד**.

שתי דרכים לסגור, טרם הוכרע:
- **א׳** — להוסיף ל-WF-07 כתיבה מקבילה לעסקת הבוסט. נקי, לא נוגע ב-WF-17
- **ב׳** — WF-17 יקרא מ-1051 עם נפילה לעסקה. עמיד יותר, שאילתה נוספת

---

## 12. מצב הדאטה בזמן הבנייה · 02.08.2026

- **176** תהליכי מכירה · **175 בסטטוס "ליד חדש"** · **0** ב"עסקה נסגרה בהצלחה"
- **2** בלבד משויכים למחזור בוסט (`pcfCourse`)
- **2** רשומות `1051` — שתיהן "פרימיום"
- **74** מחזורי בוסט · כולם מסתיימים ביום ראשון

**סימולציה שהורצה מול הדאטה החי:**
- ריצה קרובה (רביעי 05.08 · יעד 06.08) → אין מחזור מתאים, יוצא בשקט ✅
- המחזור היחיד עם דאטה → עסקת בוסט אחת, **דולגה נכון** כי הסטטוס "ליד חדש" ✅

---

## 13. גוצ'ות שאומתו בבנייה — לחסוך לצ'אט הבא

- `/api/query` **תומך** ב-`query` עם `AND` בין סוגריים. אומת: `(pcfCourse = 'guid') AND (pcfProduct = 'guid')`
- 🔴 `accountidname` / `owneridname` **אינם שדות חוקיים** ב-`fields` — מפילים את כל השאילתה ב-`400`.
  שם הלקוחה נשלף בשאילתה נפרדת על אובייקט `1`
- picklist חוזר **כתווית עברית** בקריאה, נכתב **כמספר**. הקוד משווה לתווית וכותב מספר
- `pcfEndDate` חוזר `2026-08-16T00:00:00` — משווים 10 תווים
- שדה אופציונלי ריק **נדחה** — נכתב רק אם יש ערך
- **ה-API הציבורי של n8n לא מאפשר הרצה ידנית** של workflow. אימות DRY_RUN בוצע
  ע"י הרצת הלוגיקה מול ה-API החי, לא דרך n8n
- קרדנציאל n8n לפיירברי: `Revital Yaish new CRM` · id `BrhjRlBnrstIab7d`
- `errorWorkflow` בכל הפלואוז: `WF16ErrHandler01`

---

## 14. Change-log

```
[n8n] WF-17 · מכירת ליווי לאחר בוסט | יצירה | נוצר, כבוי, DRY_RUN=true
[n8n] WF16DeadPath0001 · שליפת עסקאות הלקוחה | עריכה | נוסף pcfParentSale ל-fields
[n8n] WF16DeadPath0001 · הכרעה | עריכה | נוסף שער דילוג לתהליך מכירה פתוח מסוג אחרי-בוסט
[Fireberry] — | ללא שינוי | לא בוצעה שום כתיבה לחשבון של רויטל
```
