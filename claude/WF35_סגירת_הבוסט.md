# WF-35 · סגירת הבוסט · שאלון ופולואפ ללא המרה

**תאריך:** 31.08.2026
**מזהה ב-n8n:** `EeisDAgszlF3sKeN` · פעיל
**וובהוק בדיקה:** `POST https://n8n.srv964196.hstgr.cloud/webhook/wf35-test`

---

## מה התהליך עושה

שני שלבים, כל אחד בזמן קבוע, שניהם נשענים על תאריך תחילת מחזור הבוסט.

| שלב | מתי | טמפלייט | פלואו | למי |
|---|---|---|---|---|
| `finalform` | **חמישי 10:00** (יום 4 במחזור) | `boost_final_form` | `f201689s4552549` | כל המשתתפות במחזור |
| `noconvert` | **ראשון 12:00** (יום 7 במחזור) | `boost_no_convert_followup` | `f201689s4552551` | משתתפות בלי פגישת מכירה ובלי עסקת פרימיום/המשך |

---

## קריטריון ההחרגה של `noconvert`

לקוחה **לא תקבל** את ההודעה אם מתקיים אחד מהשניים:

1. **יש לה פגישת מכירה** - רשומה באובייקט פגישה (6) המקושרת אליה, שמטרתה `מכירה`, שהסטטוס שלה אינו `בוטלה`. כולל פגישות שכבר התקיימו.
2. **יש לה עסקה חיה** - תהליך מכירה (4) שהמוצר שלו מסוג `פרימיום` או `שימור`, והסטטוס שלו אינו `עסקה הופסדה` ואינו `עסקה בוטלה`.

רשימת המוצרים נשלפת דינמית מאובייקט מוצר (14) לפי `pcfProductType`, ולא מקודדת קשיח. מוצר פרימיום או שימור חדש ייכלל אוטומטית.

**הנחה שנעשתה ולא אושרה:** עסקה שהופסדה או בוטלה **לא** נחשבת "מכרו לה", ולכן הלקוחה כן תקבל את ההודעה. אם ההיגיון הפוך, זה שינוי של שורה אחת.

---

## בניית הקישור לשאלון

`boost_final_form` דורש שני משתנים: `{{1}}` שם פרטי (`First name`), `{{2}}` הקישור (`boost_final_form_url`).

התהליך בונה את הקישור כך:

```
<FINAL_FORM_BASE_URL>?process_id=<מזהה תהליך הליווי>
```

מזהה תהליך הליווי הוא `customobject1051id`, שהוא בדיוק מה ש-WF-07 מצפה לקבל חזרה בשדה `question_v45z4D` של הטופס.

**חסם בטיחות:** כל עוד `FINAL_FORM_BASE_URL` ריק, שלב `finalform` **לא שולח כלום** ומחזיר בדוח: "חסר קישור לשאלון הסיום". זה מונע שליחת קישור שבור ללקוחות.

**נדרש ממייק:** כתובת הבסיס של טופס Tally לשאלון סיום הבוסט, ושם הפרמטר שממלא מראש את מזהה תהליך הליווי.

---

## חסם כפילות

שדה מנוי חדש בקלאודצ'אט:

| שם | var_ns |
|---|---|
| `wf_boost_close_last` | `f201689v16889445` |

הערך שנכתב הוא `<שלב>|<תאריך תחילת המחזור>`, למשל `finalform|2026-09-06`. הרצה שנייה על אותו מחזור לא תשלח שוב.

---

## בדיקות שבוצעו

| בדיקה | תוצאה |
|---|---|
| `noconvert` על מחזור 6-13.9 | משתתפות 1, מועמדות 0. סיבה: "יש לה פגישת מכירה". ההחרגה עובדת |
| `finalform` בלי קישור | נחסם. "חסר קישור לשאלון הסיום" |
| `finalform` עם קישור בדיקה | נבחרה לשליחה, והקישור נבנה כ-`https://tally.so/r/TESTTEST?process_id=9e87ad2e-63f7-4a12-a3e7-41ae37d397be` |

---

## הרצה ידנית

```json
{"stage":"finalform","date":"2026-09-10","dry":true,"formUrl":"https://tally.so/r/XXXX"}
{"stage":"noconvert","date":"2026-09-13","dry":true}
```

`dry: true` = הרצה יבשה, לא שולחת. `date` = התאריך שהתהליך מתייחס אליו כ"היום".
