# יומן שינויים · WF-19 — 11.8.2026

**וורקפלואו:** `CG4nPYCgushC1qDz` · WF-19 · פתיחת תהליכי מכירה אוטומטית
**סטטוס:** לא פעיל · `DRY_RUN: true` · 23 צמתים
**קישור:** https://n8n.srv964196.hstgr.cloud/workflow/CG4nPYCgushC1qDz

---

## למה מייק לא ראה תוצאה בהרצת הבדיקה

הרצה ב-11.8 (שלישי). האוטומציה מחפשת מחזור בוסט שנגמר ב־**היום+4** = 15.8.
המחזור `בוסט · אוגוסט 02` נגמר ב־**16.8**. פספוס של יום → 0 מחזורים → כל השרשרת ריקה.

חסם משני: עסקת הבוסט הייתה בסטטוס **ליד חדש**; האוטומציה סופרת רק **עסקה נסגרה בהצלחה**.

**לא היה באג בקוד ההכרעה.**

---

## באגים שנמצאו ותוקנו

### 1. שאילתות ללא גבול — סכנת קטיעה שקטה
`1.3` / `1.4` / `1.4b` / `2.2` רצו עם `query` ריק, כלומר שלפו **הכל**.
- אובייקט 4 (תהליך מכירה): 434 רשומות מול תקרת עמוד של **500**.
- אובייקט 1 (לקוח): כבר **מעל 500** — כלומר כבר נחתך בשקט.

תקרת העמוד המרבית של פיירברי היא 500 (נבדק: 1000 ו-2000 מחזירים 0).
**המשמעות הייתה:** תוך שבועות, עץ הכפילות היה מפספס עסקאות קיימות ופותח כפילויות.

**התיקון:** כל השאילתות מצומצמות ללקוחות המועמדות בלבד, באמצעות שרשרת `OR` שנבנית מהשלב הקודם. אם אין מועמדות — נשלח GUID אפסי כדי שהשאילתה תחזיר 0 ולא הכל.

### 2. שער "כבר קנתה ליווי בעקבות הבוסט הזה" היה מת
השדה **תאריך סגירת עסקה** (`pcfclosedate`) לא היה ברשימת השדות של `1.2` / `1.4` / `2.2`.
הקוד קרא אותו, קיבל `null`, והשער אף פעם לא נורה.

**התיקון:** השדה נוסף לשלוש השאילתות. אומת שהוא לא מאפס את התוצאות.

### 3. עסקאות בוסט לא נבדקו מול המחזור
`1.2` שלף את **כל עסקאות הבוסט שנסגרו אי־פעם**, ו־`1.5` לא בדק שהן שייכות למחזור שמסתיים היום.

**התיקון:** `1.2` מסונן למחזורים של הריצה הנוכחית. בנוסף נוסף ב־`1.5` שער גיבוי שדוחה עסקה ללא מחזור או ממחזור אחר.

### 4. מחזורים מבוטלים נכנסו לריצה
`1.1` סינן לפי תאריך סיום בלבד. נמצאו שני מחזורים באותו תאריך סיום, אחד מהם בסטטוס **התבטל**.

**התיקון:** נוסף `CYCLE_STATUS_EXCLUDE: ['התבטל']` בהגדרות. מונה חדש בסיכום: `מחזורים שדולגו (מבוטלים)`.

---

## תוספות

- **`CYCLE_END_OVERRIDE`** ו־**`RENEWAL_DATE_OVERRIDE`** בבלוק ההגדרות — מאפשרים הרצה ידנית בכל יום בשבוע בלי תלות ב"היום+4". ריק = התנהגות רגילה. פורמט לא תקין מפיל את הריצה עם הודעה בעברית.
- **הסיכום מציג את תאריכי היעד** בכל ריצה, ומתריע בבירור כשמתגי הבדיקה פעילים.
- **מזהה מוצר הריטיינר** נשמר בשרת: `58a4d4f9-5921-4bfc-ba8e-518877faa375`. ענף 2 כבר לא עוצר את עצמו.

---

## התנהגות פיירברי שהתגלתה

**פיירברי דורסת את "תאריך סגירת עסקה" כשהעסקה עוברת ל"עסקה נסגרה בהצלחה" באותה קריאה** — ומחתימה את רגע השינוי.

אומת: עדכון `statuscode:11` + `pcfclosedate:'2026-08-18'` יחד → התקבל `2026-08-11T16:51:33`.
עדכון התאריך בנפרד, אחרי שהסטטוס כבר 11 → נשמר כמו שהוזן.

**השלכה תפעולית:** בבדיקות ידניות חייבים שני שלבים — קודם סטטוס, לשמור, ואז התאריך. אחרת התוצאה מטעה.
**השלכה עסקית:** התאריך משקף נאמנה את רגע הסגירה בפועל — טוב ללוגיקה שלנו.

---

## בדיקות שבוצעו · 11.8.2026

הורצו מול הקוד **שנפרס בפועל** (נשלף מ־n8n) ומול פיירברי החי, עם רשומות פיקטיביות שנמחקו בסוף.

### ענף 1 — 16/16
| # | תרחיש | תוצאה |
|---|---|---|
| א | לקוחה נקייה — רק עסקת הבוסט | פותח |
| ב | תהליך פתוח בסטטוס ליד חדש | עוצר |
| ג | תהליך בסטטוס פגישת מכירה | עוצר |
| ד | נסגרה בהצלחה · סגירה בתוך המחזור | עוצר |
| ה | נסגרה בהצלחה · סגירה לפני חודשיים | פותח |
| ו | גבול · בדיוק ביום תחילת המחזור | עוצר |
| ז | גבול · יום לפני תחילת המחזור | פותח |
| ח | עסקה הופסדה בלבד | פותח |
| ט | הופסדה + ליד חדש · הפעיל גובר | עוצר |
| י | כבר יש ליווי פרימיום | עוצר |
| יא | ליווי בוסט בלבד | פותח |
| יב | המחזור בוטל | מדלג על המחזור |
| יג | עסקה במחזור אחר שגם מסתיים היום | פותח |
| יד | עסקה במחזור שלא מסתיים היום | מדלג |
| טו | עסקת בוסט "ממתין לתשלום" | לא נכנסת |
| טז | חזרה לתקין אחרי כל השינויים | פותח |

### ענף 2 — 4/4
| # | תרחיש | תוצאה |
|---|---|---|
| ב1 | ליווי פרימיום פעיל שמסתיים | פותח חידוש |
| ב2 | ליווי בהקפאה | עוצר |
| ב3 | ריטיינר (סיום 2099) | מדלג |
| ב4 | כבר יש תהליך מכירה פתוח | עוצר |

**ניקוי:** אומת אפס שאריות בכל ארבעת האובייקטים (1, 4, 1007, 1051).

---

## החלטות שהתקבלו

- עסקת בוסט בסטטוס **"ממתין לתשלום"** **לא** נכנסת לאוטומציה. רק "עסקה נסגרה בהצלחה". *(מייק, 11.8)*
- מחזורים בסטטוס **התבטל** מדולגים. *(מייק, 11.8)*

---

## פתוח לפני עלייה לאוויר

1. **WF-16** — להוסיף `'ליד חדש'` למערך הסטטוסים הפתוחים בצומת ההכרעה. תיקון של מילה אחת. **טרם בוצע.**
2. **הרצה ידנית של מייק בממשק n8n** — לאימות החיווט בין הצמתים (הקוד והשאילתות אומתו במלואם).
3. **הרצה חיה ראשונה** עם `DRY_RUN: false` ו־`MAX_PER_RUN: 1`.
4. **לרוקן את מתגי הבדיקה** לפני הפעלה.
5. **שמות המחזורים מוסטים בשבוע** — `בוסט · אוגוסט 02` מתחיל ב-9.8. לבדוק מי מייצר אותם.
6. **מזהה תבנית CloudChat לברכת בוסט** — סתירה לא פתורה (`...241` מול טענה שמחזיר 400). ממתין לטל.

---

## אזהרה תפעולית

**n8n מריץ את מה שעל המסך, גם אם לא נשמר.** מייק ערך את בלוק ההגדרות והריץ בהצלחה — אבל השינוי לא נשמר בשרת. כשעובדים במקביל דרך API ודרך העורך, יש לרענן את הדף לפני שמירה, אחרת העורך דורס.
