# יומן שינויים · 16/09/2026 (אחה"צ) — סבב סקירת ממצאי ניטור, תיקוני נתונים וכיול הבדיקות

**הקשר:** אליאל עבר על דוח הניטור של 13:52 ממצא-ממצא והכריע בכל אחד. המסמך הזה מתעד מה בוצע, מה נדחה ולמה, ומה נשאר פתוח.
**גיבוי מלא לפני השינויים:** `claude/BACKUP_2026-09-16_סבב_סקירת_ממצאי_ניטור.json`
**תוצאה:** 24 ממצאים פעילים ← **15**. גבוהים 13 ← **2**.

---

## א. שינויי נתונים בפיירברי

### 1. B1 · מיזוג זוסלין + גוס

**התקלה שלא נראתה:** שתי רשומות לקוח לאותו טלפון, **בשני פורמטים**:

```
זוסלין   telephone1 = 0504006577       נוצרה 12/09 22:50
גוס      telephone1 = +972504006577    נוצרה 10/09 23:57
```

חיפוש `0504006577` בפיירברי החזיר רשומה אחת בלבד — פיירברי מחפש מחרוזת ליטרלית. הניטור מנרמל ולכן תפס. **לשתיהן היה אותו `pcfCloudChatId` — `f201689u944220623`** — אותה אישה, מנויה אחת, שני תיקים.

**מה בוצע:**

| פעולה | פרטים |
|---|---|
| מיפוי רשומות בנות | גוס: 11 תהליכי מכירה · 1 ליווי · 1 תשלום (159 ₪, נפרע) · 1 תיעוד. זוסלין: תהליך מכירה אחד בלבד |
| השורדת | **גוס** (`d4ba9365`) — מחזיקה את כל הכסף וההיסטוריה |
| העברה | `העסקה של זוסלין` (`4df7a9d7`) הועברה לתיק השורד |
| נרמול | `telephone1` עודכן ל-`0504006577` |
| שם | עודכן ל-**זוסלין** (השם המלא יותר) |
| מחיקה | תיק זוסלין (`f3279af3`) נמחק אחרי אימות שהוא ריק מרשומות בנות |

> ⚠️ **ממצא שורש שנחשף ולא טופל: אין נרמול טלפון בכתיבה.** כל בדיקת כפילות שמשווה מחרוזות — בפיירברי, ב-Make או בטופס הליד — תחמיץ כל זוג שנכתב בשני פורמטים. **שווה לכפות פורמט אחד בכניסה.**

> ⚠️ **ממצא שני: 10 מתוך 11 תהליכי המכירה של גוס הם `עסקה - גוס | ליד` ריקים, בלי מוצר, כולם מ-11/09 בין 00:02 ל-14:26.** זה בדיוק הדפוס שאליאל זיהה אצל אתי בראל ("תהליך מכירה פתוח, לא ברור למה הוא שם"). **לא נמחקו — מחוץ להרשאה של הסבב הזה.**

### 2. B5 · מיזוג 5 כפילויות ליווי

הדפוס זהה בכל אחת: הרשומה החדשה (`ליווי סדנה בוסט - X`, 12/09 ~10:55) עשירה — מחזור בוסט, קבוצה, ובחלקן משקל אמיתי. הישנה (`ליווי - X | סדנה בוסט`) היא רשומת הגירה דלה שלא הוסיפה מידע.

**מוזגו (רשומת ההגירה נמחקה, החדשה שרדה):** חן שרביט · תהאני מוצלח · ליטל טורג'מן · רעות דמארי · לירון
לפני כל מחיקה אומת שאין טפסי שקילה או משימות התלויים ברשומה.

**❌ נורית כהן — לא מוזגה, ולא תמוזג.**

```
ישנה:  ליווי הסתיים בהצלחה · התחלה 17/08 · שבוע 5
חדשה:  ליווי בתהליך       · התחלה 11/09 · שבוע 1
```

אלה **שני מחזורים אמיתיים**, לא כפילות. מיזוג היה מוחק היסטוריה. הוחרגה קבועה בקובץ ההחרגות.

### 3. A1 · שם ללקוחה 0522090513

תיק `62924e0a` (נוצר 14/09, ללא שם וללא מייל) — נכתב השם שאליאל אישר במפורש. אומת.

### 4. A5 / G1 · שתי הלקוחות בקלאודצ'אט

רונית מחמלי וסיגל שמעון נבדקו מול קלאודצ'אט. **שתיהן כבר היו קיימות** — הסשן המקביל של אליאל הקדים. המזהים אומתו ונכתבו שוב לפיירברי (אותם ערכים). **לא נוצרה כפילות.** הבדיקה-לפני-יצירה עשתה בדיוק את עבודתה.

---

## ב. מה נדחה — ולמה

### C4 · מילוי רטרו של 37 סימוני Welcome — **נדחה**

> **`pcfWelcomeSent` ריק ב-5,754 מתוך 5,754 רשומות הליווי. בכל החשבון. מעולם לא נכתב ערך אחד.**

כולל יהודית סואייה, שהוולקאם שלה אומת חי מול קלאודצ'אט (`last_message_at = 16/09 11:55:10`) ובכל זאת השדה ריק.

**C4 לא מודד לקוחות שלא קיבלו וולקאם — הוא מודד שדה שאף תהליך לא כותב.** מילוי רטרו היה צובע את הבדיקה ירוק ליום אחד, והמונה היה מטפס מחדש למחרת מכל ליווי חדש.

**הנחיה מלאה לתיקון השורש נכתבה:** `claude/HANCHAYA_2026-09-16_תיקון_pcfWelcomeSent.md` — למשימה בסשן נפרד. מילוי רטרו רק **אחרי** שהתיקון חי, ורק למי שאומת מול קלאודצ'אט שההודעה באמת יצאה.

---

## ג. כיול הניטור

נוצר **`claude/MONITOR_exclusions.json`** — קובץ החרגות עם סיבה ותאריך לכל שורה, שהניטור טוען בכל הרצה. סשן עתידי יכול להסיר החרגה שכבר לא רלוונטית בלי לגעת בקוד.

| שינוי | סיבה (אליאל) |
|---|---|
| **B10 כובתה** | האסמכתא היא של התשלום וזהה בכל תיעודיו. לא כפילות — שדה שאינו מזהה ייחודי |
| **C7 כובתה** | אובייקט הרשמות לאירוע יצא משימוש |
| **E3 → נמוך** | WF כבוי הוא החלטה מודעת, לא תקלה |
| **A9** | שתי העסקאות הוחרגו — אין טלפון ללקוחות כלל |
| **A10-בוסט** | רונית מחמלי הוחרגה — באמת לא עברה בוסט |
| **A10-קבוצה → בינוני, באחריות רויטל** | מקרה חריג לשיוך ידני |
| **A12b** | בוסט 20-27.9.26 הוחרג — המחזור בוטל |
| **B5** | שתי רשומות נורית כהן הוחרגו לצמיתות |
| **E4** | WF-26 ו-WF-18 הוחרגו — הצמתים כובו במכוון |

### תיקון באג אמיתי ב-E4

הרגקס הישן `DRY_RUN\s*[=:"']{1,3}\s*(true|1)` תפס **הערות בקוד, שדות notes ומחרוזות הסבר**, והפיק **10 ממצאי שווא מתוך 10**. נבדקו כל 25 ה-workflows הפעילים ידנית: אף אחד לא היה ב-DRY_RUN.

התיקון פורס שורות אמיתיות, מסיר הערות `//` ו-`/* */`, מנטרל השוואות `===` והשמות מותנות, ומחפש רק את צורת הקונפיג `DRY_RUN: true`.

**תוצאה: E4 ירד מ-10 ל-0. אפס ממצאי שווא.**

### תיקון שני — Make

הבלוק של Make לא שלח `User-Agent` של דפדפן, ולכן היה נכשל ב-403 (Cloudflare 1010) גם אילו היה מפתח. **תוקן.**

---

## ד. בדיקות Make רצו לראשונה ✅

אליאל מסר את מפתח ה-API. הבדיקות רצו ועברו:

```
ארגון 4829531 · צוות 2425546 · זון eu2 · 43 תרחישים (12 פעילים)
F1 · תרחישי Make שנכשלו ב-13 השעות האחרונות: 0
```

**נקודת העיוורון הגדולה של הניטור נסגרה.**

### ואישור חשוב: `Main Leads CloudChat (1.01)` תוקן

| | |
|---|---|
| ריצות אוטומטיות היום | **14 הצלחות · 0 שגיאות** |
| אחרונה | 16/09 10:17Z |
| עריכות | 09:06–10:20Z — התיקון של אליאל |

**ההשערה שנרשמה ב-`AVCHUN_2026-09-16_ניטור_ממצא_קריטי.md` ("מסלול הקליטה עדיין שבור") הופרכה בראיות ישירות.** סיגל שמעון (10:33 שעון ישראל) נקלטה **לפני** שהתיקון עלה — היא הנפגעת האחרונה, לא ראיה לכשל מתמשך.

---

## ה. שני ממצאים חדשים שצפו בהרצת האימות

### 1. 🔴 פיירברי מגביל קצב — שלושה תהליכים נופלים יחד

שלוש הרצות n8n נכשלו **באותה שנייה בדיוק**, 16/09 12:00:00Z (15:00 שעון ישראל):

| Workflow | שגיאה |
|---|---|
| WF-06B · שליחת תפריטים מתוזמנת | `Too Many Requests` (429) |
| WF-32 · סדרת הודעות הפגישה | `Response body is not valid JSON` — **אותו 429 בתחפושת**: פיירברי החזיר עמוד טקסט והצומת ניסה לפרסר JSON |
| WF-16-ERR · מטפל שגיאות | `Too Many Requests` (429) |

**שלושה קרונים מתוזמנים לאותה שנייה מציפים את פיירברי, והוא חוסם.** גם הניטור עצמו ספג 429 באותו יום, וגם ריצת רטרו שעדכנה **1,600 רשומות בדקה** ב-14:55–14:58.

**החמור מכל: `WF-16-ERR · מטפל שגיאות` נכשל בעצמו, וגם כבוי (`active=false`).** כרגע אין רשת ביטחון — כשל שקט לא מדווח לאיש.

**המלצה:** לפזר את שלושת הקרונים (12:00 / 12:03 / 12:06), להוסיף retry עם השהיה מדורגת על 429 בצמתי ה-HTTP, ולהחזיר את WF-16-ERR לפעולה.

### 2. C2 עלה מ-0 ל-7 — תוצר לוואי של הדדופ שרץ במקביל

7 עסקאות "נסגרו בהצלחה" בלי רשומת תשלום. **שלוש מהן של ענת שמיר** — אותו שם עסקה (`תוכנית ליווי 17 שבועות`), כולן נוצרו **באותה שנייה** ב-09/09 09:00:21 (אצוות הגירה), כולן עם אפס תשלומים. ארבע הנותרות הן רשומות הגירה עם תאריכי סגירה מ-2024–2025.

זה **לא נזק חדש** — אלה כפילויות הגירה שהתשלומים שלהן נותבו לעסקה השורדת בעבודת הדדופ, וכעת הן חשופות. B4 לא תופס אותן כי כולן legacy. **הן מועמדות למחיקה, לא לחקירה.**

---

## ו. תמונת המצב אחרי הסבב

```
לפני (13:52):  24 פעילים · 6 קריטיים · 13 גבוהים
אחרי (15:38):  15 פעילים · 5 קריטיים ·  2 גבוהים
```

| ממצא | היה | עכשיו |
|---|---|---|
| E4 · DRY_RUN | 10 | **0** |
| B5 · כפילויות ליווי | 6 | **0** |
| A5 · ללא מזהה קלאודצ'אט | 6 | **1** |
| G1 · לא בקלאודצ'אט | 2 | **0** |
| B1 · כפילות טלפון | 1 | **0** |
| A9 / A12b / A10-בוסט / A1 | 5 | **0** |
| B4 · עסקאות כפולות | 3 | **2** |
| F1 · Make | לא רץ | **0** ✅ |

---

## ז. פתוח

1. **נרמול טלפון בכתיבה** — אין. זה שורש B1 וימשיך לייצר כפילויות בלתי נראות.
2. **`עסקה - X | ליד` ריקות** — 10 אצל גוס, וכנראה מאות בחשבון. לכמת ולזהות את המקור.
3. **`pcfWelcomeSent`** — לפי ההנחיה הייעודית, בסשן נפרד.
4. **429 מפיירברי** — לפזר קרונים, retry, ולהחזיר את WF-16-ERR.
5. **7 עסקאות C2** — למחוק את כפילויות ההגירה.
6. **A5 · יונית אנגל** (14/09) — הנותרת היחידה בלי מזהה קלאודצ'אט.
7. **WF-35 כפול** — שני workflows שונים באותו מספר.
8. **1,600 עדכונים בדקה** ב-14:55–14:58 — לאשר שזו ריצת הרטרו של אליאל ולא לופ.
