# יומן שינויים · 22/09/2026 — WF-27 · סוג תשלום + תשובה מהירה + אשף מדורג

## מה בוצע (הכל אומת בריצה חיה על רשומות טסט ומחיקתן)

### 1 · WF-27 כותב סוג תשלום (`pcfPaymentCategory` על 1039)
ההיגזר מהטופס: אמצעי=הוראת קבע ⇒ סוג 3 · מספר תשלומים>1 ⇒ סוג 2 · אחרת ⇒ סוג 1.
**אומת בטסט חי:** 3 תשלומי טסט — העברה×1 ⇒ "רגיל", מזומן×3 ⇒ "תשלומים", הור"ק×10 ⇒ "הוראת קבע".

### 2 · אמצעי תשלום על תיעודי תשלום (1018) לעולם לא 7
על הקמת הו"ק דרך הטופס: `pcfPaymentType` 7 ⇒ 1 (אשראי) — החלטת אליאל 08/09.
**אומת בטסט חי:** 12 תיעודי טסט של הו"ק — כולם "אשראי".

### 3 · תשובה מהירה לטופס (התהליך המקצועי עובר לרקע)
- לפני: WF-27 השיב רק אחרי WF-22→WF-24 (כולל וולקאם) — ריצה של אתי מנשה **31.9 שניות**
  ⇒ הטופס (timeout 20s) הציג "בעיית תקשורת" בזמן שהכול הצליח.
- אחרי: `סיכום הריצה → תגובה · סיכום → הפעלת התהליך המקצועי` — התגובה לטופס יוצאת מיד
  אחרי יצירת רשומות התשלום; קריאת ה-pro רצה אחריה (timeout הוגדל 30s→120s + ריטרי אחד).
- **אומת בטסט חי:** ענף מלא (3 אמצעי תשלום, 22 פעולות) — תשובה ב-**6.8 שניות**.

### 4 · הטופס (כפתור) — גרסה 2 · אשף מדורג
- **חסימת שליחה כפולה:** דגל `sent` פר-טעינת עמוד — אחרי לחיצה אחת הכפתור נעול
  ("נשלח — התהליך פועל…"), בנוסף למזהה האידמפוטנטיות ב-n8n.
- **timeout מוארך:** 20s → 120s, ובמקרה של אין-תשובה ההודעה השתנתה:
  "התהליך ממשיך ברקע — אין לשלוח שוב; לרענן את הכרטיס בעוד דקה" (ולא "נסי שוב").
- **אשף 4 מסכים** (נקודות התקדמות בראש):
  1. אמצעי תשלום — "האם שילמה שלא באשראי?" (קיים)
  2. מוצר — "האם נדרש להקים מוצר לתשלום זה?" (כן/לא; בכן — בחירת מוצר + מחיר)
  3. קבוצת וואטסאפ — וידוא הקבוצה עם הצגת ברירת המחדל (שדה העסקה) והסבר מקור הקבוצה
  4. פרטי התשלום — אמצעים, סכומים, פריסה, תאריכים
- כפתורי חזרה בין השלבים; כפתור "המשך" נעול עד בחירת מוצר.

## גיבויים ובדיקות
- גיבוי לפני שינוי: `claude/BACKUP_2026-09-22_WF27/WF27_before.json`
- גרסה חדשה: `claude/BACKUP_2026-09-22_WF27/WF27_after_n8n.json`
- גיבוי רשומות הטסט שנמחקו: `BACKUP_test_records.json` (4 תשלומים + 16 תיעודים + תהליך מכירה טסט — הכל נמחק ואומת 0 שאריות)
- טופס v2: `buttons/קליטת_תשלום_שאינו_אשראי_v2.html` — **להעלות לכפתור בפיירברי**

## נשאר (רשימת אישור מוכנה: `retro_payment_type_approval.html` — 204 עדכונים)
- **א'** — 4 תשלומים ידניים בלי סוג תשלום (אתי מנשה, מונה אשקר, יהודית סואייה, רותם בדיקה)
- **ב'** — 55 תשלומים עם אמצעי=הוראת קבע (54 GROW + יהודית): אמצעי→אשראי, סוג→הוראת קבע
- **ג'** — 145 תיעודי תשלום עם אמצעי=הוראת קבע (136 GLOW רטרו + 9 יהודית): אמצעי→אשראי
- בוצע **רק לאחר אישור של סער** מהעמוד (העמוד נפתח בפריוויו ונדחף לריפו).

## ממצא פתוח חדש (נתפס בטסט)
צומת "משימת בדיקה — אין רשומת ליווי" ב-WF-27 כותב `accountid` למשימה (אובייקט 10)
ופיירברי דוחה: `Invalid field name: 'accountid'` — נכשל רק כשאין רשומת ליווי ללקוחה.
לתקן ל-`pcfaccountid` (נפרד מהעבודה הזו).

## Change-log
```
[n8n]      WF-27 עודכן (גיבוי WF27_before.json): category על 1039, אמצעי 1018 לא 7, pro-call אחרי התגובה
[n8n]      אומת בטסט חי: 3 סוגים נכונים · 12 תיעודים אשראי · תשובה 6.8s (היתה 31.9s)
[טופס]     גרסה 2 — אשף 4 מסכים · חסימת שליחה כפולה · timeout 120s · הודעת רקע במקום "נסי שוב"
[פיירברי]  רשומות טסט נוצרו ונמחקו (4×1039, 16×1018, 1×תהליך מכירה) — גיבוי מלא
[פתוח]     רשימת אישור ל-204 עדכונים רטרואקטיביים · טופס v2 להעלות לכפתור · task-fallback 'accountid' באג
```
