# סיכום סשן · 14/09/2026 — שני מסלולי תשלום, הו"ק מהבק-אופיס, ושלושה באגי שורש

## 1. שני מסלולי תשלום לכל מוצר (אשראי רגיל / הו"ק)

**פיירברי — אובייקט מוצר (14):**
- `pcfpaymenturl` → "קישור תשלום - אשראי רגיל" · `pcfpaymentpageid` → "מזהה מוצר GROW - אשראי רגיל"
- נוצרו: `pcfPaymentUrlSO` ("קישור תשלום - הו״ק") · `pcfPaymentPageIdSO` ("מזהה מוצר GROW - הו״ק")
- 3 מוצרי ההמשך — הקישור והמזהה הועתקו לשדות ההו"ק

**WF-20:** `matchProduct()` בודק שני מסלולים שווי-מעמד ומדווח באיזה נמצא.

**מסך הסליקה:** שני כפתורים; כפתור בלי קישור **לא מוצג כלל**; הנבחר מלא והשני מתוחם;
שורת "הקישור להעתקה" עם החלפה. 17 בדיקות דפדפן.

⚠️ **פתוח:** לגרור את שני השדות החדשים למסך המוצר · ליצור דפי הו"ק בגרואו ולמלא את שני
השדות · להטמיע את ה-HTML · לנקות את שדות האשראי בשלושת מוצרי ההמשך (רק אחרי ההטמעה).

## 2. הו"ק שהוקמה מתוך גרואו מפעילה את שרשרת הקמת המוצר

ענף חדש `STANDING_ORDER_BACKOFFICE`. המבחין: `paymentSource="מערכת חיצונית"` +
מזהה הו"ק שאינו מוכר לפיירברי. הפרדה של 100% על 250 ריצות אמיתיות
(30 חיובים ותיקים נשארו בחוץ). **אומת חי.**

## 3. משימת שיוך קבוצה עם הסבר הסיבה
כשהשדה "לאיזו קבוצת WhatsApp לשייך?" ריק — משימה עם שתי פעולות חובה (במערכת ובוואטסאפ),
הסבר למה זה קרה, והנחיה למניעה. נוסח ייעודי כשהמקור הוא הו"ק מגרואו. **אומת חי.**

## 4. שלושה באגי שורש — כולם במסלול "לקוחה חוזרת בלי עסקה פתוחה"

1. **התאמת מוצר לפי שם** — ספרה בודדת נזרקה, ולכן ליווי 5/6/8 שבועות מעולם לא זוהה.
   במקרה אחד נרשם **מוצר שגוי** (אורית רון → סדנה בוסט במקום ליווי 5). תוקן + אזהרת סתירה.
2. **חילוץ מזהה רשומת האב** — חיפש `customobject4id` במקום `opportunityid`.
   תוצאה: רשומות בלי שיוך, תיעוד תשלום חסר, התהליך המקצועי לא רץ. תוקן למיפוי לפי אובייקט.
3. **דילוג בגל 3** — בדיקת `objectid` שנועדה להערות הוחלה על כל יצירה, ולכן
   **תיעוד תשלום דולג תמיד** במסלול הזה. תוקן.

## 5. רגרסיה שלי — נדרשת תשומת לב בכל סשן עתידי

עותק העבודה המקומי היה ישן משלושה ימים. הפריסות שלי דרסו:
`pcfPaymentCategory` (08/09) · תיקון ה-`@ref` בצומת הסיכום (14/09) ·
**`DRY_RUN` הוחזר ל-`true` בתהליכים המקצועיים**.

**אפס לקוחות אמיתיות נפגעו** (נבדק: בחלון הפגיע רצו רק שתי ריצות של סער).
הכל שוחזר מהגיבוי החי ואומת בדיף מלא.

> **כלל לכל סשן עתידי:** לפני פריסה — למשוך את הוורקפלואו החי, לעשות דיף מול מה
> שעומדים לפרוס, ולוודא שכל הבדל הוא מכוון. הבדל שלא אתה יצרת = עצירה.
> גיבוי לפני פריסה הכרחי אבל לא מספיק — צריך גם **לקרוא** אותו.

## 6. תיקוני דאטה
- **אורית רון** — הוראת הקבע ותיעוד התשלום הועברו מ"סדנה בוסט" ל"תוכנית ליווי 5 שבועות".
- **חשבון הבדיקה של סער** — 170 רשומות נמחקו (גיבוי מלא בפרויקט), ואופס לסביבת טסט נקייה.

## 7. פתוח לטיפול

1. **הודעת וולקאם של הליווי לא מגיעה.** WF-24 עושה הכל נכון — כותב את השדות, מפעיל
   את הפלואו `f201689s4391239`, מקבל `{"status":"ok"}` — אבל `allow_send_message=false`
   (מחוץ לחלון 24 השעות). פלואו מגיע מחוץ לחלון רק אם ההודעה הראשונה בו היא **תבנית מאושרת**.
   **כל** ריצות הליווי היום היו מחוץ לחלון וקיבלו ok: מיי יששכר, דיתי פאר לוי, מירב דאלי,
   רננה בריסק. **צריך לבדוק בקלאודצ'אט אם הפלואו מתחיל בתבנית מאושרת.** הפלואו של הבוסט
   (`f201689s4391241`) שונה — אם הוא כן מתחיל בתבנית, זו התשובה.
2. **`SALE_MATCH_BY_PRODUCT` = false** — כשיש עסקה פתוחה, התשלום נתלה עליה **בלי קשר
   למוצר**. החלטה של אליאל: להשאיר או להפוך ל-`true` (מדויק יותר, יותר עסקאות).
3. **חבילת הבדיקות של WF-20 ישנה** ביחס לקוד החי — 15 בדיקות מצפות להתנהגות שבוטלה
   בכוונה (אמצעי תשלום 7, רשימות בדיקה שרוקנו). הציפיות צריכות להתעדכן לקוד החי.
   עד אז — אין להסתמך עליה כשער לפריסה.
4. **כלל עבודה לצוות רויטל:** בהקמת הו"ק מהבק-אופיס **חובה לבחור מוצר** —
   בלי תיאור אין זיהוי מוצר (אביגל קבילו, 10/09).
5. **שרה (06/09) ואורית רון (10/09)** לא קיבלו הקמת מוצר. ממתין להחלטה אם להריץ ידנית.
