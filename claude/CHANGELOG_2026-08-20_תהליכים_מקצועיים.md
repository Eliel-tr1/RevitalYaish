# יומן שינויים · הלילה שבין 19 ל-20/08/2026
## התהליכים המקצועיים + הכרעת הייבוא מגרואו

---

## חלק א' · הייבוא מגרואו — **לא בוצע, בהחלטתך**

בדקתי את שני האקסלים. הם מכסים **22/05/26 → 19/08/26** בלבד.

| | סה"כ | לקוחות שקיימים בפיירברי |
|---|---|---|
| הו"ק פעילות | 238 | **5 מתוך 226** |
| פריסות אשראי פתוחות | 330 | **15 מתוך 323** |

ייבוא היום היה יוצר 98% יתומים ומשכפל את מעבר הנתונים של 04–05/09.
**ההחלטה: מייבאים אחרי המעבר.**

### מה כן שינינו בעקבות זה

**🔴 הריפוי העצמי של הוראות קבע כובה.** קודם, כל חיוב הו"ק שהגיע מ-238 ההוראות
הפעילות היה יוצר רשומת הוראת קבע רטרואקטיבית — כ-120 רשומות עד 06/09, שכולן היו
משוכפלות במיגרציה. עכשיו: **משימה בלבד, אפס רשומות כספיות.**

המשימה נקראת `[להשלמה אחרי המעבר]` ונושאת את כל מה שצריך כדי לשייך אחר כך:
טלפון · סכום · **4 ספרות הכרטיס** · אסמכתא · מזהה הו"ק · מוצר.

> ⚠️ **להדליק בחזרה** את `SELF_HEAL_STANDING_ORDER` אחרי שהמיגרציה הושלמה ואומתה.

### שלושה ממצאים מהאקסלים ששווים לתכנון המיגרציה

1. **בפריסת אשראי האסמכתא זהה בכל התשלומים.** אסמכתא `452030787` חוזרת בתשלום 7, 8 ו-9.
   ⇒ המפתח `GROW-{אסמכתא}` שבנינו נכון, וכל חיוב המשך יימצא.
2. **בהוראת קבע האסמכתא מתחלפת כל חודש.** המפתח היציב היחיד הוא `directDebitId` —
   **והוא לא קיים באקסל.** אבל: **טלפון + סכום חודשי + 4 ספרות הכרטיס מזהים כל אחת
   מ-238 ההוראות באופן חד-ערכי — אפס התנגשויות** (נבדק על כל 12 הלקוחות עם שתי הוראות).
   ⇒ אחרי המיגרציה, החיוב הראשון יאמץ את ה-`directDebitId` אוטומטית ומשם הכל לפי ID.
3. **כל תשלום בפריסה מקבל חשבונית נפרדת.** מסביר חלק גדול מהמשימות "חשבונית ללא תשלום".

---

## חלק ב' · חמישה תהליכים חדשים

| # | תהליך | תפקיד |
|---|---|---|
| WF-22 | ראוטר תהליכים מקצועיים | מסווג לפי המוצר ומעביר הלאה |
| WF-23 | סדנת בוסט | ליווי + מחזור + קבוצה + הקצאת תפריט |
| WF-24 | תוכנית ליווי | ליווי + קבוצה + משימת תפריט |
| WF-25 | תוכנית המשך | ליווי המשך בלבד |
| WF-26 | צירוף לקבוצת וואטסאפ | היחיד שמחזיק את מפתח GreenAPI |

**WF-20 עודכן ל-24 צמתים** ומפעיל את WF-22 בסיום רכישה חדשה בלבד.

**בדיקות:** 69 חדשות + 190 בתשלום = **259, כולן עוברות.**

---

## חלק ג' · שינויים בחשבון של רויטל

### נוצר
- **3 שדות משתמש בקלאודצ'אט:** `wa_group_invite_link` · `purchased_product_name` · `program_weeks`

### שוחזר אחרי הבדיקות
- רשומת הליווי של סער וינברג — הוחזרה למצבה המקורי (פרימיום · שבוע 11 · תחילה 07/06)
- "מכירת תוכנית המשך - סער וינברג" — הוחזרה לסטטוס "ליד חדש"

### 🔻 מסומן למחיקה ידנית
- `[בדיקה — למחיקה]` — תשלום, תיעוד תשלום, הקצאת תפריט ומשימה מבדיקות הלילה
- מהסבב הקודם: 5 רשומות הוראת קבע כפולות + רשומת ה-PROBE

**סוכן אוטומטי אינו מוחק. המחיקה בידיים שלך.**

---

## חלק ד' · שלושה דברים שדורשים ידיים

| # | מה | למה זה חוסם |
|---|---|---|
| 1 | **🔴 GreenAPI במצב `expired`** | אף לקוחה לא תצורף אוטומטית לקבוצה עד סריקת QR מחדש |
| 2 | **חיבור שני פלואוז הוולקאם לשדות** בממשק קלאודצ'אט | ההודעה תצא בלי קישור הקבוצה ובלי שם המוצר. אין API לעריכת פלואו — נפתחה משימה [86cb7qq4a](https://app.clickup.com/t/86cb7qq4a) |
| 3 | **השלמת "קבוצת וואטסאפ" בכרטיסי המוצרים** | **בענף הליווי בלבד.** בבוסט הקבוצה נלקחת ממחזור הבוסט ולא חסר כלום |

> ✏️ **תיקון לניסוח בגרסה הקודמת:** כתבתי "לאף מוצר אין קבוצת וואטסאפ" בלי לסייג
> שזה נוגע **רק לענף הליווי**. בענף הבוסט הקבוצה מגיעה ממחזור הבוסט, וכל 22
> המחזורים מ-09/08/26 ועד 27/12/26 מקושרים לקבוצה עם מזהה GreenAPI וקישור הזמנה.
> **אין שם שום פער.**

בנוסף — **כל 30 הקבוצות רשומות על "ויטרו ראשי" כמדריכה.** המנגנון נכון, אבל עד
ששיוך המדריכות יעודכן, זה מה שייכתב בכל רשומת ליווי.

---

## חלק ה' · המצב בבוקר

```
🔒 DRY_RUN לכל הלקוחות · לייב בפועל רק לסער וינברג
   אפס הודעות · אפס צירופים לקבוצות · אפס כתיבה — לכל השאר
```

**המתג מחכה לך.** להדלקה מלאה: `DRY_RUN: false` בצומת "קליטה וסיווג" בשלושת
התהליכים הענפיים.

⚠️ **המלצה: לא להדליק לפני שסעיפים 1–3 בחלק ד' סגורים** — אחרת לקוחות יקבלו
הודעת וולקאם בלי קישור לקבוצה, ולא יצורפו אליה.

---

# עדכון · 20/08/2026 · ניקוי מלא של חשבון הבדיקה

לבקשת אליאל — **כל הנתונים של סער וינברג נמחקו מפיירברי** כדי לאפשר טסט מאפס.

## מה נמחק — 74 רשומות

| אובייקט | כמות |
|---|---|
| תיעוד תשלום (1018) | 22 |
| תשלום (1039) | 14 |
| מסמך (1038) | 8 |
| משימה (10) | 8 |
| הקצאת תפריט (1059) | 7 |
| טופס שקילה (1069) | 6 |
| תהליך מכירה (4) | 3 |
| פגישה (6) | 3 |
| תהליך ליווי (1051) | 1 |
| רישום שיווקי (1002) | 1 |
| **לקוח (1)** | **1** |

המחיקה בוצעה בסדר תלויות — בנים לפני אבות — ואומתה: **אפס רשומות נותרו**,
גם בסריקת טקסט לפי שם ולפי טלפון.

**גיבוי מלא נשמר לפני המחיקה:** `BACKUP_saar_full.json` (כל 74 הרשומות, שדה-שדה).

> ⚠️ **נמחקה גם דאטת ההדגמה** — טפסי שקילה, פגישות ושבוע 11 בתהליך הליווי.
> אם היא נחוצה למצגת, היא ניתנת לשחזור מהגיבוי.

**לא נמחק:** המנוי בקלאודצ'אט (`f201689u860230363`) — הוא נדרש לבדיקת הוולקאם.

## שינוי שנגזר מהמחיקה

מזהה הלקוחה של סער כבר לא קיים, ולכן נוסף ל-WF-20 זיהוי חשבון בדיקה
**לפי שם הלקוחה** (`LIVE_TEST_ACCOUNT_NAMES`).

**למה זה נדרש:** וובהוק החשבונית אינו נושא טלפון כלל, ומזוהה רק דרך הלקוחה
שברשומת התשלום. בלי זיהוי לפי שם, החשבונית הייתה נופלת ל-DRY_RUN
והמסמך החשבונאי לא היה נוצר בטסט.

**בדיקות:** 192 ב-WF-20 + 69 בתהליכים המקצועיים — כולן עוברות.

---

# 🔴 תקלה · 20/08/2026 · חשבונית של לקוחה אחת שויכה ללקוחה אחרת

## מה קרה

| שעה | אירוע |
|---|---|
| 08:32 | **מיטל** (0549468700) רכשה סדנת בוסט ב-159 ₪. נוצרו לקוחה, מכירה, תשלום ותיעוד תשלום |
| 09:38 | **ליאת גבאי** (0524317412) — חיוב הוראת קבע, 1,238 ₪, ליווי 17 שבועות. הריפוי העצמי כבוי ⇒ **לא נוצרה לה רשומת תשלום**, רק משימה |
| 09:40 | **חשבונית 49676 של ליאת** התקבלה. לא נמצא לה תשלום תואם ⇒ מנגנון "שיוך לפי קרבת זמן" בחר את **התשלום של מיטל** — המועמד היחיד — וצירף אליו את המסמך |

**התוצאה:** חשבונית מס/קבלה על 1,238 ₪ של ליאת ישבה על כרטיס הבוסט של מיטל,
ומספר המסמך נכתב על תיעוד התשלום שלה.

## שורש הבעיה — באג בחישוב חלון הזמן

```js
const newest  = max(createdon של כל התשלומים האחרונים);
const inWindow = payments.filter(p => newest - p.createdon <= 15 דקות);
```

**החלון נמדד מהתשלום החדש ביותר ברשימה — ולא מרגע קבלת החשבונית.**
מכיוון שהתשלום של מיטל היה הכי חדש, המרחק שלו מ-`newest` היה **אפס**,
והוא נחשב "בתוך החלון" — למרות שעברו **68 דקות** מאז שנוצר.

**מכפיל הנזק:** כיבוי הריפוי העצמי אמש (בהחלטה נכונה לגבי המיגרציה) הפסיק ליצור
רשומות תשלום לחיובי הו"ק ותיקות. לכן חשבוניות של אותן לקוחות מגיעות ללא תשלום
תואם — ונופלות ישר לזרוע הניחוש. **את הצירוף הזה לא צפיתי.**

## מה תוקן

**בנתונים:**
- מספר המסמך 49676 נוקה מתיעוד התשלום של מיטל
- המסמך נותק ממיטל, הוצמד ל**ליאת גבאי** ללא קישור לתשלום/מכירה
- נפתחה משימה על ליאת להשלמת השיוך אחרי המיגרציה

**בקוד — שתי שכבות:**

1. **`INVOICE_FALLBACK_CANDIDATES: 0`** — שיוך מסמך חשבונאי לפי קרבת זמן **כבוי**.
   חשבונית ללא התאמת מזהה ⇒ **משימה בלבד, אפס מסמכים.**
   זו בדיוק ההחלטה שקיבלנו אתמול לגבי פירעונות: שיוך לפי מזהה בלבד.
   ⚠️ ה-payload של וובהוק החשבונית מכיל **רק** `transactionCode`, `invoiceNumber` ו-`invoiceUrl` —
   אין בו סכום ואין בו טלפון, ולכן אין שום דרך לאמת התאמה. ניחוש הוא הדבר היחיד שנשאר, ולכן הוא נפסל.

2. **תוקן הבאג עצמו** — החלון נמדד עכשיו מרגע קבלת החשבונית. גם אם המנגנון יודלק
   שוב בעתיד, התרחיש הזה לא יחזור. נוספה בדיקת רגרסיה על 68 הדקות המדויקות.

## שני המסמכים האחרים שנוצרו באותו מסלול — נבדקו ותקינים

| מסמך | פער מהתשלום | סכום | מסקנה |
|---|---|---|---|
| 49674 · רבקה | דקה אחת | 159 ₪ בוסט | ✅ |
| 49669 · ולרי סיגאוי | 13 דקות | 159 ₪ בוסט | ✅ |

רק 49676 היה החריג — 68 דקות.

**בדיקות:** 192 — כולן עוברות.

---

# 🔒 20/08/2026 · שיוך לפי מזהה חד-ערכי בלבד — הכלל הפך למוחלט

בהוראת אליאל, אחרי תקלת חשבונית 49676: **המנגנון לא כובה — הוא הוסר מהקוד.**

> שיוך של תשלום או של מסמך חשבונאי נעשה אך ורק לפי מזהה חד-ערכי.
> אין חלון זמן · אין "המועמד היחיד" · אין "האחרון שנוצר" · אין ניחוש לפי סכום.

## שלושה מנגנוני ניחוש שהוסרו

| # | מה היה | מה קרה בפועל |
|---|---|---|
| 1 | **שיוך חשבונית לפי קרבת זמן** — "התשלום האחרון שחסר לו מסמך" | חשבונית של ליאת (1,238 ₪) ישבה על התשלום של מיטל (159 ₪) |
| 2 | **זיהוי הלקוחה מ"התשלום האחרון"** — נפילה שלישית ב-`ACC_EXPR` | אותו סוג ניחוש בדיוק, רק בשכבת הלקוחה. הוסר יחד עם שאילתת "התשלומים האחרונים" |
| 3 | **"תיעוד ממתין יחיד ללא מזהה"** בפירעון פריסה | תיעוד בלי מזהה תשלום היה מתעדכן רק כי היה יחיד |

**גם המתגים הוסרו** — `INVOICE_FALLBACK_CANDIDATES`, `INVOICE_FALLBACK_WINDOW_MINUTES`
ו-`INSTALLMENT_MATCH_REQUIRE_ID` אינם קיימים יותר. אין מה להדליק בחזרה.

## מה קורה עכשיו כשאין מזהה תואם

| מצב | תוצאה |
|---|---|
| חשבונית ללא תשלום תואם | `INVOICE_NO_MATCH` — **משימה בלבד, אפס מסמכים** |
| חיוב ללא רשומת תשלום | `*_NO_SOURCE` — משימה בלבד |
| תיעוד ממתין ללא מזהה תשלום | `INSTALLMENT_AMBIGUOUS` — משימה בלבד, אפס עדכונים |

## בדיקות

נוספו בדיקות שמוודאות שהקוד **לא מכיל** את המנגנונים — לא רק שהם כבויים:
חיפוש טקסטואלי אחרי `INVOICE_FALLBACK`, `recentPays` ו-`INSTALLMENT_MATCH_REQUIRE_ID`
בקוד ה-workflow הבנוי, ובדיקה שהצומת "תשלומים אחרונים (1039)" הוסר.

**192 בדיקות — כולן עוברות. WF-20: 23 צמתים, פרוס.**

## אומת חי

| בדיקה | תוצאה |
|---|---|
| חשבונית עם מזהה שאינו קיים | `INVOICE_NO_MATCH` · משימה אחת · **אפס מסמכים** ✅ |
| תשלום שנמצאה לו לקוחה לפי טלפון | נקלט תקין ✅ |

רשומות בדיקת העשן נמחקו.

---

# 🔄 20/08/2026 · שני שינויי חוקיות מהלקוחה — ענף הליווי

## שינוי 1 · הקבוצה מגיעה מתהליך המכירה, לא מהמוצר

בענף **הליווי** קבוצת הוואטסאפ נקבעת **ידנית בתהליך המכירה**, בשדה
"לאיזו קבוצת WhatsApp לשייך?" (`pcfWhatsAppToRef`).

| ענף | מקור הקבוצה |
|---|---|
| סדנת בוסט | מרשומת **מחזור הבוסט** (ללא שינוי) |
| **תוכנית ליווי** | **מתהליך המכירה — שיוך ידני** |
| תוכנית המשך | אין קבוצה |

**מה הוסר:** הקבוצה מכרטיס המוצר, **וגם הבחירה האוטומטית** של קבוצת פרימיום
פנויה. בחירת קבוצה בשביל הלקוחה היא ניחוש — ולפי הכלל שנקבע היום, ניחוש נפסל.
שדה ריק ⇒ אין שיוך, אין צירוף, נפתחת משימה.

**וגם:** התהליך **אינו כותב יותר** לשדה הזה בענף הליווי. הוא קלט ידני של הצוות,
ודריסה שלו הייתה מוחקת בחירה מכוונת.

## שינוי 2 · המדריכה מגיעה מהמוצר שנרכש

| ענף | מקור המדריכה |
|---|---|
| **תוכנית ליווי** | **מנהל המוצר בכרטיס המוצר שנרכש** |
| סדנת בוסט | מדריכת הקבוצה של המחזור (ללא שינוי) |
| תוכנית המשך | ללא דרישה |

אין מדריכה במוצר ⇒ נציג המכירות מתהליך המכירה, עם אזהרה.

> ⚠️ כרגע **בכל המוצרים** "מנהל מוצר" = "ויטרו ראשי". המנגנון נכון ויעבוד ברגע
> שישויכו מדריכות אמיתיות — אבל עד אז זה מה שייכתב בכל רשומת ליווי.

## שני מסכי הסליקה — בקרה בולטת לפני חיוב

נוספה בקרה בשני המסכים, בדיוק לפני החיוב:

**כשיש קבוצה** — באנר ירוק עם **שם הקבוצה בגדול**, ולצידו מספר הנרשמות,
המקומות הפנויים וסטטוס הקבוצה. הטקסט אומר במפורש שזו הקבוצה שהלקוחה תצורף אליה.

**כשהשדה ריק** — באנר אדום: "לא שויכה קבוצת וואטסאפ", ומתחתיו הסבר שהלקוחה
לא תצורף לאף קבוצה ולא תקבל קישור הצטרפות.

בשני המצבים יש כפתור **"עדכנתי — רענון"** שטוען מחדש את העסקה ואת הקבוצה בלי
לצאת מהמסך.

**הבאנר מודע למוצר:** בסדנת בוסט הוא אומר שהקבוצה נקבעת ממחזור הבוסט ואין צורך
למלא; בתוכנית המשך — שלא נדרשת קבוצה. במסך התשלום שאינו אשראי הוא מתעדכן גם
כשמשנים את המוצר בבחירה "רכישה של מוצר חדש".

בנוסף, ה-payload של הטופס הידני נושא עכשיו גם `whatsapp_group_name`
ו-`whatsapp_group_source` — לתיעוד ולבקרה בצד השרת.

**בדיקות:** 74 בתהליכים המקצועיים — כולן עוברות. WF-24 פרוס.

---

# 🆕 20/08/2026 · WF-27 · הטופס הידני מחובר למנגנון התשלומים

הטופס "קליטת תשלום שאינו באשראי" כבר לא מסתיים ב-payload בקונסול. הוא שולח
ל-**WF-27** (`LhstekfLJ0AClk5i`, 16 צמתים, פעיל), שמבצע בדיוק את אותה חוקיות
כמו WF-20 ואז מפעיל את התהליך המקצועי.

```
טופס ידני ──► WF-27 ──► תשלום (1039) + תיעודי תשלום (1018) + הערות
                 └──► WF-22 — רק אם "רכישה של מוצר חדש" = כן
```

| כלל | פירוט |
|---|---|
| רשומת תשלום **לכל אמצעי תשלום** | מזומן + שיקים ⇒ שתי רשומות נפרדות |
| תיעוד לכל חיוב | הפרש חודש, תאריך מפורש לכל אחד |
| סטטוס | תאריך שעבר ⇒ נפרע · עתידי ⇒ ממתין לפירעון |
| התהליך המקצועי | רק כשסומן "רכישה של מוצר חדש", **וגם** הקליטה הצליחה |
| אידמפוטנטיות | מזהה שליחה לכל טעינת עמוד — לחיצה כפולה ⇒ `DUPLICATE` |

## 🔴 מלכודת: `/api/v3/query` תומך ב-`eq` בלבד

`like` / `contains` / `startwith` מחזירים **400**, ועם `neverError: true` זה נראה
כמו "לא נמצאה כפילות". **נתפס בריצה חיה — שליחה כפולה יצרה רשומות פעמיים.**
שתי השאילתות עברו לסינון לפי `pcfSale` והשוואה בקוד. יש בדיקה שמוודאת
שאין `like` בשום שאילתה.

## הבאנר בטופס הידני — רק לפי הבחירה בטופס

לבקשת אליאל, הבקרה נגזרת **אך ורק** מ"רכישה של מוצר חדש" ומהמוצר שנבחר שם —
לא מהמוצר שמקושר לעסקה:

| מצב בטופס | מה מוצג |
|---|---|
| "לא" | הלקוחה לא תשויך לאף קבוצה ולא ייווצר תהליך ליווי |
| "כן" בלי מוצר | 🔴 טרם נבחר מוצר — לא ניתן לדעת לאיזו קבוצה |
| "כן" + מוצר ליווי | הקבוצה מהעסקה — **שם הקבוצה בגדול** או התראה אדומה אם ריק |
| "כן" + בוסט | הקבוצה נקבעת ממחזור הבוסט |
| "כן" + תוכנית המשך | לא נדרשת קבוצה |

הטופס גם **חוסם שליחה** כשסומן "כן" ולא נבחר מוצר.
