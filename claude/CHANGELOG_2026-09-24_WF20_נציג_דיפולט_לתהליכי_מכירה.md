# יומן שינויים · 24/09/2026 — WF-20 · נציג דיפולט לשיוך תהליכי מכירה

## הבעיה
תהליכי מכירה של בוסט נפתחו **בלי נציג מכירות משויך** (דוגמה: "העסקה של לינדה שם טוב").

## שורש
כשלקוחה חדשה רוכשת בוסט בגרואו, WF-20 יוצר באותה ריצה גם את רשומת הלקוחה וגם את תהליך המכירה.
יצירת הלקוחה החדשה (תיקון 14/09) לא כותבת נציג, ואז יצירת העסקה קוראת את הנציג מהלקוחה —
שנייה אחרי שהלקוחה נוצרה בלי נציג ⇒ עסקה בלי נציג.

## ההגדרה (סער, 24/09)
נציג דיפולט נשלף מאובייקט ההגדרות (1036) · רשומה `8eff841e-a61f-461e-b9cc-44e7c6593941` ·
שדה **"משתמש לשיוך תהליכי מכירה"** (`pcfSalesManger`) — כרגע לימור לוי.

## מה שונה ב-WF-20 (פרוס, פעיל)
1. **צומת חדש "הגדרות מערכת (1036)"** — שולף את נציג הדיפולט מהרשומה; שולב בשרשרת
   בין "תיעוד המקור לפי מספר מסמך (1018)" ו"חיפוש תהליך מכירה (4)". כל הצמתים
   בהמשך קוראים לפי שם (`$('...')`) ולכן ההוספה שקופה.
2. **יצירת עסקה חדשה:** `ownerid = נציג הלקוחה || נציג הדיפולט מההגדרות`.
3. **עסקה קיימת שנבחרה בלי נציג:** פעולת עדכון חדשה (`saleOwner`) משייכת את נציג
   הדיפולט. עסקה שיש לה נציג — **לא נוגעים בה.**

## אומת
- קוד ה-LOGIC נטען תקין ב-Node (syntax OK).
- טסט יחידה של הלוגיקה: עסקה בלי נציג ⇒ עודכן לדיפולט · עסקה עם נציג ⇒ נוגע ·
  יצירה חדשה עם נציג לקוחה ריק ⇒ דיפולט.
- אימות חי בפיירברי יתבצע בתשלום בוסט אמיתי הבא (בקשת סער).

## תוצאות קיימות (פתוח לאישור)
7 תהליכי מכירה בוסט בלי נציג מ-22–24/09 (הכל מאותה תקופה):
עמית בן גיגי · שרית · שלומית · הילה ארזי · ויטל זוארץ-פליישר · Hila Shmuely · לינדה שם טוב.
רשימה עם קישורים: `_reconciliation/sales_without_owner.json`.
התיקון הרטרואקטיבי (שיוך נציג דיפולט לכולן) **ממתין לאישור סער**.

## נקודה פתוחה (לא טופל — מחוץ להיקף)
יצירת לקוחה חדשה ב-WF-20 עדיין לא כותבה נציג; פיירברי משייך בהמשך (טריגר פנימי/ידני).
אפשר להוסיף את נציג הדיפולט גם ביצירת הלקוחה — הכרעת סער.

## גיבויים
`claude/BACKUP_2026-09-22_WF20/WF20_before.json` (לפני) · `WF20_after_n8n.json` (אחרי)

## אימות חי +3 שעות
מתוזמן ל-24/09 15:21 (cron job `cd5ce6ecdb1a`): קריאת כל תהליכי המכירה שנוצרו מאז הפריסה,
בדיקת נציג מאוכלס לכולם, בדיקת ריצות WF-20, ובדיקת סוגי תשלום בטופס הידני אם היו.
תוצאה תתעדכן כאן.

## Change-log
```
[n8n]      WF-20 + צומת "הגדרות מערכת (1036)" · שרשרת עודכנה
[n8n]      יצירת עסקה: owner = נציג לקוחה || דיפולט מההגדרות
[n8n]      עסקה קיימת בלי נציג ⇒ עדכון לדיפולט (עסקה עם נציג — לא נוגעים)
[פיירברי]  רטרו 24/09 (אושר סער): 7 עסקאות בוסט ללא נציג ⇒ שויכו ללימור לוי; אומת 7/7 בקריאה חוזרת
[גיבוי]    claude/BACKUP_2026-09-24_7_ownerless_sales.json (רשומות מלאות לפני העדכון)
[פתוח]     יצירת לקוחה חדשה ב-WF-20 עדיין בלי owner — להכרעה
[מתוזמן]   אימות חי +3 שעות (cron cd5ce6ecdb1a)
```
