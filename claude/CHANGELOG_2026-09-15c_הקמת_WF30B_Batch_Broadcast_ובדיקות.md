# CHANGELOG — הקמת WF-30B (מקביל ל-WF-30, Batch+Broadcast) + בדיקות ראשוניות

**תאריך:** 2026-09-15, 22:00-22:20 (ערב, לפני וובינר 16.9)
**סטטוס:** ✅ נבנה ונבדק בהצלחה. ⏸️ **נשאר inactive** — לא מתוזמן, לא רץ אוטומטית.

---

## מה נבנה

Workflow חדש ונפרד לגמרי ב-n8n:
- **שם:** `WF-30B · תזכורות וובינר (Batch+Broadcast, מקביל) [TEST_MODE]`
- **n8n ID:** `8vIR2OCJiGhxeHFA`
- **מצב:** `active: false` (מאומת)
- **34 nodes**, לפי המפרט שאושר ב-`TOCHNIT_2026-09-15_תהליך_מקביל_תזכורות_וובינר_Batch_Broadcast.md`

מבנה: 4 טריגרים (schedule הכנה T-60, schedule שליחה, הרצה ידנית, webhook בדיקה `wf30b-test`) → קונפיג משותף → פיצול הכנה/שליחה → ענף הכנה (שאילתות פיירברי זהות ל-WF-30, בניית רשימה, בדיקת/יצירת תג ייעודי לסבב, `create-many` ב-batches של עד 100 עם תיוג) → ענף שליחה (וידוא מחזור, איתור תג, `broadcast-by-tag`).

**בטיחות מובנית:** `TEST_MODE:true` קבוע בקוד (לא ניתן לשינוי דרך קלט חיצוני) — כל עוד הוא דלוק, רשימת הנמענים נכפית להיות אך ורק מנוי הבדיקה (0533435240), ללא קשר לתוצאת פיירברי בפועל.

## מה נבדק (על מנוי הבדיקה בלבד — 0533435240 / "סער וינברג")

הרצות דרך webhook הבדיקה, עם המחזור **האמיתי** "בוסט 13-20.9.26" (כדי לוודא שהלוגיקה מוצאת נכון את המחזור בפועל), שלב `live`:

1. **הכנה (Prep):** מצא את המחזור האמיתי, בנה רשימת השתתפות אמיתית מפיירברי — **ואז כפה** אותה לנמענת בדיקה אחת בלבד (מאומת: `totalRecipients:1`). יצר תג חדש `תזכורת_וובינר_live_130926_test` (`tag_ns: f201689t3140039`) ותייג את מנוי הבדיקה דרכו `create-many` (`updated:1`).
2. **שליחה (Send):** מצא את אותו מחזור, חישב את אותו שם תג, איתר את ה-`tag_ns`, קרא ל-`broadcast-by-tag` עם ה-flow האמיתי של שלב live (`f201689s2623973`). **תוצאה: `num_users:1`** — כלומר ה-broadcast היה ממוקד באופן מדויק לנמענת הבדיקה בלבד, לא נגע באף אחת מ-56 המשתתפות האמיתיות.
3. **וידוא מסירה:** `GET /subscriber/broadcasts` הראה `users_count:1, sent_count:1, failed_count:0` — ההודעה אכן נשלחה ונמסרה בהצלחה, אך ורק למספר הבדיקה.

**באג שנמצא ותוקן תוך כדי בדיקה:** צומת הסיכום של שלב ההכנה (P13) קרא בטעות שדה לא נכון מתשובת `create-many` והציג 0/0 גם כשההרשמה הצליחה בפועל (הבעיה הייתה קוסמטית בלבד — בדוח הסיכום, לא בביצוע עצמו). תוקן ונבדק שוב בהצלחה (`updated:1` מוצג נכון כעת).

## מצב נוכחי

- ה-workflow **inactive** — אין תזמון פעיל, לא ירוץ אוטומטית על שום מחזור.
- ה-webhook `wf30b-test` פעיל רק כשה-workflow מופעל זמנית (נבדק ידנית דרך API, כובה מיד בסיום כל בדיקה).
- WF-30 / WF-29 הקיימים — **לא נגעתי בהם כלל**. הוובינר של מחר (20:00) ממשיך להסתמך אך ורק עליהם.
- נותר תג בדיקה אחד בחשבון קלאודצ'אט: `תזכורת_וובינר_live_130926_test` (מסומן `_test`, לא מתנגש עם שום תג עתידי).

## מה נשאר לפני הפעלה על מחזור אמיתי

1. אישור מייק שרואה את הדוח הזה, כולל אישור שהתוצאה (num_users:1, sent_count:1) משכנעת.
2. החלטה מתי להעביר את `TEST_MODE` ל-`false` ולתזמן את הטריגרים בפועל — **לא לפני שהוובינר של 16.9 יסתיים**, ורק על מחזור עתידי, בתיאום מראש.
3. אופציונלי, לא חוסם: ניקוי תג הבדיקה, ו/או הוספת ניטור אוטומטי על `failed_count` מה-broadcasts (הוזכר בתוכנית המקורית כשיפור עתידי).
