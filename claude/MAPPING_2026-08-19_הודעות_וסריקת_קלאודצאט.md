# מיפוי ההודעות · ממצאי סריקת קלאודצ'אט — 19.8.2026

## 1. ארכיטקטורת השליחה שכבר קיימת

**מנוע השליחה: `WF-06B · שליחת תפריטים מתוזמנת`** (`WF06BMenuSend01`, פעיל, 39 צמתים).
זה **השלד לשכפול לכל הודעה חדשה**. הוא כולל:
נרמול טלפון · `get-info-by-user-id` עם fallback ל־`subscribers?phone` · **בדיקת חלון 24 שעות** · `set-user-fields-by-name` · אימות שהשדות נקלטו · `send-sub-flow` · אימות · חתימה בפיירברי · הערות ברשומה · דוח ריצה.

**אין צורך לבנות שולח לכל הודעה.** משכפלים, מחליפים טריגר, שדות ומזהה פלואו.

---

## 2. הבוט בקלאודצ'אט

| | |
|---|---|
| Bot ns | `f201689` · שם "רויטל יעיש" · סוג `whatsapp_cloud` |
| מספר מקושר | 972552639552 |
| שדות בוט (גלובליים) | 218 |
| שדות מנוי (user fields) | 354 |

⚠️ **הבוט הוא תבנית הצ'טבוט הגנרית של ויטרו**, לא בוט ייעודי לרויטל: `bot_name = טלי`, `admin = אליאל טרבולסקי`, `company_name_en = Vitrue`, מודול AI, פלואוז של lead magnet. רויטל "הולבשה" עליו.

### 2א. פלואוז קיימים שרלוונטיים למיפוי

| מזהה | שם השדה בבוט | ההודעה במפה |
|---|---|---|
| `f201689s403387` | Client_Notification_meeting_reminder_1day | **C1** תזכורת פגישה יום לפני |
| `f201689s403399` | Client_Notification_meeting_reminder_Sameday | **C2** תזכורת ביום הפגישה |
| `f201689s403395` | Client_Notification_new_meeting | **C4** זימון לפגישה |
| `f201689s403397` | Client_Notification_new_payment | **F2** אישור תשלום |
| `f201689s4391449` | menu_sending (בשימוש ב-WF-06B) | **D4** שליחת תפריט |
| `f201689s4391239` | ליווי (אישר מייק) | **A2 / E1** |

**המשמעות:** ארבע הודעות שסומנו "חדשות" הן למעשה **קיימות מבנית** ודורשות רק ניסוח מחדש בקול של רויטל, לא בנייה מאפס. הן הועברו ל"חלקי".

⚠️ `menu_flow_id = f201689s2100069` הוא **תפריט השיחה של הבוט**, לא תפריט התזונה. אין כפילות.

בנוסף קיימים 34 פלואוז של התבנית הגנרית (התראות לנציג, לידים, פידבק, שפות). לא רלוונטיים ללקוחות רויטל.

### 2ב. שדות מנוי קיימים — לשימוש חוזר, לא ליצור מחדש

**גילוי חשוב: קיימים `data_param1` עד `data_param10`.** עשרה סלוטים, לא ארבעה כפי שתועד. זה פותר את החוסם של הסלוט החסר בתבנית האורח השבועי.

| שדה | מזהה | שימוש |
|---|---|---|
| `data_param1..10` | `f201689v440967` ואילך | פרמטרים דינמיים לכל תבנית |
| `diet_menu_name` | `f201689v15923499` | שם התפריט |
| `diet_menu_link` | `f201689v15923501` | לינק לתפריט |
| `diet_menu_doc_id` | `f201689v11517935` | מזהה מסמך |
| `sent_menu` | `f201689v15923503` | דגל נשלח |
| `document_link_after_sign` | `f201689v15852771` | חתימה בטופסי |
| `registred_to_webinar` | `f201689v8099241` | דגל הרשמה לוובינר |
| `meeting_date/time/url/status/type` | `f201689v5937xx` | פגישות |
| `cancel_meeting_url` · `reschedule_meeting_url` | `f201689v593857/739` | ניהול פגישה |
| `payment_url` · `payment_deal_number` · `payments_count` | `f201689v593769/773/763` | תשלומים |
| `whatsapp_group` | `f201689v8442407` | קבוצה |

### 2ג. מגבלת ה-API

`/flows` ו-`/templates` מחזירים בוטים ותבניות web בלבד. **אין endpoint ציבורי שמחזיר את רשימת הסאב-פלואוז או את תבניות ה-Meta.** 40 המזהים לעיל חולצו מערכי שדות הבוט. תבניות שטל יצר ולא קושרו לשדה בוט אינן ניתנות לגילוי דרך ה-API, ודורשות בדיקה ידנית בקונסולה.

---

## 3. כללי כתיבה שנקבעו (מייק, 19.8)

1. **לעולם בלי מקף ארוך** בשום הודעה. 18 מתוך 30 הנוסחים הופרו והם נכתבו מחדש.
2. **בכל הודעה משמעותית מצורפת תמונה** בשפת המותג. אם אין קובץ קיים, מוצע בריף.
3. בהודעת הבוסט מצורף **PDF: המדריך למצטרפות חדשות** (יתקבל בהמשך; להגיש את התבנית עם placeholder גנרי).

---

## 4. שפת הבאנרים

**מקור:** שתי גרפיקות קיימות ומאושרות — *"התפריט שלך הגיע!"* ו-*"שנחתום?"*

```
יחס 16:9 · רקע גרדיאנט מג'נטה עמוק לבורדו
קשת עגולה דקה ורודה מאחורי הדמות · אשכולות נקודות בשתי פינות
לוגו Revital Yaish Fitness&Health בלבן, פינה שמאלית עליונה
רויטל בגזירה בצד ימין עם הילת אור ורודה רכה
כותרת ענקית לבנה מודגשת, מיושרת לימין
תת-כותרת קטנה עם קו מכחול ורוד מתחתיה
צ'יפים עגולים תחתונים (אופציונלי)
```

**הפקה:** Flora · מודל `is2i-gpt-image-2` · הלוגו מוזן כתמונת רפרנס.
**קרדיטים:** ל-workspace של ויטרו (`ws_qd79kdg2gvhsj8hs09h0gk35j17v8sr4`) **נגמרו הקרדיטים**. ההפקה רצה מ-`ws_qd754tkbqe72r2qvqczdmwvvc57v9krc`.
**עלות:** כ-0.5 קרדיט לבאנר.

✅ **העברית יוצאת מושלמת** במודל הזה. איות, כיוון וטיפוגרפיה נקיים. לא מובן מאליו.
✅ **הלוגו יוצא מדויק** כשמזינים אותו כרפרנס.
🔴 **הפנים אינן של רויטל.** נדרש קובץ קאט-אאוט שלה. המלצה: להעלות ל-`ai.vitrue.co.il/revital/` כמו הלוגו.

---

## 5. סטטוס המפה

31 הודעות · 7 קיימות · 10 חלקיות · 14 חדשות · 15 עם בריף תמונה.
ארטיפקט: `roital-message-map` (גלריית הארטיפקטים של מייק).

---

## 6. תהליך העבודה שסוכם

1. מייק מאשר נוסחים במפה
2. קלוד מגיש את התבניות המאושרות לאישור וואטסאפ בקלאודצ'אט
3. מייק בונה פלואו לכל תבנית ומוסר את מזהי הפלואו
4. קלוד בונה את האוטומציות שמזינות את השדות ויורות את הפלואו

---

## 7. פתוח

- **קאט-אאוט של רויטל** — חוסם הפקת באנרים סופיים
- **PDF למצטרפות חדשות** — לא חוסם הגשת תבנית
- **קרדיטים בפלורה** ב-workspace של ויטרו
- **רשימת הסאב-פלואוז המלאה מטל** — לא ניתנת לגילוי דרך ה-API
- ⚠️ **מפתח ה-API של קלאודצ'אט נמסר בצ'אט** · לרוטציה בסיום
