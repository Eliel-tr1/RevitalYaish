# CHANGELOG · 10/09/2026 — יישום 7 התיקונים + טסט שני (דנה שמר ישראלי)

**סטטוס: 76/76 ✅ PASS.** הרצה נקייה מאפס על לקוחה חדשה, כולל התאוששות מקריסה באמצע ריצה — בלי כפילות אחת.

---

## א. שבעת התיקונים — מה נעשה בפועל

| # | מה ביקשת | מה מומש | איך נבדק |
|---|---|---|---|
| 1 | הערות: מי כתב + שעה מדויקת | כותרת `[מהמערכת הישנה · <שם המשתמש> · dd/mm/yyyy HH:MM]` בראש כל הערה. השם נגזר ממפת 9 המשתמשים הישנים | ✅ `<span>[מהמערכת הישנה · רויטל יעיש · 28/05/2025 15:14]</span>` |
| 2 | מקור הגעה + איזה וובינר | `pcfUTMSsource` (רשומת מקור הגעה אמיתית, 17 רשומות ב-1043) + שדה חדש `pcfLegacyFirstWebinar` על לקוח | ✅ "הרשמה לוובינר" → "הרשמה לוובינר"; וובינר: "הדרכת חינם עם רויטל יעיש יום שלישי 15.03" |
| 3 | קבצים ותמונות בהערות | `filename` + `fileurl` נכתבים ישירות. **עובד.** ההערות ה"קליפה" הריקות שפיירברי יוצר לצד הקובץ — מסוננות | ✅ `דנה שמר פרטים.jpg` עבר, 1/1 |
| 4 | שדה "זמן ביצוע שיחה (מערכת ישנה)" | נוצר `pcfLegacyCallTime` על אובייקט 100, מלא בכל שיחה | ✅ 2/2 שיחות |
| 5 | תשלום בלי מוצר | נבנה מנוע זיהוי מוצר. **דף הסליקה הוא המקור האמין, רשימת הבחירה היא רק גיבוי.** כיסוי 99.99% על 7,519 התשלומים | ✅ ראה §ב |
| 6 | סטטוס "ליד חדש" | אומת: **זו לא הייתה השתלה שגויה שלי.** סטטוס הלקוחה היה 5 תקין; "ליד חדש" היה הסטטוס של **תהליך המכירה**, שנקבע ע"י דיפולט. עכשיו נכתב מפורשות `statuscode=11` (עסקה נסגרה בהצלחה) | ✅ כל 5 העסקאות = 11 |
| 7 | לא נוצר ליווי | גל חדש: ליווי (1051) אחד, על **המוצר האחרון שנרכש**, עם מוצר מקושר, תוכנית, מס' שבועות, שבוע נוכחי, תאריכי התחלה/סיום וסטטוס | ✅ ראה §ג |

---

## ב. מנוע זיהוי המוצר — הממצא המרכזי

רשימת הבחירה במערכת הישנה **משקרת**. דוגמה חיה מדנה:

| דף הסליקה (מה שהלקוחה באמת קנתה) | רשימת הבחירה | מה שנכתב בפועל |
|---|---|---|
| `המשך תשלום על הרשמה 22 שבועות` · ₪5,460 | **"מוצר בוסט"** ❌ | תוכנית ליווי 22 שבועות ✅ |
| `סדנת בוסט לירידה במשקל` · ₪159 | "מוצר בוסט" | סדנה בוסט ✅ |
| `ליווי תזונה אונליין - מנטורית אישית` · ₪649 | ליווי אונליין - מנטורית אישית | תוכנית המשך אישית ✅ |

קוד 8 ("בוסט") הוא **סל אשפה** במערכת הישנה. לו הייתי סומך עליו, ₪5,460 של דנה היו נרשמים כסדנת בוסט של ₪159.

**סדר ההכרעה:** מספר שבועות בדף → מילות מפתח בדף (בוסט / אונליין / מנטורית / הרצאה) → רשימת בחירה (למעט קוד 8) → קוד 8 כמוצא אחרון.

---

## ג. ליווי (1051) — הלוגיקה

- **ליווי אחד ללקוחה**, על המוצר האחרון שנרכש (שובר-שוויון: חותמת יצירה, לא רק תאריך).
- **תוכנית**: בוסט→1 · תוכנית המשך→4 · כל השאר→2 (פרימיום).
- **הוראת קבע**: אין תאריך סיום. פעילה אם החיוב האחרון בתוך 31 יום → "ליווי בתהליך"; אחרת "הסתיים בהצלחה".
- **רגיל/תשלומים**: סיום = התחלה + מס' שבועות המוצר. עבר → "הסתיים"; עתידי → "בתהליך" + שבוע נוכחי מחושב.

דנה: `תוכנית המשך אונליין · תוכנית=המשך · 4 שבועות · התחלה 10/04/2025 · ליווי הסתיים בהצלחה` ✅

---

## ד. מה שהטסט חשף מעבר לרשימה שלך

### 1. אוטומציה דרסה סכומי עסקה — נתפסה ותוקנה
האוטומציה **`שינוי סכום עסקה ואחוז הנחה בעת שינוי\הוספה של שירות`** לקחה שתי עסקאות נפרדות של דנה (₪200 + ₪5,460) ורשמה **₪5,660 בשתיהן**. סה"כ מדווח: ₪12,477 במקום ₪6,817.

**זו לא באג במנוע — זו האוטומציה.** נוסף **גל 8ב — אשרור סופי**, שרץ אחרי כל השאר ומאשרר מחדש סכום עסקה, אחוז הנחה, סטטוס, שם ובעלים. נבדק: העדכון הישיר **נדבק** ולא מפעיל את האוטומציה מחדש.

זה מחזק את ההחלטה שלך לכבות את כל האוטומציות הפנימיות — אבל עכשיו יש גם רשת ביטחון אם משהו נשאר דלוק.

### 2. פיירברי מחזיר את המחרוזת `"None"` במקום ריק
ב-v3 שדות ריקים חוזרים לפעמים כטקסט `"None"` — מה שהיה גורם לכתיבת המילה "None" לשדות. נוסף נרמול גורף בשכבת ה-API.

### 3. מיזוג לכרטיס קיים לפי טלפון/מייל
המערכת החדשה **אינה ריקה** (969 לקוחות, 716 חופפים). הגל הראשון עכשיו מחפש קודם לפי מזהה חיצוני, אחר כך לפי טלפון, אחר כך לפי מייל. אם נמצא כרטיס קיים — **לא נוצרת כפילות**; ממלאים רק שדות ריקים והמזהה החיצוני נכתב כעוגן. דנה לא הייתה קיימת → יצירה נקייה.

### 4. שתי קטגוריות תשלום מזוהמות במערכת הישנה
מתוך 7,519 תשלומים: `566` (1 רשומה — זבל) ו-`אשראי הו"ק` (1 רשומה). השנייה מופתה להוראת קבע. הראשונה תיפול לברירת מחדל "רגיל" — שווה בדיקה ידנית.

### 5. אובייקט 4 חוסם יצירת שדות (403 License Permission)
לא ניתן להוסיף `pcfLegacyCreatedOn` לתהליך מכירה. עקיפה: תאריך היצירה הישן נרשם ב**תיאור** העסקה. בכל שאר האובייקטים השדה קיים ומלא.

---

## ה. תקציב קריאות API — מדידה אמיתית, לא הערכה

**היקף שנמדד היום מול המערכת הישנה:**

| אובייקט | כמות |
|---|---|
| לקוחות | 20,504 |
| תשלומים | 7,519 |
| תיעודי תשלום (מחושב מפירוט התשלומים) | 13,896 |
| רישומים שיווקיים | 38,963 |
| שיחות | 15,296 |
| ליווי (לקוחות עם תשלום) | 5,712 |
| תהליכי מכירה | 7,519 |
| משימות | 24 |
| הערות אמיתיות (הערכה — 20% מסך ההערות) | ~28,000 |
| **סה"כ רשומות ליצירה** | **~137,400** |

**קריאות:**

| צד | פירוט | קריאות |
|---|---|---|
| חדש | יצירה באצוות של 20 | 6,870 |
| חדש | סבבי אשרור/תיקון | 5,470 |
| חדש | סינון קיימים + בקרה עצמית (100 לקריאה) | 2,750 |
| **סה"כ חדש** | | **~15,100** |
| ישן | חילוץ (500 לעמוד) | ~400 |
| ישן | כתיבת מזהה הפוך | ~4,100 |
| **סה"כ ישן** | | **~4,500** |

**המפתח: המערכת הישנה והחדשה הן שני ארגונים נפרדים = שתי מכסות נפרדות של 100/דקה.** הן רצות במקביל.

בקצב **30 קריאות לדקה** (30% מהמכסה — 70% נשארים לכל השאר):
- צד חדש: **~8.5 שעות** · צד ישן: **~2.5 שעות** (במקביל)
- **זמן ריצה כולל: ~9 שעות**

חלון סוף שבוע = ~60 שעות. **מקדם ביטחון ×6.**

---

## ו. תשובות לחמש השאלות הכלליות

### 1. מי עודכן ומתי
כל רשומה מוגרת נושאת בשני הכיוונים:
- **בחדש**: `pcfExternalSoftwareID1` = המזהה הישן · `pcfLegacyCreatedOn` = תאריך היצירה המקורי · `pcfExternalDBUpdate` = תאריך העדכון האחרון בישן · `pcfCreatedByPersonOrSystem = 2` (מערכת)
- **בישן**: `pcfNewSystemId` = המזהה החדש · `pcfMigratedAt` = חותמת ההגירה המדויקת

זה מה שמאפשר את הסנכרון ההמשכי: "כל מה ש-`modifiedon` שלו מאוחר מ-`pcfMigratedAt`".

### 2. קצב
30 קריאות/דקה לכל צד (מכסה: 100). הריצה בלילה. ניתן להאיץ ל-60 ולקצר לחצי אם צריך.

### 3. כמה קריאות
~15,100 בחדש + ~4,500 בישן. ראה §ה.

### 4. כל כמה זמן אני בודק את עצמי
**בכל אצווה.** לא כל 100 רשומות — **כל אצווה של 20**:
- `rows_ok()` בודק `success` לכל שורה בנפרד (בגלל "ההצלחה המדומה" שהוכחתי לך)
- `selfcheck()` אחרי כל גל: הצלבה מלאה של כל המזהים + דגימת שלמות קישור לאב
- כשל = `Halt` — **הריצה נעצרת מיידית**, לא ממשיכה עם נתונים שבורים
- בהרצה המלאה אני נכנס בלייב כל כמה דקות, בודק מדגם מול המקור, ויכול לעצור, לתקן רטרו ולהמשיך

**הוכחה חיה מהיום:** הריצה הראשונה על דנה נקטלה באמצע (timeout). הרצתי שוב — המנוע זיהה 5 עסקאות, 5 תשלומים ו-14 תיעודים שכבר קיימים, דילג עליהם, השלים את מה שחסר. **אפס כפילויות.**

### 5. שילוב עם WF-20 (חיובי המשך) — הבעיה האמיתית
חיוב עתידי מהוראת קבע או מתוכנית תשלומים יגיע מ-GROW עם מזהה שלא קיים בחדש. פתרון בשלוש שכבות:
1. **תיעוד ממתין**: להוראת קבע פעילה (חיוב אחרון בתוך 45 יום) נוצר תיעוד 1018 אחד בסטטוס "לפני פירעון" עם תאריך החיוב הבא — ל-WF-20 יש למה להצליב. *(אצל דנה: 0 — שתי ההוראות שלה מתות מ-2025.)*
2. **`pcfID`** — קוד העסקה המקורי מ-GROW נשמר בכל תשלום. זו ההצלבה הראשונה.
3. **נפילה חכמה**: אם אין התאמה לפי מזהה → טלפון + סכום + חלון של 7 ימים. אם גם זה נכשל → **התראה, לא יצירה עיוורת.**

**סריקת כפילויות אחרי ההגירה**: סריקה על 1018 לפי (לקוחה + סכום + תאריך ±3 ימים) שמסמנת חשודים לאישור ידני. לא מוחקת כלום אוטומטית.

---

## ז. פתוח

| # | נושא | בעלים |
|---|---|---|
| 1 | כיבוי 45 האוטומציות הפנימיות (הרשימה נמסרה) | אליאל |
| 2 | תשלום עם קטגוריה `566` — בדיקה ידנית | אליאל |
| 3 | הכרעה על שדה מזהה קלאודצ'אט (ראה מסמך נפרד) | אליאל |
| 4 | רוטציית כל המפתחות בסיום ההטמעה | מייק |
