# יומן שינויים · 20/08/2026 — WF-06B · ביטול חסם חלון 24 השעות

## הבעיה

ריצה `189609` (16:30) מצאה את הקצאת התפריט של סער וינברג, ודילגה:

```
דילוג · 7א-מחוץ-לחלון · allow_send_message=false — מחוץ לחלון 24 השעות.
לא נשלח ולא נחתם.
```

הצומת `בדיקת חלון 24 שעות` חסם כל שליחה שבה `allow_send_message === false`.

**החסם היה שגוי.** הודעת התפריט יוצאת דרך **תבנית וואטסאפ מאושרת**, ותבנית מאושרת מותרת לשליחה גם מחוץ לחלון 24 השעות. `allow_send_message` משקף רק את חלון השיחה החופשית — הוא לא רלוונטי לתבניות.

התוצאה: תפריטים לגיטימיים לא נשלחו, בשקט, ורק הדוח הפנימי הראה זאת.

## מה שונה

הצומת `בדיקת חלון 24 שעות` הוסב ל-`מצב חלון · לדיווח בלבד`:

```js
c.outsideWindow = c.allow_send_message === false;
return [{ json: c }];
```

הוא כבר לא עוצר כלום. מצב החלון נרשם לדוח בלבד.
עודכנה גם ההפניה אליו בצומת `אימות 7ב` ושני החיווטים.

**גיבוי לפני השינוי:** `BACKUP_WF-06B_before.json` (39 צמתים).

## מה נשאר במקום — ולמה

| מנגנון | נשאר? | סיבה |
|---|---|---|
| `pcfsenttimestamp is-null` בשאילתה | ✅ | מונע שליחה כפולה |
| חתימה אחרי השליחה (סטטוס 2 + חותמת) | ✅ | **חובה.** בלעדיה כל תפריט יישלח מחדש כל 30 דקות |
| חסימה על שגיאת API של קלאודצ׳אט (401/403/429/5xx) | ✅ | תקלת תשתית אינה "הלקוחה לא קיימת" |
| חסימה כשהלקוחה לא קיימת בקלאודצ׳אט | ✅ | אין `user_ns` — אי אפשר לשלוח |
| אימות שכל 3 שדות התפריט נקלטו (7ב) | ✅ | בלעדיו ההודעה יוצאת עם קישור שבור |
| **חסם חלון 24 השעות** | ❌ **בוטל** | תבנית מאושרת עוברת גם מחוץ לחלון |

## אימות אמפירי

נבדק חי מול קלאודצ׳אט על המנוי של סער (`allow_send_message: false`):

| שעה | פעולה | תוצאה |
|---|---|---|
| 16:44:55 | `send-sub-flow` (menu_sending) | `status: ok` · `last_message_at` התקדם, `type: out` |
| 16:45:24 | `send-sub-flow` שוב, אחרי הזנת שדות תקינה | `status: ok` · `last_message_at` התקדם שוב |

**קלאודצ׳אט אכן שולחת החוצה גם כשהמנוי מחוץ לחלון.** ההנחה שהייתה בקוד הופרכה.

⚠️ השליחה הראשונה (16:44:55) יצאה עם ערכי שדות ישנים — טעות שלי בבדיקה. השנייה תקינה.

## המלצה שלא בוצעה

כרגע ה-workflow חותם על הרשומה כ"נשלחה" על סמך `status: ok` מקלאודצ׳אט בלבד.
עכשיו, כששולחים גם מחוץ לחלון, גדל הסיכון לאי-מסירה שקטה (למשל תבנית שהושהתה בוואטסאפ).
אפשר להוסיף אימות מסירה: לקרוא `last_message_at` לפני ואחרי השליחה ולוודא שהוא התקדם.
**לא הוספתי** — זה מוסיף חסם חדש, ותפריט שלא ייחתם יישלח שוב ושוב. דורש החלטה.
