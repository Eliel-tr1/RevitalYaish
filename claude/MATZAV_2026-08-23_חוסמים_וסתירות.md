# מצב המערכת — רויטל יעיש

**נכון ל-23/08/2026 · מסמך פנימי לוויטרו בלבד · לא עולה לדומיין של הלקוחה**

מקורות: 78 מסמכי הפרויקט · סריקה חיה של 115 הוורקפלואוז ב-n8n · 319 משימות ב-ClickUp · סריקה חיה של CloudChat, GreenAPI ו-Fireberry.

> **למה המסמך הזה נפרד מפורטל ההדרכה.** הפורטל מלמד איך המערכת עובדת ונשאר תקף לאורך זמן. המסמך הזה מתאר מצב רגעי שמשתנה כל שבוע. ערבוב של השניים היה הופך את הפורטל לשקר תוך חודש.

---

## א. חוסמים לפני עלייה לאוויר — קריטי

| # | הממצא | ההשפעה | בעלים |
|---|---|---|---|
| **A1** | 🔴 **WF-23, WF-24 ו-WF-25 פעילים אבל רצים עם `DRY_RUN: true`.** רק רשימת ההיתר (טלפון `0533435240`) מבצעת בפועל. | **כל לקוחה אמיתית שקונה היום עוברת בתהליך ולא נוצר עבורה דבר**: אין תיק ליווי, אין הודעת ברוכה הבאה, אין צירוף לקבוצה, אין הקצאת תפריט, אין משימה למנטורית. זה הפער החי הגדול ביותר בחשבון. | מייק |
| **A2** | 🔴 `1036.pcfSmsName = "Vitrue"` | שם השולח בהודעות SMS עדיין ויטרו ולא רויטל. חובה להחליף לפני שליחה ראשונה ללקוחות אמיתיות. | מייק / רויטל |
| **A3** | 🔴 **`WF16ErrHandler01` (מטפל השגיאות) אינו פעיל**, אך רשום כ-`errorWorkflow` ב-WF-18 וב-WF-27 (Tally). | כשל בשני התהליכים האלה **לא פותח משימה ולא מתריע**. הוא פשוט נעלם. | מייק |
| **A4** | 🔴 **התנגשות נתיב webhook.** `grow-payment-roital` תפוס גם ע"י WF-20 (פעיל) וגם ע"י `Revital_grow_payment --> Fireberry` (`H4DEVdHMRbzRdBoF`, כבוי). | הדלקה בטעות של הישן שוברת את כל קליטת התשלומים. מומלץ למחוק אותו, לא רק לכבות. | מייק |
| **A5** | 🔴 **525 אירועי webhook תקועים בתורי Make** (471 מהם מהעולם הישן). | הדלקת תרחיש בלי לרוקן את התור קודם מציפה את החשבון החי. | מייק |
| **A6** | 🔴 **רוטציית 5–7 טוקנים לא אומתה כבוצעה** (Fireberry, Make, טופסי, GreenAPI, CloudChat, n8n, `pcfPowerlinkToken`). | חשיפה. ⏱️ לבצע **רק אחרי סיום הטסטים**, בחלון מתואם — הרוטציה שוברת את כל חיבורי Make עד לעדכון ידני. | מייק |
| **A7** | 🔴 **`categorycode` ריק בכל 11 המוצרים**, אבל שער החתימה קורא ממנו. | הלוגיקה נכשלת בשקט וכל המוצרים מקבלים אותו יחס. השדה המאוכלס בפועל הוא `pcfProductType`. | מייק |

---

## ב. פערי תצורה — ערכים שלא מולאו

| # | היכן | מה חסר | ההשפעה |
|---|---|---|---|
| B1 | WF-18 | `ALERT_TASK_OWNER_ID` מכיל את הפלייסהולדר `<<ODED_USER_GUID>>` | שתי משימות הבקרה של הוובינר נוצרות **בלי בעלים** ולא יופיעו ברשימה של אף אחד |
| B2 | WF-18 | `LOGO_DATA_URI` ריק; `REPORT_EMAILS` מכיל נמען אחד למרות ששם הצומת "מייל לרויטל ולעודד" | דוח בלי לוגו; עודד לא מקבל |
| B3 | WF-18 | הצמתים `הכנת נוכחות פר-לקוחה` ו-`עדכון הרשמה לאירוע` (שלב ב') **מנוטרלים** | אין רשומות נוכחות פר-לקוחה על אובייקט 1030 |
| B4 | WF-23/24/25 | `GOOGLE_CONTACTS_ENABLED: false` בשלושתם | סנכרון אנשי הקשר בגוגל לא רץ. דורש יצירת credential `Google Contacts Revital Yaish` (OAuth, בממשק בלבד) והדלקת הדגל |
| B5 | Boost 6.6 · 6.6a · 6.7 | צמתי הכתיבה/השליחה **מנוטרלים**, ערכי `ZOOM_LINK`, `TASK_OWNER_ID`, `ALERT_WEBHOOK` ריקים | הדלקה שלהם היום תריץ קרון שלא עושה כלום |
| B6 | `3dOrk3a6TJk8wIkN` | שם הוורקפלואו: "כל יום ב-12 בלילה". הכלל בפועל: `{"triggerAtMinute": 1}` בלבד | רץ ב-00:01 שעון ישראל בפועל, אבל השם מטעה ומי שיקרא את הכלל יחשוב שהוא רץ כל שעה |
| B7 | `Green API Revital Yaish (1)` | הפילטר עדיין `senderData.chatName contains "TESTING"` | **רק תעבורת קבוצת הבדיקה נכנסת ל-Supabase.** ה-Response Agent מורעב מהודעות אמיתיות |
| B8 | WF-20 / WF-23/24/25 | `LIVE_TEST_ACCOUNT_IDS` מצביע על `3188D146-…-A97B` — רשומת לקוחה **שנמחקה ב-20/08** | ב-WF-20 לא מזיק. ב-WF-23/24/25 (שרצים ב-DRY_RUN) אין חשבון בדיקה חי לפי מזהה, רק לפי טלפון |

---

## ג. סתירות פתוחות — אסור לבנות עליהן

| # | הנושא | עמדה א' | עמדה ב' | בעלים |
|---|---|---|---|---|
| **C1** 🔴 | **סימן החישוב בשקילה.** `pcfWeightDelta` | התיעוד נועל: **חיובי = ירידה** | הגדרת דגל "לקוחה בסיכון" בלוח הבקרה: **`pcfWeightDelta > 0` = עלייה** | **מייק — הכי דחוף בטבלה.** אם המימוש הולך לפי אחד והשדה מחושב לפי השני, **הדגל האדום יידלק בדיוק על הנשים שירדו הכי הרבה.** בדיקה: לפתוח שתי שקילות של אותה לקוחה ולראות את הסימן |
| **C2** | **חלון 24 השעות** | WF-06B: החסימה **הוסרה** ב-20/08 בהוראת אליאל, החלון מדווח בלבד | WF-23/24/25: החלון עדיין **חוסם** שליחה | מייק — שתי מדיניות סותרות רצות במקביל באותו חשבון |
| **C3** | **`pcfGreenApiId` מול `pcfGroupid`** ב-1068 | `pcfGreenApiId` סומן כקנוני | מפרט WF-03 מפנה ל-`pcfGroupid`; מייק: "צריך לבדוק" | מייק — **בדיקה של 5 דקות:** לפתוח 2–3 רשומות קבוצה ולראות איפה יושב `1203630xxxxx@g.us` |
| **C4** | **יום השקילה השבועי** | ראשון 09:00 (האפיון + ClickUp + התרחיש שרץ) | חמישי (המצגת ללקוחה + ציר הזמן של לקוחת הבדיקה) | רויטל / מייק |
| **C5** | **תאריך הזקיף של תוכנית המשך** | 31.12.2099 (האפיון ופילטר הדשבורד) | 01.01.2099 (WF-25 כפי שנבנה) | מייק — לאחד, אחרת הפילטר בלוח 4 מפספס |
| **C6** | **סטטוס תיעודי התשלום מהטופס הידני** | התיעוד: "נפרע בהצלחה" לחיוב שתאריכו עבר | **הקוד החי:** `const paid = false` קשיח — **כל** תיעוד נפתח ב"ממתין לפירעון", `pcfPaymentDate` נשלח `null`, ו-1039 נשאר "בתהליך פירעון" | מייק — הקוד עקבי והגיוני ("אין אסמכתא, אין סימון נפרע"), אבל התיעוד צריך להתיישר |
| **C7** | **`SELF_HEAL_STANDING_ORDER`** | התיעוד: כובה 19/08 עד אחרי המיגרציה | **הקוד החי: `true`** | מייק — להכריע. עם 238 הוראות קבע ו-330 פריסות פתוחות מלפני המערכת, ריפוי עצמי דלוק ייצר רשומות שיישכפלו במיגרציה |
| **C8** | **גורל אובייקט 1005** | להפעיל | הופל לטובת one-to-many | מייק — כל בנייה שנשענת עליו מוקפאת |
| **C9** | **מכנה אחוז ההמשך לריטיינר** | לספור רק `pcfStatus IN (3,7)` | האם לכלול `6` (ליווי בוטל) | מייק |
| **C10** | **מתי תפריט "נספר" לתשלום לדיאטנית** | ביצירה | בשליחה (ההמלצה) | מייק / רויטל |
| **C11** | **`orderBy` בשאילתות v3** | הקוד משתמש בו ב-8 שאילתות | הכלל המתועד: מפתח לא מוכר מתעלמים ממנו בשקט, ו-`sort` לא נתמך | מייק — **לא הוכח שהוא עובד.** בינתיים אין להסתמך על סדר שמגיע מפיירברי |

---

## ד. חוב טכני

| # | הממצא | למה זה חשוב |
|---|---|---|
| D1 | 🔴 **מזהה וטוקן GreenAPI יושבים כטקסט גלוי בצומת Set — בשני וורקפלואוז** (WF-26 **וגם WF-21**), ובנוסף בכתובת ה-QR. התיעוד טוען שרק WF-26 מחזיק אותם. | **כל ייצוא של אחד משני הוורקפלואוז מדליף את הטוקן.** להעביר ל-credential |
| D2 | WF-28: `'7'` (הערות) מופיע ב-`DELETE_ORDER` אך **אינו קיים במפת הסריקה** | הערות לעולם לא נמחקות ונשארות יתומות. פשרה מתועדת — פיירברי מחזירה 500 על סינון הערות |
| D3 | WF-19: `cycleEndTarget` ו-`renewalTarget` מחושבים ומודפסים, אבל **אף שאילתה לא מסננת לפיהם** | `RENEWAL_DAYS_BEFORE_END` אינו אכיף בפועל. כל ליווי פעיל שאינו ריטיינר הוא מועמד |
| D4 | WF-06B: `pcfScheduledSendAt` **אינו קיים** באובייקט 1059. השדה בפועל הוא `pcfsendtime`, והוא ממולא **ידנית בלבד** | אין תהליך שמאכלס אותו ⇒ רשימת המועמדות לשליחה עלולה לצאת ריקה |
| D5 | WF-27 (Tally): ההבחנה `lookupFailed` קיימת בקוד אבל **אינה עוצרת את הזרימה** ואינה מגיעה לגוף התשובה | שקילה יתומה נוצרת בשקט |
| D6 | 14 וורקפלואוז `TEMP -` + 2 קלונים של WF-16 עם טריגר מתוזמן פעיל (אך כבויים) | לנקות. אחד מהם עלול להידלק בטעות |
| D7 | `Revital - Response Agent (2)` מכיל **שני צינורות כמעט זהים במקביל**, אחד מהם יתום בלי טריגר | קוד מת שמבלבל בניפוי |

---

## ה. דאטה שחסרה לפני עלייה לאוויר

1. **`pcfpaymentpageid` על המוצרים** — 12 מתוך 27 חיובי הוראת קבע בסוף השבוע לא זיהו מוצר.
2. **מנטוריות על המוצרים ועל קבוצות הוואטסאפ** — כל 30 הקבוצות רשומות על "ויטרו ראשי", ו-628 מתוך 629 תהליכי המכירה בבעלות משתמש אחד. **כל פילוח פר-אדם בלוח הבקרה יראה עמודה אחת.**
3. **משתמש התזונאית לא הוקם.**
4. **קיבולת קבוצה (`pcfCapacity`)** — ריקה ברוב הקבוצות; לשתיים ערכי בדיקה (999, 99). כל אוטומציית תפוסה חסומה.
5. **52 מחזורי בוסט של 2027** (מ-03/01/2027) עדיין בלי קבוצות מקושרות. 22 המחזורים מ-09/08/2026 עד 27/12/2026 מחווטים במלואם.
6. **הרשאות דרייב** — 14 קובצי התפריטים משותפים ברמת התיקייה בלבד. כל קובץ צריך "כל מי שיש לו הקישור — צופה", אחרת השליחה נכשלת אצל הלקוחה. הקבצים בבעלות `revitalfh@gmail.com`; מחיקה או הזזה שוברת את כל 14 הקישורים.
7. **ניקוי ערכי UTM כפולים** — `Facebook` (479) מול `פייסבוק` (1), `organic_bot` (3) מול `אורגני - צ'אטבוט וואטסאפ` (1), ושני שמות קמפיין שבורים מצד המודעות. בלי איחוד ידני הפילוח בלוח 2 משקר.
8. **4 לקוחות עם חיוב שנכשל** (יעל לוק, אסתר סימן טוב, אורית אלחרל, שירלי סופר) צריכות לעדכן אמצעי תשלום. **חיובים שנכשלו לא נרשמים כרשומה היום — רק כמשימה.**
9. **6 מסמכי חתימה קיימים ללא גרסה דו-שלבית** ולכן לא ניתנים לשליחה דרך המערכת: סיום התקשרות · אישור הקפאה · קבוצה שקטה · 5+שבוע מתנה · 7 שבועות 2 · 8+2 שבועות.
10. **דרישת הזיכוי טרם הוטמעה** — בזיכוי, תיעוד התשלום המקורי צריך לעבור ל"זיכוי בוצע בהצלחה" + מילוי `pcfrefundtotal` + `pcfsStatusUpdateTime`. השדה נוצר ע"י אליאל ב-23/08.

---

## ו. מסומן "הושלם" ובפועל לא

⚠️ **סטטוסי ClickUp אינם אמינים.** עשרות משימות מסומנות `complete` בזמן שהתיאור שלהן נפתח ב-❌.
- **שלב 0 (הקמה)** מסומן הושלם — משתמש התזונאית לא הוקם, ולמנטוריות אין גישה.
- **שלב 9 (טופסי)** מסומן הושלם — 5 משימות פתוחות, כולל אימות פרסום הוובהוק על כל 9 החוזים.

**המשימה בעלת התשואה הגבוהה ביותר בפרויקט, לפי התיעוד: אימות שהוובהוק פורסם בכל 9 חוזי טופסי, ושליחת בדיקה ל-1–2 מהם. כחצי שעת עבודה.** ב-Tofsy אפשר להגדיר וובהוק, לראות אותו בממשק, לקבל "הטופס התקבל בהצלחה" — ובכל זאת שום הודעה לא נשלחת, כי השינויים נשארו בטיוטה. זה בדיוק מה ששבר את חוזה "תזונה אונליין" ב-23/07.

---

## ז. מה כן עובד — כדי לא לאבד פרופורציה

- **WF-20 הוכיח את עצמו בסוף שבוע חי:** 161 ריצות · **27 רשומות הוראת קבע נוצרו, אפס חסרות, אפס כפילויות** · 38 חשבוניות שויכו · 19 כפילויות נחסמו · 9 כשלי חיוב זוהו נכון.
- מרוץ הוובהוקים במסמכים תוקן ב-23/08 עם המתנה של 25 שניות. הוא גרם ל-45 חשבוניות לא משויכות בסוף שבוע אחד.
- לוח הבקרה חי ורץ על דאטה אמיתית. Backfill מלא: 22 קריאות, 65 שניות, 2,307 רשומות.
- שלושת מנגנוני הניחוש בשיוך תשלומים **הוסרו מהקוד ולא רק כובו**. אין להחזיר אותם.
- 13 שדות נתוני הוובינר על רשומת המחזור עובדים, ולכן לוח הוובינר לא תלוי כלל באובייקטים 1030/1032 הריקים.

---

## ח. סדר הפעולות המומלץ

1. **A1** — לכבות את `DRY_RUN` ב-WF-23/24/25 אחרי בדיקת קצה-לקצה אחת מלאה. בלי זה אין מערכת.
2. **ו'** — אימות פרסום הוובהוק ב-9 חוזי טופסי. חצי שעה, תשואה מקסימלית.
3. **A2** — החלפת שם השולח מ"Vitrue".
4. **C1** — הכרעת סימן החישוב בשקילה. בדיקה של 5 דקות שמונעת דגלים הפוכים בלוח הכי חשוב עסקית.
5. **A3** — הפעלת מטפל השגיאות.
6. **ה'.2** — הקמת משתמשים למנטוריות ולתזונאית והקצאת בעלויות. בלי זה כל מסלול המנהל בפורטל מלמד לוחות שמציגים עמודה אחת.
7. **A4 + A5** — מחיקת הוורקפלואו הישן וריקון תורי Make.
8. **C3** — בדיקת 5 הדקות של שדה מזהה הקבוצה.
9. **D1** — העברת מפתחות GreenAPI ל-credential.
10. **A6** — רוטציית טוקנים, בחלון מתואם, אחרי סיום הטסטים.
