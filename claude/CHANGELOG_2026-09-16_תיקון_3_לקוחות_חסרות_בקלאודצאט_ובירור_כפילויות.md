# CHANGELOG — תיקון 3 לקוחות חסרות בקלאודצ'אט (תזכורת בוקר 16.9) + בירור כפילויות

**תאריך:** 2026-09-16, 09:50-10:00
**נוגע ל:** קלאודצ'אט (production, לקוחות אמיתיות)

---

## 1. בירור: מה זה "נחסמו ככפילות"?

בבדיקת הריצה של 9:00 (WF-30, שלב morning) נמצאו 2 מקרים שסומנו "נחסמו ככפילות" — **תהאני מוצלח** ו**רעות דמארי**. מייק שאל אם זה אומר שהן קיבלו את ההודעה פעמיים או שהן רשומות פעמיים לבוסט.

**התשובה: השנייה.** לשתיהן יש **2 רשומות ליווי (1051) נפרדות** בפיירברי, שתיהן מקושרות לאותו `accountId` ולאותו מחזור בוסט. ה-loop של WF-30 ניגש לשתי הרשומות בנפרד, ההודעה נשלחה בהצלחה בפעם הראשונה (וחסם האנטי-כפילות (`wf_webinar_last`) נכתב), והניסיון השני (מאותה הרצה, מהרשומה הכפולה) נחסם כמו שצריך. **כל אחת קיבלה את ההודעה פעם אחת בלבד** — המנגנון עבד נכון. אבל זה מצביע על כפילות ברמת הנתונים בפיירברי (רשומת ליווי כפולה), שכדאי לטפל בה בנפרד (בדומה לכפילויות אחרות שטופלו בפרויקט הזה בעבר).

## 2. תיקון: 3 לקוחות שלא היו קיימות בקלאודצ'אט

שלוש לקוחות נכשלו בשלב 9:00 עם "הלקוחה לא קיימת כמנוי בקלאודצ'אט": **נורית כהן** (972549498275), **אילנה לוי** (972522682017), **פנינה שטיינברג** (972526707116).

**מה בוצע:**
1. נוצרו כמנויות חדשות בקלאודצ'אט (`POST /subscriber/create-many`), עם השדות `webinar_time=20:00` ו-`wf_webinar_last=morning|2026-09-13` (כדי למנוע כפילות עתידית ולתעד שהן קיבלו את התזכורת).
2. נשלח אליהן הפלואו הרגיל (`send-sub-flow`, אותו flow ששולח WF-30/WF-29).
3. **בעיה שזוהתה:** מכיוון שהן מנויות חדשות לגמרי (0 אינטראקציות קודמות), `allow_send_message` חזר `false` — זה בדיוק חלון 24 השעות שכבר תיעדנו כתקלה אמיתית ב-14.9 (status:ok לא מוכיח שההודעה התקבלה). כדי לא לסמוך על זה, **נשלחה גם התבנית המאושרת `boost_webinar_morning`** (WhatsApp Business Template, שעוקפת את חלון ה-24 שעות לגמרי) לכל שלוש, עם שם פרטי ושעת הוובינר (20:00). כל שליחה החזירה `mid` (message ID אמיתי מ-WhatsApp Business API) — אישור מוצק שהמסר התקבל אצל מטא ונשלח בפועל, לא רק "status:ok".

**תוצאה:** שלוש הלקוחות קיבלו את תזכורת הבוקר בפועל (יתכן שקיבלו אותה פעמיים — גם דרך הפלואו הרגיל וגם דרך התבנית — עדיף על פספוס מוחלט לקראת הוובינר של הערב).

## 3. מה נשאר פתוח (לא נחסם, לא דחוף)

- לבדוק ולנקות את רשומות הליווי הכפולות של תהאני מוצלח ורעות דמארי בפיירברי.
- לשקול: האם כדאי שגם WF-29/WF-30 יעברו לשימוש בתבנית המאושרת (`boost_webinar_*`) במקום ה-flow הרגיל, כדי לפתור את בעיית ה-24 שעות באופן שיטתי לכל הלקוחות החדשות, לא רק כתיקון ידני נקודתי. (הוזכר כבר כאפשרות הקשחה עתידית ב-`TOCHNIT_2026-09-15_תהליך_מקביל...`)
