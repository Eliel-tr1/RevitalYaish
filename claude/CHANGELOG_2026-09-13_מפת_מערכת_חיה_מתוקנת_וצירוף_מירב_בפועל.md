
# תיקון קריטי להבנת המערכת החיה + ביצוע בפועל על מירב · 13/09/2026

## ⚠️ מדוע המסמך הזה קיים

מייק תיקן אותי בצדק: הסתמכתי על מסמכי תיעוד ישנים (מה-07-08/2026) שאמרו "WF-06B לא נבנה", "GreenAPI לא זמין", "אין טוקן". **זה היה נכון פעם, לא עכשיו.** המערכת התקדמה מאז והתיעוד לא עודכן. כל סשן עתידי חייב לקרוא את זה **לפני** שהוא מניח שמשהו "לא בנוי" — לבדוק את n8n החי קודם, לא את המסמכים.

**המקור הנכון לאמת תפעולית: `GET https://n8n.srv964196.hstgr.cloud/api/v1/workflows` (144 workflows חיים), לא מסמכי ה-MD.**

---

## מה קיים בפועל ב-n8n (מאומת 13/09/2026, לא מהתיעוד)

| WF | שם | מזהה n8n | פעיל? | תפקיד |
|---|---|---|---|---|
| **WF-26** | צירוף לקבוצת וואטסאפ (GreenAPI) | `0xf7yD9IeCyXDgWp` | ✅ | **בנוי, חי, עובד.** היחיד שמחזיק את מפתח GreenAPI. חשוף כוובהוק פנימי — כל תהליך אחר קורא לו, לא נוגע בטוקן ישירות |
| **WF-06B** | שליחת תפריטים מתוזמנת | `WF06BMenuSend01` | ✅ | **בנוי, חי, עובד.** רץ א'-ה' כל חצי שעה 08:00-19:30 + 20:00. שולח תפריט אמיתי דרך CloudChat (תבנית מאושרת) |
| WF-21 | ניטור חיבור GreenAPI | `LAcYZPZP4VksE1Ne` | ✅ | כבר ידוע מקודם |
| WF-20 | ראוטר תשלומים מרכזי GROW→Fireberry | `p1AfsAeXcrlhtiNk` | ✅ | תוקן היום (ראה CHANGELOG הקודם מהיום) |
| WF-22/23/24/25 | ראוטר תהליכים מקצועיים + תתי-תהליכים | פעילים | ✅ | |

**מסמכים ישנים שיש להתעלם מהם על הנקודות האלה:** `06_N8N_WORKFLOW_SPECS.md`, `00_PROJECT_CONTEXT.md`, `08_TECHNICAL_PLAYBOOK.md` (כולם 07-08/2026) — מתארים את WF-03/WF-06B כ"טרם נבנה" ו-GreenAPI כ"חסום". **זה מיושן.** לא למחוק אותם (יש בהם היגיון עסקי שעדיין רלוונטי), אבל **לבדוק תמיד מול n8n החי לפני שמסיקים "לא בנוי"**.

---

## איך WF-26 עובד (צירוף לקבוצת וואטסאפ) — להפעלה עתידית

**וובהוק פנימי:** `POST https://n8n.srv964196.hstgr.cloud/webhook/wa-join-roital`

**גוף הבקשה:**
```json
{
  "groupId": "<pcfGreenApiId של קבוצת ה-1068, פורמט XXXXXXXXXXXXX@g.us>",
  "chatId": "972XXXXXXXXX@c.us",
  "groupName": "<שם הקבוצה, לתיעוד בלבד>",
  "groupRecordId": "<GUID של 1068, לתיעוד בלבד>",
  "accountName": "<שם הלקוחה, לתיעוד בלבד>",
  "source": "<הסבר קצר למה זה נקרא, לתיעוד בלבד>"
}
```

**תשובה:** `{joined: true/false, skipped, state, reason, response}`. `state` חייב להיות `authorized`. `joined:false` עם `reason` שמזכיר "הגדרות פרטיות" = **לא באג** — GreenAPI לא הצליח להוסיף כי הלקוח/ה חסמ/ה הוספה אוטומטית לקבוצות. הפתרון היחיד: הצטרפות עצמית דרך קישור ההזמנה.

**⚠️ ל-WF-26 אין יכולת "הסרה מקבוצה" (רק הוספה).** אם נוסף מישהו/י לקבוצה הלא-נכונה בטעות — ההסרה חייבת להתבצע ידנית מהטלפון (רויטל/מייק), כי אין endpoint חשוף לזה.

**אין צורך לגעת בטוקן GreenAPI בשום שלב** — הוא יושב רק בתוך WF-26 עצמו (node "קונפיגורציה"). כל קריאה חיצונית עוברת דרך הוובהוק הזה בלבד.

---

## איך WF-06B עובד (שליחת תפריט) — להפעלה עתידית

**הטריגר לשליחה הוא רשומת הקצאת תפריט (1059) עם שלושה תנאים בדיוק:**

| שדה | ערך נדרש |
|---|---|
| `pcfMenuStatus` | `5` ("מוכן - לשליחה") |
| `pcfsendtime` | תאריך+שעה בעבר/עכשיו, פורמט `YYYY-MM-DD HH:MM:SS` (שעון ישראל) |
| `pcfsenttimestamp` | ריק (דגל מניעת כפל-שליחה) |
| `pcfrelatedietmenu` | GUID תקין מקטלוג 1060, עם `pcfMenuUrl` שמכיל קישור Google Drive בפורמט `/file/d/<id>/` |
| `pcfaccountid` | מקושר ללקוחה |

**זהו. אין צורך לקרוא ל-CloudChat ידנית — WF-06B רץ אוטומטית כל חצי שעה (א'-ה' 08:00-20:00) ומטפל בהכל.**

⚠️ **תנאי מוקדם קריטי ש-WF-06B לא בודק/יוצר בעצמו:** הלקוחה חייבת להיות **מנוי קיים בקלאודצ'אט**. אם לא — הרשומה **תידלג בשקט**. ליצירת מנוי: `POST /subscriber/create` עם `{phone, first_name, last_name, valid:true, type:"MOBILE"}`, לאמת עם GET חוזר, ולכתוב את ה-`user_ns` שחוזר לשדה `pcfCloudChatId` בחשבון.

⚠️ **גוצ'ת "חלון 24 השעות" הישנה — לא רלוונטית יותר ל-WF-06B.** תוקן ב-20/08/2026: התפריט יוצא דרך תבנית מאושרת שמותרת גם מחוץ לחלון. אל תחסמו על בסיס `allow_send_message = false`.

---

## 🔴 באג שנמצא ותוקן היום: `pcfEventLink` (1007) בשימוש מקביל ל-`pcfGroupLink` (1068)

**דיווח מייק:** הקישור להזמנה לקבוצת וואטסאפ מוחזק כרגע גם בשדה על **מחזור הבוסט (1007)** וגם על **קבוצת הוואטסאפ המקושרת (1068)**. כשקבוצה מוחלפת למחזור — ה-1007 נשאר עם קישור ישן. **מייק עומד למחוק את השדה מ-1007 ולהסתמך אך ורק על 1068.**

### מה נבדק (144 workflows חיים, לא רק המסמכים)

`grep` על כל ה-workflows החיים איתר `pcfEventLink` ב-12 workflows. **הלוגיקה בפועל תקינה בכל מקום שנבדק** — כל workflow פעיל שבודק מחזור→קבוצה כבר משתמש ב-`group = groupById(cohort.pcfwhatspgrouprelated)` ואז ב-`group.pcfGroupLink`/`group.pcfGreenApiId` (**לא** ב-`cohort.pcfEventLink`). `pcfEventLink` נמצא **רק ברשימת השדות שנשלפים בשאילתה**, ולא נקרא בשום מקום בקוד בפועל.

**אבל זו עדיין פצצה מתקתקת:** Fireberry מחזיר **400 על השאילתה כולה** אם שדה ברשימה לא קיים (מתועד: `"fields":"*" לא עובד. שדה אחד לא קיים → כל השאילתה נופלת ב-400"). ברגע שמייק ימחק את השדה — **4 workflows פעילים היו נופלים מיידית**, כי הם עדיין שולפים `pcfEventLink` בשאילתת ה-1007.

### תוקן ואומת (4 workflows פעילים, כולם עודכנו והועלו לשרת, `active:true` נשמר בכולם):

| WF | מזהה | מה תוקן |
|---|---|---|
| WF-23 · תהליך מקצועי — סדנת בוסט | `vRP5tdFeS3AWPdRv` | הוסר `pcfEventLink` משאילתת "מחזורי בוסט (1007)" |
| WF-24 · תהליך מקצועי — תוכנית ליווי | `LciQfMEuU2GKWRjI` | אותו תיקון |
| WF-25 · תהליך מקצועי — תוכנית המשך | `ffdORtb9E6haYWy1` | אותו תיקון |
| WF-30 · סדרת הודעות הוובינר | `rLcLLXt9tuVt3Rx4` | אותו תיקון (פורמט CSV, לא JSON, אבל אותה בעיה) |

**אומת:** בכל ארבעת ה-workflows — `pcfEventLink` נעלם לגמרי מהקוד החי, `active:true` נשמר.

### נמצא אך **לא תוקן** (workflows מנוטרלים — לא דחוף, ללא סיכון מיידי)

כותב/קורא `pcfEventLink` בפועל, אך `active:false` כרגע — לתקן רק אם מפעילים אותם מחדש:
- `WFBoostB00000001` (WF-BOOST-B, קבוצת וואטסאפ מתגלגלת) — **כותב** `pcfEventLink` בפועל בשלב "קישור חזרה למחזור". אם יופעל אי-פעם — יש לשנות שיכתוב רק `pcfwhatspgrouprelated`.
- `BoostFbLink00001` (WF-BOOST · backfill) — אותה כתיבה.
- `WFBoostA00000001`, `8vr5JFjhBmjhvD0R`, `aIM9lWgQsHnkDGd2`, `nqmn47c795JMmUkH`, `BoostDisc0000002`, `BoostFbWrite0001` — שולפים את השדה ברשימה בלבד, לא משתמשים בו בלוגיקה (כמו הפעילים, לפני התיקון).

**מסקנה למייק: אפשר למחוק את `pcfEventLink` מ-1007 בבטחה** — כל מה שרץ בפועל כבר תוקן ומאומת. אם בעתיד תפעילו מחדש workflow מהרשימה השנייה, יש לבדוק אותו לפני ההפעלה (הרשימה למעלה מפרטת מה בדיוק לתקן בכל אחד).

---

## 🔴 טעות שלי שתוקנה: מירב וסער נוספו לקבוצה הלא-נכונה

תוך כדי הבדיקה למעלה התגלה שגם **אני** טעיתי באותה נקודה בדיוק: השתמשתי בקבוצה **CAD7798B** ("מחזור 13.09.2026") עבור מירב, כי כך זוהה "המחזור הפתוח" מוקדם יותר בסשן. אבל **הקישור החי כרגע בפועל דרך `pcfwhatspgrouprelated`** של המחזור הפתוח האמיתי (`FFB97E47`, מכירה פתוחה 06-20.9, הסדנה עצמה 20-27.9) מצביע על קבוצה **אחרת**: `3104b034` ("מחזור 20.09.2026"). **זה בדיוק הבאג שמייק תיאר** — קרה גם לי, לא רק ל-1007 הישן.

**מה תוקן:**
1. עודכנו (ואומתו ב-GET): תהליך המכירה של מירב (`pcfWhatsAppToRef`), תהליך הליווי שלה (`pcfActualGroup`), החשבון שלה (`pcfWhatsAppReferans`) — כולם עברו מ-`CAD7798B` ל-`3104b034`.
2. מירב **נוספה בפועל** לקבוצה הנכונה (`3104b034`, "מחזור 20.09.2026") דרך WF-26 — `joined: true`.
3. סער וינברג (0533435240) **נוסף בפועל** לאותה קבוצה נכונה — `joined: true` (בניסיון הראשון לקבוצה השגויה זה נכשל; בקבוצה הנכונה זה הצליח).

**⚠️ נשאר פתוח:** מירב וסער עדיין חברים גם בקבוצה **הלא-נכונה** (`CAD7798B`, "מחזור 13.09.2026") מהניסיון הראשון שלי. **אין ל-WF-26 יכולת הסרה** — צריך שרויטל/מייק יסירו אותם משם ידנית מהטלפון (חיפוש חבר → הסרה, כמה שניות לכל אחד).

---

## מה בוצע בפועל היום על מירב (לא סימולציה — פעולות אמיתיות)

1. **הצטרפות פיזית לקבוצת הוואטסאפ הנכונה** (מחזור 20.09.2026) — מאומת `joined: true`.
2. **מנוי בקלאודצ'אט** — לא היה קיים. נוצר (`POST /subscriber/create`), אומת ב-GET חוזר, `user_ns = f201689u944994869` נכתב ל-`pcfCloudChatId`.
3. **הקצאת התפריט** (`C3E7D690-...`) — עודכנה ל-`pcfMenuStatus=5`, `pcfsendtime="2026-09-13 15:00:00"`. WF-06B ירוץ ב-15:00 בדיוק וישלח תפריט אמיתי, ויחתום את פיירברי בעצמו.
4. **כל שיוכי הקבוצה שלה תוקנו לקבוצה הנכונה** (ראה סעיף למעלה).

---

## מה עדיין פתוח (ממתין להנחיית מייק)

בבדיקת "מי עוד הצטרף ב-24 השעות האחרונות" נמצאו עוד 3 רכישות בוסט תקועות מהיום, בשלושה דפוסי כשל **שונים זה מזה וממירב**:

1. **טלי אברהם** — יש לה חשבון **ויש תהליך מכירה פתוח בדיוק על בוסט**, אבל התשלום לא נמצא מקושר לחשבון (`pcfAccountid` ריק ברשומת התשלום). קוד החיפוש-לפי-טלפון ב-WF-20 נראה תקין. **השערה לא מאומתת:** ייתכן שהטלפון שנשלח בפועל מ-GROW שונה מהטלפון הרשום בחשבון שלה. דורש בדיקה מול ה-payload הגולמי.
2. **ימית ליבנת, פזית עינב** — **אין להן חשבון בפיירברי בכלל.** קשור כנראה לתקלת ה-Make שכבר אובחנה בנפרד (`AVCHUN_2026-09-13_שורש_התקלה_Make_קליטת_ליד.md`), אבל לא הותאם 1:1.

**המלצה:** לפני תיקון גורף על כל 23+ המקרים ההיסטוריים — לסווג כל מקרה לפי דפוס הכשל שלו, כי לכל אחד תיקון שונה.
