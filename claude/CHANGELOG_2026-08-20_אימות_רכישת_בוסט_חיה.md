# יומן שינויים · 20/08/2026 — אימות רכישת בוסט חיה + 3 תיקונים

## 1. הרכישה של סער וינברג (0533435240) — שרשרת מלאה, אומתה רשומה־רשומה

| שלב | רשומה | תוצאה |
|---|---|---|
| לקוח | `0E7734D1-…` סער וינברג | ✅ |
| תהליך מכירה | `07703D12-…` | ✅ סטטוס "עסקה נסגרה בהצלחה", מוצר סדנה בוסט, מחזור `בוסט 23-30.8.26`, קבוצת וואטסאפ משויכת |
| תשלום (1039) | `39FB402C-…` | ✅ `GROW-510263673`, 1 תשלומים, **מוצר מקושר = סדנה בוסט** |
| תיעוד תשלום (1018) | `6A89C0AD-…` | ✅ נפרע בהצלחה, אסמכתא 510263673, **מספר מסמך 49679** |
| מסמך (1038) | `8C5567DD-…` | ✅ חשבונית מס/קבלה 49679, מקושרת ללקוח + לתשלום + לתיעוד + לתהליך המכירה |
| תהליך ליווי (1051) | `4E6D54C0-…` | ✅ "ליווי סדנה בוסט", בתהליך, מחזור + קבוצה |
| הקצאת תפריט (1059) | `615EC87C-…` | ✅ "תפריט בוסט", מוכן - לשליחה |

הריצות: WF-20 `189568` (רכישה) → `189573` (זוהה ככפילות, לא נכתב שוב) → `189578` (חשבונית).
WF-22→WF-23 ריצה `189570`.

### מה לא רץ, ולמה
* **הודעת וולקאם בקלאודצ'אט — לא נשלחה.** הלקוחה מחוץ לחלון 24 השעות (`allow_send_message = false`).
  זו התנהגות מכוונת: שליחה מחוץ לחלון מחזירה `ok` מהאייפיאיי אבל ההודעה לא מגיעה, ולכן אנחנו מדלגים במפורש.
  ברכישה אמיתית מהמשפך הלקוחה כמעט תמיד בתוך החלון.
* **צירוף אוטומטי לקבוצת הוואטסאפ — לא בוצע.** ראה סעיף 2.

## 2. 🔴 באג שהתגלה ותוקן — WF-26 קרא את הצומת הלא נכון

**מה קרה:** ה-workflow של הצירוף לקבוצה (`WF-26`) קרא את הפרמטרים מ-`$input`,
אבל הצומת שלפניו הוא צומת הקונפיגורציה — ולכן `groupId` ו-`chatId` הגיעו ריקים.
התוצאה: כל צירוף לקבוצה החזיר "חסר מזהה קבוצה או טלפון" ודילג — **בשקט**.

**מה תוקן:**
1. הקריאה נעשית במפורש מצומת הוובהוק. נוסף מבחן מבני שנכשל אם מישהו יחזיר את הקריאה מ-`$input`.
2. הטיימאאוט של GreenAPI הועלה ל-60 שניות, ושל הקריאה מהתהליך המקצועי ל-70 — GreenAPI נטתה לתקוע קריאות ל-30 שניות ולהפיל את הריצה.
3. הצומת סומן `continueRegularOutput`: כשל בצירוף לקבוצה כבר **לא יכול** להפיל את התהליך המקצועי. הסיבה מדווחת בסיכום.

**אומת אחרי התיקון:** הפרמטרים מגיעים נכון, ההכרעה מגיעה עד `addGroupParticipant`.

**חסם שנותר:** מופע GreenAPI מחזיר `notAuthorized` — **נדרשת סריקת QR מחדש**.
עד אז כל צירוף לקבוצה מדלג עם הסיבה "המכשיר לא מחובר". הלקוחה עדיין מקבלת את קישור ההזמנה בהודעת הוולקאם.

## 3. 🔴 תיקון — רשומת התשלום לא נסגרה אף פעם

**מה היה:** כל רשומת "תשלום" נוצרה בסטטוס "בתהליך פירעון" ונשארה שם לנצח, גם כשהתשלום היה חד־פעמי ונפרע במלואו.

**מה תוקן:**
* **רכישה בתשלום אחד שנפרע** ⇒ הרשומה נוצרת ישר כ**"נפרע בהצלחה"**.
* **פריסת תשלומים** ⇒ נשארת "בתהליך פירעון", וכשנפרע התיעוד ה**אחרון** — הרשומה נסגרת אוטומטית.
* **הוראת קבע** ⇒ **לעולם לא נסגרת**. היא פתוחה מעצם טבעה.
* **טופס תשלום שאינו אשראי (WF-27)** ⇒ אותה חוקיות: כל החיובים בעבר ⇒ סגור; יש חיוב עתידי ⇒ בתהליך פירעון.

הרשומה של סער תוקנה ידנית ל"נפרע בהצלחה".

## 4. ריצות נוספות מהיום — צפויות, לא תקלות

| ריצה | מה קרה | הסבר |
|---|---|---|
| `188893` · חשבונית 49677 | `INVOICE_NO_MATCH` — נפתחה משימה | אין רשומת תשלום עם מזהה העסקה. חשבונית של הו"ק שהוקמה לפני המערכת. |
| `189300` · חשבונית 49678 | `INVOICE_NO_MATCH` — נפתחה משימה | אותו דבר. |
| `189268` · קורין ענבר 385 ₪ | `RECURRING_CHARGE_NO_SOURCE` — נפתחה משימה | חיוב הו"ק `51945332` שאין לו רשומת הוראת קבע. |

שלושתם ייעלמו אחרי מעבר הנתונים ב-04–05/09, כשנייבא את כל ההוראות והתשלומים מגרואו.
**עד אז זו ההתנהגות הנכונה** — משימה במקום ניחוש, לפי הכלל של שיוך לפי מזהה חד־ערכי בלבד.

## 5. בדיקות
* WF-20: **195** בדיקות — עוברות.
* WF-22…26: **78** בדיקות — עוברות.
* WF-27: **32** בדיקות — עוברות.

כל ה-workflows נפרסו מחדש והופעלו.
