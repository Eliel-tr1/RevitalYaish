# CHANGELOG · 15/09/2026 (ה) — מילוי רטרואקטיבי טלפון בתהליכי מכירה + אבחון שורש

**הטריגר:** אליאל: "יש כאן רשימה של לקוחות בלי מספר טלפון בתהליך המכירה, למרות שיש טלפון בלקוח המקושר. 1. תתקן רטרו. 2. תבדוק מה מקור התקלה — הגירה / N8N / Make, ואם כן איזה סנריו/WF."

---

## 1. הבהרה טכנית קודם כל

שם המערכת של שדה הטלפון בתהליך מכירה (אובייקט 4) הוא **`pcfTelephone1`/`pcfTelephone2`** — לא `telephone1`/`telephone2` (אלה שייכים ללקוח, אובייקט 1). זה שדה **פיזי, לא שיקוף** — כלומר **אין שום מנגנון בפיירברי שמעתיק אוטומטית טלפון מהלקוח לתהליך המכירה**. כל תהליך שיוצר תהליך מכירה חייב לכתוב את הטלפון בעצמו במפורש, אחרת הוא נשאר ריק לצמיתות, גם אם ללקוח יש טלפון.

## 2. מה בוצע — תיקון רטרו

שאילתה על כל אובייקט 4: `pcfTelephone1` ריק **וגם** `pcfTelephone2` ריק **וגם** יש לקוח מקושר.

**נמצאו 113 רשומות.**

| | כמות |
|---|---|
| תוקנו (הועתק טלפון מכרטיס הלקוח) | **111** |
| לא ניתן לתקן — גם ללקוח המקושר אין שום טלפון | **2** |

לכל רשומה: `pcfTelephone1` ← טלפון ראשי של הלקוח (או משני, אם הראשי ריק) · `pcfTelephone2` ← טלפון משני של הלקוח (אם קיים). כתיבה אחת-אחת (`PUT /api/record/4/{id}`), השהיה 0.35 שנ' בין קריאות, retry אוטומטי על כשל. **111/111 הצליחו בניסיון הראשון, אין כשלים.**

**אימות:** הרצה חוזרת של אותה שאילתה בדיוק אחרי התיקון מחזירה **2 בלבד** — בדיוק שתי הרשומות חסרות-הטלפון-לגמרי. אפס רשומות שגויות נותרו.

**שתי הרשומות שנשארו ריקות (דורשות בירור ידני מולך/מול רויטל — אין מאיפה לשלוף טלפון דרך ה-API):**

| תהליך מכירה | לקוחה | accountid |
|---|---|---|
| עסקה - עדי \| ליד | עדי | `5B203E23-6C82-4A3B-AFC0-F0799D9D2028` |
| העסקה של ריקי | ריקי | `08F13C6F-8AFE-4488-A7F2-E173508F659A` |

**גיבוי מלא לפני התיקון** (113 רשומות — מזהים, ערכים לפני, ערכים שנכתבו): `BACKUP_2026-09-15_תהליכי_מכירה_ללא_טלפון_לפני_תיקון.json`

**לא נגעתי:** במבנה שדות, ב-Make, ב-N8N, ובשום רשומה מעבר לשדה הטלפון בשתי-עשרה... (111) הרשומות הנ"ל.

---

## 3. שורש התקלה — שני מקורות נפרדים, לא אחד

### מקור א' · הגירה — 83/113 (73%) — סגור, היסטורי

כל 83 הרשומות נוצרו ב-**10–11/09/2026**, בדיוק בחלון ההגירה (שהסתיימה 11/09 14:58 לפי `MONITOR_2026-09-12`), עם שם בחתימת מנוע ההגירה: `"עסקה - <שם> | <תגית>"`. סטטוסים: 49 נסגרו בהצלחה, 32 הופסדו, 2 פולואפ — אין קורלציה ברורה לסטטוס יחיד.

**זו לא Make ולא N8N.** התזמון חד-משמעי: המיגרציה הסתיימה 11/09 14:58, ואוטומציות חיות (`DRY_RUN=false`) עלו רק מ-12/09 ואילך (`CHANGELOG_2026-09-12_מעבר_ללייב`). **מנוע ההגירה (`build.py`) הוא המקור** — הוא לא כתב טלפון לתהליכי המכירה האלה ספציפית, בעוד שבבדיקות אחרות של אותו מנוע (`AIMUT_4.3`) דווח "אפס רשומות ללא טלפון". לא בדקתי את קוד המנוע לעומק — זה מחוץ לתחום הבקשה, וההגירה כבר נסגרה וסופקה. **חוב היסטורי, לא דורש פעולה נוספת** מעבר למה שתוקן היום.

### מקור ב' · חי ופעיל — 30/113 (27%) — לא הגירה, עדיין דורש תיקון שורש

30 רשומות נוצרו ב-**13–14/09/2026**, כולן בשם `"העסקה של <שם>"` — **זה השם שפיירברי מייצר כברירת מחדל** לתהליך מכירה חדש כשלא נשלח `name` מפורש ביצירה. 29/30 בסטטוס **"ליד חדש"**.

**זו בדיוק החתימה שכבר תועדה כממצא חי ופעיל היום** ב-`AVCHUN_2026-09-15_1609_ניטור_ממצא_קריטי.md` (ממצאים B4/D2): תהליך קליטת ליד שמאתר לקוחה קיימת לפי טלפון/מייל אבל **פותח לה תהליך מכירה כפול בלי לבדוק אם כבר יש לה עסקה לאותו מוצר**. באותו מסמך המקור החשוד **טרם הוכרע** בין `Main Leads CRM` ב-Make לבין WF-20.

**תוספת חדשה לאבחון הקיים, מהנתונים שאספתי עכשיו:**
מתוך 30 הרשומות, **16 נוצרו בתוך חלון של 30 דקות בדיוק ב-14/09, בין 13:58:48 ל-14:28:37** (שעון ישראל, לפי הערת הפלייבוק שחותמות הזמן של פיירברי הן שעון מקומי עם `Z` ולא UTC אמיתי). זה **לא תואם** את דפוס ה"טפטוף" (ליד אחד כל ~2.5 שעות) שתואר באבחון הקיים. זה נראה כמו **ריצת אצווה/backfill חד-פעמית**, לא כניסת לידים אורגנית בזמן אמת. 14 הרשומות הנותרות (13/09 ובוקר 14/09) כן מתפזרות על פני שעות שונות ותואמות דפוס טפטוף רגיל.

**המשמעות המעשית: כנראה שני אירועים שונים מתחת לאותה חתימת שם** —
1. הבאג המתמשך בקליטת ליד (כבר מנוטר ב-AVCHUN, עדיין פעיל כרגע).
2. ריצה חד-פעמית של סקריפט/אצווה ב-14/09 בסביבות 14:00–14:28, שיצרה 16 עסקאות כפולות בבת אחת. **לא זיהיתי את מקור הריצה הזו** — לא נמצא בפרויקט תיעוד לריצה בשעה הזו בדיוק, ואין לי גישה ללוגים של Make (ואסור לי לגעת בו).

---

## 4. מה עוד פתוח — צריך החלטה/בירור שלך

1. **הבאג החי בקליטת ליד עדיין לא נסגר.** כל עוד הוא פעיל, הוא ימשיך לייצר תהליכי מכירה כפולים וחסרי טלפון (כמו שקרה גם ב-15/09 היום — קרן ברזילי, גלית ישראלי, אורית אלחרל). **המילוי הרטרואקטיבי שעשיתי היום ידרוש חזרה מחר אם לא יתוקן השורש.** ההמלצה שלי: לתקן קודם את בדיקת "עסקה קיימת לאותו מוצר" בתהליך קליטת הליד, ורק אחר כך למלא טלאים נוספים.
2. **מי מתקן** — אם המקור הוא Make (`Main Leads CRM`) זה תיקון ידני שלך/מייק בלבד (אסור לי לגעת ב-Make). אם המקור הוא N8N (WF-20 או אחר) — אני יכול לתקן בעצמי, בכפוף לאישורך.
3. **ריצת ה-14:00–14:28 ב-14/09** — 16 רשומות בחצי שעה, מקור לא מזוהה. אם אתה זוכר איזו ריצה/סשן זה היה — תגיד לי ואבדוק שם ספציפית.
4. **שתי הרשומות (עדי, ריקי) בלי טלפון בשום מקום** — דורשות בירור ידני מול רויטל/המערכת הישנה אם רוצים למלא אותן.

## 5. קבצים

- `BACKUP_2026-09-15_תהליכי_מכירה_ללא_טלפון_לפני_תיקון.json` — 113 רשומות, מצב לפני התיקון + מה נכתב + סטטוס כתיבה.
