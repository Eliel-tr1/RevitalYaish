# WF-17 · מכירת ליווי לאחר בוסט

> n8n id: `e30cNECSoBNJDQcf` · כבוי · `DRY_RUN = true`
> נבנה 02.08.2026. תאום עם `WF16DeadPath0001` (מסלול מת) — ראה §6.

## 1. מטרה
כל יום רביעי 20:00 לפתוח תהליך מכירה חדש ("מסלול ליווי") לכל לקוחה
שסיימה בוסט במחזור שנגמר ביום ראשון הקרוב ועדיין לא המירה.

## 2. טריגר
`Schedule Trigger` — שבועי, יום רביעי, 20:00, `Asia/Jerusalem`.
בנוסף `Manual Trigger` לבדיקות.

## 3. הסינון (ארבעה שערים, כל אחד עוצר בנפרד)

| שער | תנאי | מקור |
|---|---|---|
| 1 · מחזור | `1007.pcfEndDate` = היום + 4 (ראשון הקרוב) **וגם** `pcfStatus ≠ התבטל` | אומת: כל 74 המחזורים מסתיימים ביום ראשון |
| 2 · עסקה | `4.pcfCourse` = המחזור **וגם** `4.pcfProduct` = סדנה בוסט **וגם** `statuscode` = עסקה נסגרה בהצלחה | סינון התשלום |
| 3 · ליווי | לא קיימת רשומת `1051` של הלקוחה עם `pcfCurrentProgram ≠ בוסט` | פוסל פרימיום/מתקדמים/המשך |
| 4 · כפילות | לא קיים `4` עם `pcfParentSale` = עסקת הבוסט | הגנת ריצה כפולה |

כל כשל טכני בשער = `skip` מפורש, לא יצירה. ספק אף פעם לא מייצר רשומה.

## 4. הרשומה שנוצרת (Opportunity · 4)

| שדה | ערך |
|---|---|
| `name` | `מכירת ליווי לאחר בוסט - {accountname}` |
| `accountid` | מעסקת הבוסט |
| `ownerid` | מעסקת הבוסט (fallback: `FALLBACK_OWNER_ID`) |
| `statuscode` | `2` — ליד חדש |
| `pcfProduct` | `d9e842a7-f284-4596-bb61-4d2a350eebb5` — תוכנית ליווי 12 שבועות |
| `pcfCourse` | מחזור הבוסט — נשמר |
| `pcfWhatsAppToRef` | קבוצת הוואטסאפ — נשמרת (רק אם קיימת) |
| `pcfParentSale` | מזהה עסקת הבוסט |
| `pcfFollowUpTime` | מחר (חמישי) 10:00 |
| `pcfFormFilled` | `1` רק אם בעסקת הבוסט הערך "כן" |
| `pcfFormLink` | מועתק מעסקת הבוסט (רק אם קיים) |
| `pcfQuestionNumber4` | 3601.69 (לפני מע"מ) |
| `pcfsystemfield106` | 4249.99 (כולל מע"מ) |
| `pcfCreatedByPersonOrSystem` | `2` — מערכת |
| נוסף | טלפון · מייל · משפך · משקל התחלתי · משקל יעד — מועתקים אם קיימים |

## 5. Config (node `Config`)
`DRY_RUN` · `TZ` · `COHORT_END_OFFSET_DAYS=4` · `FOLLOWUP_OFFSET_DAYS=1` ·
`FOLLOWUP_HOUR=10` · `BOOST_PRODUCT_ID` · `TARGET_PRODUCT_ID` · `TARGET_PRICE_EXVAT` ·
`TARGET_PRICE_INCVAT` · `WON_STATUS_LABEL` · `NEW_STATUS_CODE=2` · `FALLBACK_OWNER_ID` · `NAME_PREFIX`

**כדי לעלות לאוויר:** `DRY_RUN → false` ואז הפעלה. שום דבר אחר.

## 6. תיאום עם WF-16 (מסלול מת)
`WF16DeadPath0001` עודכן 02.08.2026: השאילתה `שליפת עסקאות הלקוחה` מושכת גם
`pcfParentSale`, ובנוד `הכרעה` נוסף שער — לקוחה עם תהליך מכירה פתוח שמצביע
על עסקת בוסט (כלומר תוצר WF-17) **לא תסומן** "סיום בוסט — לא המירה".

## 7. גוצ'ות שאומתו בבנייה
- `/api/query` תומך ב-`query` עם `AND` בין סוגריים. אומת על `pcfCourse` + `pcfProduct`.
- picklist חוזר **כתווית עברית** בקריאה, אבל נכתב **כמספר**. הקוד משווה לתווית וכותב מספר.
- `pcfEndDate` חוזר `2026-08-16T00:00:00` — משווים 10 תווים.
- `accountidname` / `owneridname` **אינם שדות חוקיים** ב-`fields` — מפילים את כל השאילתה ב-400. שם הלקוחה נשלף בשאילתה נפרדת על אובייקט 1.
- שדה אופציונלי ריק נדחה ע"י Fireberry — נכתב רק אם יש ערך.

## 8. מצב הדאטה בזמן הבנייה (02.08.2026)
- 176 תהליכי מכירה · **175 בסטטוס "ליד חדש"** · אף אחד לא "עסקה נסגרה בהצלחה"
- רק **2** תהליכי מכירה משויכים למחזור בוסט (`pcfCourse`)
- 2 רשומות `1051` בלבד, שתיהן "פרימיום"
→ **הרצה חיה היום תחזיר אפס.** זה תקין. הפלואו יתעורר כשענף התשלום יתחיל למלא `pcfCourse` ולסגור עסקאות.

## 9. באג דאטה פתוח
`בוסט · אוגוסט 02` נושא `pcfStartDate = 2026-08-09` (צריך 08-02), כפילות מלאה עם
`בוסט · אוגוסט 09` שמסומן "התבטל". לתיקון ידני ב-UI.
