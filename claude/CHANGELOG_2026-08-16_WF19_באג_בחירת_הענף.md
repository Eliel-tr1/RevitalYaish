# יומן שינויים · WF-19 — 16.8.2026 · הבאג שהסתיר את כל ענף 2

**וורקפלואו:** `CG4nPYCgushC1qDz` · 27 צמתים · לא פעיל
**המשך של** `CHANGELOG_2026-08-16_WF19_ענף_החידוש.md`

---

## הבאג · צומת `🔀 בחירת ענף` שלח רק ליציאה הראשונה

צומת ה-Switch של n8n (typeVersion 3.2) שולח כברירת מחדל **רק ליציאה הראשונה שמתאימה**, אלא אם מסמנים במפורש *Send data to all matching outputs*.

`options` היה `{}`.

**המשמעות:** `⚙️ הגדרות` מחזיר `branch: 'both'`, שני התנאים מתקיימים, אבל n8n עצר ביציאה 0 (בוסט). **ענף 2 מעולם לא רץ ב-n8n — לא ב-16.8 ולא באף ריצה קודמת מאז 11.8.**

אימות מהריצות: בכל ריצה היסטורית (`164613`, `170520`, `170617`) רשימת הצמתים שרצו מסתיימת ב־`1.x` בלבד. אין ולו ריצה אחת שבה `2.1` הופיע.

**התיקון:**
```json
"options": { "allMatchingOutputs": true }
```

---

## שורש הכשל שלי · בדקתי קוד, לא חיווט

כל בדיקות ענף 2 שלי (12/12) רצו ברתמה מקומית שהזינה את צמתי הקוד ישירות — היא **עקפה את צומת ה-Switch לגמרי**. הלוגיקה אכן הייתה תקינה; החיווט לא.

**המסקנה המבצעית:** בדיקת קוד ברתמה אינה תחליף להרצה בתוך n8n. מעכשיו כל אימות נגמר בהרצה אמיתית של המנוע.

---

## איך אומת הפעם · הרצה אמיתית בתוך n8n

נוסף Webhook זמני → הוורקפלואו הופעל → נקראה כתובת ה-Webhook → הושבת → ה-Webhook הוסר.

**ריצה `170624` (mode: webhook) — 21 צמתים רצו:**
```
🔀 בחירת ענף        items=2   ← שתי היציאות פעלו
1.1 … 1.7 · 📋
2.1 · 2.2 · 2.3 · 2.4 · 2.5 · 2.6 · ⏳ המתנה · 2.7 · 2.8
```

**הרשומה שנוצרה בפיירברי:**
| שדה | ערך |
|---|---|
| שם | `מכירת תוכנית המשך - סער וינברג` |
| מוצר | תוכנית המשך אונליין |
| סטטוס | ליד חדש |
| פולואפ | 17.8 10:00 |
| נוצר | 16.8 22:40:59 |

---

## התנהגות ידועה · צומת הסיכום רץ פעמיים

ל־`📋 סיכום הריצה` יש ארבעה חיבורי כניסה (מ־`1.7` false, `2.5` false, `1.10`, `2.8`). n8n מריץ אותו בכל פעם שמגיע קלט — לכן הוא רץ **פעמיים**, פעם אחרי כל ענף.

- הרצה 1 · מציגה רק את ענף 1
- הרצה 2 · מציגה את **שני** הענפים

**למייק: להסתכל תמיד על ההרצה האחרונה של צומת הסיכום** (בורר ההרצות בראש חלון הצומת).
פתרון עתידי אם יפריע: צומת Merge לפני הסיכום.

---

## מצב מסירה (16.8, 22:42)

```
27 צמתים · לא פעיל (Active = off)
DRY_RUN: false
CYCLE_END_OVERRIDE: ''
RENEWAL_DATE_OVERRIDE: ''
MAX_PER_RUN: 25
🔀 בחירת ענף · allMatchingOutputs = true
```
גיבוי: `WF19_backup_20260816_2242.json`

**שני הענפים אומתו בייצור:**
- ענף 1 — יצר את `מכירת ליווי לאחר בוסט - סער וינברג` (ריצה `170534`, 16.8 21:35)
- ענף 2 — יצר את `מכירת תוכנית המשך - סער וינברג` (ריצה `170624`, 16.8 22:40)

---

## פתוח

1. **WF-16** — להוסיף `'ליד חדש'` למערך הסטטוסים הפתוחים. **טרם בוצע.**
2. **האוטומציה הפנימית בפיירברי** שדורסת שמות עסקאות — לאתר ולהחליט.
3. **ניקוי נתוני הבדיקה של סער** לפני עלייה לאוויר — שתי עסקאות שנוצרו בבדיקות.
4. **הפעלה (Active)** — רק אחרי אישור מייק.
5. שמות מחזורי הבוסט מוסטים בשבוע · מזהה תבנית CloudChat לבוסט.
