# יומן שינויים · 16/09/2026 — יהודית סואייה: המסלול הידני (WF-27), לא WF-20

## הקשר
אליאל דיווח: תשלום פרימיום ליהודית סואייה — נפתחה כפילות תהליך מכירה, הליווי לא שויך לעסקה החדשה, והליווי נשאר "בוסט" במקום "פרימיום".

---

## א. האבחון — הכשל **לא** היה ב-WF-20

הרצת n8n **258218** ב-16/09 08:28 UTC: **WF-27 · קליטת תשלום שאינו באשראי**. לא WF-20.
ההגדרות של WF-20 שתוקנו אתמול לא רלוונטיות למסלול הזה בכלל — זה קוד אחר לגמרי.

### 1. הכפילות — נוצרה **ידנית**, לא ע"י המערכת
```
תהליך המכירה של יהודית סואייה
createdon                    2026-09-16T11:26:21
createdbyname                עודד יעיש
pcfCreatedByPersonOrSystem   אנושי
```
העסקה נפתחה ידנית ב-11:26:21, והתשלום בוצע ממנה ב-11:28:36 — שתי דקות אחר כך. מסך התשלום הידני רץ **בתוך** רשומת תהליך מכירה וקורא את המזהה מכתובת ההורה (`getOpportunityId`), כלומר הוא פשוט השתמש בעסקה שעמדו עליה. שום אוטומציה לא פתחה אותה.

**אבל** — בתיק שלה כבר היה תהליך מכירה פתוח: `עסקה - יהודית | ליד`, ליד מוגר מ-11/09 **בלי מוצר**. וכאן כן יש פער אמיתי במערכת, ראה סעיף ב'.

### 2. הליווי לא עודכן — תיבת סימון שלא סומנה
ה-payload מהטופס:
```
new_product_purchase : false
purchased_product_id : null
```
וההחלטה שנרשמה: *"לא סומנה רכישת מוצר חדש — התהליך המקצועי לא יופעל."*

נסלק תשלום של **4,455 ₪ על "תוכנית ליווי 17 שבועות"** בזמן שרשומת הליווי הייתה על **"בוסט"** — ובכל זאת שרשרת התהליך המקצועי לא רצה, כי תיבת סימון בטופס נשארה על "לא". **תיבת סימון שאפשר לשכוח לסמן היא נקודת כשל, לא הגנה.**

---

## ב. התיקון ב-WF-20 — ליד פתוח בלי מוצר קולט את הרכישה

`selectSale()` התאים לפי מוצר או קטגוריה בלבד. ליד מוגר הוא **בלי מוצר**, ולכן חסר קטגוריה — וכל החיפושים דילגו עליו ונפתחה עסקה חדשה. **במערכת 14,764 לידים כאלה.**

נוסף שלב, במיקום מדויק בסדר העדיפויות:
1. רשומת תשלום מקורית · 2. פתוחה + אותו מוצר · 3. פתוחה + אותה קטגוריה · 4. כלשהי כולל סגורה + אותו מוצר · 5. כלשהי כולל סגורה + אותה קטגוריה · **6. ליד פתוח בלי מוצר → נתלים עליו והמוצר שלו נקבע** · 7. עסקה חדשה.

**למה דווקא אחרי הסגורות:** אחרת לקוחה חוזרת עם עסקה סגורה מאותה קטגוריה + ליד ישן הייתה מקבלת עסקה שנייה באותה קטגוריה דרך הליד. יש בדיקה ייעודית על זה.
מתג: `SALE_MATCH_EMPTY_LEAD: true`. **286 בדיקות עוברות** (היו 279), חטיבה 26 חדשה עם 7 בדיקות.

---

## ג. התיקון ב-WF-27 — ההחלטה נגזרת מהנתונים, לא מתיבת סימון

### שני צמתי שליפה חדשים (16 → 18 צמתים)
`רשומת הליווי (1051)` ו-`תהליכי המכירה של הלקוחה (4)` — בלעדיהם אי אפשר לדעת באיזו תוכנית הלקוחה נמצאת ואי אפשר לזהות כפילות.

### מעבר תוכנית מזוהה מהמציאות
אם המוצר שנסלק שייך לקטגוריה **אחרת** מזו שרשומת הליווי נמצאת בה — זו רכישת מוצר חדש בפועל, והתהליך המקצועי **מופעל גם בלי סימון**, עם אזהרה מפורשת שמסבירה מה קרה.

**מה במכוון לא נעשה:** כשאין רשומת ליווי כלל — **לא** יורים אוטומטית. הטופס משמש גם לתשלומים משלימים על עסקה קיימת, וירי עיוור היה שולח ללקוחה הודעת ברוכה־הבאה וצירוף לקבוצה על תשלום שאינו רכישה. במקרה כזה נפתחת **משימה** עם הסבר מה לעשות.

### זיהוי כפילות תהליך מכירה
אם ללקוחה כבר קיים תהליך מכירה באותה קטגוריה — אזהרה + **משימת איחוד** שמפרטת את שני התהליכים, מה לעשות, ו**איך למנוע בפעם הבאה** (לבדוק בתיק לפני פתיחת תהליך חדש). התשלום לא מוזז אוטומטית — המשתמש עמד על העסקה הזו במודע.
אם יש ליד פתוח בלי מוצר — אזהרה בלבד, בלי משימה.

**43 בדיקות עוברות** (היו 34), חטיבה 10 חדשה עם 9 בדיקות כולל המקרה של יהודית.

---

## ד. הפריסה
דיף מלא מול הגרסה החיה לפני כל פריסה:
- **WF-20** — 31 צמתים, שונו רק `נירמול וסיווג` ו`בניית תוכנית פעולה`.
- **WF-27** — 16 → 18, נוספו שני צמתי השליפה, שונו `בניית תוכנית תשלום` ו`סיכום הריצה`.

שניהם פעילים. גיבוי של הגרסה החיה נשמר לפני כל פריסה ב-`backup/`.

---

## ה. תיקון הנתונים של יהודית סואייה
גיבוי: `backup/BACKUP_2026-09-16_יהודית_סואייה_לפני_תיקון.json`

1. **הליד הפתוח** `CAEAAC0E` הפך לעסקה אמיתית — מוצר "תוכנית ליווי 17 שבועות", סטטוס "נסגרה בהצלחה", קבוצת הוואטסאפ והבעלות הועברו אליו. זה בדיוק מה שהמערכת המתוקנת הייתה עושה מעצמה.
2. **התשלום (4,455 ₪) ותשעת תיעודי התשלום** הועברו אליו.
3. אומת **0 קישורים** נותרו על העסקה שנפתחה ידנית — ואז היא **נמחקה**.
4. **רשומת הליווי** עודכנה בדיוק לערכים שענף MENTORING ב-WF-24 היה כותב: פרימיום · ליווי בתהליך · תוכנית ליווי 17 שבועות · העסקה החדשה · שבוע 0 · 17 שבועות · תאריך סיום 13/01/2027 · הקבוצה נשמרה.

**המצב עכשיו:** שתי עסקאות — בוסט ופרימיום, אחת לכל קטגוריה, שתיהן "נסגרה בהצלחה". הליווי פרימיום ופעיל.

**לא נשלחה שום הודעה** — התיקון נעשה בכתיבה ישירה, לפי ההנחיה שלא לירות שרשראות רטרו.

---

## פתוח
1. **הליווי של יהודית לא עבר את השרשרת המקצועית** — היא לא קיבלה הודעת ברוכה־הבאה ולא צורפה לקבוצה בוואטסאפ. נתוני הליווי נכונים, אבל התקשורת איתה לא יצאה. **דורש החלטה.**
2. הקובץ לעודד (44 פערים) — ממתין למילוי.
3. 977 לידים כפולים + 1,227 לקוחות עם ליד ורכישה.
4. 25 רשומות תשלום בלי לקוחה מקושרת.
5. 38 כפילויות תהליך ליווי — סומנו 14/09, טרם אושרו.

---

# עדכון · 16/09/2026 — השרשרת המקצועית נורתה עבור יהודית סואייה

אליאל אישר: "כן תירה". נשלחה קריאה ל-`/webhook/pro-setup-roital` עם ה-payload שהטופס היה מייצר לו סומן "רכישה של מוצר חדש" — עם **העסקה השורדת** ועם ה-`asmachta` המקורי (`MANUAL-FORM-MU3U8KZB-V06VDF`), כדי שהאידמפוטנטיות תחסום ירי חוזר בעתיד.

**הרצה 258280 · WF-24 (MENTORING) · `routed: true` · `success: true`**

| פעולה | תוצאה |
|---|---|
| עדכון תהליך הליווי (1051) | ✅ |
| עדכון תהליך המכירה (4) | ✅ |
| משימה לנציגה (בחירת תפריט) | ✅ |
| הערה על תהליך הליווי | ✅ |
| **הודעת וולקאם** | ❌ **לא נשלחה** |
| **צירוף לקבוצת וואטסאפ** | ❌ **נכשל** |

## שני כשלים אמיתיים שנחשפו

### 1. הוולקאם לא נשלח — הלקוחה אינה מנויה בקלאודצ'אט
```
welcome: { sent: false, skipped: true,
           reason: "הלקוחה אינה מנויה בקלאודצ'אט — לא ניתן לשלוח" }
```
אומת ישירות: `pcfCloudChatId` בכרטיס הלקוחה **ריק**.

זה לא כשל של הרצה בודדת. יהודית קנתה **סדנת בוסט באוגוסט** — כלומר היא כבר עברה פעם אחת בשרשרת המקצועית, ובכל זאת אין לה מנוי בקלאודצ'אט. **כל הודעה אוטומטית שתישלח אליה בעתיד תיפול באותה נקודה.**

### 2. הצירוף לקבוצת הוואטסאפ נכשל
```
whatsappGroup: { attempted: true, joined: false, state: "authorized",
                 reason: "GreenAPI לא אישר את הצירוף — ייתכן שהגדרות הפרטיות של הלקוחה חוסמות" }
```
GreenAPI מחובר ומאושר (`state: authorized`) — הכשל הוא בצד הלקוחה. יש קישור הזמנה זמין בפלט, ואפשר לשלוח לה אותו ידנית.

## פתוח — דורש הכרעה
1. **ליצור ליהודית מנוי בקלאודצ'אט** (יצירה בלבד, אפס הודעות — כמו שנעשה ל-5 לקוחות ב-12/09), לכתוב את `pcfCloudChatId`, ואז לירות שוב את הוולקאם.
2. **לשלוח לה ידנית את קישור ההזמנה לקבוצה.**
3. **לסרוק כמה עוד לקוחות פעילות חסרות `pcfCloudChatId`** — אם יהודית עברה בשרשרת ולא נוצר לה מנוי, סביר שיש עוד.
