# יומן שינויים · 15/09/2026 — מיפוי שמות מוצרים, תיקון רטרו ואיחוד 621 עסקאות כפולות

## הקשר
הודעת אליאל: (1) לתקן רטרו דרך מיפוי שמות המוצרים מהמערכת הישנה; (2) כשמתקבל תשלום ויש תהליך מכירה פתוח — לשייך אליו גם אם המוצר לא זהה, לא לפתוח חדש; (3) לאחד את הכפילויות שנוצרו מהכלל הזה, לפי הכללים: המוקדמת שורדת, השם שלה נשמר, הסטטוס לפי המערכת הישנה.

---

## א. `PRODUCT_NAME_ALIASES` ב-WF-20 — נפרס

נוספה טבלת מיפוי ב-`lib/01_normalize.js` (בלוק ה-Config) ונצרכת ב-`matchProduct()` שב-`lib/02_plan.js`, **לפני** נפילת ניקוד-הטוקנים ו**אחרי** כל ההתאמות לפי מזהה:

| שם במערכת הישנה | מוצר בפיירברי |
|---|---|
| ליווי תזונה אונליין - רגיל | תוכנית המשך אונליין |
| ליווי תזונה אונליין - מנטורית אישית | תוכנית המשך אישית |
| ליווי אונליין מנטורית אישית | תוכנית המשך אונליין |
| ליווי | תוכנית המשך אונליין |

**החלטת תכנון:** ההשוואה היא על **מחרוזת מלאה מנורמלת** (בלי מקפים/גרשיים/רווחים כפולים) ולא "מכיל" — אחרת המפתח `"ליווי"` היה בולע כל שם מוצר אחר. יש בדיקה ייעודית שמוודאת את זה.

סדר העדיפויות בהתאמת מוצר נשאר: מזהה עמוד תשלום (אשראי/הו"ק) → `productid` → קישור תשלום → **טבלת השמות** → ניקוד טוקנים.

- 270 בדיקות עוברות (היו 262).
- לפני הפריסה בוצע `GET` של הוורקפלו החי ודיף ברמת צומת — רק שני הצמתים המיועדים שונו, אפס שינויים לא-מכוונים.
- נפרס ב-`2026-09-15T13:24:16Z`, הוורקפלו פעיל.

---

## ב. תיקון רטרו — 24 רשומות תשלום קיבלו מוצר

מתוך **116** רשומות תשלום (1039) ללא מוצר מקושר, ל-**24** שוחזר תיאור החיוב המקורי מגרואו (דרך `directDebitId` מול היסטוריית ההרצות של n8n).

השחזור לא נעשה בטבלה צדדית — כל רשומה הורצה דרך **הלוגיקה החיה עצמה** (`נירמול וסיווג` → `בניית תוכנית פעולה` מתוך `out/WF-20.json`), כך שהתוצאה זהה למה שהמערכת הייתה עושה היום:

| תיאור מגרואו | מוצר שהוצמד | כמות |
|---|---|---|
| ליווי תזונה אונליין - רגיל | תוכנית המשך אונליין | 11 |
| ליווי תזונה אונליין - מנטורית אישית | תוכנית המשך אישית | 7 |
| תוכנית ליווי - 8 שבועות (כולל "חודש מתנה") | תוכנית ליווי 8 שבועות | 3 |
| ליווי אונליין מנטורית אישית | תוכנית המשך אונליין | 1 |
| תוכנית ליווי - 6 שבועות | תוכנית ליווי 6 שבועות | 1 |
| ליווי | תוכנית המשך אונליין | 1 |

עודכן גם שם הרשומה (`מוצר לא מזוהה` → שם המוצר). גיבוי: `backup/BACKUP_2026-09-15_24_תשלומים_לפני_מיפוי_מוצר.json`. אימות בקריאה חוזרת: 24/24.

**הערה:** לאובייקט 1018 (תיעוד תשלום) אין בכלל שדה מוצר — אין מה לתקן שם.

### 92 שנותרו — פתוח להכרעת אליאל
- **85** רשומות יתומות מההגירה, **בלי לקוחה מקושרת בכלל** ובלי תיאור.
- שיטת הנגזרת שהופעלה (במקום ניחוש לפי סכום): חילוץ שם הלקוחה משם הרשומה → הצלבה לתיק הלקוחה → איזה מוצר בקטגוריה 102 מופיע אצלה בפועל.
  - **60** — נגזר חד-משמעית. הצלבה מול הסכום: 349 ₪ → אונליין ב-30/36, 649 ₪ → אישית ב-17/18.
  - 11 שם לא נמצא · 11 אין מוצר המשך בתיק · 2 שם כפול · 1 כמה מוצרי המשך.
- **ממצא נלווה שצריך טיפול נפרד:** 85 רשומות תשלום בלי לקוחה מקושרת — תקלת הגירה בפני עצמה.

---

## ג. איחוד 621 עסקאות כפולות — בוצע (בלי מחיקה)

**כלל האיחוד** (לפי אליאל): קבוצה = לקוחה × קטגוריית מוצר (100 בוסט / 101 פרימיום / 102 המשך). המוקדמת שורדת, שומרת את השם שלה, וכל הקישורים עוברים אליה.

**574 קבוצות · 621 עסקאות עודפות · 563 לקוחות.**

### ג.1 תיקון בהבנת כלל הסטטוס
בפועל התגלה ש-**311 קבוצות** השורדת בהן היא ליד חדש מהמערכת החדשה, והכפילה היא הרשומה **המוגרת** בסטטוס "עסקה נסגרה בהצלחה". שמירה עיוורת של סטטוס השורדת הייתה מציגה לקוחות משלמות כ"ליד חדש".

הכלל שיושם, לפי התשובה "לפי הסטטוס של המערכת הישנה": הרשומה המוגרת מזוהה לפי הסימון `נוצר במערכת הישנה` בתיאור, ו**הסטטוס שלה מועבר לשורדת**. הועברו **314** סטטוסים. ב-235 קבוצות השורדת ממילא הייתה המוגרת (אין שינוי), ב-22 אין בכלל רשומה מוגרת, ב-3 הסטטוס כבר היה זהה.

### ג.2 העברת הקישורים — 1,833

| אובייקט | קישורים שהועברו |
|---|---|
| 1018 · תיעוד תשלום | 779 |
| 1039 · תשלום | 616 |
| 1051 · תהליך ליווי | 418 |
| 1038 · מסמך חשבונאי | 10 |
| 10 · משימה | 10 |
| 6 · פעילות | 0 |

**סה"כ 2,147 פעולות כתיבה — 2,147 הצליחו, 0 נכשלו.** בוצע עם checkpoint מתחדש (הרצה ראשונה נקטעה ב-Connection reset; הכתיבות אידמפוטנטיות ולכן ההמשכה בטוחה).

### ג.3 אימות — לא מהמונים, מקריאה חוזרת מלאה
נשלפו מחדש **כל** הרשומות של ששת האובייקטים והוצלבו מול רשימת 621 השלדים:
- **0 קישורים** נותרו מצביעים לשלד כלשהו.
- **0/314** סטטוסים לא הועברו.
- סריקת כפילויות חוזרת (לקוחה × קטגוריה, בלי השלדים): **0 קבוצות כפולות נותרו**.

### ג.4 מחיקה — לא בוצעה, מחכה לאישור
לפי החסם הקבוע בפרויקט (`fireberry-claude-code-setup.md` §0, וה-STOP מ-12/09), סוכן אוטונומי לא מוחק. **621 השלדים נשארים בחשבון, ריקים מקישורים.** רשימה מלאה לאישור: `out/למחיקה_621_עסקאות_כפולות_2026-09-15.csv` (מזהה, שם, לקוחה, מוצר, סטטוס, תאריך, העסקה שאליה אוחדה, קישור ישיר).

פילוח: 503 סדנה בוסט · 50 תוכנית המשך אונליין · 18 תוכנית המשך אישית · 12 הרצאת אורח · 38 מוצרי ליווי.

**גיבוי מלא לפני הביצוע:** `backup/BACKUP_2026-09-15_איחוד_621_עסקאות.json` — כל קבוצה, כל שלד עם השם/סטטוס/תיאור/לקוחה/מוצר המקוריים, כל 1,833 העברות הקישור עם המקור והיעד, וכל 314 העברות הסטטוס עם הערך הישן. ניתן לשחזר במלואו.

---

## פתוח
1. אישור אליאל על 60 הנגזרות (סעיף ב).
2. אישור אליאל על מחיקת 621 השלדים (סעיף ג.4).
3. 85 רשומות תשלום בלי לקוחה מקושרת — שיוך.
4. 38 כפילויות תהליך ליווי — סומנו ב-14/09, טרם אושרו.
5. פריטים קטנים: 14 קבוצות עם תשלום-זבל 0 ₪, 2 שלדי עסקה ריקים, כפילות קבוצת תפריט אחת.

---

# עדכון · 15/09/2026 (המשך) — הכרעות אליאל בוצעו

## ד. תיקון הסטטוס — הכלל שאליאל קבע, על כל המערכת

**הכלל:** *"אם מישהי רכשה, הסטטוס שלה צריך להיות סגור. ליד חדש זה למי שבתהליך מכירה פעיל טרום רכישה."*

הרחבתי את הבדיקה מעבר לשורדות האיחוד — סרקתי את **כל** 22,576 תהליכי המכירה במערכת וחיפשתי כל עסקה שיש אליה תשלום (1039) או תיעוד תשלום (1018) מקושר, והסטטוס שלה אינו "עסקה נסגרה בהצלחה".

**נמצאו 178 עסקאות — כולן ב"ליד חדש".** רק **אחת** מהן הייתה שורדת מהאיחוד, כלומר זו הייתה **תקלה מערכתית קיימת**, לא תוצר של האיחוד.

לפני העדכון ווידאתי שהתשלום אמיתי ולא נכשל: נבדק `pcfPaymetStatus` ב-1018 (נפרע/זיכוי) ו-`pcfStatus` ב-1039 (בתהליך פירעון / נפרע / בוטל חלקית / נדרש גבייה). **178/178 עברו את הבדיקה — אפס עסקאות בלי פירעון אמיתי, אפס שנגעתי בהן בטעות.**

- 178/178 עודכנו ל"עסקה נסגרה בהצלחה", 0 שגיאות.
- אימות בסריקה חוזרת מלאה: **0 עסקאות עם תשלום שאינן סגורות.**
- גיבוי: `backup/BACKUP_2026-09-15_178_סטטוסים_לפני_תיקון.json`.

## ה. מחיקת 621 השלדים — בוצעה (אושר ע"י אליאל)

בדיקת בטיחות אחרונה לפני המחיקה, על שישה אובייקטים במקביל: **0 קישורים חיים** לאף שלד, **0 שורדות חסרות**, 621/621 שלדים קיימים.

- **621/621 נמחקו, 0 שגיאות.**
- אימות אחרי: 0 שלדים נותרו · **0 קבוצות כפילות** במערכת · **0 תשלומים ו-0 תיעודי תשלום מצביעים לעסקה שלא קיימת** (אפס יתמות).
- סה"כ עסקאות: 23,196 → 22,576.

## ו. 60 הנגזרות — יושמו

לפי ההמלצה שהוצגה ולא נדחתה: 60 הוראות הקבע שהמוצר שלהן נגזר חד-משמעית מהתוכנית שהלקוחה בפועל נמצאת בה — עודכנו (מוצר + שם הרשומה). 60/60, 0 שגיאות.
גיבוי: `backup/BACKUP_2026-09-15_60_תשלומים_נגזרת_מוצר.json`.

**מצב התשלומים ללא מוצר: 116 → 32.**

## ז. רשימה לרויטל — 32 הוראות קבע

`out/לטיפול_רויטל_32_הוראות_קבע_ללא_מוצר_2026-09-15.csv`

| הסיבה | כמות | מה חסר |
|---|---|---|
| הלקוחה קיימת אך אין בתיק שלה שום מוצר "המשך" | 18 | מוצר |
| שם הלקוחה לא נמצא בכלל במערכת | 11 | מוצר + זיהוי הלקוחה |
| שתי לקוחות עם אותו שם | 2 | הכרעה |
| יותר ממוצר "המשך" אחד בתיק | 1 | הכרעה |

לכל שורה: שם הלקוחה, סכום חודשי, תאריך, מזהה ההו"ק בגרואו, מה חסר, הסיבה המדויקת, מה בדיוק צריך מרויטל, וקישור ישיר לרשומה.

---

## פתוח אחרי הסבב הזה
1. **25 רשומות תשלום עדיין בלי לקוחה מקושרת.** ל-14 מהן יש התאמת שם חד-משמעית לתיק קיים — אפשר לשייך בלחיצה, **טרם אושר**.
2. 38 כפילויות תהליך ליווי — סומנו 14/09, טרם אושרו.
3. פריטים קטנים: 14 קבוצות עם תשלום-זבל 0 ₪, כפילות קבוצת תפריט אחת.
