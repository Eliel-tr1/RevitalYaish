# יומן שינויים · 15/09/2026 (ערב) — שורש הכפילויות נסגר: שיוך תשלום לעסקה סגורה

## הקשר
אליאל עבר בעין על המערכת אחרי איחוד 621 העסקאות ומצא לקוחה עם **שתי** עסקאות "המשך" ועם רשומת ליווי שמציגה "פרימיום": חן בוסקילה אורן (`F9225739-3F4C-47AC-A1F2-AA99FBC06F38`).

זו לא הייתה שארית מההגירה. זו הייתה **תקלה חיה** שנוצרה היום ב-11:54, אחרי האיחוד, ע"י המערכת עצמה.

---

## א. חקירה — שלושה כשלים בשרשרת אחת

הרצה 254620, 15/09 08:54 UTC. ה-payload מגרואו:
`paymentDesc: "ליווי אונליין מנטורית אישית"` · `paymentSource: "Payment Links"` · `directDebitId: 53035069` · 649 ₪.

### כשל 1 — המוצר לא זוהה
התיאור הוא שם מהמערכת הישנה. מיפוי השמות (`PRODUCT_NAME_ALIASES`) נפרס ב-13:24 — **שעה וחצי אחרי** ההרצה. ניקוד הטוקנים נכשל, והתוצאה: `מוצר לא מזוהה`.

### כשל 2 — 🔴 השורש האמיתי: `selectSale()` חיפש **רק בין העסקאות הפתוחות**
ההחלטה שנרשמה בהרצה:
> *"תהליך המכירה היחיד/האחרון של הלקוחה סגור/הופסד — ייפתח תהליך מכירה חדש עבור הרכישה הנוכחית."*

לחן כבר הייתה עסקת "תוכנית המשך אונליין" — אבל בסטטוס "עסקה נסגרה בהצלחה". חיוב הו"ק מגיע **תמיד** אחרי שהעסקה נסגרה, ולכן הכלל הישן פתח עסקה חדשה בכל פעם.

**זה בדיוק המנגנון שייצר את 621 הכפילויות שאיחדנו.** האיחוד ניקה את התוצאה; השורש נשאר פתוח והמשיך לייצר כפילויות חדשות.

זה גם סותר ישירות את הכרעת 13/09 *("לקוחה חוזרת ⇒ עסקה חדשה")*. **ההכרעה ההיא בוטלה** לטובת הכלל של אליאל: תהליך מכירה אחד לכל קטגוריה.

### כשל 3 — שרשרת התהליך המקצועי לא רצה
WF-22 החזיר `routed: false` עם הנימוק *"לא נמצא מוצר תואם בקטלוג — לא ניתן לסווג"*. לכן WF-25 (תוכנית המשך) לא רץ, ורשומת הליווי של חן נשארה על **פרימיום · ליווי הסתיים בהצלחה** מהתוכנית הקודמת שלה, במקום לעבור ל**המשך · ליווי בתהליך**. זה מה שאליאל ראה.

**ממצא נלווה (קוסמטי, לא תוקן):** בתוך התשובה של WF-22 מופיע `{"error":"Unauthorized","status":401,"message":"Invalid token undefined"}`. מקורו בצומת `העברה לתהליך המקצועי` ב-WF-22, שאין לו קרדנציאלס וכשאין ניתוב הוא פונה ל-endpoint של פיירברי בלי אימות. זו קריאת סרק — היא לא משפיעה על כלום, אבל היא מייצרת רעש שעלול להסתיר שגיאה אמיתית.

---

## ב. התיקון בקוד — `SALE_MATCH_INCLUDE_CLOSED`

`lib/02_plan.js`, `selectSale()` נבנה מחדש לסדר עדיפויות מפורש:

1. רשומת תשלום מקורית (חשבונית/חיוב חוזר) — גוברת על הכל, ללא שינוי.
2. עסקה **פתוחה** של אותו מוצר בדיוק.
3. עסקה **פתוחה** מאותה קטגוריה — המוצר שבה מתעדכן.
4. **חדש:** עסקה **כלשהי, כולל סגורה**, של אותו מוצר בדיוק.
5. **חדש:** עסקה **כלשהי, כולל סגורה**, מאותה קטגוריה — המוצר שבה מתעדכן.
6. רק אם אין כלום — עסקה חדשה.

**הסטטוס של עסקה סגורה לא משתנה** — יש בדיקה ייעודית שנכשלת אם מישהו יוסיף `statuscode` לגוף העדכון. נוסף מתג `SALE_MATCH_INCLUDE_CLOSED: true` ב-Config, ובדיקה ששומרת שהוא דלוק.

### בדיקות
**279 עוברות** (היו 270). נוספה חטיבה 25 עם 9 בדיקות, כולל המקרה האמיתי של חן.
שתי בדיקות ישנות שקיבעו את כלל 13/09 **עודכנו במפורש**, עם הערה בקוד שמתעדת שההכרעה בוטלה — כדי שמישהו לא "יתקן" את זה בחזרה.

### A/B על 250 payloads אמיתיים מהיסטוריית ההרצות

| תצורה | עסקאות חדשות שהיו נפתחות |
|---|---|
| לפני התיקון (רק פתוחות, בלי מיפוי שמות) | **81** |
| רק מיפוי שמות | 82 |
| **אחרי התיקון המלא** | **52** |

מתוך 100 השיוכים לעסקה קיימת, **42 הם לעסקה סגורה** — כלומר 42 מקרים שבעבר היו פותחים כפילות.
**שיוך חוצה קטגוריות: 0.** אף תשלום לא נתלה על עסקה מקטגוריה אחרת.

נפרס ב-`2026-09-15T18:47:03Z` אחרי דיף מלא מול הגרסה החיה — רק שני הצמתים המיועדים.

---

## ג. תיקון הנתונים של חן בוסקילה אורן

גיבוי מלא לפני: `backup/BACKUP_2026-09-15_חן_בוסקילה_לפני_תיקון.json`.

1. התשלום (649 ₪), תיעוד התשלום והקבלה (`קבלה - 50135`) הועברו מהעסקה הכפולה לעסקה הקיימת `522C619C` ("עסקה - חן בוסקילה אורן | תוכנית המשך אונליין").
2. אומת: **0 קישורים** נותרו על הכפילה — ואז היא נמחקה.
3. רשומת הליווי `E6356C1B` עודכנה **בדיוק למה ש-WF-25 ענף CONTINUE היה כותב** (לא ערכים שהמצאתי): `pcfCurrentProgram` → המשך · `pcfStatus` → ליווי בתהליך · `pcfProduct` → תוכנית המשך אונליין · `pcfSale` → העסקה השורדת · `pcfEstimatedEndDate` → 01/01/2099 · שם מעודכן. הקבוצה בפועל לא נגעתי בה.

**אימות סופי:** לחן שלוש עסקאות — בוסט, ליווי 12 שבועות, המשך אונליין. אחת לכל קטגוריה, כולן "נסגרה בהצלחה".

### הרצה חוזרת של ה-payload המקורי שלה נגד הקוד הפרוס
```
מוצר : תוכנית המשך אונליין
עסקה : קיימת (לא @newSale)
יצירת עסקה חדשה: 0
```
ההחלטות שנרשמות: *"המוצר הותאם לפי טבלת שמות המערכת הישנה"* + *"התשלום שויך לתהליך מכירה קיים שכבר נסגר — לפי הכלל שתהליך מכירה אחד לכל קטגוריה. הסטטוס שלו לא שונה."*

---

## ד. סריקה חוזרת של כל המערכת

- **כפילויות לקוחה × קטגוריה: 0.** חן הייתה היחידה, וטופלה.
- 14,764 עסקאות ללא מוצר — **אלו לידים** מההגירה (`עסקה - X | ליד`), לא כפילויות רכישה. הן בלי קטגוריה ולכן מחוץ לכלל.
  - 700 לקוחות עם יותר מליד אחד (977 עודפים) · 1,227 לקוחות עם גם ליד וגם עסקת רכישה.
  - **זה נושא נפרד שטרם הוכרע** — האם לאחד לידים כפולים, ומה לעשות עם ליד של לקוחה שכבר רכשה.

---

## פתוח
1. **האם לירות את שרשרת התהליך המקצועי עבור חן** — היא רכשה היום ולא קיבלה וולקאם/קבוצה/תפריט. זה ישלח לה הודעות בפועל. **דורש אישור.**
2. 977 לידים כפולים + 1,227 לקוחות עם ליד ורכישה — להכרעה.
3. 25 רשומות תשלום בלי לקוחה מקושרת (ל-14 יש התאמת שם חד-משמעית).
4. 32 הוראות קבע לטיפול רויטל.
5. 38 כפילויות תהליך ליווי — סומנו 14/09, טרם אושרו.
6. קריאת הסרק ב-WF-22 שמחזירה 401 — רעש, כדאי לנקות.
