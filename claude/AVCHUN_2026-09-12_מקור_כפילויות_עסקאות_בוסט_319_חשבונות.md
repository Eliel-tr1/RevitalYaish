# אבחון · 12/09/2026 — מקור הכפילויות בתהליכי מכירה (בוסט)

## השאלה של מייק
"עכשיו אני רואה כפילויות של תהליכי מכירה, למה?" — עם 4 דוגמאות קונקרטיות (8edabb6f/5428c2fd, f7d51f87/3a29e7b5).

## התשובה הישירה

**זה מנוע ההגירה (`bulk.py`).** שלפתי **את כל** תהליכי המכירה על מוצר הבוסט (6,580 רשומות, לא מדגם) וניתחתי אותם:

| דפוס שם | כמות | מקור |
|---|---|---|
| "העסקה של X" | 1,173 | המערכת החיה (WF-20→22→23) |
| "עסקה - X \| סדנה בוסט" | 5,406 | מנוע ההגירה — נוצר מהיסטוריית תשלומים בפיירברי **הישן** |

השדה `description` על כל רשומה מהמנוע כתוב מפורש: `"נוצר במערכת הישנה: {תאריך}"` — זה חתימת האצבע של `bulk.py` (ראו §5.1 ב-HANDOFF: "כל לקוחה מקבלת עסקה... לפי התשלומים").

**זה כשלעצמו לא באג** — זו בדיוק המשימה של ההגירה: להביא היסטוריית רכישות בוסט מ-20,505 הלקוחות בישן. רוב ה-5,406 הן רכישות ישנות אמיתיות (יולי/אוגוסט 2026 ואף 2025) שמוגרות עכשיו כי ההגירה חודשה ורצה היום (`createdon` נע בין 10/09 09:43 ל-12/09 10:27 — **המנוע פעיל ורץ כרגע**, לא רק ב-11/09 כמו שדווח).

## הבעיה האמיתית: התנגשות

**319 חשבונות** יש להם **גם** רשומת "העסקה של X" (חיה) **וגם** רשומת "עסקה - X | סדנה בוסט" (מוגרת) — לאותו לקוח, לאותו מוצר. זו הכפילות בפועל שאתה רואה. המנוע לא בודק אם כבר קיימת עסקה חיה לפני שהוא יוצר עסקה מוגרת.

ל-6 מתוכן (כולל רעות דמארי ותהאני מוצלח מהדוגמאות שלך) **תאריך התשלום ב"מערכת הישנה" קרוב מאוד (יום-יומיים) לרגע יצירת הרשומה המוגרת ולרכישה בפועל** — כלומר: **החשבון הישן של פיירברי עדיין מקבל נתוני תשלום חיים במקביל למערכת החדשה**, לפחות עבור לקוחות שטרם הוגרו. זו לא רק "היסטוריה ישנה שמוגרת באיחור" — זו רכישה של היום שמגיעה לשני המקומות.

⚠️ **המשמעות קדימה**: כל עוד המנוע ממשיך לרוץ וממשיך לתפוס לקוחות שרוכשות היום ממש (לפני שסיימו להיות מוגרות), **כל רכישה חדשה של לקוחה "לא מוגרת עדיין" עלולה לקבל שתי רשומות עסקה** — אחת מ-WF-23 החי ואחת מההגירה. זה לא מוגבל ל-27 הלקוחות התקועות מה-DRY_RUN — זה יכול לקרות לכל לקוחה שנמצאת עדיין בתור ה-20,505.

## מה תוקן ומה לא

- **3 מתוך ה-319** (רעות דמארי, תהאני מוצלח, נורית כהן) תוקנו כבר — הם היו חלק מ-27 הלקוחות התקועות שביקשת שאשייך, ואומתו בקריאה חוזרת.
- **316 הנותרות — לא נגעתי.** זה חורג לגמרי מהמשימה שביקשת (שיוך מחזור+קבוצה ל-27 תקועות) וזו בעיית איכות נתונים מבנית שדורשת החלטה, לא תיקון טכני שלי לבד.

## מה אני צריך ממך

1. **האם ההגירה עדיין אמורה לרוץ עכשיו?** אם כן — היא תמשיך ליצור התנגשויות כאלה על כל רכישה חדשה של לקוחה לא-מוגרת. אם המנוע לא אמור לרוץ באופן פעיל כרגע, זה ממצא דחוף בפני עצמו.
2. **לגבי 316 הזוגות הקיימים** — מה עדיף: למחוק/למזג את הרשומה המוגרת כשיש כבר רשומה חיה לאותו לקוח ומוצר? למזג הפוך? זה נוגע במחיקת רשומות בפיירברי — לא אבצע בלי אישורך המפורש לפי הכלל הקבוע.
3. **לגבי המשך ההגירה** — אם היא ממשיכה, כדאי להוסיף למנוע בדיקה: "האם כבר קיימת עסקה חיה (`pcfProduct`=בוסט, ללא `description` של מערכת ישנה) לאותו accountid" לפני יצירת עסקה מוגרת — זה ימנע התנגשויות עתידיות. זה שינוי בקוד המנוע (`bulk.py`/`build.py`), לא ב-Make — רוצה שאכין את זה?

## נספח — 6 המקרים עם חפיפת תאריך יום-יומיים (חשד ל"רכישה של היום מוכפלת")

| לקוחה | תאריך ישן (מהתיאור) | נוצרה ברשומה מוגרת |
|---|---|---|
| רעות דמארי | 12/09 04:49 | 12/09 10:27:09 |
| תהאני מוצלח | 11/09 22:35 | 12/09 10:27:09 |
| סמדר רוזן | 10/09 11:49 | 10/09 23:09:43 |
| אביגל קבילו | 10/09 15:35 | 11/09 12:07:33 |
| שרון | 11/09 09:07 | 11/09 11:29:04 |
| ניצה משה | 11/09 03:57 | 11/09 12:07:56 |

כל שאר ה-5,400 הרשומות המוגרות — תאריך ה"מערכת הישנה" רחוק ימים עד חודשים מרגע היצירה (רכישות היסטוריות אמיתיות), לא חשודות כהתנגשות עם רכישה חיה של היום.
