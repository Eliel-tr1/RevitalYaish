# CHANGELOG · 14/09/2026 — תיעוד תשלום דולג בשקט בגל השלישי

## הבאג
WF-20 כותב בשלושה גלים: **אב → בנים → גל 3**. בגל 3 הייתה בדיקה:
> אם ליצירה אין `objectid` — דלג.

הבדיקה נכונה **רק להערה** (הערה בלי רשומת יעד חסרת ערך), אבל הוחלה על **כל** יצירה בגל.
ל**תיעוד תשלום (1018)** אין `objectid` כלל — הקישורים שלו הם `pcfPaymentCollection`
ו-`pcfSale` — ולכן הוא **דולג תמיד** כשנפל לגל 3.

**מתי הוא נופל לגל 3:** כשרשומת האב היא **תהליך מכירה חדש**, כלומר בכל רכישה של
לקוחה שכל העסקאות שלה סגורות. התשלום נרשם — **תיעוד התשלום לא נוצר בכלל.** בשקט.

## התיקון
```
const needsObjectId = String(op.object) === 'note';
const missing = op.kind === 'update' ? !rid : (needsObjectId && !body.objectid);
```
הערה בלי יעד — עדיין מדולגת. יצירה אחרת שקישור שלה לא נפתר — **נוצרת בכל זאת**:
עדיף רשומה לא משויכת על פני כסף שלא נרשם.

## בדיקות
7 בדיקות על צומת גל 3, כולל בקרת שלילה שהערה יתומה עדיין מדולגת ושעדכון בלי מזהה יעד מדולג.

## אימות חי — מקרה א' מלא, ריצה 253817
לקוחה בלי אף תהליך מכירה פתוח, הו"ק שהוקמה מתוך גרואו:

| | |
|---|---|
| ענף | `STANDING_ORDER_BACKOFFICE` ✓ |
| תהליך מכירה חדש | נוצר, המזהה חולץ ✓ |
| הוראת קבע | מקושרת לעסקה ✓ · סוג תשלום = הוראת קבע ✓ |
| **תיעוד תשלום** | **נוצר ומקושר להוראה** ✓ |
| תהליך ליווי | בוסט → פרימיום · 6 שבועות · סיום 26/10 · רשומה אחת ✓ |
| משימות | בחירת תפריט + שיוך קבוצה (עם נוסח הסיבה הייעודי לגרואו) ✓ |
| התהליך המקצועי | הופעל ✓ |

`success=true` · `parentIdMissing=false` · `failedCount=0`.

## המכנה המשותף של שלושת הבאגים שנחשפו היום
כולם באותו מסלול: **לקוחה חוזרת בלי תהליך מכירה פתוח.**
1. חילוץ המזהה לא הכיר את `opportunityid`.
2. גל 3 דילג על תיעוד התשלום.
3. הרגרסיה שלי מפריסה על קוד ישן.

המסלול הזה נפוץ בפרודקשן ולא היה מכוסה בבדיקות. שלושתם תוקנו ואומתו חי.
