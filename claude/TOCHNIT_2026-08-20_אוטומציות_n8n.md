# תוכנית האוטומציות · רויטל יעיש

**תאריך:** 20.08.2026 · **סטטוס:** ממתין לאישור מייק. שום דבר לא נבנה.

5 תהליכים חדשים ותהליך אחד קיים לבדיקה. 14 הודעות, 36 מקרי קצה, 2 שדות להוספה, 5 שאלות פתוחות.

## מה כבר סגור

- **הוובינר** קבוע: כל יום רביעי ב-20:00. נקבע בקונפיגורציה, ללא תלות בשדה בפיירברי (אין שדה כזה).
- **משימות** משויכות לנציגה שרשומה כאחראית על תהליך המכירה, עם נפילה ל`0fa826fa-cbc8-4ba5-9cd4-3e59f129f4de` (ויטרו ראשי).
- **פגישות** נוצרות ידנית בפיירברי על ידי נציגה. האוטומציה סורקת ומגיבה, לא יוצרת.
- **תזכורות שקילה** לכל מי שבליווי פעיל (1051, סטטוס 2 = ליווי בתהליך).

## WF-30 · סדרת הוובינר

**מצב:** חדש

במהלך שבוע הבוסט הלקוחה מקבלת 5 הודעות סביב המפגש עם רויטל. הזמנה יום לפני, תזכורת בבוקר, תזכורת שעה לפני, הודעה שמתחילים, ופולואפ למחרת.

**מתי רץ:** רץ 5 פעמים בשבוע, בשעות קבועות. כל הרצה בודקת אם יש מחזור בוסט שהיום הוא היום הנכון עבורו.

### מה נשלח ומתי

| מתי | תבנית | הערה |
|---|---|---|
| שלישי 10:00 | `boost_webinar_invite_v2` | הזמנה. הוובינר מחר בערב. |
| רביעי 09:00 | `boost_webinar_morning` | תזכורת בוקר. נכנס לתוכה webinar_time = 20:00. |
| רביעי 19:00 | `boost_webinar_1h` | שעה לפני. |
| רביעי 20:00 | `boost_webinar_live` | מתחילים עכשיו. |
| חמישי 10:00 | `boost_webinar_followup` | פולואפ עם הכפתור לפגישה. |

### איך זה עובד

1. מוצא מחזור בוסט שהיום הוא היום הנכון עבורו לפי תאריך ההתחלה
2. בודק שהמחזור לא בוטל ולא הסתיים
3. בודק שההודעה הזו לא נשלחה כבר למחזור הזה
4. שולף את כל הלקוחות שרשומות למחזור
5. לכל לקוחה: מוצא אותה בקלאודצ'אט לפי טלפון
6. כותב את השדות הנדרשים למנוי
7. מפעיל את הפלואו ששולח את התבנית
8. מסמן במחזור שההודעה נשלחה, וכותב הערה

### מקרי קצה

| מה יכול לקרות | מה התהליך עושה |
|---|---|
| מחזור מבוטל או שהסתיים | מדלגים לגמרי, לא שולחים כלום |
| לקוחה בלי טלפון תקין | מדלגים עליה, מופיעה בדוח הריצה עם סיבה |
| לקוחה שלא קיימת בקלאודצ'אט | מדלגים עליה, מופיעה בדוח. לא יוצרים מנוי חדש כי אין הסכמה לשליחה |
| שתי הרצות באותו יום | החסם במחזור עוצר את השנייה. שום לקוחה לא מקבלת פעמיים |
| שני מחזורים במקביל | כל מחזור מטופל בנפרד עם החסם שלו |
| הפלואו בקלאודצ'אט נכשל באמצע | לא מסמנים שנשלח. ההרצה הבאה תנסה שוב |
| הודעה נשלחה אבל הסימון נכשל | מסומן בדוח כתקלה קריטית ולא מנסים שוב אוטומטית, כדי לא לשלוח פעמיים |

### מה נדרש ממייק

- מזהי הפלואוז של 5 התבניות
- אימות איך לקוחה מקושרת למחזור בוסט בפיירברי
- לכבות את האוטומציה הישנה 6.6 שלא תתנגש

## WF-31 · בקשת פגישה מהוובינר

**מצב:** חדש

כשלקוחה לוחצת בוואטסאפ על הכפתור לפגוש את רויטל, נפתחת משימה בפיירברי לנציגה עוד היום. בלי זה הלחיצה נעלמת ואף אחד לא יודע שהיא רוצה פגישה.

**מתי רץ:** רץ ברגע שהלקוחה לוחצת. לא מתוזמן.

### מה נשלח ומתי

| מתי | תבנית | הערה |
|---|---|---|
| מיידי | `קלאודצ'אט קורא לוובהוק` | הכפתור בתבנית boost_webinar_followup |

### איך זה עובד

1. הוובהוק מקבל את הטלפון של הלקוחה
2. מחפש אותה בפיירברי לפי טלפון
3. מוצא את תהליך המכירה הפתוח שלה
4. פותח משימה: לתאם פגישה עם רויטל, לביצוע היום, עדיפות גבוהה
5. משייך את המשימה לנציגה שאחראית על תהליך המכירה
6. מקשר את המשימה ללקוחה ולתהליך המכירה
7. כותב בתוך המשימה מאיפה הבקשה הגיעה, עם קישור להרצה

### מקרי קצה

| מה יכול לקרות | מה התהליך עושה |
|---|---|
| לא נמצאה לקוחה לפי הטלפון | פותחים משימה בכל זאת לאחראי ברירת המחדל, עם הטלפון הגולמי בתוכה. לא מאבדים ליד |
| אין תהליך מכירה | פותחים משימה מקושרת ללקוחה בלבד |
| יש כמה תהליכי מכירה | לוקחים את הפתוח והעדכני ביותר |
| לתהליך המכירה אין אחראי | נופלים לאחראי ברירת המחדל |
| הלקוחה לוחצת פעמיים | בודקים אם כבר יש משימה פתוחה כזו מהיום. אם כן, לא פותחים שנייה, רק מוסיפים הערה |
| פיירברי לא זמין | הוובהוק מחזיר שגיאה וקלאודצ'אט מנסה שוב |

### מה נדרש ממייק

- כתובת הוובהוק שאתן לך, להדביק בקלאודצ'אט בתוך הפלואו של boost_webinar_followup

## WF-32 · סדרת הפגישה

**מצב:** חדש

אחרי שנציגה קובעת פגישה בפיירברי, הלקוחה מקבלת זימון ואז 3 תזכורות. יום לפני, ביום עצמו, וחצי שעה לפני.

**מתי רץ:** רץ כל רבע שעה ובודק אילו פגישות צריכות הודעה עכשיו.

### מה נשלח ומתי

| מתי | תבנית | הערה |
|---|---|---|
| מיד אחרי הקביעה | `meeting_invite` | תאריך ושעה נכנסים להודעה |
| יום לפני, 10:00 | `meeting_reminder_day_v2` | עם שני כפתורי אישור הגעה |
| ביום הפגישה, 09:00 | `meeting_reminder_today` | רק למי שעדיין לא אישרה הגעה |
| 30 דקות לפני | `meeting_reminder_30m` | תזכורת אחרונה |

### איך זה עובד

1. שולף פגישות עתידיות מאובייקט פגישה
2. מסנן: רק פגישות שהסטטוס שלהן עתידה להתקיים
3. לכל פגישה בודק איזו הודעה מגיעה לה עכשיו, ואם היא כבר נשלחה
4. מוצא את הלקוחה בקלאודצ'אט לפי טלפון
5. כותב תאריך ושעה לשדות המנוי
6. מפעיל את הפלואו המתאים
7. כותב בפגישה את התאריך שבו ההודעה יצאה
8. מוסיף הערה בפגישה: איזו תזכורת נשלחה ומתי

### מקרי קצה

| מה יכול לקרות | מה התהליך עושה |
|---|---|
| פגישה בלי לקוחה מקושרת | מדלגים ופותחים משימת בקרה. אי אפשר לשלוח למי שלא יודעים מי היא |
| פגישה בלי שעה | מדלגים ופותחים משימת בקרה |
| פגישה שנקבעה למחר בעוד שעתיים | שולחים זימון מיד ומדלגים על התזכורת של יום לפני, אין זמן |
| פגישה שנדחתה לתאריך אחר | הסימונים מתאפסים והתזכורות יוצאות מחדש לתאריך החדש |
| פגישה שבוטלה | מפסיקים לשלוח מיד |
| הלקוחה כבר אישרה הגעה | מדלגים על התזכורת של יום הפגישה. זו בדיוק הסיבה שהכפתורים חייבים לחזור לפיירברי |
| פגישה שנקבעה בסוף השבוע | התזכורות יוצאות בשעות שנקבעו, גם בשישי ושבת. אם לא רוצים, תגיד |
| שתי פגישות לאותה לקוחה | כל פגישה מקבלת את ההודעות שלה בנפרד |

### מה נדרש ממייק

- מזהי הפלואוז של 4 התבניות
- אישור להוסיף שדה תאריך אחד לאובייקט פגישה, לסימון תזכורת יום הפגישה
- הגדרה מה נציגה חייבת למלא כשהיא פותחת פגישה

## WF-33 · חיווי אישור הגעה

**מצב:** חדש

כשלקוחה לוחצת אני אגיע או צריכה להזיז, התשובה נרשמת על הפגישה בפיירברי. בלי זה התזכורת ביום הפגישה תישלח גם למי שכבר אישרה.

**מתי רץ:** רץ ברגע הלחיצה. לא מתוזמן.

### מה נשלח ומתי

| מתי | תבנית | הערה |
|---|---|---|
| מיידי | `כפתור אני אגיע` | סטטוס אישור הגעה עובר לאישרה, וסטטוס הפגישה לאישרה הגעה |
| מיידי | `כפתור צריכה להזיז` | נפתחת משימה לנציגה לתאם מחדש |

### איך זה עובד

1. הוובהוק מקבל את הטלפון ואת הכפתור שנלחץ
2. מחפש את הלקוחה בפיירברי
3. מוצא את הפגישה העתידית הקרובה ביותר שלה
4. מעדכן את שדה אישור ההגעה בהתאם ללחיצה
5. אם ביקשה להזיז, פותח משימה לנציגה שאחראית על הפגישה, לביצוע היום
6. כותב הערה בפגישה עם מה שנלחץ ומתי

### מקרי קצה

| מה יכול לקרות | מה התהליך עושה |
|---|---|
| לא נמצאה לקוחה | פותחים משימת בקרה עם הטלפון הגולמי |
| אין פגישה עתידית | פותחים משימת בקרה. יכול לקרות אם היא לוחצת אחרי שהפגישה כבר עברה |
| שתי פגישות עתידיות | לוקחים את הקרובה ביותר |
| לוחצת אני אגיע ואז צריכה להזיז | הלחיצה האחרונה קובעת, וכל לחיצה נרשמת בהערות |
| לוחצת אחרי שהפגישה כבר התקיימה | לא נוגעים בסטטוס, רק כותבים הערה |

### מה נדרש ממייק

- כתובת הוובהוק להדבקה בשני הפלואוז של התזכורות
- החלטה: כשלקוחה מבקשת להזיז, מה סטטוס אישור ההגעה אמור להיות

## WF-34 · תזכורות שקילה שבועית

**מצב:** חדש

כל לקוחה בליווי פעיל מקבלת תזכורת למלא את טופס השקילה. בחמישי בבוקר, בחמישי בערב למי שלא מילאה, ובראשון בבוקר למי שעדיין לא.

**מתי רץ:** 3 הרצות קבועות בשבוע.

### מה נשלח ומתי

| מתי | תבנית | הערה |
|---|---|---|
| חמישי 09:00 | `weekly_weighin` | לכל מי שבליווי פעיל |
| חמישי 19:00 | `weekly_weighin_fu1` | רק למי שלא מילאה |
| ראשון 09:00 | `weekly_weighin_fu2` | רק למי שעדיין לא מילאה |

### איך זה עובד

1. שולף את כל תהליכי הליווי בסטטוס ליווי בתהליך
2. בהרצות הפולואפ: בודק מי כבר שלחה טופס שקילה השבוע ומוציא אותה מהרשימה
3. לכל לקוחה: מוצא אותה בקלאודצ'אט לפי טלפון
4. מפעיל את הפלואו המתאים
5. כותב הערה בתהליך הליווי

### מקרי קצה

| מה יכול לקרות | מה התהליך עושה |
|---|---|
| לקוחה שהתחילה ליווי אתמול | מקבלת. אין סיבה לדלג |
| לקוחה שהליווי שלה נגמר השבוע | מקבלת עוד תזכורת אחת ואז יוצאת מהרשימה בשבוע הבא |
| לקוחה שמילאה ביום שני | נחשבת כמי שמילאה השבוע ומדלגים עליה בחמישי? צריך החלטה, ראה שאלות פתוחות |
| לקוחה בבוסט בלבד | לא מקבלת. הסינון לפי ליווי פעיל מטפל בזה |
| לקוחה בתוכנית המשך | לא מקבלת בשלב זה. אם צריך, תגיד ואוסיף |
| חג שחל בחמישי | נשלח כרגיל. אם רוצים לוח חגים, זו עבודה נוספת |

### מה נדרש ממייק

- מזהי הפלואוז של 3 התבניות
- החלטה על חלון הזמן שנחשב השבוע

## WF-07 · בדיקת טופס סיום הבוסט

**מצב:** קיים · בדיקה בלבד

התהליך הזה כבר קיים ופעיל. הוא מקבל את טופס הסיום מהבוסט ומעדכן את תהליך הליווי. לא בונים אותו מחדש, רק מוודאים שהוא עובד.

**מתי רץ:** רץ כשלקוחה שולחת את הטופס.

### מה נשלח ומתי

| מתי | תבנית | הערה |
|---|---|---|
| בשליחת הטופס | `WF-07 קיים` | מעדכן ציון מוכנות, צפייה בזום, וקישור לטופס |

### איך זה עובד

1. שולח טופס בדיקה לוובהוק עם נתונים מלאים
2. בודק שהתהליך זיהה את הלקוחה הנכונה
3. בודק שכל השדות התעדכנו בתהליך הליווי
4. בודק שנכתבה הערה
5. מריץ גם תרחישים חלקיים: בלי מזהה, בלי טלפון, עם טלפון שלא קיים

### מקרי קצה

| מה יכול לקרות | מה התהליך עושה |
|---|---|
| טופס בלי מזהה תהליך | אמור ליפול לזיהוי לפי טלפון או מייל |
| טלפון שלא קיים במערכת | אמור לסיים בלי לכתוב כלום ולדווח |
| ללקוחה כמה תהליכי ליווי | אמור לבחור את העדכני ביותר |
| טופס שנשלח פעמיים | צריך לבדוק. יכול להיות שהוא דורס את הנתונים |

### מה נדרש ממייק

- אישור להריץ טסטים על רשומה אמיתית או על לקוחת בדיקה

## שדות להוספה

| אובייקט | שם השדה | סוג | למה |
|---|---|---|---|
| מחזור בוסט (1007) | `pcfWebinarStagesSent` | טקסט | סימון אילו הודעות וובינר כבר יצאו למחזור. בלי זה הרצה כפולה תשלח לכל הנשים פעמיים. |
| פגישה (6) | `pcfReminderSameDay` | תאריך | סימון שהתזכורת ביום הפגישה יצאה. יש כבר שדות לזימון, ליום לפני ול-30 דקות, חסר רק זה. |

שניהם שדות סימון נגד שליחה כפולה. פעולה מוסיפה בלבד.

## שאלות פתוחות

### מה זה השבוע לעניין השקילה?

האם לקוחה שמילאה ביום שני נחשבת כמי שמילאה השבוע ולכן מדלגים עליה בחמישי, או שכל שבוע מתחיל מחדש בחמישי בבוקר? זה משנה למי נשלחת התזכורת.

### צריכה להזיז, מה הסטטוס?

כשלקוחה לוחצת שהיא צריכה להזיז את הפגישה, מה שדה אישור ההגעה צריך להראות? הערכים הקיימים הם אישרה, לא אישרה, לא ענתה. אף אחד מהם לא מדויק.

### תזכורות בסוף שבוע

אם פגישה נקבעה ליום ראשון, התזכורת של יום לפני יוצאת בשבת. לשלוח כרגיל או להקדים לשישי?

### מה נציגה חייבת למלא

כשנציגה פותחת פגישה, אילו שדות חובה עליה למלא כדי שהאוטומציה תעבוד? אני מציע: תאריך ושעה, לקוחה מקושרת, תהליך מכירה מקושר, אחראי, סטטוס עתידה להתקיים. תאשר או תשנה.

### תוכנית המשך ושקילה

לקוחות בתוכנית המשך לא מקבלות תזכורת שקילה בתכנון הנוכחי. זה נכון?

## סדר ביצוע

| # | תהליך | למה בסדר הזה |
|---|---|---|
| 1 | WF-31 · בקשת פגישה | הכי דחוף. בלי זה כל לחיצה על הכפתור בפולואפ נעלמת. גם הקצר ביותר לבנייה. |
| 2 | WF-30 · סדרת הוובינר | 5 הודעות, הרבה נשים בבת אחת. אחרי שהמשימות עובדות. |
| 3 | WF-33 · חיווי אישור הגעה | חייב להיות לפני סדרת הפגישה, אחרת התזכורת ביום הפגישה תרדוף גם את מי שאישרה. |
| 4 | WF-32 · סדרת הפגישה | הכי מורכב. 4 הודעות עם חסמים ותלות בשדות שנציגה ממלאת ידנית. |
| 5 | WF-34 · תזכורות שקילה | פשוט יחסית, אבל נוגע בכל הלקוחות בליווי. |
| 6 | WF-07 · בדיקות | בסוף, כשהכל בנוי ואפשר לבדוק את השרשרת המלאה. |

## ממצאי סקירה שרלוונטיים לבנייה

- **התבנית לחיקוי היא WF-06B.** רק היא משתמשת בדפוס הנכון: צמתי httpRequest מקוריים + קרדנשל שמור בשם `CloudChat Revital Yaish` (id `CCRevitalYaish01`). לא להעתיק מ-`h4Hx36yLH6ErOd9t` שמחזיק טוקן בטקסט גלוי.
- **רצף הקריאות לקלאודצ'אט:** `GET /subscriber/get-info-by-user-id?user_id=972...` → אימות `user_ns` → `PUT /subscriber/set-user-fields-by-name` → אימות `matched_items` → `POST /subscriber/send-sub-flow`.
- **קלאודצ'אט מחזיר `status: ok` גם על שדה שלא קיים.** חובה לאמת `matched_items` מול השדות שנשלחו, אחרת ההודעה יוצאת עם משתנים ריקים.
- **אין error handler מחובר לרוב התהליכים.** `WF16ErrHandler01` קיים אך כבוי, ורק WF-18 ו-WF-27 מפנים אליו. מומלץ לחבר את כל התהליכים החדשים.
- **שדות התזכורת באובייקט 6 הם תאריכים, לא כן/לא.** `pcfZoomReminderDay`, `pcfZoomReminder30`, `pcfZoomMsgSent`. הבדיקה היא null / not null.
- **פיקליסטים באובייקט 6:** `statuscode` 1=התקיימה, 2=בוטלה, 3=עתידה להתקיים, 7=אישרה הגעה. `pcfArrivalConfirmation` 1=אישרה, 2=לא אישרה, 3=לא ענתה.
- **אובייקט משימה = 10.** `statuscode` 1=פתוח, `prioritycode` 5=גבוהה, `tasktypecode` 5=מכירה, `scheduledend`=תאריך לביצוע, קישור דרך `pcfClient` ו-`pcfSale`.
- **האוטומציה הישנה 6.6** (הזמנה לוובינר ביום 3) כבויה וכל צמתי הכתיבה בה מנוטרלים. לוודא שהיא נשארת כבויה.
- **`h4Hx36yLH6ErOd9t`** (תזכורת יום לפני התור) פעילה, שייכת ללקוח וטרינרי אחר (Provet), ומחזיקה טוקן קלאודצ'אט גלוי. לא קשורה לרויטל. שווה להחליט אם היא בכלל צריכה לשבת כאן.