# יומן שינויים · 15/09/2026 — תיקון: הודעת וולקאם בוסט נדחית במטא (#131008)

## הבקשה
אליאל דיווח על שגיאה בקלאודצ'אט בעסקה של גל (0535002122):
`(#131008) Required parameter is missing — Parameter of type text is missing text value`,
וביקש בדיקה + גריפה אחורה.

## מה נמצא — שורש הבעיה

התבנית שנשלחת בפועל לענף הבוסט היא `welcome_boost_v2` (`flowNs: f201689s4391241`),
שאושרה במטא עם **3 משתנים**:

| משתנה | תפקיד |
|---|---|
| `{{1}}` | שם פרטי (שדה מערכת) |
| `{{2}}` | **תאריך תחילת מחזור הבוסט** |
| `{{3}}` | קישור הצטרפות לקבוצה |

בקלאודצ'אט קיים שדה שנוצר בדיוק בשביל `{{2}}`: `boost_cycle_start_date`
(`var_ns f201689v16638207`, תיאור: *"תאריך תחילת מחזור הבוסט (מוזן מ-Fireberry
1007) - הודעת וולקאם בוסט"*).

**בקוד החי של WF-23 (צומת "בניית תוכנית מקצועית") אף פעם לא נכתב לשדה הזה.**
לענף הבוסט נכתבו רק `wa_group_invite_link` (תקין) ו-`mentor_name` (שדה שהתבנית
הזו כלל לא משתמשת בו — שייך לתבנית הליווי). הנתון עצמו — `cohort.pcfStartDate`
— כבר נשלף בקוד ומשמש לבחירת המחזור הפתוח, פשוט לא מופה הלאה לקלאודצ'אט. שורת
קוד חסרה, לא בעיית דאטה.

**תוצאה:** `{{2}}` תמיד ריק ⇒ מטא דוחה כל שליחה עם #131008. מכיוון ש-`send-sub-flow`
מחזיר `status: ok` ברגע שהוא קולט את הבקשה (לא ברגע שהיא נשלחת בפועל), n8n
לא ראה שום כשל — זו הייתה תקלה שקטה לגמרי.

### היקף בפועל — אומת על 8 ריצות אמיתיות מאז המעבר ללייב (12/09)

| תאריך | לקוחה | טלפון |
|---|---|---|
| 12/09 | רעות דמארי | 0546401483 |
| 13/09 | מעיין | 0522878938 |
| 13/09 | אביטל אדרי | 0502143184 |
| 13/09 | דנה בנישטי | 0525520852 |
| 14/09 | אילנה לוי | 0522682017 |
| 14/09 | אתי כהן | 0528741341 |
| 15/09 | גל | 0535002122 |
| 15/09 | אורטל בדישי | 0549536747 |

כולן דיווחו `status: ok` ב-n8n, כולן היו נועדות להיכשל באותה שגיאה. **אף אחת
מהן לא קיבלה הודעת וולקאם עד לתיקון הזה** — יש להחליט אם לשלוח backfill ידני
(ראה "פתוח" למטה).

שתי חריגות שעלו בדרך, לא קשורות לתקלה הזו: מירב (13/09) נפלה ל-`BOOST_NO_SALE`
— לא נמצא תהליך מכירה (כבר מתועד בפרויקט בנפרד); וסער (חשבון בדיקה) קיבל
`Too Many Attempts` מקלאודצ'אט על ריצה כפולה ב-12/09.

## מה שונה

**workflow:** `WF-23 · סדנת בוסט` — מזהה n8n `vRP5tdFeS3AWPdRv`, פעיל.

שני צמתים עודכנו (JS בלבד, שום פרמטר/קונקשן/הגדרה אחרת לא נגעו — אומת בדיף
מלא לפני/אחרי, שורה מול שורה):

1. **"קליטה וסיווג"** — נוסף ל-CFG: `CC_FIELD_BOOST_CYCLE_DATE: 'boost_cycle_start_date'`.
2. **"בניית תוכנית מקצועית"**:
   - נוסף `formatDMY()` — ממיר ISO ל-DD.MM.YYYY (פורמט הדוגמה בתבנית).
   - בבניית `cc.fields`, לענף הבוסט בלבד: נוסף `{ name: CC_FIELD_BOOST_CYCLE_DATE, value: formatDMY(cohort.pcfStartDate) }`.
   - נוספה אזהרה: אם למחזור אין `pcfStartDate` — מתועד ב-warnings במקום להיכשל בשקט שוב.

**בוצע דרך:** n8n Public API (`PUT /api/v1/workflows/vRP5tdFeS3AWPdRv`). גיבוי מלא
של המצב הקודם נלקח **לפני** הביצוע: `claude/BACKUP_2026-09-15_WF23_לפני_תיקון_שדה_תאריך_מחזור_בוסט.json`.

## אימות

1. דיף מלא מקומי לפני שליחה: רק שני הצמתים הללו השתנו, ורק שדה ה-`jsCode`
   שלהם. `connections`/`settings`/`staticData`/שם ה-workflow זהים ביט-לביט.
2. GET עצמאי אחרי ה-PUT (לא מסתמך על תגובת ה-PUT עצמה): 34/34 הצמתים זהים
   למה שהתכוונתי לפרוס, `active: true` נשמר.
3. **טסט חי ראשון על סער וינברג (0533435240)** למחזור הפתוח להרשמה כרגע —
   **בוסט 20–27.9.26** (הקבוצה: "סדנת בוסט לירידה במשקל מחזור 20.09.2026..."):
   - נכתבו 3 השדות בקלאודצ'אט: `wa_group_invite_link`, `mentor_name`,
     `boost_cycle_start_date=20.09.2026` — כולם `matched_items` תקין.
   - נשלח `send-sub-flow` על `welcome_boost_v2`.
   - **נבדק בפועל מול `/subscriber/chat-messages` ו-`/flow/error-logs/data`
     (לא הסתפקתי ב-`status: ok`)** — גוף ההודעה שיצאה בפועל מכיל את שני
     המשתנים החדשים נכון: *"המחזור שלך מתחיל ביום ראשון - 20.09.2026"* +
     קישור הקבוצה הנכון. **אפס שגיאת #131008.** התיקון עובד ברמת התוכן.
   - בטסט הראשון הזה ההודעה כן נחסמה במטא בשגיאה **נפרדת** — #131049 (ראו
     סעיף למטה) — ולכן לא הגיעה בפועל לוואטסאפ של סער.
4. **טסט חי שני, אחרי שסער שלח הודעה לבוט (פתיחת חלון 24 שעות,
   `allow_send_message` עבר מ-`false` ל-`true`):**
   - `send-sub-flow` נשלח שוב על אותו `welcome_boost_v2`, בלי לכתוב מחדש את
     השדות (כבר היו מוגדרים נכון מהטסט הראשון).
   - נבדק שוב מול `/subscriber/chat-messages` ו-`/flow/error-logs/data`:
     ההודעה יצאה עם `wamid` תקין, **אפס רשומות ב-error-logs הפעם** (השוואה
     ישירה לטסט הראשון, ששם השגיאה נרשמה שם מיד).
   - **אליאל אישר בפועל: "קיבלתי מעולה".** ✅ **קבלה מלאה מקצה לקצה —
     מהקוד ב-n8n, דרך קלאודצ'אט, עד המכשיר בפועל.**

### 🔴 תופעה נפרדת שעלתה בטסט הראשון — לא קשורה לתיקון, ולא חזרה בטסט השני

בטסט הראשון ההודעה נחסמה במטא עם קוד אחר: **#131049** — *"This message was not
delivered to maintain healthy ecosystem engagement"*. זו הגבלת קצב/איכות של
מטא, ומתועדת כבר ב-`PLAYBOOK_מתודולוגיית_הטמעה_ויטרו_v1.md` (א.4.5): שליחת
תבניות שיווקיות רבות לאותו מספר בזמן קצר גורמת למטא לווסת. מספר הבדיקה של
סער קיבל באותו יום עוד תבנית (`boost_webinar_live`) רגע לפני הטסט — בדיוק
התבנית של הרעש. **בטסט השני, אחרי שסער יצר אינטראקציה אמיתית עם הבוט (הודעה
נכנסת, לא רק סדרת תבניות מהבוט), ההגבלה לא הופיעה וההודעה נמסרה.** תואם
לחלוטין את התיאוריה — לא רגרסיה מהתיקון ולא הבעיה המקורית (#131008), אלא
תופעת-לוואי של סביבת בדיקה שמוצפת בתבניות שיווקיות חד-כיווניות. לא צפויה
לפגוע בלקוחות אמיתיות בזרימה רגילה (הן שולחות/מקבלות הודעות רגילות דרך הבוט).

## פתוח / לתשומת לב

1. **Backfill ל-8 הלקוחות שנפגעו (12–15/09)** — לא בוצע. דורש כתיבת שלושת
   השדות + `send-sub-flow` פר-לקוחה, לפי הקבוצה/מחזור שהיא שויכה אליו בפועל
   (לא בהכרח המחזור הפתוח כרגע). מחכה להחלטה/אישור.
2. **`mentor_name` נכתב גם לענף הבוסט** למרות ש-`welcome_boost_v2` לא משתמש
   בו בכלל — לא מזיק, רק מיותר. אפשר לנקות בנפרד, לא דחוף.
3. שווה לעקוב אחרי #131049 על מספר הבדיקה של סער אם ממשיכים לבצע בו טסטים
   רצופים של תבניות שיווקיות — כדאי לפזר בזמן, או לוודא שיש אינטראקציה
   נכנסת אמיתית לפני כל סבב טסטים.

## סטטוס: ✅ סגור ומאומת מקצה לקצה
