# CHANGELOG · 14/09/2026 — רגרסיה: פריסה על בסיס קוד ישן, ושחזור מלא

## מה קרה — תקלה שלי

עותק העבודה המקומי של WF-20 ושל התהליכים המקצועיים היה **ישן**: הוא לא כלל תיקונים
שנעשו בסשנים אחרים ב-08/09 וב-14/09. שלוש הפריסות שביצעתי היום (16:19, 16:52, 17:03)
דרסו את הקוד החי בגרסה הישנה **פלוס** השינויים של היום.

**איך זה התגלה:** אליאל הבחין שרשומת תשלום נוצרה בלי "סוג תשלום".

## מה נדרס בפועל

### WF-20
1. **`pcfPaymentCategory` ("סוג תשלום") — כל המנגנון נעלם.** התיקון מ-08/09 שמפריד
   "סוג תשלום" (רגיל/תשלומים/הו"ק) מ"אמצעי תשלום" לא היה בקוד שפרסתי.
   גם שני צמתי השאילתה של 1039 איבדו את השדה מרשימת השדות.
2. **תיקון פתרון ה-`@ref` בצומת "סיכום הריצה"** (14/09, "לקוחה חדשה ללא ליד") — נמחק.
   `accountid`/`saleid` היו חוזרים כמחרוזת הפניה גולמית במקום כמזהה.

### WF-22…WF-26 — החמור מבין השניים
**`DRY_RUN` הוחזר ל-`true`** ורשימת חשבונות הבדיקה שוחזרה, בעוד שבחי הם היו
`false` ורשימה ריקה מאז 12/09.

**המשמעות:** בין 17:03 ל-19:05 כל לקוחה **שאינה** סער וינברג הייתה רצה בתצוגה מקדימה
בלבד — בלי תהליך ליווי, בלי צירוף לקבוצה, בלי וולקאם.

## היקף הנזק בפועל — נבדק

**אפס לקוחות אמיתיות נפגעו.** בחלון 17:03–19:05 רצו בתהליכים המקצועיים שתי ריצות
בלבד, שתיהן של סער וינברג, ושתיהן עם `DRY_RUN=false` (הוא היה ברשימת ההיתר).

ב-WF-20 נוצרה רשומת תשלום אחת בלי "סוג תשלום" — של סער. **תוקנה ידנית ל"תשלומים".**

## השחזור

1. שוחזרו מהגיבוי החי שנלקח לפני הפריסה הראשונה:
   `01_normalize.js` · `02_plan.js` · `05_summary.js` · `06_prohook.js`.
2. `pcfPaymentCategory` הוחזר לרשימת השדות של 1039 ב-`build_wf.py`.
3. `DRY_RUN: false` ורשימות ריקות הוחזרו ב-`10_input.js`.
4. שלושת השינויים של היום הורכבו **מחדש** על הבסיס הנכון:
   שני מסלולי תשלום · ספרה בודדת + אזהרת סתירה · ענף הו"ק מהבק-אופיס · משימת הקבוצה.
5. **אימות דיף מלא** מול הגיבוי החי: בכל חמשת הוורקפלואו, ההבדל היחיד שנותר הוא
   השינויים המכוונים של היום. אפס שינויים לא-מכוונים.
6. הרצה חוזרת של 250 הפיילואדים האמיתיים: 5 ריצות משנות התנהגות (בדיוק ההקמות
   מהבק-אופיס), 245 ללא שינוי, **אפס** חיובים ותיקים נכנסו לענף החדש.

כל חמשת הוורקפלואו נפרסו מחדש ואומתו: `DRY_RUN=false`, רשימות ריקות,
`pcfPaymentCategory` קיים, תיקון ה-`@ref` חזר.

## שורש הבעיה — והמסקנה

עותק עבודה מקומי בסביבה ארעית **אינו** מקור האמת. הקוד החי ב-n8n הוא.

**כלל לכל סשן עתידי בפרויקט הזה:**
> לפני כל פריסה — למשוך את הוורקפלואו החי, לעשות דיף מול מה שעומדים לפרוס,
> ולוודא שכל הבדל הוא מכוון. אם יש הבדל שלא אתה יצרת — עצור.
> גיבוי לפני פריסה זה הכרחי אבל לא מספיק: צריך גם **לקרוא** אותו.

## חוב שנותר

**חבילת הבדיקות המקומית של WF-20 ישנה** ביחס לקוד החי — 15 בדיקות נכשלות כי הן
מצפות להתנהגות שהוחלפה בכוונה (למשל `אמצעי תשלום = 7`, שבוטל ב-08/09, ורשימות
חשבונות הבדיקה שרוקנו ב-12/09). **הציפיות בבדיקות צריכות להתעדכן לקוד החי** —
לא להפך. עד שזה ייסגר, אין להסתמך על החבילה הזו כשער לפריסה.
