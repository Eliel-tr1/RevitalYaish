
# תיקון 13/09/2026 — פתיחת תהליך מכירה חדש (לא שימוש חוזר בישן), אימות על מירב

## מה השתנה בייצור

### 1. קוד n8n — WF-20 (`p1AfsAeXcrlhtiNk`, node "בניית תוכנית פעולה")
**התיקון הראשון (שנפרס קודם) היה שגוי** — הוא השתמש מחדש בתהליך המכירה הישן (הפך "עסקה הופסדה" ל"עסקה נסגרה בהצלחה"). מייק תפס את זה ותיקן: אסור לגעת בישן, יש לפתוח חדש לגמרי.

**התיקון הנכון, פרוס וחי מ-13/09/2026 ~13:42:**
- כשלחשבון קיים אין תהליך מכירה פתוח (`openSales.length === 0`), הקוד כבר לא מנסה לעשות שימוש חוזר ב-`sales[0]`.
- `selectSale()` מחזיר `{ sale: null, createNew: true, why: ... }`.
- מיד אחרי הקריאה, אם `createNew && accountFound` — נוצרת פעולת `add({kind:'create', object:'4', ...})` שפותחת **תהליך מכירה חדש לגמרי**: `statuscode:2` ("ליד חדש"), משויך לחשבון ולמוצר, ואז ממשיך לזרימה הרגילה של WF-22/23 (סגירה, שיוך מחזור+קבוצה) בדיוק כמו כל רכישה תקינה.
- התהליך הישן (אם היה "הופסד"/"בוטל") **נשאר כמו שהוא** — היסטוריה אמיתית, לא נגעו בו.
- אומת: נשלף ה-workflow החי אחרי הפריסה, `active:true` נשמר, הקוד המעודכן קיים בשרת.

### 2. רשומות מירב (חשבון `705972BF-DFD5-47B3-958F-706CA65B9713`) — תוקנו ואומתו ב-GET ישיר
| רשומה | פעולה | אומת |
|---|---|---|
| תהליך מכירה ישן `FCBA6A5B...` ("עסקה - מירב \| ליד") | הוחזר למצבו המקורי: `statuscode=12` (הופסדה), מוצר/מחזור/קבוצה = ריק | ✅ GET |
| תהליך מכירה **חדש** `f1ca0c2a-aa78-48fe-81eb-f4fb7ebf0ba5` ("העסקה של מירב") | נוצר: `statuscode=11` (נסגרה בהצלחה), מוצר=בוסט, מחזור=בוסט 13-20.9.26, קבוצת וואטסאפ מקושרת | ✅ GET |
| תשלום `2607751E...` | `pcfSale` שוייך לתהליך המכירה **החדש** | ✅ GET |
| תהליך ליווי `ed57df70...` | `pcfSale` שוייך לתהליך המכירה **החדש** | ✅ GET |
| חשבון | `pcfWhatsAppReferans` = קבוצת הבוסט הפעילה | ✅ GET (מסבב קודם) |
| משימה `33C3F64E...` ("שיוך ידני לתשלום מ-GROW · מירב") | נסגרה, **תיאור עודכן** לשקף את התיקון הנכון (היה כתוב שם בטעות שהתהליך הישן נסגר — זה תוקן) | ✅ |

## ⚠️ ממצא חדש שנפתח בבדיקת "המצטרפות מ-24 השעות האחרונות"

בדקתי אילו עוד לקוחות בוסט מהיממה האחרונה תקועות (תשלום בלי שיוך). נמצאו עוד 3, אבל **זה לא אותו באג אחד**:

1. **טלי אברהם** — יש לה חשבון **ויש תהליך מכירה פתוח בדיוק על בוסט** (`statuscode=2`) — ובכל זאת התשלום לא שוייך אליו. **זה באג שלישי, שונה גם ממירב וגם מ"חשבון לא נמצא"** — עדיין לא חקרתי את השורש.
2. **ימית ליבנת**, **פזית עינב** — **אין להן חשבון בפיירברי בכלל.** זה הבאג הגדול יותר שכבר תועד קודם (21 מתוך 23 המשימות התקועות) — קשור לתקלת טלפון/Make שכבר מתועדת בנפרד, **לא** לבאג שתיקנתי עכשיו.

**המשמעות:** התיקון של היום פותר רק חלק מהתמונה (מקרה מירב: חשבון קיים, אין תהליך פתוח). "השלמת הרטרו" לכל ההיסטוריה היא לא הרצה אחת — יש לפחות 3 סוגי כשל שונים וצריך טיפול נפרד לכל אחד.

## 🔴 חסמים פתוחים — מחכים להחלטת מייק

1. **הצטרפות פיזית לקבוצת וואטסאפ (GreenAPI):** אין בסשן הזה טוקן GreenAPI כלל (`idInstance`/`apiTokenInstance`) — לא סופק מעולם לאף שיחה בפרויקט. לא ניתן להוסיף אף אחד לקבוצה עד שיסופק, או שמישהו יעשה זאת ידנית מהטלפון.
2. **שליחת תפריט אוטומטית ב-15:00:** WF-06B (שליחת תפריטים מתוזמנת) מעולם לא נבנה. יש גישת CloudChat תקינה, אבל: הלקוחות הרלוונטיות אינן מנויות ב-CloudChat, וקיימת "גוצ'ת חלון 24 שעות" מתועדת — הקריאה יכולה להחזיר `ok` גם כשההודעה לא הגיעה בפועל, בלי שום סימן.

## פעולות ממתינות
- החלטת מייק על שני החסמים לעיל.
- חקירת הבאג השלישי (טלי אברהם).
- טיפול בשני החשבונות החסרים (ימית ליבנת, פזית עינב) — קשור לתקלת ה-Make/טלפון המתועדת בנפרד.
- לאחר סגירת כל אלה — מעבר לרטרו הכולל (23 המשימות התקועות מ-19/08).
