# יומן שינויים · 20/08/2026 — הזיכוי שלא נכנס לפיירברי

## מה באמת קרה

הוובהוק **כן** הגיע — פעמיים, ריצות `189900` ו-`189901` ב-18:36.

```json
{ "transactionCode": "yycWAdXi1qtylTWyh3E2nw==",
  "invoiceNumber": "50851",  "invoiceUrl": "…", "identifyParam": "" }
{ "transactionCode": "yycWAdXi1qtylTWyh3E2nw==",
  "invoiceNumber": "60284",  "invoiceUrl": "…", "identifyParam": "" }
```

שניהם סווגו `INVOICE_NO_MATCH` ופתחו משימה. **לא נוצרה רשומת מסמך** — ולכן זה נראה כאילו כלום לא קרה.

## למה השיוך נכשל — הסיבה השורשית

| | מזהה העסקה |
|---|---|
| רכישת הבוסט המקורית | `pCwo2/jFdaM1mLdjASi82g==` |
| הזיכוי | `yycWAdXi1qtylTWyh3E2nw==` |

**גרואו פותחת עסקה חדשה לגמרי לזיכוי.** המזהה שלה אינו קשור לעסקה המקורית בשום צורה.

הפיילואד של מסמך מגרואו מכיל בסך הכול ארבעה שדות: `transactionCode`, `invoiceNumber`,
`invoiceUrl`, `identifyParam`. **אין סכום, אין טלפון, אין הפניה לעסקה המקורית.**
`identifyParam` — השדה היחיד שיכול היה לגשר — הגיע **ריק**.

בדקתי גם: גרואו **לא** שלחה שום וובהוק עסקה נפרד על הזיכוי. רק שני מסמכי החשבונאות.

לכן, תחת הכלל "שיוך רק לפי מזהה חד-ערכי", המערכת נהגה נכון כשסירבה לנחש.
מה שלא היה בסדר — שהמסמך עצמו נעלם.

## מה תוקן

**מסמך שלא ניתן לשייך נשמר עכשיו בפיירברי** — עם מספר המסמך, הסוג והקישור,
ו**בלי שום שיוך**: אין לקוחה, אין תשלום, אין תיעוד, אין תהליך מכירה.
שם הרשומה נושא `⚠️ לא משויך`, ולצידה נפתחת משימה עם כל הפרטים.

זה לא ניחוש — מספר המסמך והקישור ידועים בוודאות. מה שלא ידוע פשוט לא נכתב.

מתג: `CREATE_UNMATCHED_DOCUMENT` (ברירת מחדל `true`).

**הבדיקות שמגנות על זה** מוודאות שהמסמך הלא-משויך אינו מכיל `pcfaccountid`,
`pcfPaymentCollection`, `pcfPaymentRecord` או `pcfLinkedSale` — כולל בתרחיש
ליאת גבאי שגרם לתקלה של הבוקר.

## שני המסמכים נכנסו

ריפליי מהפיילואדים השמורים:

| מסמך | סוג | תוצאה |
|---|---|---|
| 50851 | קבלה | ✅ `74183A80-…` |
| 60284 | חשבונית זיכוי | ✅ `C7419FE3-…` |

## מה עדיין לא קורה — ודורש החלטה

### 1. הזיכוי לא מקטין את רשומת התשלום

לאובייקט 1039 יש `pcfTotalRefunded` ו-`pcfTotalRefundedIncludingVat`.
אי אפשר למלא אותם: הפיילואד לא נושא סכום, ואין דרך לדעת לאיזה תשלום הזיכוי שייך.

### 2. 🔴 הפתרון האמיתי — `identifyParam`

זהו שדה חופשי שגרואו מחזירה בכל וובהוק מסמך. כרגע הוא ריק בכל עמודי התשלום.
אם עמודי התשלום יעבירו בו מזהה יציב — **טלפון הלקוחה** או מזהה תהליך המכירה —
כל מסמך, כולל זיכוי, ישויך אוטומטית ובוודאות.

זו הגדרה בצד גרואו, לא בקוד.

### 3. מיפוי קידומות המסמכים — טעון אימות

| קידומת | סוג |
|---|---|
| 49 | חשבונית מס/קבלה |
| 25 | חשבונית מס |
| 50 | קבלה |
| **60** | **חשבונית זיכוי ← הנחה שלי, לא אומתה** |

הסקתי את `60` מהזוג שהגיע בזיכוי. יש לאמת מול המסמכים עצמם.

## בדיקות

WF-20: **196** — כולן עוברות. פרוס ופעיל.
