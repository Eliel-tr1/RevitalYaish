# אימות מצב חי לקראת משימה 4.3 — חיבור טופס יצירת הליד
תאריך: 04/08/2026 · מבצע: Sensei (Roital) לבקשת אליאל · **read-only בלבד, אפס כתיבה בוצעה**
מקור: `POST https://api.fireberry.com/api/v3/query` + `GET /metadata/records/{obj}/fields` מול החשבון החי של רויטל.

משימת ClickUp: `86cawzjex` — "❌ 4.3 · 🔥 חיבור טופס יצירת הליד לוובהוק המרכזי" (משויכת לטל, תחת שלב 4 `86cawzg3b`).

---

## 1. הממצא המרכזי — הצינור תוקן ב-28/07, והתיעוד מפגר אחריו

| יום | לידים שנוצרו | מתוכם **ללא** תהליך מכירה |
|---|---:|---:|
| 23/07 | 12 | **11** 🔴 |
| 24/07 | 29 | **24** 🔴 |
| 25/07 | 40 | **39** 🔴 |
| 26/07 | 13 | **13** 🔴 |
| 27/07 | 21 | **9** 🔴 |
| **28/07** | 28 | **0** ✅ |
| 29/07 → 04/08 | 174 | **0** ✅ |

**מסקנה חד-משמעית: משהו תוקן ב-27–28/07/2026.** מאז, כל ליד יוצר במקביל רשומת לקוח (1) + תהליך מכירה (4) + רישום שיווקי (1002), תוך שנייה אחת. הצינור **חי ועובד**.

⚠️ **זה מבטל את הנחת המוצא של המשימה.** התיעוד (`05_GAP_CHECKLIST.md` חלק א' שורות 1–2, וסתירה §6.1) מתאר "מקורות לידים כבויים" ו-"96 לקוחות". בפועל: **326 לקוחות, 225 תהליכי מכירה, 274 רישומים שיווקיים, לידים נכנסים בזמן אמת**. הסתירה §6.1 ("5.3/5.4 complete מול 4.3 פתוח — אחד מהם שקרי") **נפתרת: 5.3/5.4 נכונים.** 4.3 הוא באמת המשימה היחידה שנשארה בנושא.

## 2. חוב היסטורי — 102 לידים יתומים

| תקופה | יתומים |
|---|---:|
| מאי 2026 | 4 |
| יוני 2026 | 2 |
| **23–27/07/2026** | **96** |
| 28/07 ואילך | 0 |

102 רשומות לקוח **ללא שום תהליך מכירה**. כולן מלפני התיקון. אלה לידים אמיתיים (שמות אמיתיים, טלפונים תקינים) שנפלו בין הכיסאות בזמן שהצינור היה שבור.
**דרושה הכרעה: backfill או השארה כפי שהם.** אם משאירים — הם לעולם לא יופיעו בשום דוח מכירות ולא יקבלו פולואפ.

## 3. שדות הצ'אטבוט — התגובה בקליקאפ שגויה חלקית

התגובה של שחר על 4.3 טוענת: *"אין שדות ייעודיים לגיל, משקל, מטרה וכו' בתהליך המכירה ובלקוח, וצריך להיות."*

**בדיקה מול המטא-דאטה: השדות קיימים. אין ליצור חדשים.**

| נושא | אובייקט 1 (לקוח) | אובייקט 4 (תהליך מכירה) |
|---|---|---|
| גיל | `pcfAge` | `pcfAge` |
| משקל | `pcfweight` (ק״ג) | `pcfQuestionNumber1` משקל התחלתי · `pcfQuestionNumber2` משקל יעד · `pcfQuestionNumber3` משקל נוכחי |
| מטרה / למה חשוב | `pcfreason` (סיבה שזה חשוב) | — |
| קושי עיקרי | `pcfstruglle` | — |
| מוכנות לתהליך | `pcfreadyornot` | — |
| רגש בעוד 3 חודשים | `pcf3monthsfromnow` | — |
| העדפות תזונה | — | `pcfQuestionSelect1` |
| רמת פעילות גופנית | — | `pcfQuestionSelect2` |
| רגישויות תזונה | — | `pcfQuestionText1` |

**הבעיה האמיתית: השדות ריקים, כלומר מיפוי — לא חוסר שדות.**

| שדה | מלא ב־ |
|---|---|
| `1.pcfAge` | 3 / 326 |
| `1.pcfweight` | **0 / 326** |
| `1.pcfreason` | **0 / 326** |
| `1.pcfstruglle` | **0 / 326** |
| `4.pcfAge` | 1 / 225 |
| `4.pcfQuestionNumber1` (משקל התחלתי) | 1 / 225 |
| `4.pcfQuestionNumber2` (משקל יעד) | 1 / 225 |
| `4.pcfLeadLevel` (דירוג חום ליד) | 1 / 225 |

✅ **מה כן חסר באמת:** **שדה קישור לשיחת הצ'אטבוט.** חיפוש `chat` / `bot` / "צ'אט" / "בוט" / "שיחה" על אובייקטים 1, 4 ו-1036 החזיר **אפס תוצאות.** התגובה צודקת בנקודה הזו. זהו השדה היחיד שצריך ליצור.

⚠️ **שדה זומבי שהתגלה:** אובייקט 1 מכיל **שני** שדות בתווית "גיל" — `pcfAge` ו-`pcfsystemfield111`. לקבוע מי הקנוני לפני שכותבים.

## 4. באגים פעילים שאותרו בזרם הלידים

### 4.1 `pcfFollowUpTime` לא נכתב על לידים חדשים
מלא ב-**172 / 225** תהליכי מכירה — אבל **כל התהליכים שנוצרו היום (04/08) עם `pcfFollowUpTime = null`**.
זה בדיוק מה שמתארת המשימה הפתוחה בשלב 5: *"לעדכן תאריך פולואפ לרגע כניסת ליד ביצירה/עדכון תהליך מכירה"* — **מאומת כשבור.**

### 4.2 `pcfLastentry` (כניסה אחרונה) — עובד
מלא ב-**223 / 225**. המשימה המקבילה בשלב 5 כמעט סגורה.

### 4.3 משפך (Funnel) — ריק לחלוטין
`4.pcfFunnels` מלא ב-**0 / 225**. אף תהליך מכירה לא משויך למשפך.

### 4.4 UTM חלקי
`4.pcfUTMSsource`: מלא ב-149 / 225. פילוח: Facebook 140 · **ריק 76** · דף נחיתה מוצר בוסט 4 · אחר 4 · חבר מביא חבר 1.
כלומר **ל-34% מתהליכי המכירה אין אטריביושן בכלל.** תואם את הפער הידוע ("UTM לא מוגדר טוב בסנריו", בבעלות שחר מול ברק).

### 4.5 דה-דופ דולף
4 מספרי טלפון מופיעים פעמיים:
- `0534303508` — מיטל כוכבי ×2 (כפילות אמיתית)
- `0505493349` — קטי ×2 (כפילות אמיתית)
- `0526052091` — שולמית ×2 (כפילות אמיתית)
- `0501234567` — יוסי / דוד (דאטת בדיקה)

3 כפילויות אמיתיות מתוך 326 = ~1%. הדה-דופ עובד ברובו אך אינו הרמטי. **אפס רשומות ללא טלפון** — טוב, הטלפון הוא מפתח הזיהוי.

## 5. ברירת המוצר ב-1036 — כנראה **לא** באג

רשומת ההגדרות (`8EFF841E-A61F-461E-B9CC-44E7C6593941`):
```
pcfproduct            = 2F73403A-20DE-47A7-B0CB-DF4761B45F21  ("סדנה בוסט")
pcfNumOfHoursNewOpportunity = 24
pcfUpdateLeadSameProductInterest = 1 (עדכון)
pcfAutomatedOrderCreated = 1 (כן)
pcfSalesManger        = null   ← ריק
```
224 מתוך 225 תהליכי המכירה נושאים "סדנה בוסט". התיעוד מסמן את זה כבאג ("ברירת מוצר שגויה, כל עסקה חדשה נכנסת עם מוצר שגוי").

**הערכה שלי: זה כנראה תקין.** כל התנועה מגיעה מקמפייני בוסט (Facebook + דף נחיתה מוצר בוסט), והבוסט הוא שער הכניסה היחיד למשפך. "סדנה בוסט" היא ברירת המחדל הנכונה. **טעון אישור מייק לפני שמסמנים את הבאג כסגור.**
⚠️ `pcfSalesManger` ריק — אין משתמש ברירת מחדל לשיוך תהליכי מכירה. לבדוק אם זה מכוון.

## 6. 🔴 ממצא צדדי חשוב — `categorycode` ריק בכל 11 המוצרים

| מוצר | `categorycode` | `pcfProductType` | מחיר | דף סליקה |
|---|---|---|---:|---|
| סדנה בוסט | **ריק** | היכרות | 134.75 | ✅ (Grow id 3753502) |
| הרצאת אורח | **ריק** | היכרות | 83.90 | — |
| תוכנית ליווי 5/6/8/12/17/22/28 שבועות | **ריק** | פרימיום | 1,673–5,737 | רק ל-12 שבועות ✅ |
| תוכנית המשך אישית | **ריק** | שימור | 550 | — |
| תוכנית המשך אונליין | **ריק** | שימור | 295.76 | — |

**זה מכריע את סתירה פתוחה #12** (`06_N8N_WORKFLOW_SPECS.md`): שער החתימה של WF-01 אמור לעבוד לפי `categorycode = 101`. **השדה ריק בכל המוצרים ⇒ כל לוגיקה שנשענת עליו תיכשל בשקט ותחסום/תעביר את כולם.**
`pcfProductType` הוא היחיד שמאוכלס בפועל (היכרות / פרימיום / שימור). **המפרט המקורי צדק, המימוש טועה.**
🛑 **לא לגעת ב-WF-01 עד שמייק מכריע** — אבל זה כבר לא "סתירה", זו עובדה בדאטה.

**בונוס:** רק **2 מתוך 11** מוצרים קיבלו `pcfpaymenturl`. תואם את החוסם "דפי סליקה במשולם לא הוקמו (עודד)".

## 7. מה שנשאר לאמת ידנית (אין לזה API)

1. **הקוד של טופס/כפתור יצירת הליד** — לא ניתן לשליפה דרך API (ווידג'ט בממשק). **צריך שאליאל יעתיק את הקוד או יצלם מסך.**
2. **האם באמת מוטמע שם טוקן Fireberry** — `05_GAP_CHECKLIST.md` חלק ח' טוען שהטוקן מוטמע "בקוד טופס הליד". אם כן — זה **סותר ישירות את כלל האבטחה הנעול של שלב 4** ("אפס טוקנים בקוד צד-לקוח") וחייב לרדת במסגרת המשימה הזו.
3. **תור הוובהוק ב-Make** — טריטוריה של מייק, ידנית.

## 8. תובנה מתודולוגית — `sort` ב-v3 מתעלם

`POST /api/v3/query` עם `"sort": {"fieldName":"createdon","order":"desc"}` **מוחזר לא ממוין** (המיון בפועל לפי GUID). כל מיון חייב להתבצע client-side אחרי משיכת כל העמודים.
בנוסף: **שדה שלא קיים ב-`fields` מחזיר `data: []` בשקט (לא 400).** זהו מקרה נוסף של "אפס תוצאות בשקט" — הכשל המסוכן ביותר ב-Fireberry.

---

## סיכום — מה 4.3 באמת דורש (מעודכן לפי הדאטה)

| # | פעולה | בעלים | סטטוס |
|---|---|---|---|
| 1 | לחבר את הטופס ל-`hook.eu2.make.com/srwyryrt7083d17hk1aidxnt9bjj7fqx` | אנחנו (קוד Fireberry) | הליבה של המשימה |
| 2 | להוציא טוקן מהקוד אם קיים שם | אנחנו | חובה, לא אופציונלי |
| 3 | צ'קבוקס "שלח וואטסאפ כן/לא" בטופס | אנחנו (טופס) + **מייק** (סנריו) | הרחבה מהתגובות |
| 4 | ליצור **שדה קישור לצ'אטבוט** על 1 ו-4 | אנחנו, אחרי אישור פר-שדה | פער אמיתי יחיד |
| 5 | ❌ **לא** ליצור שדות גיל/משקל/מטרה — קיימים | — | התגובה שגויה |
| 6 | לתקן מיפוי הצ'אטבוט לשדות הקיימים | מייק (Make) / טל | שלב 5.7, לא 4.3 |
| 7 | לתקן `pcfFollowUpTime` שלא נכתב | מייק (Make) | באג מאומת |
| 8 | הכרעה על 102 הלידים היתומים | מייק/אליאל | חוב היסטורי |
| 9 | בירור ערוצי לידים חסרים מול ירדן ועודד | שחר | מהתגובות |

**המשימה איננה "ניצחון מהיר" כפי שמתואר בקליקאפ.** החיבור עצמו זול, אבל הוא יושב על צינור שיש בו 3 באגים פעילים (פולואפ, משפך, UTM) ו-102 רשומות יתומות. חיבור הטופס בלי לטפל בהם פשוט יוסיף עוד מקור לאותם פערים.
