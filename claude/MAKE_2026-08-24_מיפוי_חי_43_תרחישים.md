# MAKE_LIVE — מיפוי מלא של שכבת Make.com · רויטל יעיש
נסרק 23/08/2026 · חשבון `revital.vitrue@gmail.com` · ארגון **4829531** · צוות **2425546** · אזור **eu2**
מקור: blueprints מלאים של כל 43 התרחישים (`/home/claude/make/bp/*.json`) + `/hooks`, `/connections`, `/data-stores`, `/scenarios-folders`, `/scenarios/{id}/logs`.
קריאה בלבד — לא הודלק, לא כובה, לא נערך ולא נמחק דבר.

> ⚠️ **תיקון לתיעוד הקיים.** מסמכים בפרויקט (`a-16.html` הישן, `WORKFLOWS_DOCS.md`, `CLICKUP.md` 16.2) מתארים את Make כמחזיק "רק קליטת לידים וחתימות" / "11 חיים". **לא נכון.** בחשבון 43 תרחישים, **18 פעילים**, מהם 9 רצו היום (23/08). Make הוא עדיין **מנוע ההודעות הראשי ללקוחות** (5 תרחישי CloudChat, ~186,000 אופרציות מצטברות) והוא גם **הצינור שדרכו תשלומי GROW מגיעים ל-n8n WF-20**.

---

## תוכן
- [א. טבלת מלאי — 43 תרחישים](#א-טבלת-מלאי)
- [ב. פירוק 18 הפעילים](#ב-פירוק-18-הפעילים)
- [ג. חפיפה מול n8n — הממצא המרכזי](#ג-חפיפה-מול-n8n)
- [ד. שכבת הבוט — חמשת תרחישי CloudChat כמכלול](#ד-שכבת-הבוט)
- [ה. מפתחות חשופים](#ה-מפתחות-חשופים)
- [ו. תיקונים לתיעוד הקיים](#ו-תיקונים-לתיעוד-הקיים)

---

## א. טבלת מלאי

תיקיות: `403482 Boost` · `523276 Fireberry-->Cloudchat-->Messages` · `519085 Green API +` · `522416 Grow-->Fireberry` · `512207 Vitrue - New CRM - Revital Yaish` · `388771 Vitrue Scenarios - Revital Yaish` · `392917 Webinars` · `440192 דיוור` · `523329 פגישות` · `—` = שורש.

### פעילים (18)

| id | שם | טריגר מדויק | תיקייה | מה הוא עושה (שורה) | מקבילה ב-n8n |
|---|---|---|---|---|---|
| 9339860 | Main Leads CRM - Revital Yaeish N (1.02) | webhook 4176436 `srwyryrt7083d17hk1aidxnt9bjj7fqx` | New CRM | קליטת ליד מרכזית: בונה/מוצא UTM master-data (1043/1044/1028/1003/1045/1046/1047/67/1042), dedup לפי טלפון, יוצר/מעדכן לקוח (1) + הזדמנות (4) + רישום שיווקי (1002) | ❌ אין. נתיב יחיד |
| 7133758 | Main Chatbot Scenario - CloudChat (1.01) | webhook 3167901 `uwmi6h5v2qgeyv2cqxjnriwq55kqlbkd` | Vitrue Scenarios | מוח הבוט. 11 ראוטים לפי `action`. כותב לגיליון "לידים"/"פניות"/"משובים"/"רכישות"/"דיוורי בוטקאסט"/"יומן פגישות", מחזיר JSON דינמי לבוט | ❌ (חלקית: `Revital - Response Agent` `VcsxDn3cd60CL4FE`) |
| 7133760 | Main Leads CloudChat (1.01) | webhook 3167903 `vjoddgjbvdwj5nxcl0wq4w8yc2qnbx3y` | Vitrue Scenarios | נקודת כניסת לידים לבוט: מזהה מקור (מרכזייה/אורגני/פייסבוק/וורדפרס/Fireberry), יוצר מנוי CloudChat, יורה פלואו התראה למפעילים | ❌ |
| 7133755 | New payment - CloudChat (1.01) | webhook 3167900 `chw3swfshc1yicbwj7nf68k4lu6tkkmr` | Vitrue Scenarios | קליטת תשלום Cardcom/Meshulam/GROW → מנוי CloudChat + שדות רכישה + איש קשר גוגל + **POST ל-n8n `/webhook/grow-payment-roital`** | 🔴 **WF-20** — Make הוא ה-upstream |
| 7133762 | Create contact - Cloudchat (1.01) | webhook 3167904 `848xf9z43h71ifoy7f8s2skh5ewqdffa` | Vitrue Scenarios | שירות עזר: יוצר מנוי CloudChat ומחזיר `user_ns`/`user_id`. 0 אופרציות — לא נקרא בפועל | ❌ |
| 7133769 | Facebook Leads - Cloudchat (1.01) | Facebook Lead Ads hook 3167907 | Vitrue Scenarios | ליד מפייסבוק → JSON → POST ל-`leads_scenario_webhook_id_botfield`. 0 אופרציות | ❌ |
| 7158111 | Firberry CRM | webhook 3179611 `uvhg84wahk9n86ne5al0obsb6pj57mpw` | Vitrue Scenarios | מקבל מהבוט תשובות q1–q5 וכותב אותן ל**פיירברי הישן** (`pcfsystemfield149-153` על אובייקט 1) | ❌ — כותב ל-CRM הישן |
| 6812655 | Integration Facebook Lead Ads | Facebook Lead Ads hook 3025302 | — | 2 מודולים בלבד: מושך ליד ומפסיק. **דלת פתוחה ללא המשך** | ❌ |
| 9523761 | Document button from CRM | webhook 4186124 `53w7nb0ktyb42h5ymsfwmokpp2r6ovdf` | New CRM | כפתור "שליחת מסמך": מושך עסקה/לקוח/מוצר/טופס, מתחבר לטופסי, יוצר טופס לחתימה, כותב `pcfSignatureStatus=1` + `pcfcfDocLink`, יורה WhatsApp | ❌ |
| 9547511 | Signed Document from Tofsy | webhook 4268332 `7g2xvjmgulpqoic8ai69e5sfm60vp4n7` | New CRM | קליטת חתימה: `pcfSignatureStatus=2` + `pcfcfDocLink`=PDF, יוצר רשומת מסמך **1038** | ❌ |
| 9472247 | עדכון מסמכים בכפתור שליחת מסמכים | webhook 4235980 `c3lcf9yf35xfngvxmv1xhw28i3cmi2p2` | New CRM | טעינת קטלוג הטפסים (**1058**) לכפתור. **מפתח פיירברי גלוי בכותרת** | ❌ |
| 9580074 | Welcome - General Message - CloudChat | webhook 4282258 `b7ca4dujc848jbru83a0psoalhqloqg7` | Fireberry→Cloudchat | ניזון מאוטומציה פנימית על **1051** "רשומה נוצרה": בורר `welcome_livuy` / `welcome_boost_v2` ויורה אותו | 🔴 **WF-23 / WF-24** — כפילות ישירה |
| 8223019 | BoTcast Sequencial | webhook 3660229 `2gove73j2ihq3aqpmlqiwxc0n8crbem7` | דיוור | רישום שליחות ותגובות בוטקאסט לגיליון "דיוורי בוטקאסט" | ❌ |
| 7810658 | Webinar_Welcome | webhook 3470095 `7ppiik6vvpxiz1hs21q4h5kdjx2a3vin` | Webinars | הרשמה לוובינר → מנוי CloudChat + `meeting_date/time` + פלואו `f201689s2837851`. 0 אופרציות | 🟡 WF-18 (נתונים בלבד) |
| 7166372 | Webinar Reminders - Old CRM | schedule כל **1800 שניות**, ימים א-ה+ש 09:00–21:00 | Webinars | סורק **את הפיירברי הישן** (1001/1002) ושולח תזכורות יום-לפני ושעה-לפני דרך CloudChat | 🔴 מתנגש עם WF-18 / 6.6 |
| 7415737 | Boost Reminder | weekly · שבת 20:45 | Boost | קורא "הרשמות לבוסט" בגיליון, יורה `f201689s2725301` ומסמן "נשלח" | 🔴 WF-23 / WF-BOOST-B |
| 7133754 | Performance Report (1.01) | weekly · ראשון 10:00 | Vitrue Scenarios | מרכז 5 מדדים מגיליון הלידים ושולח ל-`Notifications System - Cloudchat` | ❌ |
| 7413890 | עדכון קישור קבוצה שבועי | weekly · ראשון 05:00 | Green API + | קורא את הקבוצה הפעילה מגיליון "קבוצות וואטסאפ" וכותב אותה ל-bot field `whatsapp_group` | 🔴 WF-26 / WF-BOOST-B |

### כבויים (25)

| id | שם | טריגר | תיקייה | מה הוא עושה | מקבילה n8n |
|---|---|---|---|---|---|
| 9560342 | Finance 3 - Create Payments Records (Template) | webhook 4274104 | Grow→Fireberry | תבנית Vitrue ליצירת תיעודי תשלום (1018) מהזמנה. **תור 42 תקוע** | WF-20 |
| 9614987 | Finance 4 - Cardcom - New Credit Card Payment (Template) | webhook 4297011 | Grow→Fireberry | תבנית Cardcom: יוצר הזמנה + פריטים + תשלום + לקוח בסליקה | WF-20 |
| 9484992 | Green API | interval 900 | Green API + | מודול HTTP יחיד: `getChatHistory` מ-GreenAPI. סקיצה | WF-26/21 |
| 9490447 | טופס שקילה שבועי - תהליך ליווי - מוכן | webhook 4246730 | New CRM | Tally → יוצר **1069** סטטוס שקילה + מעדכן `pcfCurrentWeight`/`pcfLastWeighDate` על 1051 | 🔴 **WF-27b** — הועבר |
| 9696910 | מענה לפי האם הייתה בבוסט או לא 23.8.2026 | webhook 4330041 | Vitrue Scenarios | חיפוש לקוח לפי טלפון + בדיקת היסטוריית בוסט. **נבנה היום, לא גמור** | — |
| 9575585 | עדכון אישור הגעה לפגישה עם רויטל (עבר לN8N) | webhook 4280325 | New CRM | 2 מודולים בלבד. **7133758 עדיין שולח לכאן** ⚠️ | WF-32 מתוכנן |
| 9513015 | שאלון סיום סדנת Boost (לא פעיל, עבר לN8N) | Tally hook 4253774 | New CRM | Tally → מעדכן 1051 + עסקה | **WF-07** |
| 9581051 | Weekly Guest Broadcast - הזמנה לאורח שבועי | interval 2700, א-ה 08:00–09:00 | Fireberry→Cloudchat | מפגשי מחר + ליוויים פעילים → CloudChat `f201689s4392327` | הוצא משימוש 20/08 |
| 9581458 | Meetings Reminders - חיפוש פגישות למחר | interval 900 | פגישות | פגישות מחר מפיירברי → CloudChat + סימון `pcfZoomReminderDay` | WF-32 מתוכנן |
| 9363636 | Webinar Reminders (New CRM) | interval 900 | New CRM | גרסת ה-CRM החדש של 7166372. **מוכן ולא הודלק** | WF-18 / 6.6 |
| 9362315 | Facebook Leads > Main Leads CRM רויטל יעיש | FB hook 3021983 | New CRM | ליד FB + נתוני קמפיין → הסנריו הראשי. כולל מודול "הקפצת שגיאה בכוונה" | — |
| 9362762 | Website Leads > Main Leads CRM (רויטל יעיש) | webhook 4186110 | New CRM | ליד מהאתר → הסנריו הראשי. מודול JSON מסומן «לתקן» | — |
| 7133774 | Notifications System - CloudChat (1.01) | webhook 3167909 | Vitrue Scenarios | מנוע ההתראות למפעילים. **כבוי + תור 5** בזמן ש-7133754 עדיין שולח אליו | Notfication System - V2 `dqFsbHViUi3OG6cg` |
| 7155969 | 5. Webinar Movements | Zoom hook 3479103 | Webinars | מודול Zoom יחיד. **תור 682 — התור הגדול ביותר בחשבון** | WF-18 |
| 7228986 | After Webinar (עבר למייק שלהם) | webhook 3211877 | Webinars | פולו-אפ אחרי וובינר דרך CloudChat | WF-17 |
| 7414624 | עדכון בטבלה לאחר הצטרפות לקבוצה | webhook 3293927 | Boost | מוסיף שורה לגיליון אחרי הצטרפות. **תור 1** | WF-26 |
| 7133771 | Create CloudChat Message Template For Botcast (1.01) | interval 900 | Vitrue Scenarios | יוצר תבניות הודעה ב-CloudChat מגיליון | — |
| 7133773 | Testing Scenario - CloudChat (1.01) | interval 900 | Vitrue Scenarios | ארגז חול. **מכיל נתוני הרצה עם טוקנים וטלפונים אמיתיים** | — |
| 8119255 | דיוור | interval 900 | דיוור | סקיצת דיוור מגיליון | — |
| 9504448 | Integration Tally | Tally hook 4250062 | — | מודול יחיד. **תור 6** | WF-27b |
| 9523034 | חיבור טפסים - מקור (טופסי) | interval 900 | — | 3 קריאות HTTP לטופסי. **מפתח טופסי גלוי** | — |
| 9461766 | Integration HTTP | interval 900 | New CRM | מודול HTTP יחיד | — |
| 9513781 | Test #! | webhook 4254124 | — | webhook בלבד | — |
| 7904290 | HTTP | interval 900 | — | קריאה בודדת ל-`New payment - CloudChat` | — |
| 7107797 | Integration Google Sheets | interval 900 | — | `watchRows` בלבד | — |

**חיבורים בחשבון (11):** `Revital - New` (14295790, powerlink app — הפיירברי **החדש**) · `Revital - New#2` (14315315) · `Revital` (11474697) + `Revial` (11474723) — הפיירברי **הישן** · `Revi` google (11392714, revital.vitrue@gmail.com) · `Revital 6.7.2026` google (14403027, revitalfh@gmail.com) · `My GREEN-API connection` (instance 710701674604) · `My Tally Personal API Key` · `My Zoom OAuth2` · 2 חיבורי Facebook.

**Data stores:** 173238 (15 רשומות, משמש כ-error saver ב-Finance 3) · 173237 (ריק).

---

## ב. פירוק 18 הפעילים

### 1 · 9339860 · Main Leads CRM - Revital Yaeish N (Version 1.02)
**טריגר:** `https://hook.eu2.make.com/srwyryrt7083d17hk1aidxnt9bjj7fqx` (hook 4176436 `Revital_NewCRM_Michael`, תור 0)
**חיבור פיירברי:** 14295790 (`Revital - New`) · **41,795 אופרציות · 886 הרצות לא הושלמו** · רץ אחרון 23/08 18:28.

**זרימה:**
1. `[1] gateway:CustomWebHook` — מטען הליד.
2. `[54] plquery «משיכת הגדרות»` — `objecttype 1036`, `name = "1"`. **כל התנהגות הסנריו נקבעת ברשומת ההגדרות הזו**: `pcfproduct`, `ownerid`, `pcfSalesManger`, `pcfNumOfHoursNewOpportunity`, `pcfUpdateLeadSameProductInterestname`, `pcfCreateUTM*NotFoundname`, `pcfupdatemarketingregisname`.
3. `[69] SetVariables` — `productid` = `54.Data[].pcfproduct`.
4. `[74] BasicRouter` — 10 ראוטים.

**ראוטים 1–8 + 10 (master-data של UTM):** תבנית זהה — חיפוש לפי שם, ואם לא נמצא ויש הרשאה בהגדרות, יצירה.

| ראוט | שדה במטען | אובייקט | תנאי היצירה verbatim |
|---|---|---|---|
| 1 | `utm_source` | 1043 | `pcfCreateUTMsourceNotFoundname = "כן"` AND `75.Data[].customobject1043id notexist` |
| 2 | `utm_medium` | 1044 | `pcfCreateUTMmediumNotFoundname = "כן"` |
| 3 | `utm_ad` | 1028 | `pcfCreateUTMadNotFoundname = "כן"` |
| 4 | `utm_adset` | 1003 | `pcfCreateUTMadsetNotFoundname = "כן"` |
| 5 | `utm_content` | 1045 | `pcfCreateUTMcontentNotFoundname = "כן"` |
| 6 | `utm_keyword` | 1046 | `pcfCreateUTMkeywordNotFoundname = "כן"` |
| 7 | `placement` | 1047 | `pcfCreateUTMplacementNotFoundname = "כן"` |
| 8 | `utm_campaign` | 67 (`campaignname`) | `pcfCreateUTMcampaignNotFoundname = "כן"` |
| 10 | `funnel` | 1042 | `pcfCreateFunnelNotFoundname = "כן"` |

**ראוט 9 — הליבה:**
- `[123] GetVariables` — שולף `utm_source, utm_medium, utm_campaign, utm_ad, utm_adset, utm_content, utm_keyword, placement, funnel`.
- `[59] google-sheets:filterRows «משיכת שדות מותאמים אישית»` — גיליון `18dKg8S8HLEi86XRgseko5JBZXvGGyWsrhO96td89zQg`, לשונית "מיפוי שדות מותאמים אישית", שורה `A="1"`. **גיליון גוגל שולט במיפוי שדות הבחירה בפיירברי** — התשובות נחתכות בעזרת `split(...; "~~")`, `"||"` ו-`"@@"`.
- `[3] phonenumber:TransformerParseNumber` — `{{ifempty(ifempty(1.fields.phone.raw_value; 1.phone); 1.`אפשר טלפון?`)}}`, `defaultCountry: IL`, onerror = **Ignore**.
- `[6] BasicRouter` filter `"מספר תקין"` verbatim: `3.valid = true` AND `3.type = mobile` AND `3.countryCallingCode = 972` AND `length(3.phone) = 9`.
  - **ראוט 1:** מוצא מוצר לפי `utm_product` (חיפוש לפי `name` ואם 0 תוצאות — לפי `productid`), אובייקט 14.
  - **ראוט 2:** `[20] plquery «חיפוש לקוח»` — 5 וריאציות טלפון + `emailaddress1`. `[22] BasicFeeder` על התוצאות (או מערך בגודל 1 אם ריק).
    - **תת-ראוט 1 — `[25] createobject «יצירת לקוח»`** (`אין לקוח קיים`: `20.Data[].accountid notexist`), אובייקט **1**:
      `accountname` · `telephone1 = 0{{3.phone}}` · `emailaddress1` · `pcfGender` (`switch male=1/female=2/other=3`) · `pcfAge` · `pcfbirthdaydate` · `pcfRelationshipStatus` (`bachelor=1…known_in_public=6`) · `idnumber` · `statuscode = 1` · `ownerid = 54.Data[].ownerid` · `pcfCompany` · `pcfDepartment` · `pcfRole` · `pcfOccupation` · `billingcity/street/zipcode` · `pcfRegistrationForm` · `pcfFunnelName` · `pcfPage` · `pcfMailingConfirmation` (`on=1/off=2`) · `pcfUTMSsource/medium/campaign/ad/adset/content/keyword/placement` · `pcfUtmProduct` · **`pcfCreatedByPersonOrSystem = 2`** · `pcfQuestionSelect1-5` · `pcfQuestionText1-5` · `pcfQuestionNumber1-3`.
    - **תת-ראוט 2 — `[44] updateObject «עדכון לקוח»`** (`לקוח קיים במערכת`: `22.accountid exist`) — מעדכן את אותו סט ללא `statuscode`/`ownerid`.
    - **תת-ראוט 3 — ההזדמנות והרישום השיווקי:**
      - `[30] plquery «חיפוש מכירה»` — אובייקט 4 לפי `accountid`, ממוין `createdon desc`.
      - `[37] createobject «יצירת מכירה»` filter verbatim `"אין מכירה או יש מכירה לא עם אותו מוצר ויותר מהזמן המוגדר"`:
        `[[30.Data[].opportunityid notexist]] OR [[30.Data[].pcfProduct != 23.productid] AND [30.Data[].createdon < addHours(now; "-" + 54.Data[].pcfNumOfHoursNewOpportunity)] AND [54.Data[].pcfUpdateLeadSameProductInterestname = "יצירה"]]`
        שדות: `name = "העסקה של {שם}"` · `accountid` · `pcfProduct` · `statuscode = 1` · `pcfTelephone1 = 0{{3.phone}}` · `pcfemailaddress` · כל שדות ה-UTM · `ownerid = ifempty(54.Data[].pcfSalesManger; 54.Data[].ownerid)` · `pcfQuestionSelect1-5` (ממופים מלשונית `59.2`).
      - `[49] updateObject «עדכון מכירה מלא»` filter `"יש מכירה בתוך טווח הזמן המוגדר"`: `48.createdon > addHours(now; 54.Data[].pcfNumOfHoursNewOpportunity)` — מסמן `pcfReregistration = 1`.
      - `[46] updateObject «עדכון מכירה כ-כפול»` filter `"יש מכירה עם אותו מוצר ויותר מהזמן המוגדר"`: `48.pcfProduct = 23.productid` AND `48.createdon < addHours(now; …)` AND `pcfUpdateLeadSameProductInterestname = "עדכון"` — מוסיף `pcfisactive`, `pcfLastentry`, `pcfFollowUpTime`.
      - `[40] GetVariables` filter verbatim `"לא ממשיכים אם לא רוצים לפתוח רישום שיווקי"`: `[[1.marketing_record = "yes"]] OR [[1.action = "fireberry_lead_form"]]`.
      - `[41] createobject «יצירת רישום שיווקי»` — אובייקט **1002**: `name = "הרישום של {fname}"` · `pcfAccountid` · `pcfSale` · `pcformname` · `pcfPage` · `pcfFunnelName` · UTMs · `pcfMailingConfirmation` · `pcfMarketingRecordType = if(marketing_record_timeframe = "in-frame"; 2; 1)` · **`pcfExecutionUrl = var.scenario.executionUrl`**.
      - `[151] WebhookRespond` — `{"account_id","oppurtunity_id","marketing_record_id"}` סטטוס 200.
    - **תת-ראוט 4 — `[166] updateObject «עדכון לקוח / עדכון שדות תשובות»`** — mapper מכיל **רק** `objectid` ו-`objecttype`, בלי `fields`. **מודול ריק.**

**מלכודות 9339860:**
- 🔴 **סדר הראוטים שובר את `funnel`.** `[123] GetVariables` בראוט 9 קורא את המשתנה `funnel`, אבל הוא נקבע רק בראוט **10**, שרץ אחריו. `pcfFunnelName` על הלקוח ועל העסקה יוצא ריק תמיד.
- 🔴 **באג באובייקט בראוט 10.** `[145] createobject` יוצר `objecttype 1047` (מיקום הגעה) במקום 1042 (פאנל), ו-`[146]` קורא `145.customobject1047id`. גם `[147]` בודק `143.Data[].customobject1047id` על שאילתה שהחזירה 1042.
- 🔴 **באג בראוט 6.** `[107]` filter בודק `103.Data[].customobject1043id` בזמן שהשאילתה היא על 1046. `utm_keyword` לא ישויך אף פעם למילת מפתח קיימת.
- ⚠️ **פילטר הטלפון שקט.** `length(3.phone) = 9` פוסל מספר עם קידומת בת 10 ספרות, מספר קווי, ומספר לא-ישראלי. הליד נופל בשקט, בלי שגיאה, בלי רשומה.
- ⚠️ `[3]` onerror = `Ignore` — מספר לא-פרסבילי לא עוצר, פשוט ממשיך עם שדות ריקים.
- ⚠️ **886 הרצות לא הושלמו** בתור ה-DLQ. לפני כל שינוי בסנריו הזה צריך להבין מה יש שם.
- ⚠️ תלות בגיליון גוגל חיצוני (`18dKg8S8…`) שאין עליו בקרת גרסאות.

---

### 2 · 7133758 · Main Chatbot Scenario - CloudChat (1.01)
**טריגר:** `https://hook.eu2.make.com/uwmi6h5v2qgeyv2cqxjnriwq55kqlbkd` (hook 3167901) · **123,652 אופרציות — התרחיש הכבד בחשבון · 770 הרצות לא הושלמו** · רץ אחרון 23/08 18:42.

**זרימה ראשית:**
1. `[290] SetVariables` — **שני טוקני CloudChat כתובים גלויים**: `API Auth` (הבוט הראשי) ו-`Organic Bot API Auth`.
2. `[430] FunctionSleep 2s` «פתרון זמני עד שאבנה sequencial».
3. `[4]/[302]/[435] Get Bot Fields 1/2/3` — `GET console.thecloud.chat/api/flow/bot-fields?limit=100&page=1..3`.
4. `[377] SetVariables «Bot Fields»` — `main_processes_spreadsheet_id_botfield`, `leads_spreadsheet_id_botfield`, `data` = מיזוג שלושת העמודים.
5. `[433] FunctionSleep 2s`.
6. `[292] Get Contact Info` — `GET /api/subscriber/get-info-by-user-id?user_id={{1.user_id}}`, כותרת מותנית: `if(1.bot_type = "organic_bot"; Organic Bot API Auth; API Auth)`.
7. `[318] BasicRouter` — 5 ראוטים ראשיים.

**ראוט 1 — `[308] WebhookRespond` מהיר.** filter `"fast response - no waiting needed"`: `action ∈ {finished_main_flow, chat_progress, opt_in}` → מחזיר `{"status":"200"}` מיד.

**ראוט 2 — `get_main_processes_json`.** `[306]` קורא לשונית "תהליכים" בגיליון התהליכים לפי `main_flow_name`; `[298]` קורא לשונית "לידים" לפי `user_ns` + `main_flow_name`; `[296]` מחזיר את גוף התהליך + `a1..a10` (עמודות 16–25).

**ראוט 3 — `get_all_dynamic_processes`.** `[316]` קורא תא `F2` בלשונית "הגדרות" ומחזיר אותו כמות שהוא.

**ראוט 4 — הליבה (11 תת-ראוטים):**
`[221] SetVariables «User Fields»` — filter verbatim: `action != split_fullname` AND `action != format_phone` AND (`API Auth exist` OR `Organic Bot API Auth exist`). ממפה `fullname/fname/lname/phone/email/utm_source/main_flow_q1..q15/cloudchat_user_ns/cloudchat_chat_url`.
`[403] TransformerParseNumber` — onerror = `Resume` עם שדות ריקים.

| תת-ראוט | filter verbatim | פעולה |
|---|---|---|
| 1 | `action ∈ {finished_main_flow, opt_out, opt_in, chat_progress, new_payment, …}` | `[2]` חיפוש בלשונית "לידים" לפי `user_ns`; `[11]` עדכון שורה או `[10]` הוספה. עמודות: 2 שם, 3 טלפון, 4 מייל, 5 מקור (`ifempty(utm_source; "צאטבוט")`), 7 תאריך opt_out, 8 "כן" אם יש מדיה בהודעה, 9 קישור צ'אט, 10 מספר השאלה האחרונה, 11 `main_flow_status`, 12 `main_flow_name`, 13 `contact_score`, 15 מגדר, 16–25 תשובות |
| 2 | `action = new_ticket` | `[35]` שורה בלשונית "פניות" |
| 3 | `action = developer` | `[39]` מציג/מנקה שדות מנוי, `[194]` `DELETE /api/subscriber/clear-user-field-by-name` |
| 4 | `action = new_feedback` | `[239]` שורה בלשונית "משובים" |
| 5 | `action = new_payment` | `[240]` שורה בלשונית "רכישות" |
| 6 | `action ∈ {scheduling, rescheduling, *cancel*}` | `[440]` בונה JSON ענק, `[441]` **POST ל-`https://hook.eu2.make.com/k7w5w314zmn8tavte2gf92pounsd15di`**, `[243]/[245]` עדכון לשונית "יומן פגישות" |
| 7 | `action = dynamic_data` | `[319]` קורא לשונית "תוכן דינמי" לפי `data_filter1`/`next_nest`, בונה תפריט ממוספר ומחזיר JSON |
| 8 | `action = botcast` | `[378]` קורא לשונית "התנור" לפי `botcast_number` ומחזיר הודעה + כפתור |
| 9 | `action contains botcast` | `[385]` שורה ב"דיוורי בוטקאסט", `[387]/[389]` עדכון תגובה |
| 10 | `action = get_scheduled_appointments` | `[398]` פגישות "נקבעה" של המנוי → JSON |
| 11 | `action ∈ {finished_main_flow, opt_out, opt_in, chat_progress, new_payment, …}` | `[428]` בונה `{q1..q6, email, phone, f_name, l_name, full_name, utm_source, marketing_record:"yes"}`, `[429]` **POST ל-`https://hook.eu2.make.com/uvhg84wahk9n86ne5al0obsb6pj57mpw`** = `Firberry CRM` (7158111) |

**ראוט 5 — `get_ai_processes`.** תא `F2` בהגדרות + שורת הליד → JSON משולב.

**מלכודות 7133758:**
- 🔴 **שני טוקני CloudChat גלויים במודול `[290]`.**
- 🔴 **`[441]` שולח לוובהוק של תרחיש כבוי** (4280325 → 9575585 "עבר לN8N"). כל אירוע `scheduling`/`rescheduling`/`cancel` נופל לתור של תרחיש שלא ירוץ.
- 🔴 **`[429]` מזין את `Firberry CRM` שכותב לפיירברי הישן** — ראה §ג.
- ⚠️ `FunctionSleep` בשלושה מקומות — פתרון עוקף לחוסר סנכרון, לא מנגנון תור אמיתי. תחת עומס אפשריות התנגשויות כתיבה לגיליון.
- ⚠️ כל המצב יושב ב-Google Sheets, לא בפיירברי. שינוי סדר עמודות בגיליון שובר הכול בשקט.
- ⚠️ 770 הרצות לא הושלמו.

---

### 3 · 7133760 · Main Leads CloudChat (1.01)
**טריגר:** `https://hook.eu2.make.com/vjoddgjbvdwj5nxcl0wq4w8yc2qnbx3y` (hook 3167903) · **43,707 אופרציות · 1,037 הרצות לא הושלמו** — התור הגדול ביותר בין הפעילים.

1. `[109] SetVariables «API auth»` — שני הטוקנים.
2. `[58]/[129]/[146] Get Bot Fields` (3 עמודים).
3. `[131] SetVariables` — `leads_spreadsheet_id_botfield`, `make_zone_botfield`, `create_contact_scenario_webhook_id`, `main_scenario_webhook_id_botfield`, `chatbot_status_botfield`.
4. `[94] BasicRouter` filter verbatim `"סטטוס בוט = 2 או 1"`: `chatbot_status_botfield = "1"` OR `= "2"`. **מתג ראשי לכל השכבה** — משנים אותו ב-CloudChat, לא ב-Make.

| ראוט | filter verbatim | `utm_source` | `template_flow_name` |
|---|---|---|---|
| 1 «Omnitelecom» | `1.from_phone exist` | `מרכזיה` | `Client_Notification_new_phonecall_flow_botfield` |
| 2 «Organic Bot» | `1.type = "organic_bot"` | `1.custom_fields.utm_source` | `Client_Notification_social_new_lead_flow_botfield` |
| 3 «VoiceCenter» | `1.HTML content exist` + חילוץ טלפון מ-`1.text` | `מרכזיה` | `…new_phonecall…` |
| 4 «Facebook» | `1.utm_source = "facebook"` (ci) | `facebook` | `Client_Notification_new_lead_flow_botfield` |
| 5 «Other» | **`Condition = "SOME VALUE"`** | — | — |
| 6 «Wordpress» | `1.form_name exist` OR `1.form_id exist` | `אתר` | `…new_lead…` |
| 7 «MacroDroid» | `1.utm_source = "MacroDroid"` | `מרכזיה` | `…new_phonecall…` |
| 8 «Fireberry» | `1.type != "organic_bot"` | `1.utm_source` | `…new_lead…` |
| 9 | — | ביצוע | ראה למטה |

**ראוט 9:** `[100] GetVariables` → `[127] parseNumber` → `[141] CreateJSON` (filter `127.valid = TRUE` AND `127.type = MOBILE`) → `[132] POST /api/subscriber/create` → `[112] get-info-by-user-id` → `[134] BasicRouter`:
- `[142]/[135] PUT /api/subscriber/update` (filter `132.statusCode = 200`)
- `[137]/[138] PUT /api/subscriber/set-user-fields-by-name` — `fullname`, `fname`, `lname`, `utm_source`
- `[143]/[125] POST hook.{make_zone}.make.com/{main_scenario_webhook_id}` — `action: "new_lead"` → **7133758**
- `[114]` חיפוש בלשונית "הגדרת התראות" לפי טלפון
- `[64] POST /api/subscriber/send-sub-flow` filter verbatim `"סטטוס בוט פעיל"`: `[[114.__IMTLENGTH__ > 0 AND chatbot_status_botfield = "1"]] OR [[chatbot_status_botfield = "2"]]`

**מלכודות 7133760:**
- 🔴 **ראוט 5 «Other» הוא שריד תבנית** — התנאי `Condition = SOME VALUE` לא מתקיים לעולם. ראוט מת.
- 🔴 **ראוט 8 «Fireberry» דורס את ראוטים 4 ו-6.** התנאי שלו הוא רק `type != organic_bot`, ולכן הוא מתקיים כמעט תמיד. Make מריץ ראוטים לפי הסדר, המשתנים הם `roundtrip`, והראוט האחרון שמתקיים הוא זה שמנצח. ליד מפייסבוק או מוורדפרס יקבל את המיפוי של ראוט 8, ואם השדות שלו במטען ריקים — הליד נכנס בלי שם ובלי מקור.
- ⚠️ **`chatbot_status_botfield` הוא מתג הרג.** כל תת-השכבה תלויה בערך שיושב ב-CloudChat. אם הוא לא 1 או 2, הסנריו מקבל את הליד ולא עושה כלום, בלי שגיאה.
- ⚠️ 1,037 הרצות לא הושלמו.

---

### 4 · 7133755 · New payment - CloudChat (1.01)
**טריגר:** `https://hook.eu2.make.com/chw3swfshc1yicbwj7nf68k4lu6tkkmr` (hook 3167900 «Cardcom - new payment - Cloudchat») · **6,870 אופרציות · 175 הרצות לא הושלמו** · רץ אחרון 23/08 18:30.

1. `[93] SetVariables` filter verbatim `"if cardcom and responsecode = 0 || if meshulam"`: `[[92.responsecode = "0"]] OR [[92.webhookKey exist]] OR [[92.data.status = "שולם"]]`.
   מנרמל שלושה פורמטים בבת אחת: `product_sold` = `ifempty(ifempty(Custom24; paymentDesc); data.productData[].name)` · `sum_sold` = `ifempty(ifempty(suminfull; paymentSum); data.sum)` · `payment_deal_number` = `ifempty(ifempty(internaldealnumber; transactionCode); data.asmachta)` · `payment_page` = `ifempty(GroupDescription; "Meshulam")` · `personal_id = 92.invCompanyID` · `phone` = `ifempty(ifempty(InvMobile; payerPhone); data.payerPhone)`.
2. `[101]/[105] Get Bot Fields` → `[107]` `Client_Notification_new_payment_flow_botfield`.
3. `[118] parseNumber` → `[119] CreateJSON` (רק `valid=TRUE` ו-`type=MOBILE`) → `[108] POST /api/subscriber/create` → `[110] get-info-by-user-id`.
4. `[112] BasicRouter`:
   - **ראוט 1:** `[120]/[113] PUT /api/subscriber/update`.
   - **ראוט 2:**
     - `[115]/[116] PUT set-user-fields-by-name` — `fullname`, `fname`, `lname`, `utm_source`, `product_sold`, `count_sold`, `sum_sold`, `payments_count`, `payment_deal_number`, `payment_page`, `personal_id`.
     - `[103] POST /api/subscriber/send-sub-flow` filter verbatim `"Cloudchat id exist"`: `108.data.data.user_ns exist` **AND `92.data.sum = "159"`**.
     - `[139] BasicRouter` → `[124]` קורא לשונית "קבוצות וואטסאפ" בגיליון `1_wcGFEXLJ9yDUE0MpUrAfyzQYbDyN6u_i_Yq-mChBO0` (עמודה C ≥ היום) → `[122] google-contacts:searchContacts` לפי `national` → `[121] createAContact` עם `givenName = "{fullname} {שם הקבוצה}"`, מייל fallback `noemail@invalid.example` (חיבור `Revital 6.7.2026`, revitalfh@gmail.com).
   - **ראוט 3:** `[148] CreateJSON` בונה מחדש את מטען GROW במלואו → **`[149] POST https://n8n.srv964196.hstgr.cloud/webhook/grow-payment-roital`** «Revital_grow_payment --> Fireberry».

**מלכודות 7133755:**
- 🔴 **זה הצינור שמזין את WF-20 ב-n8n.** התיעוד מתאר את WF-20 כמקבל ישירות מ-GROW. בפועל, לפחות חלק מהתשלומים עוברים דרך Make ורק אז מגיעים ל-n8n. **כיבוי התרחיש הזה מנתק את WF-20.**
- 🔴 **`92.data.sum = "159"` קשיח.** הודעת "תשלום התקבל" ב-CloudChat נשלחת **רק** על סכום 159 ₪ בדיוק. כל תשלום אחר עובר בשקט בלי הודעה.
- ⚠️ מזהה גיליון "קבוצות וואטסאפ" קשיח בקוד.
- ⚠️ מודול יתום `[133]` (לא בזרימה) מכיל **URL של GreenAPI עם מזהה instance וטוקן גלוי**.
- ⚠️ 175 הרצות לא הושלמו.

---

### 5 · 7133762 · Create contact - Cloudchat (1.01)
**טריגר:** `https://hook.eu2.make.com/848xf9z43h71ifoy7f8s2skh5ewqdffa` (hook 3167904) · **0 אופרציות.**
`[90]` טוקן גלוי + נרמול שם/טלפון/מייל, קיצוץ שמות מעל 50 תווים · `[3] parseNumber` (onerror Ignore) · `[20] POST /api/subscriber/create` filter `"מעבר רק למספרים תקינים"`: `3.valid = TRUE` AND `3.type = MOBILE` · `[60]` ניסיון שני בלי מייל אם השגיאה היא `"The email must be a valid email address"` · `[101] get-info-by-user-id` · ואז 4 ראוטים: `[88]` מחזיר `{cloudchat_ns, user_id, phone, valid, type}`, `[100]` משלים `fullname/fname/lname` אם חסר, `[99]` משלים מייל, `[98]` משלים `utm_source`.
**מלכודת:** התרחיש דלוק אבל **אף אחד לא קורא לו** (0 אופרציות). שדה `create_contact_scenario_webhook_id` קיים ב-bot fields ומופנה לכאן, אך בפועל 7133760 יוצר מנוי בעצמו. מועמד לכיבוי, לא לפני בדיקה בקלאודצ'אט.
**⚠️ ה-blueprint מכיל נתוני הרצה שמורים עם טוקן CloudChat של חשבון אחר (`kDND6g…`) ופרטי לקוח אמיתיים.**

---

### 6 · 7133769 · Facebook Leads - Cloudchat (1.01)
**טריגר:** Facebook Lead Ads, hook 3167907 «facebook leads» (אין URL ציבורי) · **0 אופרציות.**
`[1] NewLeadMultiple` → `[2] GetLeadDetails` (חיבור `Revital` 10901999, "Oded Yaish") → `[15] SetVariables` (טוקן גלוי; `utm_campaign`, `utm_medium`, `utm_adname` **מוגדרים כמחרוזת ריקה קשיחה**) → `[7] CreateJSON` → `[5]/[17] Get Bot Fields` → `[9] POST hook.{make_zone_botfield}.make.com/{leads_scenario_webhook_id_botfield}`.
**מלכודות:** שלושת שדות ה-UTM ריקים בכוונה, ולכן כל ליד מפייסבוק מגיע בלי ייחוס קמפיין. חיבור פייסבוק על שם משתמש שאינו רויטל — פקיעת הרשאה תשבית בשקט. 0 אופרציות — לא רץ בפועל.

---

### 7 · 7158111 · Firberry CRM
**טריגר:** `https://hook.eu2.make.com/uvhg84wahk9n86ne5al0obsb6pj57mpw` (hook 3179611) · **25,880 אופרציות · 18 הרצות לא הושלמו.**
`[2] parseNumber` על `ifempty(1.phone; 1.payerPhone)` · `[22] BasicRouter`:
- **ראוט 1** (חיבור **11474697 `Revital`** — הפיירברי **הישן**): `[13] plquery` אובייקט 1 לפי 4 וריאציות `telephone1`.
  - `[7] updateObject` filter `"לקוח קיים ויש שאלות לעדכון"` (`13.accountid exist`): כותב `pcfsystemfield149 = q1`, `150 = q2`, `151 = q3`, `152 = q4`, `153 = q5`.
  - `[14] createobject` filter `"לקוח לא קיים"` (`13.telephone1 notexist`): `accountname = 1.full_name`, `telephone1 = 2.national`, `pcfsystemfield113 = 1.utm_source`.
- **ראוט 2** (חיבור **11474723 `Revial`**): `[27] plquery` בלבד, ללא כתיבה.

**מלכודות:**
- 🔴 **התרחיש כותב ל-CRM הלא נכון.** `pcfsystemfield149-153` הם שדות של הפיירברי הישן. כל תשובה שלקוחה נותנת בבוט נשמרת שם, לא בחשבון החדש. 25,880 אופרציות של נתונים שיושבים במקום שגוי.
- 🔴 יוצר לקוחות חדשים בחשבון הישן.
- ⚠️ אין לו טריגר עצמאי: 7133758 מודול `[429]` הוא מי שקורא לו.

---

### 8 · 6812655 · Integration Facebook Lead Ads
**טריגר:** Facebook Lead Ads, hook 3025302 «Revital» · **0 אופרציות.**
שני מודולים: `[3] NewLeadMultiple` ו-`[5] GetLeadDetails`. **אין המשך.**
**מלכודת:** תרחיש דלוק שקולט לידים מפייסבוק ולא עושה בהם דבר. אם מישהו יחבר אליו טופס פעיל, הלידים ייעלמו בלי עקבות.

---

### 9 · 9523761 · Document button from CRM
**טריגר:** `https://hook.eu2.make.com/53w7nb0ktyb42h5ymsfwmokpp2r6ovdf` (hook 4186124 «טופס מסמך לחתימה #1») · **333 אופרציות · 8 הרצות לא הושלמו.**
1. `[3] plquery «חיפוש תהליך מכירה מקושר»` — אובייקט 4 לפי `opportunity_id`. filter verbatim `"רק אם whatsapp"`: `2.send_method contains "whatsapp"` (ci).
2. `[4] «חיפוש לקוח מקושר»` — אובייקט 1 · `[5] «חיפוש מוצר מקושר»` — אובייקט 14 · `[6] «חיפוש מסמך מקושר»` — אובייקט **1058** לפי `pcfformid = 5.Data[].pcfRelevantForm`.
3. `[7] «חיבור לטופסי»` — `POST https://my.tofsy.co.il/api/auth/login` עם **email + api_key גלויים**.
4. `[8] «שליחת מסמך»` — `POST https://my.tofsy.co.il/api/fill/forms/{{ifempty(2.form_id; 6.Data[].pcfformid)}}/create`, `multipart/form-data`. שדות מוזרקים: `Opportunityid`, `Full_Name`, `email`, `phone`, **`Gift = ifempty(3.Data[1].pcfWeeksGift; 0)`**, `date = YYYY-MM-DD`, `id = idnumber`.
5. `[11] updateObject` — אובייקט **4**: `pcfSignatureStatus = 1` · `pcfcfDocLink = https://fill.tofsy.co.il/{{8.data.url_code}}`.
6. `[10] WebhookRespond` — `{"response":"success","submission_url":"…"}`.
7. `[35] BasicRouter`:
   - **ראוט 1 «whatsapp»:** `[19] parseNumber` → `[42]` טוקן CloudChat גלוי → `[21]/[22] POST /api/subscriber/create` → `[24] get-info-by-user-id` → `[43]/[44] PUT set-user-fields-by-name` עם `document_link_after_sign = 8.data.url_code` → `[26] POST /api/subscriber/send-sub-flow` עם `sub_flow_ns` **קשיח** `f201689s4376835`.
   - **ראוט 2 «email»:** `[39] GetVariable2` בשם `"ב"` — **מודול ריק**.

**מלכודות:**
- 🔴 **מסלול המייל לא קיים.** אם המשתמשת בוחרת "מייל" בכפתור, המסמך נוצר ב-Tofsy, `pcfcfDocLink` נכתב, אבל **שום דבר לא נשלח ללקוחה**. הכפתור מחזיר "success".
- 🔴 **`[3]` מסונן ל-whatsapp בלבד.** בבחירת מייל, מודולים `[4]`, `[5]`, `[6]` מקבלים `3.Data[]` ריק, ולכן `Full_Name`, `phone`, `id` וה-`pcfformid` יוצאים ריקים. הטופס נוצר ריק.
- 🔴 **`Gift` מגיע מ-`3.Data[1]`** — האינדקס השני של תוצאות השאילתה. אם יש רק עסקה אחת, שבועות המתנה יוצאים 0.
- 🔴 **פרטי התחברות לטופסי (מייל + api_key) גלויים במודול `[7]`.**
- 🔴 `sub_flow_ns` קשיח בקוד ולא מ-bot field.
- ⚠️ **מוסכמת השמות בתיעוד הפוכה.** `CLICKUP.md` §B.9 מייחס את הוובהוק `53w7nb0k…` לתרחיש "עדכון מסמכים בכפתור שליחת מסמכים". בפועל `53w7nb0k…` = **Document button from CRM** (השליחה) ו-`c3lcf9yf…` = **עדכון מסמכים** (הטעינה). `WORKFLOWS_DOCS.md` ו-`MESSAGING.md` מדויקים.

---

### 10 · 9547511 · Signed Document from Tofsy
**טריגר:** `https://hook.eu2.make.com/7g2xvjmgulpqoic8ai69e5sfm60vp4n7` (hook 4268332 «קבלת מסמך מטופסי») · **12 אופרציות · 0 בתור.**
1. `[4] plquery «חיפוש תהליך מכירה»` — אובייקט 4 לפי `2.Opportunityid` (retry 3× כל 30 שניות).
2. `[5] updateObject «עדכון תהליך מכירה»` — אובייקט **4**: `pcfSignatureStatus = 2` · `pcfcfDocLink = 2.pdf_url` (**דורס** את הקישור לחתימה).
3. `[7] createobject «יצירת רשומה - מסמך»` — אובייקט **1038**: `name = "{{2.Full_Name}} {{2.date}}"` · `pcfLinkToDocument = 2.pdf_url` · `pcfLinkedSale = 2.Opportunityid` · `pcfaccountid = 4.Data[].accountid` · `pcfdocsigned = 1` · `pcfDocumentType = "הסכם חתום"`.

**מלכודות:**
- 🔴 **וובהוק מוגדר ≠ וובהוק פעיל.** ב-Tofsy צריך ללחוץ "פרסום שינויים" אחרי כל שינוי בטופס, אחרת ה-POST לא נשלח והלקוחה עדיין רואה "הטופס התקבל". זה מה ששבר את "תזונה אונליין" ב-23/07.
- ⚠️ `pcfDocumentType` נכתב כטקסט חופשי `"הסכם חתום"`. אם השדה בפיירברי הוא רשימת בחירה, הערך עלול להידחות.
- ⚠️ אין fallback אם `Opportunityid` לא חוזר מ-Tofsy — הכל נופל.

---

### 11 · 9472247 · עדכון מסמכים בכפתור שליחת מסמכים
**טריגר:** `https://hook.eu2.make.com/c3lcf9yf35xfngvxmv1xhw28i3cmi2p2` (hook 4235980 «שליחת מסמך לחתימה») · **114 אופרציות.**
1. `[2] «קרא נתונים מפיירברי»` — `POST https://api.fireberry.com/api/query`, כותרת `tokenid` **עם המפתח כתוב בגלוי**. גוף: `{"objecttype": 1058, "fields": "name,pcfformid,pcfformactive,pcfdefultplatform", "query": "", "page_size": 50, "page_number": 1}`.
2. `[6] WebhookRespond «החזר לכפתור»` — `{"recommended_id":"", "product_name":"", "catalog": {{2.data}}}`.

**מלכודות:**
- 🔴 **מפתח ה-API של פיירברי כתוב גלוי בכותרת ה-HTTP.**
- ⚠️ `recommended_id` ו-`product_name` מוחזרים **ריקים תמיד** — הכפתור לא מקבל המלצה, המשתמשת חייבת לבחור ידנית.
- ⚠️ `page_size: 50` קשיח. טופס ה-51 לא יופיע בקטלוג לעולם.
- ⚠️ `query: ""` — גם טפסים עם `pcfformactive = false` חוזרים.

---

### 12 · 9580074 · Welcome - General Message - CloudChat
**טריגר:** `https://hook.eu2.make.com/b7ca4dujc848jbru83a0psoalhqloqg7` (hook 4282258) · **238 אופרציות · 5 הרצות לא הושלמו.**
מוזן מ**אוטומציה פנימית בפיירברי** על **1051** (תהליך ליווי), אירוע "רשומה נוצרה", מטען שמן של 96 שדות.

1. `[24] util:Switcher` — filter verbatim `"רק אם סטטוס רלוונטי"`: `1.status contains "ליווי בתהליך"` (ci).
   `input = if(1.message_type = "welcome"; 1.product_name; 1.message_type)` · regex:
   - `^תוכנית ליווי` → **`f201689s4391239`** (`welcome_livuy`)
   - `^סדנה בוסט$` → **`f201689s4391241`** (`welcome_boost_v2`)
   - `elseOutput = ""`
2. `[2] SetVariables` — טוקן גלוי + `param_1 = mentor_name` · `param_2 = if(product_name = "סדנה בוסט"; boost_cycle_name; whatsapp_group_name)` · `param_3 = if(…; boost_cycle_group_link; whatsapp_group_link)` · `param_4 = if(…; boost_cycle_start; "")` · `param_5 = boost_cycle_start` · `param_6..10` מהמטען.
3. `[4]/[6] Get Bot Fields` · `[9] parseNumber` · `[10] CreateJSON` (רק `valid=TRUE`, `type=MOBILE`) · `[11] POST /api/subscriber/create` · `[13] get-info-by-user-id`.
4. `[15] BasicRouter`:
   - **ראוט 1:** `[16]/[17] PUT /api/subscriber/update`.
   - **ראוט 2:** `[19]/[20] PUT set-user-fields-by-name` — `data_param1..data_param10` · `[22] POST /api/subscriber/send-sub-flow` filter `11.data.data.user_ns exist`.

**מלכודות:**
- 🔴 **כפילות ישירה מול n8n WF-23/WF-24** — ראה §ג. שני מנועים שולחים את אותה הודעת ברוכה הבאה מאותם `sub_flow_ns`.
- 🔴 **`elseOutput = ""`.** אם `product_name` הוא לא "תוכנית ליווי*" ולא "סדנה בוסט" בדיוק, `[22]` נשלח עם `sub_flow_ns` **ריק**. ה-API יחזיר שגיאה ו-`onerror` הוא `Break` — הלקוחה לא תקבל כלום, בשקט.
- 🔴 **הפילטר `status contains "ליווי בתהליך"` הוא נקודת כשל שקטה.** רשומת 1051 שנוצרת בסטטוס אחר (למשל "טרם התחיל") לא תפעיל את התרחיש כלל, והתור לא יראה שגיאה.
- ⚠️ `param_2..param_5` תלויים בהתאמת מחרוזת מדויקת `product_name = "סדנה בוסט"`. שינוי שם המוצר בפיירברי שובר את פרמטרי ההודעה בלי להשבית את השליחה — הלקוחה מקבלת הודעה עם `-` במקום קישור הקבוצה.
- ⚠️ מספר לא-נייד → `[10]` לא רץ → `[11]` נכשל → אין הודעה.

---

### 13 · 8223019 · BoTcast Sequencial
**טריגר:** `https://hook.eu2.make.com/2gove73j2ihq3aqpmlqiwxc0n8crbem7` (hook 3660229) · **18,571 אופרציות · 14 הרצות לא הושלמו.**
`[12]` טוקן גלוי (`Organic Bot API Auth` **ריק**) · `[14]/[15] Get Bot Fields` · `[28] Sleep 2s` · `[18] get-info-by-user-id` · `[22] SetVariables «User Fields»` · `[24] parseNumber` (onerror Resume) · `[2] BasicRouter` filter `action contains "botcast"`:
- **ראוט 1:** `[3] addRow` בלשונית "דיוורי בוטקאסט" (filter `action = botcast_sent`), סטטוס `"הודעה נשלחה"` · `[30] Sleep 2s`.
- **ראוט 2:** `[5] filterRows` (filter `action = botcast_reply`) → `[7] updateRow` עמודות 8/9/10 (סוג תגובה, תאריך, תוכן) · `[29] onerror Ignore` · `[31] Sleep 2s`.

**מלכודות:** זהו העתק כמעט מדויק של תת-ראוט 9 ב-7133758. **שני תרחישים פעילים כותבים לאותה לשונית.** `Organic Bot API Auth` ריק, ולכן פנייה של בוט אורגני תיכשל. `[29] Ignore` מבליע כישלון עדכון בשקט.

---

### 14 · 7810658 · Webinar_Welcome
**טריגר:** `https://hook.eu2.make.com/7ppiik6vvpxiz1hs21q4h5kdjx2a3vin` (hook 3470095 «Welcome Webinar») · **0 אופרציות.**
`[13]` טוקן גלוי · `[2] parseNumber` (Ignore) · `[4] CreateJSON` (`email` = `ifempty(; emptystring)` — **תמיד ריק**) · `[5] POST /api/subscriber/create` · `[7] get-info-by-user-id` · `[9] SetVariables` — `meeting_date = formatDate(1.webinar_date; "MM/DD/YYYY")` (**פורמט אמריקאי**) · `[10]/[11] PUT set-user-fields-by-name` (`fname`, `meeting_date`, `meeting_time`) · `[14] POST send-sub-flow` `f201689s2837851` **קשיח**.
**מלכודות:** דלוק עם 0 אופרציות — אף אחד לא קורא אליו. `MM/DD/YYYY` יבלבל את מקבל ההודעה בעברית. `f201689s2837851` אינו ברשימת 17 הפלואוז המתועדת ב-`MESSAGING.md` — פלואו יתום.

---

### 15 · 7166372 · Webinar Reminders - Old CRM
**טריגר:** **schedule כל 1800 שניות (30 דקות)**, ימים 0,1,2,3,4,6, בין 09:00 ל-21:00 · **650 אופרציות.**
כל המודולים על חיבור **11474697** = **הפיירברי הישן**.
1. `[4] plquery` — אובייקט **1001** (וובינרים) `WHERE pcfsystemfield105 is-null OR pcfsystemfield106 is-null`.
2. `[5] plquery` — אובייקט **1002** לפי `pcfwebinar`. filter verbatim `"סנן - באותו יום ויום לפני"`:
   `[[now+1h ≥ pcfwebinarstarttime] AND [now+1h < pcfwebinarstarttime+15min]] OR [[date(pcfwebinarstarttime) = date(now+1day)]]`
3. `[6] plquery` — לקוח לפי `5.pcfcustomer`.
4. `[12]` טוקן גלוי · `[26] parseNumber` (Ignore) · `[17]/[13] POST subscriber/create` · `[15] get-info-by-user-id`.
5. `[39] SetVariables` — `fname`, `meeting_type = 5.pcfwebinarname`, `meeting_date/time` מ-`4.pcfwebinarstarttime`, `meeting_location = 4.pcfwebinarregisterlink`.
6. `[36]/[37] PUT set-user-fields-by-name`.
7. `[10] BasicRouter`:
   - **ראוט 1 «יום לפני»:** `[34] send-sub-flow` `f201689s2623961` → `[55] updateObject` אובייקט 1001 `pcfsystemfield105 = "נשלחה"`.
   - **ראוט 2 «שעה לפני + דקה לפני»:** `[30] send-sub-flow` `f201689s2623971` → `[53] updateObject` `pcfsystemfield106 = "נשלחה"`.

**מלכודות:**
- 🔴 **תרחיש מתוזמן פעיל שקורא וכותב בפיירברי הישן** — כל 30 דקות, 12 שעות ביום, 6 ימים בשבוע.
- 🔴 **הוא שולח הודעות אמיתיות ללקוחות** דרך CloudChat, מהחשבון הפעיל של הבוט.
- 🔴 גרסת ה-CRM החדש (`9363636 Webinar Reminders (New CRM)`) בנויה ו**כבויה**. אם מישהו ידליק אותה בלי לכבות את זו — **תזכורת כפולה**.
- ⚠️ הסימון `"נשלחה"` הוא טקסט חופשי בשדה מערכת ישן.
- ⚠️ הראוטים לא בלעדיים: `parseDate` על מחרוזות בפורמט `DD/MM/YYYY HH:mm` הוא נקודת שבירה קלאסית.

---

### 16 · 7415737 · Boost Reminder
**טריגר:** **weekly, שבת 20:45** (Asia/Jerusalem) · **25 אופרציות** · הרצה הבאה 29/08.
`[2]` טוקן גלוי · `[5]/[7] Get Bot Fields` · `[10] SetVariables` — `Whatsapp Group` מ-bot field `whatsapp_group` (**שני האיברים ב-`ifempty` זהים — באג העתקה**) · `[4] filterRows` — גיליון `1_wcGFEXLJ9yDUE0MpUrAfyzQYbDyN6u_i_Yq-mChBO0`, לשונית "הרשמות לבוסט", filter: `B exist` AND `C = {{Whatsapp Group}}` AND `D notexist` · `[16] parseNumber` (filter `4.__IMTLENGTH__ ≥ 1`) · `[15]/[11] POST subscriber/create` · `[13] get-info-by-user-id` · `[18] POST send-sub-flow` `f201689s2725301` **קשיח** · `[20] Sleep 2s` · `[21] updateRow` עמודה D = `"נשלח"` · `[22] Sleep 2s`.
**מלכודות:** `spreadsheetId` במודול `[21]` כתוב **`/1_wcGFEXLJ…`** עם לוכסן מוביל — מזהה שגוי שעלול להפיל את הסימון, ואז אותן לקוחות יקבלו את התזכורת שוב בשבת הבאה. תלות מלאה בעמודת D כמנגנון דדופ. חופף ל-WF-23 ול-`WF-BOOST-B` ב-n8n.

---

### 17 · 7133754 · Performance Report (1.01)
**טריגר:** **weekly, ראשון 10:00** · **90 אופרציות.**
`[68] «Set API TOKEN»` (טוקן גלוי) · `[10]/[75] Get Bot Fields` · `[77] SetVariables` — `make_zone_botfield`, `notifications_webhook_id_botfield`, `Agent_Notification_perfomance_report_flow_botfield`, `performance_repeat_every(days)` · `[6] «Set Dates»` — חלון הדיווח · `[29] BasicRouter` עם 5 ראוטי ספירה מלשונית הלידים (`report_leads_count`, `report_started_conv_count`, `report_start_main_flow_count`, `report_finish_main_flow_count`, `report_converted_count`) + ראוט 6 שמאחד ושולח:
`[70] POST https://hook.{{make_zone_botfield}}.make.com/{{notifications_webhook_id_botfield}}` עם `notification_type: "Agent_Notification_perfomance_report"`.
**מלכודת 🔴:** ה-`notifications_webhook_id_botfield` מצביע ל-hook **3167909** = `Notifications System - CloudChat` (7133774), שהוא **כבוי, ובתור שלו 5 בקשות תקועות**. הדוח השבועי נשלח כבר חודשים אל תוך תרחיש שלא רץ. אף אחד לא מקבל אותו, ואין שגיאה.

---

### 18 · 7413890 · עדכון קישור קבוצה שבועי
**טריגר:** **weekly, ראשון 05:00** · **20 אופרציות.**
`[14] filterRows` — גיליון `1_wcGFEXLJ…`, לשונית "קבוצות וואטסאפ", `limit 1`, filter `C ≥ formatDate(now; "DD.MM.YYYY"; "Asia/Jerusalem")` · `[3] SetVariables` (טוקן גלוי) `Whatsapp Group = 14.0` · `[11] CreateJSON` `{name: "whatsapp_group", value: …}` · `[9] PUT https://console.thecloud.chat/api/flow/set-bot-field-by-name`.
**מלכודות:** אם אין שורה עתידית בגיליון, `14.0` ריק ו-bot field `whatsapp_group` **נדרס בריק** — ואז `Boost Reminder` (7415737) לא ימצא אף לקוחה ב-20:45 באותו שבוע. השוואת תאריכים על מחרוזת `DD.MM.YYYY` היא השוואת מחרוזות בפועל. חופף ל-WF-26 ול-`WF-BOOST-B` שמנהלים את קישורי הקבוצות ב-n8n.

---

## ג. חפיפה מול n8n

מקור n8n: `/home/claude/src/N8N_INDEX.md` (נסרק 23/08/2026).

| התהליך | תרחיש ב-Make | וורקפלואו ב-n8n | שניהם פעילים? | סיכון כפילות |
|---|---|---|---|---|
| **הודעת ברוכה הבאה (ליווי/בוסט)** | **9580074** Welcome - General Message ✅ | **WF-24** `LciQfMEuU2GKWRjI` ✅ + **WF-23** `vRP5tdFeS3AWPdRv` ✅ | 🔴 **כן** | 🔴🔴🔴 **קריטי.** שניהם יורים את אותם `f201689s4391239` / `f201689s4391241`. Make מופעל מאוטומציה פנימית על 1051 "רשומה נוצרה"; n8n מופעל מ-WF-22 אחרי תשלום. תשלום שיוצר 1051 מפעיל **את שניהם**. לקוחה מקבלת שתי הודעות ברוכה הבאה |
| **קליטת תשלום GROW/Meshulam** | **7133755** New payment - CloudChat ✅ | **WF-20** `p1AfsAeXcrlhtiNk` ✅ | 🔴 **כן, בשרשרת** | 🟡 **תלות סמויה.** Make אינו מקביל ל-WF-20 אלא **ה-upstream שלו** (`[149]` POST ל-`/webhook/grow-payment-roital`). כיבוי 7133755 מנתק את WF-20. גם ה-blueprint הישן `Revital_grow_payment` `H4DEVdHMRbzRdBoF` יושב על אותו path |
| **תזכורות וובינר** | **7166372** Webinar Reminders - Old CRM ✅ (כל 30 דק') | **WF-18** `QYIWAcr2Pa0GYtSz` ✅ + 6.6 `aIM9lWgQsHnkDGd2` (כבוי) | 🔴 **כן** | 🔴 **גבוה.** Make שולח תזכורות אמיתיות מהפיירברי **הישן**. WF-18 מושך נתוני זום לפיירברי החדש. כרגע לא חופפים באובייקטים, אבל ברגע ש-6.6 יודלק או ש-9363636 יודלק — תזכורות כפולות |
| **תזכורת בוסט שבועית** | **7415737** Boost Reminder ✅ (שבת 20:45) | **WF-23** ✅ · `WF-BOOST-B` (כבוי) · עדכון מחזורי בוסט `3dOrk3a6TJk8wIkN` ✅ | 🔴 **כן** | 🔴 **גבוה.** Make עובד מגיליון "הרשמות לבוסט"; n8n עובד מאובייקט 1054 בפיירברי. שני מקורות אמת לאותה רשימת נמענים |
| **ניהול קישור קבוצת וואטסאפ** | **7413890** עדכון קישור קבוצה שבועי ✅ (ראשון 05:00) | **WF-26** `0xf7yD9IeCyXDgWp` ✅ · `WF-BOOST-B` (כבוי) | 🔴 **כן** | 🟡 **בינוני.** Make דורס bot field גלובלי אחד בקלאודצ'אט לפי גיליון; n8n מזרים קישור פר-לקוחה מפיירברי. אם הגיליון לא מעודכן, Make מוחק את הערך של n8n |
| **טופס שקילה שבועי** | **9490447** (כבוי, 2 בתור) | **WF-27b** `ujJNu28LEvBguzlP` ✅ | ❌ לא | 🟢 נמוך — אבל שני הוובהוקים חיים ב-Tally. אם מישהו יחזיר את הישן, שתי רשומות 1069 |
| **שאלון סיום בוסט** | **9513015** (כבוי, 2 בתור) | **WF-07** `WF07BoostForm01` ✅ | ❌ לא | 🟢 נמוך, אותה הערה |
| **תשלום שאינו אשראי** | **9560342** / **9614987** (כבויים, 42 בתור) | **WF-27a** `LhstekfLJ0AClk5i` ✅ | ❌ לא | 🟡 42 בקשות תקועות ב-hook 4274104. הדלקה תיצור תיעודי תשלום כפולים ל-42 עסקאות |
| **אישור הגעה לפגישה** | **9575585** (כבוי) — ו-**7133758** עדיין שולח אליו ✅ | WF-32 מתוכנן, לא קיים | 🟡 חלקית | 🔴 **אירועי scheduling נכנסים לתור של תרחיש כבוי.** אף מערכת לא מטפלת בהם |
| **התראות למפעילים** | **7133754** ✅ שולח אל **7133774** (כבוי, 5 בתור) | `Notfication System - V2 Revital` `dqFsbHViUi3OG6cg` ✅ | 🟡 חלקית | 🔴 הדוח השבועי נופל לתור של תרחיש כבוי |
| **תשובות שאלון לקוחה (q1–q5)** | **7158111** Firberry CRM ✅ | ❌ אין | ✅ Make בלבד | 🔴 **כותב לפיירברי הישן.** 25,880 אופרציות של נתונים בחשבון שכבר לא בשימוש |
| **צירוף לקבוצת וואטסאפ** | **7414624** (כבוי, 1 בתור) | **WF-26** ✅ | ❌ לא | 🟢 נמוך |
| **קליטת ליד מרכזית** | **9339860** ✅ | ❌ אין | ✅ Make בלבד | 🟢 אין כפילות — אבל **נקודת כשל יחידה** |
| **שרשרת החתימות** | **9523761** + **9472247** + **9547511** ✅ | ❌ אין | ✅ Make בלבד | 🟢 אין כפילות |
| **מנוע הבוט** | **7133758/7133760/8223019** ✅ | `Revital - Response Agent` `VcsxDn3cd60CL4FE` ✅ (כל 15 דק') | 🟡 חלקית | 🟡 שני מנועים נוגעים באותם מנויי CloudChat. סיכון למרוץ על אותו שדה מנוי |

### שלוש שורות התחתונה של §ג
1. 🔴 **הודעת ברוכה הבאה נשלחת פעמיים.** זו הכפילות היחידה שהלקוחה רואה בעיניים. חייבת החלטה: או שמכבים את `9580074`, או שמכבים את שליחת ההודעה ב-WF-23/24. **לא לגעת בלי מייק ובלי בדיקה על לקוחת הבדיקה.**
2. 🔴 **Make הוא upstream של n8n בתשלומים, לא מקביל לו.** כל תוכנית "לכבות את Make" חייבת קודם להעביר את מטען GROW ישירות ל-`/webhook/grow-payment-roital`.
3. 🔴 **שלושה תרחישים פעילים עדיין נוגעים בפיירברי הישן** (7158111, 7166372, וראוט 2 של 7158111). הם לא שבורים — הם פשוט כותבים למקום שאף אחד כבר לא קורא ממנו.

---

## ד. שכבת הבוט

חמשת תרחישי CloudChat + `BoTcast Sequencial` הם המנוע ששולח את רוב ההודעות ללקוחות. הם עובדים כמערכת אחת עם **חמישה מקורות אמת חיצוניים**: bot fields ב-CloudChat, שני גיליונות גוגל, פיירברי חדש, ופיירברי ישן.

### מפת הקריאות

```
פייסבוק Lead Ads ──► 7133769 ──┐
מרכזייה / VoiceCenter ─────────┤
וורדפרס / אתר ─────────────────┼─► 7133760 Main Leads CloudChat (hook 3167903)
בוט אורגני ────────────────────┤        │
פיירברי ───────────────────────┘        │
                                        ├─► POST /api/subscriber/create
                                        ├─► PUT  /api/subscriber/set-user-fields-by-name
                                        ├─► POST /api/subscriber/send-sub-flow  (התראה למפעיל)
                                        └─► POST hook {main_scenario_webhook_id_botfield}
                                                     │
                                                     ▼
CloudChat (הבוט מדבר) ──────────────► 7133758 Main Chatbot Scenario (hook 3167901)
                                        │
                                        ├─► Google Sheets: לידים · פניות · משובים · רכישות ·
                                        │                  דיוורי בוטקאסט · יומן פגישות · תוכן דינמי · התנור
                                        ├─► POST hook uvhg84wa… ──► 7158111 Firberry CRM ──► פיירברי הישן
                                        ├─► POST hook k7w5w314… ──► 9575585  ⚠️ כבוי
                                        └─► WebhookRespond (JSON חזרה לבוט)

Cardcom / Meshulam / GROW ──────────► 7133755 New payment (hook 3167900)
                                        ├─► CloudChat: מנוי + 11 שדות רכישה
                                        ├─► Google Contacts: "{שם} {קבוצה}"
                                        ├─► send-sub-flow  ⚠️ רק אם sum = 159
                                        └─► POST n8n /webhook/grow-payment-roital ──► WF-20

פיירברי 1051 "רשומה נוצרה" ─────────► 9580074 Welcome (hook 4282258)
                                        └─► send-sub-flow  4391239 / 4391241  🔴 כפילות מול WF-23/24

בוט (דיוור) ────────────────────────► 8223019 BoTcast Sequencial (hook 3660229)
                                        └─► Google Sheets: דיוורי בוטקאסט

(שירות עזר, 0 קריאות) ──────────────► 7133762 Create contact (hook 3167904)
```

### שדות המנוי שהשכבה כותבת

| שדה | נכתב ע"י | מקור הערך |
|---|---|---|
| `fullname` / `fname` / `lname` | 7133760, 7133762, 7133755, 9580074 | מטען הליד או `subscriber/get-info` |
| `email` | 7133762, 7133755 | מטען |
| `utm_source` | 7133760, 7133762, 7133755 | ראוט המקור; ברירת מחדל `"צאטבוט"` / `"מרכזיה"` / `"אתר"` |
| `product_sold`, `count_sold`, `sum_sold`, `payments_count`, `payment_deal_number`, `payment_page`, `personal_id` | **7133755** | מטען הסליקה, מנורמל משלושה פורמטים |
| `document_link_after_sign` | **9523761** | `8.data.url_code` מטופסי |
| `data_param1..data_param10` | **9580074** | `mentor_name`, שם/קישור קבוצה, תאריך מחזור בוסט |
| `fname`, `meeting_type`, `meeting_date`, `meeting_time`, `meeting_location` | 7166372, 7810658 | וובינר בפיירברי הישן |
| `main_flow_q1..q15`, `main_flow_status`, `main_flow_name`, `contact_score` | **נקראים** ע"י 7133758/8223019, לא נכתבים | הבוט כותב אותם בעצמו |

### הפלואוז שהשכבה יורה

| `sub_flow_ns` | שם ב-`MESSAGING.md` | נורה מ- | קשיח בקוד? |
|---|---|---|---|
| `f201689s4391239` | `welcome_livuy` | **9580074** (וגם n8n WF-24) | לא — מ-Switcher |
| `f201689s4391241` | `welcome_boost_v2` | **9580074** (וגם n8n WF-23) | לא — מ-Switcher |
| `f201689s4376835` | `send_signature_request` | **9523761** | 🔴 **כן** |
| `f201689s2623961` | `boost_webinar_invite (1day before)` | **7166372** | 🔴 **כן** |
| `f201689s2623971` | *(לא ברשימת 17 הפלואוז)* | **7166372** | 🔴 **כן** |
| `f201689s2837851` | *(לא ברשימת 17 הפלואוז)* | **7810658** | 🔴 **כן** |
| `f201689s2725301` | *(לא ברשימת 17 הפלואוז)* | **7415737** | 🔴 **כן** |
| `f201689s4392327` | `הרצאת אורח שבועית` (הוצא משימוש) | 9581051 (כבוי) | לא |
| `Client_Notification_*` | התראות למפעילים | 7133760, 7133755 | לא — מ-bot fields |

**⚠️ ארבעה `sub_flow_ns` שנורים מתרחישים פעילים אינם מופיעים בטבלת 17 הפלואוז ב-`MESSAGING.md`.** הם קיימים בקלאודצ'אט ונשלחים ללקוחות אמיתיות, אבל אף אחד לא מתחזק את הנוסח שלהם. `f201689s2623971`, `f201689s2837851`, `f201689s2725301` הם החשודים המיידיים.

### נקודות השבירה של השכבה כמכלול
1. **`chatbot_status_botfield`** — מתג הרג יחיד. אם ערכו אינו 1 או 2, 7133760 מקבל לידים ולא עושה איתם דבר.
2. **טוקן CloudChat אחד** מוטבע ב-18 תרחישים כטקסט. רוטציה שלו משביתה את כל השכבה בבת אחת.
3. **סדר עמודות בגיליונות גוגל** מקודד בקוד כאינדקסים מספריים (`298.11`, `319.5`, `4.0`). הוספת עמודה באמצע שוברת הכול בשקט.
4. **`FunctionSleep`** במקום מנגנון תור. תחת עומס יש מרוץ כתיבה לגיליון.
5. **הכל תלוי בטלפון נייד ישראלי תקין.** בכל אחד מהתרחישים יש `parseNumber` עם פילטר `valid = TRUE` ו-`type = MOBILE`. מספר קווי או מספר מחו"ל נופל בשקט.

---

## ה. מפתחות חשופים

**מיקום וסוג בלבד. אף ערך לא מודפס כאן.**

| # | סוג | היכן בדיוק | תרחישים | חומרה |
|---|---|---|---|---|
| 1 | **Bearer של הבוט הראשי ב-CloudChat** (60 תווים) | מודול `util:SetVariables` בשם `API Auth` / `Api` / `API` / `CLOUDCHAT_TOKEN`, כערך טקסט קבוע | **18 תרחישים**: 7133754, 7133755, 7133758, 7133760, 7133762, 7133769, 7133771, 7133773, 7133774, 7155969, 7166372, 7228986, 7413890, 7414624, 7415737, 7810658, 8119255, 8223019, 9363636, 9523761, 9580074, 9581051, 9581458 | 🔴 **קריטי** |
| 2 | **Bearer של הבוט האורגני ב-CloudChat** (60 תווים) | `SetVariables` בשם `Organic Bot API Auth` | 7133758, 7133760, 9581458 | 🔴 קריטי |
| 3 | **`tokenid` של פיירברי** | כותרת HTTP `tokenid` במודול `http:MakeRequest` / `ActionSendData` | **9472247 (פעיל!)**, 9581051, 9581458 | 🔴 **קריטי** |
| 4 | **`tokenid` של פיירברי** | שדה `pcfPowerlinkToken` ברשומת ההגדרות (1036), שנשמר בנתוני ההרצה השמורים בתוך ה-blueprint | 9339860, 9560342 | 🔴 קריטי — **המפתח דולף דרך ה-blueprint גם בלי שאף מודול כותב אותו** |
| 5 | **פרטי התחברות לטופסי** — כתובת מייל + `api_key` | גוף JSON של `POST my.tofsy.co.il/api/auth/login` | **9523761 (פעיל!)**, 9523034 | 🔴 **קריטי** |
| 6 | **JWT של טופסי** (טוקן שעה) | נתוני הרצה שמורים בתוך ה-blueprint | 9523761, 9523034 | 🟡 בינוני (פג תוקף) |
| 7 | **מזהה instance + טוקן GreenAPI** | בתוך ה-URL של מודול HTTP (`7107.api.greenapi.com/waInstance…/…/<token>`) | **7133755 (מודול יתום `[133]`)**, 9484992 | 🔴 קריטי |
| 8 | **Bearer של CloudChat מחשבון אחר** | נתוני הרצה שמורים | 7133762, 7133773 | 🟡 בינוני |
| 9 | **מפתח API של ספק חיצוני** בכותרת `apiKey` | מודול HTTP | 7133760 | 🟡 בינוני |
| 10 | **`public_token` של טופסי** ו-**`accessToken` של Tally** | נתוני הרצה שמורים | 9490447, 9523761, 9547511, 9523034 | 🟡 בינוני |
| 11 | **PII של לקוחות אמיתיות** — שמות מלאים, טלפונים, מיילים, `user_ns` | נתוני הרצה שמורים בכל blueprint כמעט | רוב התרחישים | 🟡 **כל ייצוא של blueprint הוא ייצוא של נתוני לקוחות** |

**מסקנה מבצעית:**
- Make **אינו** מנהל credentials עבור CloudChat, פיירברי, טופסי ו-GreenAPI. הוא מנהל אותם רק עבור Google, Zoom, Tally ופייסבוק (דרך `__IMTCONN__`). כל שאר המפתחות כתובים כטקסט.
- **רוטציה של טוקן CloudChat דורשת עריכה ידנית של 18+ תרחישים.** זו לא פעולה של חמש דקות.
- **רוטציה של `tokenid` פיירברי דורשת עריכה של 3 תרחישים + עדכון של שדה `pcfPowerlinkToken` ב-1036 + עדכון כל חיבורי ה-powerlink.**
- ⚠️ הסדר בכל רוטציה: חלון מתואם → סיבוב → עדכון כל החיבורים והמודולים → בדיקה של כל תרחיש פעיל אחד-אחד → אימות הכפתורים בפיירברי.

---

## ו. תיקונים לתיעוד הקיים

| מסמך | מה כתוב | מה בפועל |
|---|---|---|
| `a-16.html` (גרסה קודמת) | "Make מחזיק קליטת לידים, חתימות, שני וובהוקים והודעת ברוכה הבאה" (4 שורות) | **18 תרחישים פעילים**, מהם 5 מנוע בוט מלא, 4 מתוזמנים, ו-3 שכותבים ל-CRM הישן |
| `CLICKUP.md` §16.2 | "37 תרחישים בחשבון... אחרי ניקוי: 11 חיים" | **43 תרחישים**, **18 מסומנים כפעילים** |
| `CLICKUP.md` §B.9 | "`עדכון מסמכים בכפתור שליחת מסמכים` — webhook `53w7nb0k…`" | **הפוך.** `53w7nb0k…` = `Document button from CRM` (9523761, השליחה). `c3lcf9yf…` = `עדכון מסמכים` (9472247, הטעינה). `WORKFLOWS_DOCS.md` §543 ו-`MESSAGING.md` §540 נכונים |
| `MESSAGING.md` §42–43 | A1/A2 "Fired by WF-23 / WF-24" | **גם `9580074` ב-Make יורה בדיוק את אותם שני `sub_flow_ns`.** שדה "Fired by" חסר את Make |
| `WORKFLOWS_DOCS.md` §765 | האוטומציה על 1051 → `b7ca4duj…` "אושרה ונכונה ללא שינויים" | נכון טכנית, **אבל היא יוצרת את הכפילות מול WF-23/24** — האישור ניתן לפני שהבנייה ב-n8n הושלמה |
| `N8N_INDEX.md` / WF-20 | "webhook `POST /webhook/grow-payment-roital`" מ-GROW | **חלק מהתנועה מגיע דרך Make** (7133755 מודול `[149]`). לא כתוב באף מסמך |
| `WORKFLOWS_DOCS.md` §738–748 | טבלת תורי הוובהוקים | חסרות שתי שורות: hook **3479103** (`My Zoom Webinars`) עם **682 בתור** — התור הגדול ביותר בחשבון — ו-hook **4243469** (`Revital #1`, tally) שמסומן **`gone: true`** |
| כל המסמכים | "המפתחות מסובבים / לא סובבו" | **`tokenid` של פיירברי דולף גם דרך נתוני ההרצה השמורים בתוך blueprints של תרחישים שאף אחד לא נגע בהם.** סיבוב חייב לכלול מחיקת נתוני הרצה, לא רק עריכת מודולים |

---

*נבנה מקריאה מלאה של 43 blueprints. לא בוצעה שום פעולת כתיבה בחשבון Make.*
