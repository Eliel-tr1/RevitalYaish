# יומן שינויים · 08/09/2026 — הפרדת "סוג תשלום" מ"אמצעי תשלום" ב-WF-20

## הבאג

לקוחה משלמת בהוראת קבע ← ב-1039/1018 נכתב `pcfPaymentType` (**אמצעי תשלום**: אשראי/מזומן/העברה...) = **7 "הוראת קבע"**.
אבל הוראת קבע היא **סוג** תשלום, לא אמצעי תשלום — היא לא אמורה להופיע בשדה הזה בכלל.

## ההחלטה הסופית (אחרי דיון עם אליאל 08/09)

השדה החדש `pcfPaymentCategory` ("סוג תשלום": 1=רגיל · 2=תשלומים · 3=הוראת קבע) קיים **רק על אובייקט 1039 (תשלום)**.

**אובייקט 1018 (תיעוד תשלום) לא מקבל שדה מקביל.** תיעוד תשלום הוא נגזרת של רשומת התשלום (מקושר אליה דרך `pcfPaymentCollection`) — אם התשלום הוא הוראת קבע, כך גם כל תיעודי התשלום שלו. אין טעם לשכפל את הסיווג פעמיים; מי שצריך לדעת את הסוג של תיעוד תשלום — קורא אותו מרשומת האב. (בדיקת הביניים שקבעה אחרת בוטלה — ראו "היסטוריה" למטה.)

על **1018 תוקן רק דבר אחד:** `pcfPaymentType` לעולם לא יקבל יותר את הערך 7 — ברירת המחדל לחיובי הוראת קבע היא **1 (אשראי)**, בהתאמה לתיקון שאליאל עשה ידנית ב-1039.

## מה נעשה

### 1. שדה חדש — `pcfPaymentCategory` על 1039 בלבד

✅ נוצר ידנית ע"י אליאל, מאומת דרך ה-API.

### 2. תיקון רטרואקטיבי לנתונים קיימים

- **1039:** ✅ הושלם ע"י אליאל. אומת: 364/364 רשומות — אפס `pcfPaymentType=הוראת קבע` נותרות, 138 מסווגות נכון כ-`pcfPaymentCategory=הוראת קבע`.
- **1018:** ❌ **טרם בוצע.** לפחות 120 רשומות עם `pcfPaymentType=הוראת קבע` נמצאו בעמוד הראשון בלבד (500 רשומות מתוך יותר) — הכמות האמיתית גבוהה יותר, טרם נספרה עד הסוף. **אין צורך לחכות לשדה חדש** (אין כזה על 1018) — פשוט להחליף `pcfPaymentType`: הוראת קבע → אשראי, כמו שנעשה ב-1039.

### 3. קוד WF-20 (`p1AfsAeXcrlhtiNk`) — פרוס ופעיל

גיבוי לפני שינוי: `WF20_backup_20260908_113848_before_paymentcategory_fix.json` (בסביבת הכלי, לא בפרויקט).

**צומת "בניית תוכנית פעולה"** — 5 מקומות שכתבו `paymentType: 7` על **1039 ו-1018** תוקנו ל-`paymentType: 1` (אשראי):
- הקמת הוראת קבע (ענף C) — 1039 + 1018 המותנה
- ריפוי עצמי רטרואקטיבי של הו"ק שאין לה רשומה (ענף D) — 1039 + 1018
- חיוב מתוך הו"ק קיימת (ענף D) — 1018

בנוסף, **על 1039 בלבד** נוסף `paymentCategory: 3` (הוראת קבע) בכל אחד מהמקומות האלה, ו-`paymentCategory` = 1/2 (רגיל/תשלומים) ברכישה רגילה/פריסה (ענף A, 1039).

**זיהוי "האם זו הוראת קבע" (שורה קריטית בקוד):** קודם היה `srcType === 7` (קורא את "אמצעי תשלום" מרשומת האב). עכשיו קורא את `pcfPaymentCategory === 3` מרשומת האב (1039) קודם, עם `srcType===7` כרשת ביטחון לרשומות ישנות שטרם תוקנו/הוגרו.

**ענף B/F (עדכון/יצירת תיעוד תשלום קיים, 1018):** מוגן כך ש-`paymentType` לעולם לא יקבל 7 גם אם `srcType` הישן (מרשומת האב) עדיין נושא את הערך.

**שני צמתי החיפוש ב-1039** ("חיפוש תשלום לפי מזהה עסקה" ו"חיפוש תשלומים של הלקוחה") — נוסף `pcfPaymentCategory` לרשימת השדות, כדי שהקוד יוכל לקרוא את סיווג רשומת האב.

✅ נבדק: אין יותר אף `paymentType: 7` בקוד (על 1039 או 1018). פרוס ל-n8n (HTTP 200), הוורקפלואו עדיין פעיל (`active: true`).

### היסטוריה — גרסת ביניים שבוטלה

בסבב הראשון (08/09, לפני ההחלטה הסופית) יצרתי בקוד גם מנגנון לכתיבת `pcfPaymentCategory` על 1018, כבוי מאחורי דגל `cfg.WRITE_1018_PAYMENT_CATEGORY = false`, מתוך הנחה שיהיה צריך שדה מקביל שם. אליאל צדק וזה בוטל — התיקון היחיד שנשאר על 1018 הוא `pcfPaymentType` שלא יהיה יותר 7. הדגל והלוגיקה הוסרו מהקוד החי.

## מה נשאר לעשות

1. לתקן רטרואקטיבית את `pcfPaymentType` ב-1018 (הוראת קבע → אשראי) — לפחות 120 רשומות, כנראה יותר (יש עוד עמודים, לא נספר עד הסוף). **אין תלות בשדה חדש** — אפשר לבצע מייד.
2. **⚠️ ממצא נוסף — הועבר לסשן המיגרציה המקבילה** (ראו `MADRICH_2026-09-08_מיפוי_מיגרציה_סוג_תשלום.md`): תוכנית המיגרציה מה-CRM הישן ממפה כרגע הו"ק→7 בדיוק כמו הבאג המקורי — ישפיע על כ-630 רשומות תשלום ישנות אם לא יתוקן לפני שלב הטעינה.

## למה ברירת המחדל של אמצעי התשלום היא "אשראי" ולא ריק

באירועי חיוב חוזר של הוראת קבע, גרואו לא שולחת מידע על סוג האמצעי בפועל (רק שהחיוב הצליח/נכשל). "אשראי" נבחר כברירת מחדל כי זה בפועל מה שאליאל תיקן ידנית ב-364 הרשומות של 1039 (0 חריגות אחרי הבדיקה), ולכן זו כבר האמת התפעולית בשטח.
