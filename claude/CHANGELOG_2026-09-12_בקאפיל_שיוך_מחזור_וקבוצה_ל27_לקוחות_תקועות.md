# יומן שינויים · 12/09/2026 — Backfill: שיוך מחזור בוסט + קבוצת WhatsApp ל-27 לקוחות תקועות

## הקשר

בהמשך ל-`CHANGELOG_2026-09-12_מעבר_ללייב_הסרת_סינון_בדיקה.md` — מייק אישר לרוץ backfill על 28 (בפועל 27, ראה §4) הלקוחות ששילמו על בוסט בין 08/09–12/09 בזמן ש-WF-23 היה ב-`DRY_RUN`, **בלי** צירוף פיזי לקבוצת הוואטסאפ (GreenAPI) ובלי הודעת וולקאם — שיוך רשומות בפיירברי בלבד.

## מה נבדק לפני ביצוע

- שלפתי מ-n8n (`vRP5tdFeS3AWPdRv`, WF-23) את ה-executions בחלון 08/09–12/09 וקראתי את ה-`ops` המדויקים שהמנוע עצמו חישב לכל לקוחה (כולל מחזור וקבוצה) — **לא ניחשתי לוגיקה, שיחזרתי אותה 1:1 מהקוד**.
- כל 27 העסקאות אומתו במצב "לא טופל" (`statuscode=2`, `pcfCourse`/`pcfWhatsAppToRef` ריקים) לפני כתיבה.
- 🆕 **ממצא:** ל-21 מתוך 27 כבר היתה רשומת ליווי (1051) קיימת, שנוצרה ע"י תהליך אחר (לא WF-23) יום אחרי הרכישה בערך, בפורמט שם שונה ("ליווי - {שם} | סדנה בוסט") וללא שיוך מחזור/קבוצה. **עודכנו** (לא שוכפלו) — נוסף רק שיוך המחזור והקבוצה. ל-6 הנותרות נוצרה רשומת ליווי חדשה לפי הלוגיקה של WF-23.

## מה בוצע — לכל אחת מ-27 הלקוחות

| רשומה | פעולה |
|---|---|
| תהליך מכירה (4) | `statuscode=11` (עסקה נסגרה בהצלחה), `pcfCourse`=מחזור, `pcfWhatsAppToRef`=קבוצה |
| ליווי (1051) | חדש (6) או עדכון קיים (21) — `pcfBoostLinked`+`pcfActualGroup` |
| הקצאת תפריט (1059) | נוצרה, `pcfMenuStatus=2` ("נשלח ללקוחה") **במקום 5 ("מוכן לשליחה")** — כדי שהאוטומציה השבועית לא תשלח תפריט באיחור |
| הערה | תועדה על רשומת הליווי, מסומנת במפורש כ-backfill רטרו |
| **לא בוצע** | צירוף פיזי לקבוצת WhatsApp (GreenAPI) · הודעת וולקאם |

כל 27 הלקוחות שויכו לאותו מחזור: **בוסט 13-20.9.26** (`FFB97E47-75BC-4967-9A3D-72D86E8836A9`) וקבוצת **סדנת בוסט לירידה במשקל מחזור 13.09.2026** (`CAD7798B-CC3B-42BF-93FF-4D5A3632C66E`) — המחזור היחיד שכרגע "פתוח להרשמה" (`pcfStatus=1`).

**אומת בקריאה חוזרת** (לא רק `success:true`) — 27/27 עסקאות, 27/27 רשומות ליווי, 27/27 הקצאות תפריט תקינות.

## 🔴 שני ממצאים נפרדים שדורשים החלטה/בדיקה — לא טופלו כאן

1. **לילך ימיני** (0507977127, רכשה 08/09) — **לא מופיעה בכלל בלוג ה-executions של WF-23**. כלומר הבעיה שלה היא לא DRY_RUN — התהליך המקצועי מעולם לא נורה אצלה. צריך לבדוק את WF-22 (הראוטר) או את חיבור WF-20→WF-22 עבור העסקה שלה ספציפית.
2. **מקור 21 רשומות הליווי החלקיות** (§לעיל) — תהליך לא מזוהה יוצר רשומת ליווי בסיסית (בלי מחזור/קבוצה, בפורמט שם אחר) יום אחרי כל רכישת בוסט. זה לא WF-23. כדאי לברר מה זה — אם זו אוטומציה פנימית ישנה של פיירברי שלא כובתה, יש לה חפיפה חלקית עם WF-23 וכדאי להחליט אם לכבות אותה או לתאם ביניהן.

## מצב WF-23 מכאן והלאה

כפי שאושר ע"י אליאל היום: **DRY_RUN כבוי, פועל במלואו** — כולל צירוף פיזי לקבוצה ב-GreenAPI ושליחת הודעת וולקאם, על כל רכישת בוסט חדשה מעכשיו.
