# תוכנית בדיקות · 6 האוטומציות

**תאריך:** 20.08.2026 · **מצב:** מצב בדיקה דלוק, 49 ואריאציות

## מצב בדיקה

שער אחד ב-**WF-29** (מנוע השליחה) בנוד `⚙️ הכנה ואימות קלט`:

```js
const TEST_MODE   = true;
const TEST_PHONES = ['0533435240'];
```

לפני כל שליחה המנוע מנרמל את הטלפון ל-972 ובודק מול הרשימה. לא ברשימה = לא נשלח, ומדווח `🧪 מצב בדיקה: הטלפון לא ברשימת המורשים`.

WF-31 ו-WF-33 לא עוברים דרך המנוע (הם כותבים לפיירברי בלבד), ולכן קיבלו שער זהה בנוד ההגדרות שלהם: `cfg.TEST_MODE` + `cfg.TEST_PHONES`.

**סה"כ 3 מקומות לכבות בסוף:** WF-29, WF-31, WF-33.

### אימות שבוצע על השער (סימולציה מקומית של הקוד)

| קלט | תוצאה |
|---|---|
| `0533435240` | עובר |
| `972533435240` | עובר |
| `053-343-5240` | עובר |
| `0501234567` (לקוחה אחרת) | נחסם, מצב בדיקה |
| `flowNs = <<CREATE_FLOW>>` | נחסם, מזהה פלואו לא תקין |
| `123` | נחסם, טלפון לא תקין |
| עם `dedupField`/`dedupValue` | עובר, והסימון נוסף אוטומטית לשדות הנשלחים |

### המספר קיים בשתי המערכות (אומת מול ה-API)

- **קלאודצ'אט:** מנוי `f201689u860230363`, שם "סער וינברג"
- **פיירברי:** לקוחה `f23e28c0-af71-494c-a8ce-ad5cd19d7694`, "סער וינברג (טסט)", סטטוס לקוח פעיל

## סדר הבדיקות

1. **WF-31** ואז **WF-33** — לא דורשים פלואוז חדשים, אפשר מיד.
2. **WF-34** — שלושת הפלואוז שלו קיימים.
3. **WF-30** — אחרי הקמת 4 פלואוז.
4. **WF-32** — אחרי הקמת 3 פלואוז, ותלוי בכך ש-WF-33 כבר עובד.

---

## WF-31 · בקשת פגישה מהוובינר · `m1RC6ufxVNO7Qn7W`

**לפני:** להדליק את התהליך. מצב הבדיקה חוסם כל טלפון חוץ משלך.
**איך:** אני יורה את כל הווריאציות לוובהוק מכאן.

| # | מה עושים | מה אמור לקרות |
|---|---|---|
| 1 | טלפון `0533435240` | משימה נפתחת, משויכת לנציג של תהליך המכירה, לביצוע היום |
| 2 | `972533435240` | אותה תוצאה. בודק נרמול |
| 3 | `053-343-5240` | אותה תוצאה. בודק ניקוי תווים |
| 4 | ירייה שנייה מיד | לא נפתחת משימה שנייה, "כבר קיימת משימה פתוחה מהיום" |
| 5 | טלפון של לקוחה אחרת | נחסם, מצב בדיקה |
| 6 | `0509999999` (לא קיים) | נחסם. אחרי כיבוי מצב בדיקה: משימה עם הטלפון הגולמי |
| 7 | בקשה בלי טלפון | נחסם, הטלפון מדווח כריק |
| 8 | אחרי מחיקת המשימה מאתמול | משימה חדשה נפתחת. בודק שהחסם מוגבל ליום |
| 9 | לסגור את כל תהליכי המכירה של סער | משימה נפתחת לויטרו ראשי, מקושרת ללקוחה בלבד |

## WF-33 · חיווי אישור הגעה · `DRqd5IryThPEk6nC`

**לפני:** להדליק, ולפתוח פגישה עתידית לסער וינברג (טסט) עם תאריך, שעה, אחראי, סטטוס "עתידה להתקיים".

| # | מה עושים | מה אמור לקרות |
|---|---|---|
| 1 | "אני אגיע!" | אישור הגעה → אישרה, סטטוס → אישרה הגעה, נכתבת הערה |
| 2 | "צריכה להזיז" | אישור הגעה → לא אישרה, נפתחת משימת תיאום מחדש |
| 3 | "אני אגיע" בלי סימן קריאה | מזוהה. בודק סובלנות |
| 4 | טקסט עם רווחים מיותרים | מזוהה |
| 5 | טקסט אחר, למשל "תודה" | לא מזוהה, שום שינוי, מדווח |
| 6 | "אני אגיע" ואז "צריכה להזיז" | האחרונה קובעת, שתי ההערות נשמרות |
| 7 | אחרי מחיקת הפגישה | "אין פגישה עתידית פתוחה" |
| 8 | שתי פגישות עתידיות | נבחרת הקרובה |
| 9 | פגישה בתאריך שעבר | לא נסרקת |

## WF-34 · תזכורות שקילה · `BJLqvtIRdjZI2P1s`

**לפני:** לוודא שלסער יש תהליך ליווי בסטטוס "ליווי בתהליך".
**איך:** לשנות `STAGE_OVERRIDE` בנוד ההגדרות ואז Test workflow.

| # | מה עושים | מה אמור לקרות |
|---|---|---|
| 1 | `STAGE_OVERRIDE=weigh`, `DRY_RUN=true` | דוח בלבד, כלום לא נשלח |
| 2 | `weigh`, `DRY_RUN=false` | רק אתה מקבל. נשלחו 1, השאר תחת מצב בדיקה |
| 3 | הרצה שנייה, אותו שלב | לא מקבל שוב, נחסמו ככפילות 1 |
| 4 | `STAGE_OVERRIDE=fu1` | מקבל. חסם נפרד לכל שלב |
| 5 | למלא טופס שקילה ואז fu1 שוב | יוצא מהרשימה, "כבר מילאו 1" |
| 6 | `STAGE_OVERRIDE=fu2` | מקבל את הודעת ראשון |
| 7 | override ריק, שעה אקראית | אין שלב פעיל |
| 8 | לשנות סטטוס ליווי | לא ברשימה |
| 9 | להוסיף טלפון שני ל-`TEST_PHONES` | שניכם מקבלים |

## WF-30 · סדרת הוובינר · `rLcLLXt9tuVt3Rx4`

**לפני:** להקים 4 פלואוז ולמלא מזהים. לוודא שלסער יש תהליך ליווי מקושר למחזור בוסט.
**איך:** `STAGE_OVERRIDE` + `DATE_OVERRIDE` ואז Test workflow.

| # | מה עושים | מה אמור לקרות |
|---|---|---|
| 1 | להריץ לפני מילוי המזהים | נחסם, "מזהה פלואו לא בפורמט תקין". **לבדוק את זה קודם** |
| 2 | `invite` + תאריך התחלה+2 | מקבל הזמנה |
| 3 | `morning` + התחלה+3 | מקבל תזכורת בוקר. **לוודא שכתוב 20:00 ולא ריק** |
| 4 | `hour` | מקבל שעה לפני |
| 5 | `live` | מקבל מתחילים עכשיו |
| 6 | `followup` + התחלה+4 | מקבל פולואפ עם הכפתור. גם בדיקת קצה-לקצה של WF-31 |
| 7 | הרצה שנייה של אותו שלב | נחסם ככפילות |
| 8 | תאריך בלי מחזור | "לא נמצא מחזור" |
| 9 | סטטוס מחזור = התבטל | מדולג |
| 10 | בלי override בשעה לא רלוונטית | אין שלב פעיל |

## WF-32 · סדרת הפגישה · `UVrEPMIM23dovCL1`

**לפני:** להקים 3 פלואוז ולמלא מזהים. לפתוח פגישה לסער.

| # | מה עושים | מה אמור לקרות |
|---|---|---|
| 1 | פגישה בעוד 3 ימים, שדות סימון ריקים | זימון נשלח, `pcfZoomMsgSent` מתמלא |
| 2 | להריץ שוב | לא נשלח שוב |
| 3 | פגישה למחר + `HOUR_DAY` = השעה הנוכחית | תזכורת יום לפני, `pcfZoomReminderDay` מתמלא |
| 4 | ללחוץ "אני אגיע" ואז שלב today | **לא נשלח. הבדיקה החשובה ביותר בכל הסדרה** |
| 5 | לנקות אישור הגעה, להריץ שוב | עכשיו נשלח |
| 6 | להריץ שוב מיד | נחסם ע"י `wf_meeting_today_last` |
| 7 | פגישה בעוד 30 דקות | תזכורת 30 דקות, `pcfZoomReminder30` מתמלא |
| 8 | פגישה בעוד 20 דקות בלי זימון | זימון מדולג, אין טעם |
| 9 | למחוק לקוחה מקושרת | "לפגישה אין לקוחה מקושרת" תחת תקלות נתונים |
| 10 | לרוקן תאריך ושעה | תקלת נתונים, לא מנסה |
| 11 | סטטוס פגישה = בוטלה | לא נסרקת |
| 12 | שתי פגישות באותו יום | כל אחת מטופלת בנפרד |

## כיבוי מצב הבדיקה

בסיום: `TEST_MODE = false` בשלושה מקומות — WF-29 (`⚙️ הכנה ואימות קלט`), WF-31 ו-WF-33 (נוד ההגדרות).
