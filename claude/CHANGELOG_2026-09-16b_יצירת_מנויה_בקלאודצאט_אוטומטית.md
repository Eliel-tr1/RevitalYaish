# יומן שינויים · 16/09/2026 (ערב) — יצירת מנויה בקלאודצ'אט אוטומטית + הוולקאם של יהודית

## הקשר
הנחיית אליאל, מילה במילה:
> "אם אין למשתמשת חשבון קלאודצ'אט (**בכל WF שהוא ששולח הודעה עם קלאודצ'אט!**) — המערכת צריכה ליצור אותה. זה לא יכול להסתכם בזה שאין. היא צריכה ליצור אותה + לעדכן את השדה של cloudchatid בתיק הלקוח בפיירברי."

---

## א. התיקון — 4 workflows, לא אחד

סרקתי את **כל** 200 ה-workflows בשרת וסיננתי את אלה ששולחים דרך קלאודצ'אט **בחשבון של רויטל**:

| Workflow | מצב קודם | מה נעשה |
|---|---|---|
| WF-23 · סדנת בוסט | דילג בשקט | ✅ יוצר + שומר |
| WF-24 · תוכנית ליווי | דילג בשקט | ✅ יוצר + שומר |
| WF-25 · תוכנית המשך | דילג בשקט | ✅ יוצר + שומר |
| WF-06B · שליחת תפריטים | דילג בשקט | ✅ יוצר + שומר |
| Notfication System - V2 Revital | כבר יוצר | ללא שינוי |
| תזכורת יום לפני התור | כבר יוצר | ללא שינוי |
| WF-29 · מנוע שליחת וואטסאפ | **לא מטפל** | **כבוי** — לא נגעתי |

### המנגנון (זהה בכל הארבעה)
```
איתור מנויה → אין user_ns? → subscriber/create-many → קליטת התוצאה
           → PUT /api/record/1/{accountid} { pcfCloudChatId }
           → כתיבת שדות → שליחת הפלואו
```
כשהמנויה קיימת — שני הצמתים החדשים הם **קריאות סרק**, אפס שינוי בהתנהגות.
כשהיצירה נכשלת — דילוג מסודר עם סיבה, **בלי** כתיבה לפיירברי.
כשאין טלפון תקין — לא מנסים ליצור.
ב-WF-06B: דילוג מסיבה אחרת (טלפון פסול) **לא** מפעיל יצירה — רק `stage` של "לא-קיימת".

### שתי תקלות שנתפסו תוך כדי, לפני שהגיעו ללקוחות
1. **`$json` נדרס בכל צומת HTTP.** אחרי שהוכנסו שני צמתי HTTP בין הקוד לכתיבת השדות, הצומת קרא את כתובת ה-API מתוך תשובת פיירברי — וההרצה נפלה. תוקן: הכתובת והגוף נקראים מהצומת **בשמו**. נתפס בהרצה חיה 258327 ותוקן לפני השליחה בפועל.
2. **דריפט בעותק המקומי.** לפני הפריסה השוויתי מול הגרסה החיה וגיליתי שתיקון 15/09 (`boost_cycle_start_date` — בלעדיו מטא דוחה את תבנית הבוסט ב-#131008) קיים בשרת אך **חסר אצלי**. פריסה עיוורת הייתה מוחקת אותו. שוחזר מהגרסה החיה והוחל על שלושת הענפים לפני הפריסה.

### בדיקות
**121 בדיקות עוברות** (היו 114, פחות בדיקה ישנה שתוקנה). נוספו 5 בדיקות ייעודיות: יצירה נבנית · המזהה נשמר בפיירברי וההודעה יוצאת · יצירה שנכשלה ⇒ דילוג בלי כתיבה · אין טלפון ⇒ לא מנסים · שלושת הצמתים קיימים ומחווטים בשלושת הענפים.
ל-WF-06B הורצה סימולציה על חמשת המסלולים (לא קיימת / יצירה הצליחה / יצירה נכשלה / קיימת / דילוג מסיבה אחרת) — כולם נכונים.

---

## ב. יהודית סואייה — הוולקאם נשלח

1. נוצרה לה מנויה בקלאודצ'אט: `f201689u949888873`.
2. נכתב `pcfCloudChatId` בתיק הלקוחה, אומת בקריאה חוזרת.
3. השרשרת נורתה שוב — `routed: true` · `success: true` · **`welcome: { sent: true }`** · אפס אזהרות.
4. **אומת מול קלאודצ'אט עצמו** (לא מסתמך על תשובת n8n): `last_message_at = 2026-09-16 11:55:10`. ההודעה יצאה בפועל.

הצירוף לקבוצת הוואטסאפ לא בוצע — **זה תקין, אליאל ביטל את זה זמנית.**

---

## ג. ממצא שנחשף בסריקה — 370 לקוחות בליווי פעיל בלי מזהה קלאודצ'אט

| | |
|---|---|
| רשומות ליווי **פעילות** | 457 |
| 🔴 מהן בלי `pcfCloudChatId` | **370** |
| לפי תוכנית | 166 פרימיום · 156 המשך · 48 בוסט |
| בלי טלפון בכלל | 1 |

**מדגם של 25 מול קלאודצ'אט עצמו:** 6 קיימות שם והשדה בפיירברי פשוט לא נכתב · **19 לא קיימות בכלל.**
בהיטל על 370: בערך **90 צריכות רק השלמת שדה**, ובערך **280 באמת אין להן מנוי** — כלומר **אינן מקבלות היום שום הודעה אוטומטית**.

מהיום זה לא יקרה שוב: כל שליחה עתידית תיצור את המנויה בעצמה. אבל ה-370 הקיימות **לא יתוקנו מעצמן** — הן יתוקנו רק ברגע שתישלח אליהן הודעה.

### ההצעה
מעבר רטרו על 370: לכל אחת — בדיקה בקלאודצ'אט, יצירה אם אין, וכתיבת השדה. **יצירה בלבד, אפס הודעות** — בדיוק כמו שנעשה ל-5 לקוחות ב-12/09.
**לא ביצעתי — דורש אישור.**

---

## פתוח
1. אישור על המעבר הרטרו על 370 הלקוחות.
2. WF-29 · מנוע שליחת וואטסאפ — כבוי, לא מטפל ביצירה. לתקן לפני שיודלק.
3. הקובץ לעודד (44 פערים) — ממתין למילוי.
4. 977 לידים כפולים + 1,227 לקוחות עם ליד ורכישה.
5. 25 רשומות תשלום בלי לקוחה מקושרת · 38 כפילויות ליווי.
