# WF-28 · איפוס לקוחת בדיקה

**מזהה n8n:** `oby2q9e48LLFUS4U` · **וובהוק:** `POST /webhook/reset-test-customer` · פעיל.

זה ה-workflow **היחיד במערכת שמוחק רשומות**. הוא נבנה כך שהוא לא יכול לגעת בלקוחה אמיתית.

## איך מפעילים

```json
{ "phone": "0533435240", "confirm": "RESET" }
```

* `dryRun: true` — מחזיר את הרשימה המלאה של מה שיימחק, בלי לגעת בכלום. **תמיד להריץ קודם.**
* אפשר גם `accountName` במקום `phone`.

## ארבע שכבות הגנה

1. **מילת אישור** — בלי `confirm: "RESET"` הריצה נעצרת מיד.
2. **רשימת היתר קשיחה בקוד** — כרגע רק `סער וינברג / 0533435240`.
   הרשימה מוטמעת בקוד הפרוס ואי אפשר להרחיב אותה מגוף הבקשה. להוספת לקוחת בדיקה — משנים את `CFG.ALLOWED` ופורסים מחדש.
3. **אימות הלקוחה שנמצאה בפועל** — אחרי החיפוש בפיירברי, אם החיפוש החזיר ולו רשומה אחת שאינה ברשימת ההיתר, הריצה נעצרת **בלי למחוק כלום**. זו ההגנה מפני טלפון שמצביע בטעות על לקוחה אחרת.
4. **תקרת רשומות** — יותר מ-400 רשומות ⇒ עצירה. זה כבר לא לקוחת בדיקה.

## מה הוא סורק

28 אובייקטים, כל אחד לפי כל שדה שמקשר אותו ללקוחה. המפה נסרקה מול המטא-דאטה של פיירברי ואומתה מול ה-API — כולל תשלום, תיעוד תשלום, מסמך, הקצאת תפריט, טופס שקילה, תהליך ליווי, רישום שיווקי, משימה, פגישה, תהליך מכירה, איש קשר, הזמנה, חשבוניות, קריאת שירות ועוד.

## סדר המחיקה

מהבן לאב, סדרתי, רשומה־רשומה (`batchSize 1`):

```
מסמך → תיעוד תשלום → תשלום → הקצאת תפריט → טופס שקילה → תהליך ליווי →
הרשמות → משימה → פגישה → מסמכים חשבונאיים → תהליך מכירה → איש קשר → לקוח
```

אובייקט שנמצא אך אין לו סדר מוגדר נמחק **לפני** הלקוחה, ומדווח באזהרה — כדי שלא יישאר מאחור בשקט.

## אימות סופי

אחרי המחיקה ה-workflow **חוזר ושואל את פיירברי** אם הלקוחה עדיין קיימת.
`verified: true` ⇒ לא נותרה רשומה. אחרת `success` יורד ל-`false` ומתבקשת ריצה נוספת.
לא מדווחים "בוצע" על סמך תשובת ה-API בלבד.

## מגבלה ידועה

**הערות (אובייקט 7) לא נמחקות.** פיירברי מחזירה `500` על כל ניסיון לסנן את אובייקט ההערות לפי `objectid`/`recordid` — אין דרך למצוא את ההערות של רשומה דרך ה-API. הן נשארות יתומות ואינן מופיעות בשום מסך.

## בדיקות

36 בדיקות — עוברות. כולל: חסימת לקוחה אמיתית, חסימה בלי מילת אישור, עצירה על תוצאת חיפוש מעורבת, סדר המחיקה, תקרת הבטיחות, ו-`dryRun` שלא מייצר אף פעולת מחיקה.

## ריצה ראשונה — 20/08/2026

אופס סער וינברג: **9 רשומות נמחקו, 0 כשלים, אומת נקי.**
מסמך · תיעוד תשלום · תשלום · הקצאת תפריט · תהליך ליווי · רישום שיווקי · משימה · תהליך מכירה · לקוח.

---

## נספח · למה לא הגיע התפריט (ריצה 189609)

WF-06B רץ ב-16:30, מצא את ההקצאה של סער (`תפריט בוסט - סער וינברג 📃`), והחזיר:

```
דילוג · 7א-מחוץ-לחלון · allow_send_message=false — מחוץ לחלון 24 השעות.
לא נשלח ולא נחתם.
```

**זו התנהגות נכונה.** אותה סיבה בדיוק שבגללה הודעת הוולקאם לא נשלחה: הלקוחה לא כתבה לבוט ב-24 השעות שקדמו, ולכן וואטסאפ לא מרשה לשלוח לה הודעה יזומה. ה-workflow **לא סימן את ההקצאה כנשלחה**, ולכן היה מנסה שוב כל 30 דקות ברגע שהחלון נפתח.

**לטסט הבא:** שלח הודעה כלשהי לבוט בוואטסאפ לפני הרכישה. זה פותח את החלון, ואז גם הוולקאם וגם התפריט ייצאו.
