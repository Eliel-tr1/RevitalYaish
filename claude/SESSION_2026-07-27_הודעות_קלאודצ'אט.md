# סיכום סשן 27/07/2026 — מערך הודעות WhatsApp: פיירברי → Make → קלאודצ'אט

> **עודכן 03/08/2026 — סקירת 26 שיחות הפרויקט.**

| # | מה שונה | מקור |
|---|---|---|
| 1 | **המסמך נשמר כרישום מתוארך של 27/07 — ההיסטוריה לא נכתבה מחדש.** כל העדכונים רוכזו בפרק חדש בסוף: "מה השתנה מאז — נכון ל-03/08/2026" | הנחיית עריכה |
| 2 | נוספה אזהרה בראש: **כל `sub_flow_ns` במסמך זה טעון אימות מחדש מול טל** | chat_03 |
| 3 | נוספה הפניה צולבת: עבודת 27/07 מפוצלת בין 4 שיחות שונות | chat_12 |
| 4 | אושש: לכל 30 קבוצות ה-WhatsApp יש `pcfGroupLink` — ה"דאטה ריקה" הופרכה סופית | chat_19 |
| 5 | אושש: `1007.pcfwhatspgrouprelated` קיים ו**היה חסר בתיעוד** | chat_19 |
| 6 | אושש: באג הסטטוסים ההפוכים במחזורי בוסט — **נראה שטופל** | chat_19 |
| 7 | עודכן: 3 החוסמים שאצל טל (ns שגוי · `data_params` לא מחוברים · Delivery failed) — **עדיין פתוחים ב-03/08** | סקירה |
| 8 | נוסף: `flow_ns` של **תזכורת הפגישה** חסר גם הוא (`REPLACE_WITH_FLOW_NS_FROM_TAL` במודול 26) | chat_10 |
| 9 | נוסף אירוע אבטחה: **מפתח CloudChat כטקסט גלוי במודול 93** | chat_22 |
| 10 | נוספה מפת תרחישי CloudChat בחשבון הישן (5 באשכול + פעילים/כבויים) | chat_22 |

> ⚠️ **אזהרה מחייבת:** **כל ערך `sub_flow_ns` המופיע במסמך זה טעון אימות מחדש מול טל לפני שימוש.** הוכח ב-27/07 שה-ns של הבוסט ושל הליווי שגויים. אין לסמוך על אף `ns` ישן — לא כאן, לא בבלו-פרינט, לא ב-workflow.

> 🔗 **עבודת 27/07 מפוצלת בין ארבע שיחות:** שיחת ההודעות (**המסמך הזה**) · שיחת **מסך הסליקה** (`claude/WIDGET_SLIKA_GROW.md`) · שיחת **התפריטים** · שיחת **כפתור התשלום הידני** (`claude/BUTTON_MANUAL_PAYMENT.md`). קיים חשש לכיסוי חופף/חסר בין הארבעה — בבדיקת עקביות יש לעבור על כולם.

---

> **סטטוס בסוף הסשן (27/07/2026):** שלושה צינורות הודעות בנויים ועובדים מקצה לקצה ברמת הדאטה. חסימות שנותרו — רובן אצל טל (קלאודצ'אט).
> משלים את: `claude/AUTOMATION_1051_WEBHOOK_PAYLOAD.md` (הפיילוד המלא + מגבלות מנוע התבניות) · `claude/HODAOT_WHATSAPP_NOSACHIM.md` (נוסחים).

---

## 1. הארכיטקטורה שנקבעה

**עיקרון:** פיירברי שולח דאטה, Make מנתב, קלאודצ'אט שולח. תוכן סטטי (לינקי "כל מה שחשוב לדעת") יושב **בתוך התבניות של טל** — לא בפיילודים.

### צינור A — Welcome (ליווי + בוסט) · תרחיש "Welcome - General Message - CloudChat"
- **אוטומציה פנימית אחת** על תהליך ליווי (1051), טריגר "נוצרה", בלי סינון.
- פיילוד 96 שדות (ראה doc הפיילוד) + מעטפה: `message_type:"welcome"`, `phone`, `fullname`, `email`.
- **Switch במודול 24** (util:Switcher): Input = `{{if(1.message_type = "welcome"; 1.product_name; 1.message_type)}}`, **regex=Yes**:
  - `^תוכנית ליווי` → flow ליווי (תופס את כל 7 התוכניות + עתידיות)
  - `^סדנה בוסט$` → flow בוסט
  - Else ריק → תוכניות המשך והרצאת אורח לא מקבלות כלום (בכוונה)
- מודול 2 (SetVariables): `flow_ns = {{ifempty(24.output; 1.flow_ns)}}` (תאימות לאחור) · `param_1={{1.mentor_name}}` · `param_2/3/4` עם `if` על `product_name="סדנה בוסט"` (מחזור/קבוצה, לינק, תאריך התחלה) · param_5-10 ריקים.
- פילטר על מודול 22 (send-sub-flow): `flow_ns` Exists.
- **החלטות בדרך:** נדחתה הצעה לאוטומציות נפרדות פר-מוצר; נדחה סוויץ' על picklist קטגוריה (הרצאת אורח גם "סדנה"); נבחר regex על שם מוצר לבקשת אליאל.

### צינור B — אורח שבועי · תרחיש חדש "Weekly Guest Broadcast"
- **אין אוטומציה בפיירברי בכלל** (טריגר "בזמן יחסי" חסום ברישיון!) — Make מתוזמן יומי (18:00).
- זרימה: query מפגשים (1041) של **מחר** → query ליוויים פעילים (`pcfStatus=2`) עם `pcfaccountid_accountname` + `pcfaccountid_telephone1` (שליפה מקושרת ב-v3 — עובדת) → לולאה: נרמול טלפון (onerror Ignore) → create subscriber → set data_param1-4 (מרצה, נושא, מועד, זום) → send flow → Sleep 2s.
- נבנה כ-blueprint ויובא בהצלחה (מודול Sleep נוסף ידנית — שם המודול בקובץ היה שגוי).
- **באג שתוקן:** נתיבי ה-Iterators הם `{{2.data.data}}` / `{{4.data.data}}` (עטיפה כפולה בתשובת v3).
- **אומת בפועל:** טל קיבל 4 הודעות (4 ליוויים פעילים) ומיכאל 1.
- נוסח ההודעה: `claude/HODAOT_WHATSAPP_NOSACHIM.md` + תבנית `weeklyevent_template` אצל טל.

### חוזה ה-data_params בקלאודצ'אט
| param | Welcome ליווי | Welcome בוסט | אורח שבועי |
|---|---|---|---|
| data_param1 | שם מנטורית | שם מנטורית | שם מרצה |
| data_param2 | שם קבוצה | שם מחזור | נושא הרצאה |
| data_param3 | לינק קבוצה | לינק קבוצת מחזור | מועד (DD/MM/YYYY HH:mm) |
| data_param4 | — | תאריך תחילת מחזור | לינק זום |

## 2. ממצאים טכניים חדשים (מעבר לתועד ב-AUTOMATION_1051 doc)
1. **טריגר "בזמן יחסי" באוטומציות פנימיות חסום ברישיון הנוכחי** של חשבון רויטל ("שדרג עכשיו"). תזמונים → Make.
2. **שדה חדש על מחזור בוסט (1007):** `pcfwhatspgrouprelated` — lookup לקבוצת ה-WhatsApp של המחזור (לא בתיעוד 03_LIVE).
3. **לכל 30 רשומות הקבוצות (1068) יש `pcfGroupLink` מלא** — כולל 8 קבוצות הליווי ו-22 קבוצות מחזורי בוסט. התיעוד על "דאטה ריקה" נכון רק לקיבולת/מונים.
4. **`pcfEventLink` על המחזורים עדיין ריק ברובם** (רק אוגוסט 09 מלא) — אבל הלינקים קיימים בקבוצות המקושרות. הוכן סקריפט סנכרון (ראה §4).
5. ה-Switch של Make ב-No-regex הוא **Equal מדויק**, לא Contains. עברית ב-Pattern תקינה; ב-regex חובה להקליד `^` ראשון (תצוגת RTL מטעה).
6. באג הסטטוסים ההפוכים במחזורי בוסט (05_GAP) — נראה שטופל: n8n עדכן "ממתין לפתיחה ← נפתח להרשמה" ב-26/07.
7. הטוקן של פיירברי (חשבון רויטל) עובד מסביבת Claude — קריאות אומתו; **כתיבות המוניות דורשות אישור משתמש** (חסם הרשאות). כתיבות בודדות (יצירת מפגש/איש קשר, שיוך מרצה) בוצעו בהצלחה.

## 3. רשומות בדיקה שנוצרו בחשבון החי (לניקוי בסוף הפרויקט)
- תהליכי ליווי: "בדיקה של הבדיקות", "בדיקות", "בדיקותייישן", "בדיקת ליווי 2", בדיקת בוסט (סה"כ 5 פעילים — טל ×4, מיכאל ×1)
- מפגש: "הרצאת בדיקה - תזונה רגשית" (28/07 20:00, מרצה: מיכאל אוקס)
- איש קשר: "מיכאל אוקס" (`8f844ce2-af62-40ce-9f4d-9025391554d1`)
- מנוי בקלאודצ'אט: טל ויטרו עם data_params של בדיקות

## 4. פתוח — מי חייב מה
| # | פריט | אצל מי | חוסם |
|---|---|---|---|
| 1 | **ns מדויקים ל-3 התבניות** — `f201689s4391239` הוא בפועל תבנית הבוסט (שיבוש); ns הליווי והבוסט האמיתיים חסרים | טל | 🔴 ליוויות יקבלו הודעת בוסט |
| 2 | חיבור data_params בתוך התבניות (ליווי: 1/2/3 · בוסט: BODY2=param4, BODY3=param3 + מחיקת "S" תועה · אורח: 2=param1, 3=param2, 4=param4, מומלץ {{5}}=param3 לשעה) | טל | 🔴 הודעות עם "LINK" מילולי |
| 3 | Delivery failed — ערוץ הוואטסאפ בקלאודצ'אט לא מוסר הודעות | טל | 🔴 |
| 4 | **סנכרון לינקים למחזורים** — סקריפט מוכן (קבוצה→`pcfEventLink`), ממתין לאישור הרצה של אליאל; בהמשך פתרון קבוע (אוטומציה פנימית על 1007 או n8n) | אליאל/Claude | 🟡 בוסט בלי לינק |
| 5 | ניקוי `param_5` במודול 2 של תרחיש ה-Welcome (נשאר בו תאריך) | אליאל | 🟡 |
| 6 | דה-דופ לפי טלפון בברודקאסט (לקוחה עם כמה ליוויים = כמה הודעות) | Make, לפני פרודקשן | 🟡 |
| 7 | תרחיש התשלום חייב למלא ביצירת ליווי: מוצר, מחזור בוסט (לבוסט), קבוצה (לליווי), תוכנית נוכחית | שלב האוטומציות הבא | 🟡 |
| 8 | רוטציית טוקנים (פיירברי + קלאודצ'אט) — שניהם חשופים בקוד/בלופרינטים/שיחות | אחרי סיום הטסטים | 🟠 |
| 9 | טסט קצה-לקצה סופי ל-3 המסלולים אחרי סגירת 1-3 | כולם | — |

## 5. בונוסים מהסשן (מחוץ לציר המרכזי)
- **טופס ליד** עודכן: אימייל לא חובה (נחסם רק אם מולא שגוי), מוצר לא חובה (ריק → נשלח "בחר מוצר"), מקור הגעה הפך לדרופדאון דינמי מאובייקט 1043 + "אחר" (נבדק בסימולציית דפדפן). ⚠️ צד Make: לוודא שהתרחיש המרכזי מטפל ב-`utm_product="בחר מוצר"` שלא יימצא בחיפוש מוצר.
- נוסחי הודעות מלאים: `claude/HODAOT_WHATSAPP_NOSACHIM.md`.

---
---

# מה השתנה מאז — נכון ל-03/08/2026

> **הפרק הזה נוסף ב-03/08/2026. כל מה שמעליו הוא רישום מתוארך של 27/07 ונשאר כלשונו.** אין לתקן את גוף הסיכום — מה שהתברר כשגוי מתועד כאן.

## א. מה הופרך או הוחלף

| # | מה נכתב ב-27/07 (או בתיעוד שקדם לו) | מה נכון היום | מקור |
|---|---|---|---|
| 1 | ~~"דאטה ריקה" בקבוצות ה-WhatsApp — אין לינקים~~ ⛔ **מיושן (הוחלף 03/08/2026)** | **לכל 30 רשומות 1068 יש `pcfGroupLink` מלא** — 8 קבוצות ליווי + 22 קבוצות מחזורי בוסט. אושר בבדיקה מול החשבון החי. "הדאטה הריקה" התייחסה ל**קיבולת ולמונים בלבד**, לא ללינקים. **החוסם "אין לינקי קבוצות" בטל.** | chat_19 |
| 2 | ~~שדות מחזור הבוסט (1007) המתועדים = `pcfStartDate`, `pcfEventLink` בלבד~~ ⛔ **מיושן (הוחלף 03/08/2026)** | קיים גם **`pcfwhatspgrouprelated`** — lookup "קבוצת בוסט WhatsApp - מקושר". **השדה לא היה בתיעוד `03_LIVE_FIREBERRY.md` כלל.** זהו המפתח לאוטומציית המילוי האוטומטי של לינק הקבוצה למחזור. | chat_19 |
| 3 | ~~באג הסטטוסים ההפוכים במחזורי הבוסט — פתוח (05_GAP)~~ ⛔ **כנראה טופל** | תצפית בפיד של מחזור בוסט: **אוטומציית n8n עדכנה בבוקר 27/07 "ממתין לפתיחה ← נפתח להרשמה"** — כלומר הכיוון תקין. **טרם אושר פורמלית כסגור.** ✅ בכפוף לאימות. | chat_19 |
| 4 | ~~מקור לינק הקבוצה להודעת ה-Welcome של הבוסט = `1007.pcfEventLink`~~ ⛔ **מיושן** | מקור האמת עבר ל-**`1068.pcfGroupLink`**. `pcfEventLink` עדיין ריק ברוב המחזורים ואינו מקור אמין. ⚠️ **סתירה פתוחה** על גורלו — ראו §ג. | chat_20, chat_22 |
| 5 | ~~"אישור נוסחי ההודעות מרויטל" כתלות~~ ⛔ **מיושן** | **אישור נוסחים אינו נדרש** — הכרעת מייק, 28/07/2026. הפריט יורד מרשימת התלויות. | chat_08 |

## ב. מה עדיין פתוח — החוסמים שאצל טל לא נסגרו

**נכון ל-03/08/2026, שלושת החוסמים 🔴 שנרשמו בטבלה §4 כ"אצל טל" — עדיין פתוחים.** אין אישור סגירה לאף אחד מהם.

| # | חוסם | סטטוס 03/08 | בעלים |
|---|---|---|---|
| 1 | **`sub_flow_ns` שגויים** — `f201689s4391239` הוגדר כ-Case "ליווי" אך **בפועל זה ה-ns של תבנית הבוסט** (הוכח בטסט: ליוויות מקבלות הודעת בוסט). `f201689s4391241` הוגדר כ-Case "בוסט", **לא מאומת מהקונסול**, גורם ל-**400** מ-`send-sub-flow`. | 🔴 **פתוח** | **טל** |
| 2 | **`data_params` לא מחוברים בתוך התבניות** — הודעות יוצאות עם המילה `LINK` מילולית במקום הערך. כולל ה-**S התועה** אחרי `data_param3` בתבנית הבוסט. | 🔴 **פתוח** | **טל** |
| 3 | **Delivery failed** — ערוץ הוואטסאפ בקלאודצ'אט אינו מוסר הודעות. | 🔴 **פתוח** | **טל** |
| 4 | **`flow_ns` של תזכורת הפגישה חסר** — במודול **26** של תרחיש "Meetings Reminders" יושב `REPLACE_WITH_FLOW_NS_FROM_TAL`. התרחיש בנוי, **לא מתוזמן**, במצב Run once ידני. **פריט זה לא הופיע בטבלת §4 המקורית.** | 🔴 **פתוח** | **טל** |

> **מסקנה מבצעית:** ארבעת הפריטים יושבים על אותו בעל-משימה. **כל צינור ההודעות חסום על טל.** אין טעם להשקיע בהמשך בנייה בצד Make/n8n לפני שהארבעה נסגרים.

### רשימת `flow_ns` / `sub_flow_ns` חסרים או שגויים — מאוחדת לכל הפרויקט

| תבנית / פלואו | ns | סטטוס |
|---|---|---|
| Welcome ליווי | `f201689s4391239` | 🔴 **שגוי** — בפועל זה ns של הבוסט |
| Welcome בוסט | `f201689s4391241` | 🔴 **לא מאומת** — מחזיר 400 |
| אורח שבועי (`weeklyevent_template`) | — | 🟡 עובד בטסט; לאמת |
| תזכורת פגישה | `REPLACE_WITH_FLOW_NS_FROM_TAL` | 🔴 **חסר** |
| `menu_sending` (שליחת תפריט) | `f201689s4391449` | ✅ ידוע |
| WF-17 (מכירת ליווי אחרי בוסט) | — | 🔴 **חסר** |

## ג. סתירות שנפתחו מאז — פתוחות

- ⚠️ **סתירה פתוחה — גורל `1007.pcfEventLink`:** chat_20 (26/07) סוגר אותו כ"לא רלוונטי" אחרי המעבר ל-1068 · chat_21 מותיר אותו פתוח כבאג 2 ומוציא מסקופ · chat_22 מונה אותו כחוסם פעיל בבעלות **עודד** ("לינקי קבוצות WhatsApp"). **המלצה: לסגור** — מקור האמת עבר ל-`1068.pcfGroupLink`. **הכרעה: מייק.**
- ⚠️ **סתירה פתוחה — `pcfGroupid` מול `pcfGreenApiId` ב-1068:** chat_20 הכריע ש-`pcfGreenApiId` הוא הקנוני (אישור מייק: "הראשון"); chat_22 מתעד שמייק אמר "לא יודע — צריך לבדוק", ומפרט WF-03 מפנה ל-`pcfGroupid`. **בדיקת אימות בת 5 דקות:** לפתוח 2–3 מרשומות 1068 ולראות איפה יושב הפורמט `1203630xxxxx@g.us`. **הכרעה: מייק.**
- ⚠️ **סתירה פתוחה — endpoint של פיירברי:** `POST /api/v3/query` (chat_20) מול `POST /api/query` (chat_21). התרחישים שנבנו בסשן זה משתמשים ב-**v3**. **הכרעה + תיעוד ב-`FIREBERRY_REST_PLAYBOOK.md`: מייק.**

## ד. אירוע אבטחה שהתגלה אחרי הסשן

🔴 **מפתח ה-API של CloudChat יושב כטקסט גלוי בבלו-פרינט של מודול `93`** בתרחיש `New payment - CloudChat`.
⚠️ **נחשף בצ'אט — לרוטציה.** אין לרשום את ערכו בשום מסמך.

**עדכון לפריט 8 בטבלת §4:** ~~"רוטציית טוקנים (פיירברי + קלאודצ'אט)"~~ ⛔ מיושן (הוחלף 03/08/2026) — **מדובר בחמישה מפתחות, לא שניים:** Fireberry · Make API · Tofsy · GreenAPI (`apiTokenInstance`) · CloudChat. **אף רוטציה לא אומתה כבוצעה.**
**סייג תזמון:** רוטציה שוברת את כל חיבורי Make עד שמעדכנים אותם; אם טל בונה במקביל — דברים ייפלו לו בלי שיבין למה. **דורש חלון מתואם.** אחרי הרוטציה חובה לעדכן שלושה מקומות: החיבורים ב-Make ("Revital - New") · ה-credential ב-n8n · השדה `pcfPowerlinkToken` ב-1036.

## ה. מפת תרחישי CloudChat — להעברה מהחשבון הישן

**אשכול CloudChat בחשבון הישן = 5 תרחישים** (ClickUp `86cawzyn2`).

| תרחיש | מצב |
|---|---|
| `Main Chatbot Scenario - CloudChat` | פעיל |
| `Main Leads CloudChat` | פעיל |
| `Create contact - Cloudchat` | פעיל |
| `Facebook Leads - Cloudchat` | פעיל |
| `Notifications System - CloudChat` | כבוי |
| `Testing Scenario - CloudChat` | כבוי |
| `Create CloudChat Message Template For Botcast` | כבוי |

**`New payment - CloudChat (1.01)`** — וובהוק **`3167900`** בשם `Cardcom - new payment - Cloudchat`. שלושה ענפים: (1) יצירה/עדכון מנוי בבוט · (2) עדכון שדות מותאמים ושליחה ל-flow · (3) שליחת HTTP ל-`Finance 3`.
📌 **התרחיש הזה לא כותב לפיירברי כלל** — ההודעות ללקוחה נשלחות דרך flow בבוט, לא מה-CRM. זה משנה את הנחת העבודה של כל מי שמחפש את מקור ההודעה בצד ה-CRM.

**בעלות:** נוסחי ההודעות **וגם** החיבור הדו-כיווני לבוט = **טל**. משימת החיבור הדו-כיווני יושבת במסלול א' (Make וחיבורים) ברשימת ~40 המשימות המקבילות.
`[מקור: chat_22]`

## ו. הקשר רחב — מצב החשבון החי

**נכון ל-26/07/2026 מצב החשבון החי הוא clean state תפעולי**, מלבד שני שינויים מ-22-23/07: עדכון `pcfPowerlinkToken` ב-1036, ותיקון קריטריון חיפוש ב-Make. בשש מתוך שמונה שיחות מאוחרות **לא בוצע שום שינוי בחשבונות החיים**. כל מי שממשיך מתחיל משם.

🔴 **חסימה מקדימה לכל הדלקת תרחיש: 525 אירועים תקועים בתורים ב-Make.** פילוח: `My Zoom Webinars` 292 · `Revital 6.7.2026` 179 · `Create Payments Records` 42 · `Welcome To Premium` 6 · `Notifications System` 5 — **471 מתוכם שייכים לעולם הישן.** הדלקה עיוורת תשפוך מאות רשומות למערכת. **אסור להדליק תרחיש כלשהו לפני בדיקת תוכן התור שלו.**
