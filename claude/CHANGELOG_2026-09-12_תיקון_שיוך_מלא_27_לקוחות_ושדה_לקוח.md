# יומן שינויים · 12/09/2026 — תיקון: שיוך מלא (כולל רמת לקוח) + כיסוי עסקאות כפולות

## הקשר

המשך ל-`CHANGELOG_2026-09-12_בקאפיל_שיוך_מחזור_וקבוצה_ל27_לקוחות_תקועות.md`. מייק בדק ידנית את רעות דמארי ברשומה שהוא שלח מלכתחילה (`f7d51f87-ab9d-447e-a9f3-a2f0a63baaaa`) ומצא שהשדות עדיין ריקים — גם בתהליך המכירה, גם בליווי, וגם בלקוח עצמו. בדיקה הראתה שני פערים אמיתיים:

1. **הרשומה שמייק שלח לא הייתה הרשומה שטופלה ב-backfill.** ל-3 מתוך ה-27 לקוחות (רעות דמארי, תהאני מוצלח, נורית כהן) יש **שתי רשומות תהליך-מכירה נפרדות** לאותה רכישת בוסט — אחת שה-backfill טיפל בה (לפי ה-`saleid` מ-n8n), ואחת נוספת (`statuscode=11` אבל בלי שיוך מחזור/קבוצה) שנוצרה ע"י תהליך אחר. רעות מפנה לרשומה השנייה — זו שהייתה ריקה.
2. **שדה הלקוח עצמו (`pcfWhatsAppReferans` — "קבוצת WhatsApp נוכחית - שיוך") מעולם לא נכתב** — לא ע"י WF-23 עצמו (בדקתי בקוד המנוע) ולא ב-backfill המקורי. זה פער אמיתי בין מה ש-WF-23 עושה בפועל לבין מה שמייק בודק ברשומת הלקוח.

## מה תוקן עכשיו

### א. 3 זוגות רשומות כפולות (עסקה + ליווי) — הושלם השיוך

| לקוחה | עסקה כפולה (4) | ליווי כפול (1051) |
|---|---|---|
| רעות דמארי | `f7d51f87-ab9d-447e-a9f3-a2f0a63baaaa` | `79697d59-5dd0-4963-9b03-92717d42d729` |
| תהאני מוצלח | `5428c2fd-86b0-45fa-b185-dd0a9a6b8827` | `a8f6be72-8a7c-4556-84ce-0a4dfff87a8c` |
| נורית כהן | `7c07579a-273f-4054-af96-1172b1804911` | `73be4b2e-4baa-4f5e-9474-65947bdff4e7` |

בכל אחת מה-6: נכתב `pcfCourse`/`pcfBoostLinked` = מחזור **בוסט 13-20.9.26** (`ffb97e47-75bc-4967-9a3d-72d86e8836a9`), `pcfWhatsAppToRef`/`pcfActualGroup` = קבוצת **סדנת בוסט לירידה במשקל מחזור 13.09.2026** (`cad7798b-cc3b-42bf-93ff-4d5a3632c66e`). **אומת בקריאה חוזרת ישירה (GET) לכל 6 הרשומות** — לא רק success:true.

⚠️ **חשוב להבין**: זה לא מחק/מיזג את הרשומה הכפולה — לרעות, לתהאני ולנורית יש עכשיו **שתי** רשומות תהליך-מכירה סגורות ושתי רשומות ליווי, שתיהן משויכות נכון. זה פותר את מה שמייק ראה וביקש, אבל לא פותר את בעיית הכפילות עצמה (ראו סעיף "ממצא פתוח" למטה).

### ב. שדה לקוח (`pcfWhatsAppReferans`) — נכתב לכל 27 הלקוחות

הרצתי כתיבה + אימות בקריאה ישירה (GET) על רשומת הלקוח (object 1) של **כל 27** הלקוחות מה-backfill המקורי (לא רק רעות) — כי אם WF-23 לא כותב את השדה הזה לאף אחד, אז כולם היו חסרים אותו. **27/27 אומתו בהצלחה**, כולל רעות דמארי (`62cd2399-3642-4e49-833d-29532670f2c5`).

**המלצה קדימה**: כדאי להוסיף כתיבת `pcfWhatsAppReferans` גם ל-WF-23 עצמו (לא רק ל-backfill), אחרת כל לקוחה עתידית תמשיך לא לקבל את השדה הזה ברמת הלקוח — רק בתהליך המכירה/ליווי. זו פעולה על ה-workflow עצמו ב-n8n, לא נגיעה ב-Make — צריך אישור אם לבצע.

## ✅ רעות דמארי — מצב סופי, ברשומה המדויקת שמייק שלח

- תהליך מכירה `f7d51f87...`: `pcfCourse` = בוסט 13-20.9.26 ✓, `pcfWhatsAppToRef` = הקבוצה ✓, `statuscode=11` ✓
- ליווי `79697d59...` (מקושר ל-sale הזה בדיוק, `pcfSale=f7d51f87...`): `pcfBoostLinked` ✓, `pcfActualGroup` ✓
- לקוח `62cd2399...`: `pcfWhatsAppReferans` = הקבוצה ✓
- **לא** צורפה לקבוצת WhatsApp פיזית (GreenAPI), **לא** נשלחה הודעה — כנדרש.

## 🔴 ממצא פתוח שדורש החלטה שלך — לא טופל, לא נגעתי

תוך כדי הבדיקה גיליתי שקיים דפוס שיטתי הרבה יותר גדול: **כ-161 זוגות עסקה+ליווי בפורמט שם "עסקה - {שם} | סדנה בוסט"** (שונה גם מהפורמט של WF-23 וגם מהפורמט שה-backfill יצר), כולם `statuscode=11`, כולם בלי מחזור/קבוצה משויכים, שנוצרו בין 10/09 22:38 ל-11/09 14:58. סבירות גבוהה שזה תוצר של מנוע ההגירה (שעיבד היסטוריית רכישות בוסט ישנה) או אוטומציה נפרדת שלא זיהיתי — **המקור לא אושר**. 3 מתוכם חפפו ל-27 הלקוחות הנוכחיים ותוקנו כאן; **השאר (כ-158) לא נגעתי בהם** — זה נראה כמו נושא נפרד שדורש בדיקה משלו (מקור, האם צריך שיוך דומה, האם יש כפל שדורש ניקוי) לפני שנוגעים בו.

## מצב פתוח נוסף (מהדוח הקודם, עדיין לא טופל)

לילך ימיני (0507977127) — הרכישה שלה מעולם לא הפעילה את WF-23 בכלל (לא DRY_RUN, לא נורה). דורש בדיקת WF-22/חיבור WF-20→22 ספציפית לעסקה שלה.
