# אבחון · לקוחות שנוצרים בלי שם + רישום שיווקי כפול
**10/09/2026 · תשובה לשאלת אליאל · קריאה בלבד מול ה-API החי, לא נגעתי בפיירברי או ב-Make**

---

## 1. שתי התופעות — אותו מקור אחד

בדקתי את שתי הלקוחות:

| | `25a54c0e…` (נירית) | `94de38d8…` (רונית) |
|---|---|---|
| שם חשבון | `null` | `רונית מלול מרדכייב` (עודכן **74 שניות** אחרי היצירה) |
| טלפון | 0509028759 | 0524898677 |
| מקור | דף נחיתה - מוצר בוסט | דף נחיתה - מוצר בוסט |
| `pcfCreatedByPersonOrSystem` | 2 = מערכת | 2 = מערכת |
| `createdby` | רויטל יעיש (= זהות ה-API של האוטומציה) | רויטל יעיש (= זהות ה-API של האוטומציה) |

לשתיהן **שני רישומים שיווקיים**, לא אחד:

**נירית:**
| נוצר | UTM Source | שם ברישום |
|---|---|---|
| 19:54:34 | דף נחיתה - מוצר בוסט | "הרישום של" (ריק) |
| 19:54:35 (+1 שנ') | **ריק** (`null`) | "הרישום של **נירית**" |

**רונית:**
| נוצר | UTM Source | שם ברישום |
|---|---|---|
| 20:18:47 | דף נחיתה - מוצר בוסט | "הרישום של" (ריק) |
| 20:18:50 (+3 שנ') | **Facebook** | "הרישום של **רונית**" |

---

## 2. האבחנה

**זה לא שני באגים — זו תופעה אחת: הרצה כפולה של שרשרת קליטת הליד.**

זה בדיוק המנגנון שתועד ב-30/08 (`AVCHUN_2026-08-30_מקור_הלופ_ב-Make.md`): **כל ליד מדף נחיתה מריץ פעמיים את שרשרת שלושת התרחישים** (Main Leads CloudChat → Main Chatbot Scenario → Main Leads CRM). אז זה תועד כ"הודעות כפולות" ו"~70 אופרציות מיותרות לליד". **היום רואים תוצאה נוספת של אותה הרצה כפולה: פגיעה בנתונים, לא רק בזבוז.**

השרשרת רצה פעמיים על אותו ליד, בהפרש של 1–3 שניות, אבל **לא עם אותו payload**:

- **ריצה #1** נכנסת עם UTM `דף נחיתה - מוצר בוסט` אבל **בלי שם**.
- **ריצה #2** נכנסת עם שם (חלקי — רק פרטי, לא מלא) ו-UTM שונה או ריק — פעם `Facebook`, פעם `null`.

**מודול "יוצר לקוח" (25) עושה dedup לפי טלפון — נכון.** הריצה שמגיעה ראשונה יוצרת את הלקוחה; הריצה השנייה מוצאת שהיא כבר קיימת ולא יוצרת כפילות בלקוח — **אבל גם לא משלימה את השם החסר על הרשומה הקיימת.** מודול "רישום שיווקי" (41), לעומת זאת, **לא עושה dedup** — כל ריצה יוצרת רשומה חדשה. זו הסיבה שהלקוח אחד, אבל שני רישומים.

**מסקנה: השם לא "אבד" — הוא נמצא ברישום השיווקי השני, ופשוט לא זרם חזרה לרשומת הלקוח**, כי הריצה שיוצרת את הלקוח (הראשונה, בלי שם) מנצחת בתחרות, וההרצה השנייה (עם השם) לא כוללת עדכון-לאחור של שדה השם בלקוח קיים.

**התיקון החד-פעמי ב-`רונית` (74 שניות אחרי) לא הגיע מהאוטומציה** — אין מנגנון שמעדכן שם בדיעבד בשרשרת הזו. סביר שזו הייתה עריכה ידנית שלך/של רויטל בכרטיס. `נירית` לא תוקנה — ולכן נשארה בלי שם.

---

## 3. למה זה קריטי היום ולא רק "עוד באג ישן"

1. **זה קורה ממש עכשיו, בלב חלון ההגירה.** רישום שיווקי (1002) הוא אחד מ-10 האובייקטים שהמנוע כותב אליהם. הרצה כפולה חיה על אותו אובייקט בזמן שהמנוע כותב אליו מגדילה סיכון לבלבול בסנכרון/דיווח, גם אם לא לכפילות ישירה (למנוע יש עוגן מזהה נפרד).
2. **הבעיה תועדה לפני 11 יום ולא טופלה.** ההמלצה שם הייתה "להתנות" את הרצת ה-webhook השני, אבל זה סומן כ"שלב שני" ונדחה. התוצאה בפועל: לקוחות אמיתיות (לא טסט) נכנסות בלי שם, וצריך תיקון ידני לכל אחת.
3. **לא נגעתי ב-Make** לפי כללי הפרויקט — זו אבחנה בלבד. התיקון (חסימת ריצה כפולה, או "מיזוג" השם מהרישום השני חזרה ללקוח) הוא שינוי ב-Make, וזה בידיך.

---

## 4. מה מומלץ (לא בוצע — להחלטתך)

- **טווח קצר:** לתקן ידנית את השם על `25a54c0e…` (נירית, מייל `niritnoa1@gmail.com`, טלפון `0509028759`) — כרגע היא היחידה שנשארה בלי שם.
- **טווח בינוני:** ב-Main Leads CRM (Make, 9339860) — להוסיף בדיקה במודול "יוצר לקוח" (25): אם הלקוח כבר קיים (dedup לפי טלפון) והשם שהגיע בריצה הנוכחית לא ריק והשדה הקיים ריק — לעדכן. זה סוגר את הפער בלי לגעת בשורש ההרצה הכפולה.
- **טווח ארוך:** זה מה שכבר הומלץ ב-30/08 ולא בוצע — להתנות את ההרצה הכפולה עצמה. שווה להעלות שוב, כי עכשיו יש הוכחה שהיא לא רק שולחת הודעות כפולות אלא גם פוגעת בנתוני הלקוח.

---
*נבדק מול Fireberry API החי, 10/09/2026. לא בוצע אף שינוי בפיירברי או ב-Make.*
