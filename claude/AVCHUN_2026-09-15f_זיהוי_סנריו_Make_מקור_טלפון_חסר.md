# אבחון · זיהוי הסנריו והמודול ב-Make שיוצר תהליכי מכירה בלי טלפון — 15/09/2026 (המשך)

**הטריגר:** אליאל: "במייק אין הרצה ל-14/09/2026 06:02:21 או 04:13:22. אתה בטוח שזה Main Leads CRM? תבדוק לבד." + מפתח API של מייק ל-Make: `71f5f3a3-d678-452c-8697-1ef36f099c9b`.

**חשוב:** זו הפעם הראשונה שנעשה שימוש ב-API של Make בפרויקט הזה, ובאישור מפורש של אליאל, לצורך **קריאה בלבד** לאבחון. לא בוצע שום שינוי, הפעלה מחדש (replay), עצירה או עריכה של אף סנריו. `DO NOT touch Make.com` נשמר — זו חריגה מתועדת ומאושרת לאבחון קריאה-בלבד, לא תקדים לעריכה.

---

## 1. איתור הארגון והסנריו

מפתח ה-API תקף בשני zones (eu1, eu2) תחת חשבון "Vitrue" (`support@vitrue.co.il`) עם **273 ארגונים** (כל לקוחות ויטרו). הארגון של רויטל יעיש:

- **ארגון:** `Revital Yaish רויטל יעיש  VP` — id `4829531`, zone **eu2.make.com**
- **צוות:** `My Team` — id `2425546`
- **43 סנריוז בסה"כ**, מתוכם 12 פעילים

**הסנריו הרלוונטי:** `Main Leads CRM  - Revital Yaeish N (Version 1.02)` — id **`9339860`**, `active: true`, webhook (`scheduling: immediately`).

## 2. איתור המודול המדויק

בתוך הבלופרינט (`GET /api/v2/scenarios/9339860/blueprint`) יש נתיב ראוטרים מקונן: `#74 (ראוטר ראשי) → r8 → #6 (מספר תקין) → r1 → #24 (יש/אין לקוח) → r2 → #148 → r0 → #29 (יש/אין מכירה) → r0`.

בקצה הענף הזה יושב **מודול #37**, `app#powerlink-agjqm5:createobject`, עם התנאי (filter) המדויק:

> **"אין מכירה או יש מכירה לא עם אותו מוצר ויותר מהזמן המוגדר"**
> — כלומר: נוצרת עסקה חדשה אם **אין** לליד שום עסקה פתוחה, **או** אם יש לו עסקה אבל עם מוצר שונה וגם ישנה מהחלון המוגדר (`pcfNumOfHoursNewOpportunity`, ברירת מחדל 24 שעות).

**זו בדיוק אותה חתימה שכבר תועדה ב-`AVCHUN_2026-09-15_1609`** ("לא בודק אם כבר יש עסקה לאותו מוצר"). **הוכחה חד-משמעית שזה המודול הנכון:** שדה ה-`name` במיפוי שלו הוא:

```
name = "העסקה של {{69.fullname}}"
```

זו התבנית **המילולית המדויקת** של השם שראינו על כל 30 הרשומות הפגומות. אין דו-משמעות כאן.

## 3. האם המודול כן שולח טלפון?

**כן — כרגע.** במיפוי הנוכחי (69 שדות) יש:
```
pcfTelephone1 = "0{{3.phone}}"
```
(אין `pcfTelephone2` במיפוי בכלל — רק ראשי.)

**אבל** — לסנריו יש `lastEdit: 2026-09-15T10:43:52Z` — כלומר **הוא נערך היום, אחרי** ההרצות הבעייתיות (13-14/09). **אין ב-API של Make גישה לגרסאות היסטוריות של הבלופרינט**, אז אי אפשר לדעת בוודאות אם המיפוי הזה כלל את `pcfTelephone1` גם ב-13-14/09, או שזה נוסף/שונה בעריכה שקרתה מאז. יכול להיות שהבאג כבר תוקן חלקית בלי שמישהו שם לב שזו הסיבה, ויכול להיות שהמיפוי היה זהה גם אז וחסר טלפון מסיבה אחרת (למשל `{{3.phone}}` ריק עבור אותו ליד ספציפי).

## 4. חיפוש ההרצה המדויקת — מה כן נמצא, מה לא

משכתי את **300 רשומות ההרצה האחרונות** של הסנריו (מ-15/09 19:26 אחורה עד 13/09 10:19).

**אורית אלחרל (Fireberry: 14/09 06:02:21):** נמצאה הרצה תואמת כמעט-מדויקת —
- **Execution ID:** `886c5e262f164d349a56d727af347642`
- **הסתיימה:** 2026-09-14T06:03:51.021Z (90 שניות אחרי יצירת הרשומה בפיירברי)
- **סטטוס: הצלחה** (status 1), 22 אופרציות, ללא שגיאה ב-DLQ.

**מרב רז (Fireberry: 14/09 04:13:22) ו-עדי צ'פניק וייניש (04:56:24):** **לא נמצאה שום הרצה תואמת.** יש פער אמיתי וריק ביומן ההרצות בין 03:51:54 ל-05:03:38 (72 דקות בלי שום הרצה בכלל) — בדיוק כשהרשומות האלה נוצרו. זה לא נראה כמו פער בשליפה שלי (בדקתי את כל 300 הרשומות ברצף, אין חורי pagination) — זה נראה כמו שההרצה שיצרה את הרשומות האלה **פשוט לא מופיעה ב-endpoint של הלוגים הרגיל**, כנראה כי מדובר ב-**replay/reprocessing של DLQ** (הרצה שנכשלה במקור ותוזמנה לניסיון חוזר) — ל-DLQ יש `nextReprocessTimestamp` נפרד, וייתכן שריצת reprocess לא נכתבת לאותו endpoint `logs` באותה צורה, או שכן אבל בזמן אחר לגמרי מזמן ה-`created` המקורי של ה-DLQ.

**המסקנה הישרה:** אני **בטוח ברמה גבוהה** שהמודול הנכון הוא #37 ב-Main Leads CRM (התאמת השם המילולית לא משאירה מקום לספק), אבל **לא הצלחתי לשלוף מה-API של Make את תוכן הבאנדל (הקלט/פלט) של ההרצה הספציפית** — ה-API הציבורי של Make לא חושף את זה, רק את ה-UI (History → לחיצה על הרצה → Data inspector על כל מודול) חושף את הבאנדלים בפועל.

## 5. ממצא נוסף, חשוב בפני עצמו: 220 רשומות תקועות ב-DLQ

לסנריו יש **220 רשומות לא-פתורות בתור ה-DLQ** (Data Loss Queue) שלו כרגע (`dlqCount: 220`, `allDlqCount: 234`). כמעט כולן עם הסיבה:

```
[400] Could not update record. Check your data
```

זה קורה כי לכל מודולי היצירה/עדכון על אובייקט 4 יש error handler מסוג `Break` עם **2 ניסיונות חוזרים, כל דקה, ואז "השלמת ריצה אוטומטית"** — כלומר **הסנריו "מצליח" ונראה ירוק ביומן, בזמן שבפועל כתיבה ספציפית נכשלה בשקט ונזרקה ל-DLQ** לטיפול ידני. זה הסבר סביר מאוד לתופעות רחבות יותר מעבר לטלפון (שדות חסרים, עדכונים שלא קרו) שכבר ראיתם בניטורים הקודמים.

**לא בדקתי את תוכן ה-DLQ הספציפי** (יש endpoint ל-list אבל לא לתוכן המלא של הבאנדל) — אבל 220 רשומות תקועות זה מספר גדול שכדאי שתעיף בו מבט ב-UI של Make (`Scenarios → Main Leads CRM → ⋮ → Data Loss Queue`), בלי קשר לחקירת הטלפון.

## 6. מה כדאי שתבדוק בעצמך ב-Make UI

1. **`Main Leads CRM - Revital Yaeish N (Version 1.02)`** (id 9339860) → **History**.
2. תפתח את ההרצה `886c5e262f164d349a56d727af347642` (14/09 06:03:51 UTC = 09:03 שעון ישראל) ותבדוק את מודול **#37** — מה בדיוק היה ב-`{{3.phone}}` ובגוף הבקשה בפועל.
3. **Data Loss Queue** של אותו סנריו — 220 רשומות, תראה כמה מהן מ-13-14/09 ומה השדה שגרם ל-400.
4. אם תרצה, תגיד לי מה מצאת ב-Data inspector של הרצה 37 — ואני אעדכן את האבחון בפרויקט.

## 7. מה לא בוצע

לא נגעתי בשום סנריו, לא הרצתי replay, לא שיניתי מיפוי. שימוש קריאה-בלבד ב-API עם המפתח שקיבלתי, לצורך האבחון הזה בלבד.
