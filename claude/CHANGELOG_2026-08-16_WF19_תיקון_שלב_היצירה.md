# יומן שינויים · WF-19 — 16.8.2026 · תיקון שלב היצירה

**וורקפלואו:** `CG4nPYCgushC1qDz` · **24 צמתים** · לא פעיל · `DRY_RUN: true`
**קישור:** https://n8n.srv964196.hstgr.cloud/workflow/CG4nPYCgushC1qDz
**המשך ישיר של** `CHANGELOG_2026-08-11_WF19_תיקונים_ובדיקות.md`

---

## מה קרה

מייק הריץ במצב חי (`DRY_RUN: false`, `CYCLE_END_OVERRIDE: '2026-08-16'`).
ההכרעה עבדה מושלם — הלקוחה זוהתה, המחזור המבוטל דולג, הגוף נבנה נכון.
**אבל שום עסקה לא נוצרה.** ריצה `170520`, צומת `1.8` החזיר `400`.

---

## באג ראשי · הנוד שלח מערך במקום רשומה בודדת

`1.8` ו־`2.6` שלחו `JSON.stringify($json.payloads.map(p => p.body))` — כלומר **מערך** ל־`POST /api/record/4`.

**פיירברי לא מקבלת מערך.** אומת אמפירית:
- גוף בודד → `success: true`
- אותו גוף בתוך מערך → `400 · An internal error has occured`

**המשמעות:** הנוד הזה היה נכשל בכל ריצה, עם כל דאטה, לתמיד.

**התיקון:** `1.6` ו־`2.4` מחזירים **פריט נפרד לכל רשומה**, ו־`1.8`/`2.6` שולחים `$json.body` — קריאה אחת לכל עסקה.

---

## באג שני · תווית עברית נכתבה לשדה פיקליסט

השדה **"מילאה שאלון לאחר בוסט?"** נקרא מפיירברי כתווית `"כן"`, ונכתב בחזרה כ־`"כן"`.
פיירברי החזירה: `כן is not a valid value for 'pcfFormFilled'`.

**התיקון:** נוספה מפה בבלוק ההגדרות:
```
WRITE_CODES: { pcfFormFilled: { 'כן': 1, 'לא': 2 } }
```
`1.6` ממיר תווית לקוד לפני הכתיבה. **כל שדה פיקליסט חדש שהאוטומציה תכתוב חייב להיכנס למפה הזאת.**

---

## באג שלישי · שלושה שדות נופלים ביצירה

נבדק אמפירית — נשלח גוף מלא, נקרא בחזרה, הושווה שדה־שדה:

| שדה | נשמר ביצירה? |
|---|---|
| לקוח · סטטוס · מוצר · מחזור · עסקת אב · בעלים · שאלון · תאריך פולואפ | ✅ |
| **שם העסקה** | ❌ נדרס ל־"תהליך המכירה של {לקוח}" |
| **טלפון** | ❌ נמחק |
| **אימייל** | ❌ נמחק |

שלושתם **כן** נשמרים ב־PUT אחרי היצירה.

---

## באג רביעי · מרוץ מול אוטומציה פנימית של פיירברי

ה־PUT לתיקון השם החזיר `success: true` — ואז השם חזר לערך של פיירברי.

**סדרת ניסויים:**
| מתי בוצע ה-PUT | התוצאה |
|---|---|
| מיד אחרי היצירה | ⚠️ נדרס בחזרה תוך שניות |
| 20 שניות אחרי | ✅ נשמר |

**מסקנה:** יש **אוטומציה פנימית בפיירברי** שרצה אסינכרונית כמה שניות אחרי יצירת תהליך מכירה, ודורסת את שם העסקה. היא גם מסבירה את מחיקת הטלפון והאימייל.

**התיקון:** נוסף צומת **`⏳ המתנה לשיום` — 25 שניות** בין `1.8` ל־`1.9`.
`1.10` שונה שמו ל־**`1.10 השלמת שם · טלפון · אימייל`** ומעדכן את שלושת השדות יחד.

⚠️ **למייק:** כדאי לאתר את האוטומציה הפנימית הזאת בפיירברי ולהחליט אם היא צריכה להמשיך לרוץ. כרגע אנחנו עוקפים אותה בהמתנה, לא מנטרלים אותה.

---

## תיקונים נלווים

- **`1.7` / `2.5`** — עברו מ־`dryRun === false` ל־`dryRun === false && !!$json.body`. קודם, ריצה חיה בלי מועמדות הייתה מגיעה לנוד היצירה עם גוף ריק.
- **`1.9`** — מחזיר מערך ריק כשאין מה לעדכן (במקום פריט דמה שגרם ל־`1.10` להחזיר `Insufficient Data`).
- **`📋 סיכום הריצה`** — קורא עכשיו ישירות מהבנאים (`1.6` / `2.4`) במקום להסתמך על מה שהגיע אליו, **ומדפיס במפורש שגיאות פיירברי משלב היצירה**. קודם הוא הציג "לא הגיעו נתונים משום ענף" והסתיר את השגיאה האמיתית.

---

## אימות · יצירה חיה מקצה לקצה

הורץ הקוד **שנפרס בפועל**, מול פיירברי החי, על לקוחה פיקטיבית שנמחקה:

```
1.1 מחזורים: 1 · 1.2 עסקאות בוסט: 1 · 1.5 עברו את השערים: 1
1.7 מעבר: true → 1.8 success=true
⏳ המתנה 25 שניות
1.10 success=true

הרשומה שנוצרה:
  שם העסקה   = "מכירת ליווי לאחר בוסט - ..."   ✅
  לקוח        = תואם                            ✅
  סטטוס       = ליד חדש                         ✅
  מוצר        = תוכנית ליווי 12 שבועות          ✅
  עסקת אב     = עסקת הבוסט                      ✅
  מחזור       = המחזור הנכון                    ✅
  שאלון       = 1                               ✅
  תאריך פולואפ = מחר 10:00                       ✅
  בעלים       = מהעסקה המקורית                  ✅
```

**אימות על הדאטה האמיתית (סער וינברג), במצב יבש** — הגוף שייבנה:
```json
{"name":"מכירת ליווי לאחר בוסט - סער וינברג",
 "accountid":"3188d146…","statuscode":2,
 "pcfProduct":"d9e842a7…","pcfParentSale":"1097858e…",
 "pcfCourse":"ec34c207…","pcfWhatsAppToRef":"03acd3b9…",
 "ownerid":"0fa826fa…","pcfFormFilled":1,
 "pcfTelephone1":"0533435240","pcfemailaddress":"sahar@vitrue.co.il",
 "pcfFollowUpTime":"2026-08-17T10:00:00"}
```
טלפון ואימייל עוברים כמצופה מהדאטה האמיתית.

**ניקוי:** אומת אפס שאריות בכל האובייקטים. הרשומות שנוצרו בבידוד במהלך האבחון (9 עסקאות) אותרו ונמחקו.

---

## הערה על נפח

מספר תהליכי המכירה במערכת עלה מ־**435 (11.8)** ל־**550 (16.8)** — כ־26 לידים חדשים ביום.
זה מאשר למפרע את תיקון תקרת העמוד מ־11.8: השאילתות הרחבות הישנות כבר היו חוצות את התקרה של 500 ומאבדות רשומות בשקט.

---

## מצב מסירה

```
DRY_RUN: true
CYCLE_END_OVERRIDE: '2026-08-16'     ← להשאיר עד סיום הבדיקה
RENEWAL_DATE_OVERRIDE: ''
MAX_PER_RUN: 25
CYCLE_STATUS_EXCLUDE: ['התבטל']
RETAINER: 58a4d4f9-5921-4bfc-ba8e-518877faa375
```
גיבוי מלא: `WF19_backup_20260816_2131.json` (נשלח למייק).

---

## פתוח

1. **WF-16** — להוסיף `'ליד חדש'` למערך הסטטוסים הפתוחים. **טרם בוצע.**
2. **האוטומציה הפנימית בפיירברי** שדורסת שמות עסקאות — לאתר ולהחליט.
3. **לרוקן את `CYCLE_END_OVERRIDE`** לפני הפעלה אמיתית.
4. שמות המחזורים מוסטים בשבוע.
5. מזהה תבנית CloudChat לברכת בוסט — ממתין לטל.
