# מצב הבדיקות · לילה 23.08.2026

מסמך חי. מתעדכן תוך כדי. אם הסשן נקטע, זו נקודת ההמשך.

## סטטוס כללי

| תהליך | מזהה n8n | פעיל | בדיקות | מצב |
|---|---|---|---|---|
| WF-29 · מנוע שליחה | `cQO4ytsLzcyImT04` | לא (תת-תהליך) | נבדק דרך הקוראים | ✅ |
| WF-31 · בקשת פגישה | `m1RC6ufxVNO7Qn7W` | כן | 7/7 | ✅ הושלם |
| WF-33 · אישור הגעה | `DRqd5IryThPEk6nC` | כן | 8/8 | ✅ הושלם |
| WF-32 · סדרת פגישה | `UVrEPMIM23dovCL1` | כן | 18 עד כה | 🔄 בתהליך |
| WF-34 · שקילה | `BJLqvtIRdjZI2P1s` | כן | טרם | ⏳ |
| WF-30 · וובינר | `rLcLLXt9tuVt3Rx4` | כן | טרם | ⏳ |

**מצב בדיקה דלוק** ב-WF-29, WF-31, WF-33. רק `0533435240` מקבל הודעות.

## שינויים שבוצעו לפי הנחיות מייק

1. **שלב "שעה לפני" בוובינר בוטל.** הוסר מ-WF-30 יחד עם ה-cron של 19:00 ביום רביעי. נשארו 4 שלבים.
2. **WF-31 ו-WF-33 נכתבו מחדש** למבנה הוובהוק האמיתי: `userns` + `action` במקום טלפון. פענוח הטלפון דרך `GET /subscriber/get-info?user_ns=`.
3. **זיהוי לפי `action`** ולא לפי טקסט הכפתור: `meet_revital_request`, `schedule approved`, `schedule declined`.
4. **כל מזהי הפלואוז מולאו** מהשמות שמייק סיפק.
5. **טריגר בדיקה** נוסף ל-WF-30/32/34: `POST /webhook/wf30-test` (וכן wf32-test, wf34-test) עם עקיפות בגוף הבקשה: `stage`, `date`, `dry`, `hourDay`, `hourToday`.
6. **5 התהליכים הודלקו.** בלי זה הוובהוקים שמייק הגדיר בקלאודצ'אט לא עובדים.

## באגים שנמצאו ותוקנו בלילה

| # | תהליך | הבאג | התיקון |
|---|---|---|---|
| 1 | WF-33 | לקוחה שאישרה הגעה ואז ביקשה להזיז, סטטוס הפגישה נשאר "אישרה הגעה" | הפעולה מאפסת גם את `statuscode` ל-3 |
| 2 | WF-33 | כל לחיצה על "להזיז" פתחה משימה נוספת | נוסף חסם כפילות למשימות תיאום מחדש, כמו ב-WF-31 |
| 3 | WF-32 | צומת האיסוף ישב אחרי צומת ההערה וקלט את תשובת ה-API במקום את התוצאה | האיסוף מושך מ-"5 · עיבוד התוצאה" |
| 4 | WF-30/32/34 | פלט ה-done של splitInBatches מחזיר את פריטי הקלט, לא את התוצאות. הדוחות הראו 0 נשלחו גם כשנשלח | הדוח עובר על כל ריצה של צומת האיסוף עם `$(name).all(0, i)` |
| 5 | WF-29 | `set-user-fields-by-name` עם `data` ריק מחזיר 400. שלב 30 דקות נכשל | נוסף שער "יש שדות לכתוב?" שמדלג על כתיבת השדות |
| 6 | WF-29 | `$('צומת').first()` מחזיר undefined כשהצומת לא רץ בענף. קרס אחרי שההודעה כבר יצאה | ההקשר נמשך מ-"יש מנוי?" שרץ תמיד בשני המסלולים |

## מה אומת בפועל

**WF-31** (7): פורמט אמיתי, חסם כפילות, action לא מוכר, בלי userns, userns לא קיים, שיוך נציג לפי תהליך מכירה, חסימת לקוחה אחרת במצב בדיקה.
אומת ישירות בפיירברי: `ownerid` = ויטרו 2 (מתהליך המכירה), `pcfSale` ו-`pcfClient` מקושרים, סטטוס פתוח, עדיפות גבוהה, סוג מכירה, תאריך ביצוע היום.

**WF-33** (8): אישור הגעה, בקשה להזזה, אישור ואז הזזה, חסם כפילות למשימה, action לא מוכר, בלי userns, לקוחה אחרת.
אומת בפיירברי: `pcfArrivalConfirmation` ו-`statuscode` משתנים נכון בשני הכיוונים.

**WF-32** (עד כה): זימון, חסם זימון, תזכורת יום לפני, חסם, **תזכורת יום הפגישה למי שאישרה = לא נשלחה**, תזכורת למי שלא אישרה = נשלחה, חסם קלאודצ'אט, תזכורת 30 דקות, חסם.

## נוסחי ההודעות שהתקבלו בפועל

- `meeting_invite`: "נפגשות ב-_24.08.2026_ בשעה _14:30._" ✅ שני המשתנים נחתו
- `meeting_reminder_day_v2`: "מחר ב-_14:30_ נקבעה לך פגישה אישית" ✅
- `meeting_reminder_today`: "הפגישה שלך היום ב-_23:30_" ✅
- `meeting_reminder_30m`: "עוד חצי שעה ואנחנו נפגשות" ✅

**הפלואוז של מייק מכילים את התבניות החדשות**, לא את הישנות. אין צורך בהתאמה למשתנים אחרים.

## ממצא צדדי

בהודעות ישנות מ-20.08 שנשלחו ידנית רואים:
- `welcome_livuy`: "למנטורית שלך קוראים __" — שדה ריק
- `welcome_boost_v2`: "המחזור שלך מתחיל ביום ראשון - __" — שדה ריק

זה מאשר בדיוק למה נוצרו `mentor_name` ו-`boost_cycle_start_date`. אלה שליחות שקדמו ליצירת השדות, לא באג באוטומציות.

## נתוני בדיקה שנוצרו

| מה | מזהה | לניקוי |
|---|---|---|
| תהליך מכירה | `abe6f534-9bfc-4ec5-a5a6-32e65301cd99` | כן |
| פגישה | `117fdb5b-1cc7-4649-9c5b-6ddcae19e6c1` | כן |
| משימות שנפתחו | כמה, על לקוחת הבדיקה | כן |

## אימות שיטת בדיקת ההודעות

`GET /subscriber/chat-messages?user_ns=...&include_bot=1` מחזיר את גוף ההודעה שנשלחה בפועל, כולל שם התבנית והמשתנים אחרי ההצבה. זו דרך אמינה יותר מקריאת צילום מסך של וואטסאפ.
