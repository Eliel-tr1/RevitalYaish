# יומן שינויים · WF-19 — 16.8.2026 · ענף החידוש (תוכנית המשך)

**וורקפלואו:** `CG4nPYCgushC1qDz` · **27 צמתים** · לא פעיל
**המשך ישיר של** `CHANGELOG_2026-08-16_WF19_תיקון_שלב_היצירה.md`

---

## מה קרה

מייק בדק את ענף החידוש על תהליך הליווי `aedbd387-738f-4a3d-b14d-a042d2892e86` (סער וינברג).
הליווי מסתיים ב-**28.8**, סטטוס "ליווי בתהליך", תוכנית "פרימיום". לא נפתח כלום.

---

## באג ראשי · התאמת תאריך מדויקת במקום חלון

`2.1` חיפש `(pcfEstimatedEndDate = היום+14)` — כלומר **בדיוק 30.8**.
הליווי נגמר ב-28.8 → 0 תוצאות.

זה לא היה מקרה קצה — זה היה שובר את הענף בכל יום שבו אין ליווי שנגמר בדיוק ביום ה-14.
כל ליווי שסטה ולו ביום אחד היה נופל בין הכיסאות **לצמיתות**.

**התיקון:** שאילתת טווח.
```
(pcfEstimatedEndDate >= היום) AND (pcfEstimatedEndDate <= היום+14)
```
אומת שפיירברי תומכת ב-`>=` ו-`<=` (וגם ב-`>` / `<`).
הצומת שונה שמו ל-**`2.1 ליוויים שמסתיימים בחלון הקרוב`**.

---

## שינוי כלל עסקי · מה חוסם פתיחת תוכנית המשך

**הכלל הישן:** `אם נסגרה עסקה כלשהי היום → אל תפתח`. שרירותי — חלון של יום אחד, ולא בודק איזה מוצר.
בפועל הוא חסם את סער כי העסקה שהאוטומציה יצרה לה קודם סומנה "נסגרה בהצלחה" ופיירברי החתימה עליה את תאריך היום.

**הכלל החדש** *(החלטת מייק, 16.8)*: **רק רכישה של תוכנית ההמשך עצמה חוסמת.**
```js
const sold = sales.filter(s => cfg.ST.SOLD.includes(txt(s[F.SALE_STATUS])));
const contGuid = String(cfg.PRODUCT.RETAINER).toLowerCase();
const already = sold.filter(s => String(s[F.SALE_PRODUCT]||'').toLowerCase() === contGuid);
if (already.length) { skip('כבר רכשה תוכנית המשך'); continue; }
```
עסקאות עבר (בוסט, ליווי) אינן חוסמות — הן הסיבה שהיא מועמדת מלכתחילה.
השער "קיים תהליך מכירה פעיל" נשאר ומונע כפילויות.

**החלטה נוספת** *(מייק, 16.8)*: החלון הוא **קדימה בלבד** — ליווי שכבר הסתיים בעבר ולא נפתח לו חידוש **לא** נתפס. מונע הצפה בהפעלה הראשונה. המחיר: אם הוורקפלואו מושבת לתקופה, ליוויים שנגמרו בינתיים מפוספסים.

---

## פערים נוספים שנסגרו בענף 2

| מה היה | מה תוקן |
|---|---|
| שם העסקה `חידוש ריטיינר - X` | **`מכירת תוכנית המשך - X`** — תואם למוצר בפועל |
| אין תאריך פולואפ | נוסף פולואפ למחר 10:00, כמו בענף 1 |
| **אין שיום אחרי היצירה** — כל עסקאות החידוש היו נשארות עם השם האוטומטי של פיירברי | נוספו `⏳ המתנה לשיום (חידוש)` (25 שניות) → `2.7 שיום עסקאות החידוש` → `2.8 עדכון שם החידוש` |

**מוצר תוכנית ההמשך** אומת: `58a4d4f9-5921-4bfc-ba8e-518877faa375` = **"תוכנית המשך אונליין"**, קטגוריה 102.

---

## בדיקות · ענף 2 — 12/12

מול הקוד שנפרס בפועל ומול פיירברי החי, עם רשומות פיקטיביות שנמחקו.

| # | תרחיש | תוצאה |
|---|---|---|
| ב1 | ליווי פרימיום פעיל בתוך החלון | פותח |
| ב2 | ליווי בהקפאה | עוצר |
| ב3 | ריטיינר · סיום 2099 | מדלג |
| ב4 | מסתיים היום — גבול תחתון | פותח |
| ב5 | מסתיים בעוד 14 יום — גבול עליון | פותח |
| ב6 | מסתיים בעוד 15 יום | מחוץ לחלון |
| ב7 | הסתיים אתמול | לא נתפס (מכוון) |
| ב8 | קיים תהליך מכירה פתוח | עוצר |
| ב9 | כבר רכשה תוכנית המשך | עוצר |
| ב10 | רכשה ליווי (לא המשך) | פותח |
| ב11 | תוכנית המשך שהופסדה | פותח |
| ב12 | **יצירה חיה** — העסקה נוצרה והשם נשמר | ✅ |

הרשומה שנוצרה בבדיקה החיה: שם `מכירת תוכנית המשך - …` · מוצר תוכנית המשך אונליין · סטטוס ליד חדש · פולואפ מחר 10:00.

**ניקוי:** אפס שאריות בכל האובייקטים.

---

## מצב מסירה (16.8, 21:53)

```
DRY_RUN: false          ← מייק הפעיל מצב חי
CYCLE_END_OVERRIDE: ''  ← ריק, כמו שצריך
RENEWAL_DATE_OVERRIDE: ''
MAX_PER_RUN: 25
27 צמתים · לא פעיל (Active = off)
```
**מה יקרה בהרצה ידנית עכשיו:** ענף 1 לא ימצא מחזור (היום+4 = 20.8, אין מחזור שנגמר אז). ענף 2 ימצא את סער ויפתח לה תהליך מכירה של תוכנית המשך, בפועל.

גיבוי: `WF19_backup_20260816_2153.json`.

---

## פתוח

1. **WF-16** — להוסיף `'ליד חדש'` למערך הסטטוסים הפתוחים. **טרם בוצע.**
2. **האוטומציה הפנימית בפיירברי** שדורסת שמות עסקאות — לאתר ולהחליט.
3. שמות מחזורי הבוסט מוסטים בשבוע.
4. מזהה תבנית CloudChat לברכת בוסט — ממתין לטל.
5. **הפעלה (Active)** — רק אחרי שמייק מאשר את שני הענפים בייצור.
