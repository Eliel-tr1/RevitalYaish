# יומן שינויים · 20/08/2026 — הקמת הו"ק דרך עמוד תשלום + מסקנת הפריסות

## 1. הזיכוי החדש (60285 / 50852) — ✅ נקלט נכון

| | |
|---|---|
| טלפון מה-PDF | 0533435240 ✅ |
| הפניה למסמך מקורי | **49672** |
| האם 49672 קיים בפיירברי | ❌ לא — מסמך מלפני המערכת |

**התוצאה:** המסמכים נשמרו ושויכו **ללקוחה** (לפי הטלפון המודפס), אך **לא** לתשלום,
ו**לא** נרשם תיעוד זיכוי — כי התשלום המקורי אינו קיים במערכת.

זו התנהגות נכונה: הזיכוי בוצע על תשלום שקדם למערכת. אין מה לזכות.
נפתחה משימה, והמסמך זמין בפיירברי עם הקישור.

---

## 2. 🔴 הקמת הוראת קבע — תוקן, וזו הייתה תקלה אמיתית

### מה קרה בבדיקה שלך

הקמת ההו"ק שלך (ריצה `190225`) נכנסה כ-`RECURRING_CHARGE_NO_SOURCE` —
**משימה בלבד. בלי רשומת תשלום, בלי תיעוד, בלי הקמת מוצר.**

### הסיבה — וגילוי חשוב

**גרואו לא שולחת וובהוק "הקמת הוראת קבע" נפרד.** האירוע היחיד שמגיע הוא
החיוב הראשון, והוא נראה זהה לחלוטין לחיוב חודשי של הוראה ותיקה.
המערכת לא ידעה להבדיל, ולכן טיפלה בהקמה החדשה שלך כאילו היא חיוב של
הוראה ישנה שאין לה רשומה.

### המבדיל שנמצא — `paymentSource`

השוואת שני פיילואדים אמיתיים מהיום:

| | ההקמה שלך (190225) | חיוב של קורין (189268) |
|---|---|---|
| `paymentType` | הוראת קבע | הוראת קבע |
| `directDebitId` | 51959405 | 51945332 |
| **`paymentSource`** | **`Payment Links`** | **`מערכת חיצונית`** |

`Payment Links` = לקוחה עברה עכשיו בעמוד תשלום ⇒ **הקמה**.
`מערכת חיצונית` = מנוע גרואו גבה אוטומטית ⇒ **חיוב חוזר**.

### מה נבנה

* **מקור = עמוד תשלום** ⇒ נוצרת רשומת הוראת קבע, נרשם החיוב הראשון כנפרע,
  **ומופעל התהליך המקצועי להקמת המוצר**. רכישה חדשה לכל דבר.
* **מקור = מערכת חיצונית** ⇒ חיוב של הוראה ותיקה. ראה סעיף 3.
* **הגנה:** אם כבר קיימת רשומת הו"ק עם אותו `directDebitId` — לא נוצרת שנייה,
  גם אם המקור הוא עמוד תשלום. יש בדיקה ייעודית לזה.

### אומת חי — השרשרת המלאה

| רשומה | תוצאה |
|---|---|
| הוראת קבע (1039) | ✅ `pcfID = 51959405`, סוג "הוראת קבע" |
| תיעוד תשלום (1018) | ✅ `GROW-510328901` · נפרע בהצלחה · 1 ₪ |
| מסמך (1038) | ✅ חשבונית מס/קבלה 49683 · מקושרת לתשלום **ולתיעוד** |
| תהליך ליווי (1051) | ✅ הוקם — ענף CONTINUE |

---

## 3. ✅ "מה קורה כשמגיע חיוב בלי תשלום מקור" — נפתר

**היה:** משימה בלבד. הכסף לא נרשם בשום מקום. 11 חיובים היום נפלו לזה.

**עכשיו:** נוצרת רשומת הוראת קבע רטרואקטיבית + תיעוד תשלום לחיוב שהתקבל.
הכסף נרשם. שני מנגנוני הגנה מפני כפילות במעבר הנתונים:

1. שם הרשומה נושא את הקידומת **`[רטרו — לאיחוד במעבר]`** — כל הרשומות
   האלה ניתנות לאיתור בסינון אחד.
2. השדה "מזהה" נושא את **מזהה ההו"ק בגרואו** — מפתח איחוד ודאי.
   המשימה שנפתחת מסבירה במפורש: לאחד לפי המזהה, לא ליצור רשומה שנייה.

**⚠️ פעולה נדרשת במעבר ב-04–05/09:** לפני הייבוא, לסנן את הרשומות עם
הקידומת ולאחד אותן לפי מזהה ההו"ק. אחרת ייווצרו כפילויות.

מתג חזרה: `SELF_HEAL_STANDING_ORDER: false`.

---

## 4. 🔴 פריסות תשלומים — התופעה גורפת, וגרואו לא שולחת כלום

בדקתי את כל ארבעת השמות שנמסרו:

| לקוחה | תשלום | נמצא במערכת? |
|---|---|---|
| אנה ליברמן | 5/10 | ❌ שום זכר |
| כרמית גבאי | 5/10 | ❌ שום זכר |
| דנה ליבוביץ לוי | 5/10 | ❌ שום זכר |
| ליאור בנאנו | 6/10 | ❌ שום זכר |

ובכל היום:

* **0** ריצות מסוג פריסה
* **0** משימות מסוג פריסה (מול **11** משימות הוראת קבע)
* **0** תיעודי תשלום שעברו מ"ממתין" ל"נפרע"

**המסקנה חד־משמעית: גרואו אינה שולחת וובהוק לחיוב בפריסת אשראי.**
בפריסה ישראלית העסקה מאושרת פעם אחת וחברת האשראי מפצלת אותה — אין אירוע
נפרד לכל תשלום, ולכן אין מה לשלוח.

**המשמעות:** תיעודי התשלום שנוצרים ברכישה בפריסה הם **תחזית**, ואין היום
מנגנון שמסמן אותם כנפרעו.

### שלוש דרכי פעולה

1. **לבדוק בגרואו** אם קיים אירוע וובהוק לפריסה שלא מוגדר אצלנו. ← להתחיל מכאן.
2. **תהליך מתוזמן יומי** שמסמן תיעוד כנפרע כשתאריך החיוב הגיע.
   מהיר ליישום, אבל **מסמן כסף כנגבה בלי אישור בפועל** — חיוב שנכשל אצל
   חברת האשראי לא ייתפס.
3. להשאיר ידני.

**ההמלצה שלי: 1, ורק אם אין — לדון ב-2.**

---

## בדיקות
WF-20: **217** — כולן עוברות. פרוס ופעיל.
