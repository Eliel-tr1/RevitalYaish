# WF-27 · טופס שקילה שבועי (Tally) → שקילה 1069

> **סטטוס:** ✅ נבנה · הועלה לשרת · **פעיל** · נבדק חי מקצה לקצה · **אושר ע"י אליאל 20/08/2026**
> **תאריך:** 20/08/2026 · **מחליף:** תרחיש Make "טופס שקילה שבועי - תהליך ליווי (לא פעיל, עבר לN8N)"
> **ClickUp:** [✅ 13.6 · WF-27](https://app.clickup.com/t/86cb7v4td) — תת-משימה של "הקמת המערכת", סטטוס `complete`

---

## 1. פרטי ה-workflow

| פרט | ערך |
|---|---|
| שם | `WF-27 · טופס שקילה שבועי (Tally) → שקילה 1069` |
| מזהה n8n | `ujJNu28LEvBguzlP` |
| לינק ל-workflow | `https://n8n.srv964196.hstgr.cloud/workflow/ujJNu28LEvBguzlP` |
| **כתובת הוובהוק (להטמעה ב-Tally)** | `https://n8n.srv964196.hstgr.cloud/webhook/wf27-weekly-weigh` |
| מתודה | `POST` · `Content-Type: application/json` |
| Timezone | `Asia/Jerusalem` · Error Workflow: `WF16ErrHandler01` |
| Credential | `Revital Yaish new CRM` (Header Auth · `tokenid`) — **אפס טוקנים בתוך צמתים** |
| מצב | `DRY_RUN = false` (כותב בפועל) |

---

## 2. שרשרת הצמתים (לינארית — בלי IF+Merge מסוכן)

| # | צומת | תפקיד |
|---|---|---|
| 1 | `Webhook · Tally שקילה` | קליטת ההגשה. `responseMode = lastNode` → הסיכום חוזר בגוף התשובה |
| 2 | `Config` | `DRY_RUN` · `FB_BASE` · `ACTIVE_STATUS=2` · `ACTIVE_STATUS_LABEL` |
| 3 | `נרמול Tally` | חילוץ השדות + נרמול טלפון + בניית תאריך/שעת שקילה |
| 4 | `חיפוש לקוח (1)` | `POST /api/query` · `telephone1 OR telephone2 OR emailaddress1` |
| 5 | `בחירת לקוח` | הבחנה בין "לא נמצא" ל"כשל טכני" |
| 6 | `תהליכי ליווי של הלקוח (1051)` | כל הליוויים של הלקוח |
| 7 | `הכרעת ליווי ובניית הרשומה` | בחירת ליווי פעיל + חישוב הדלתות + בניית גוף היצירה |
| 8 | `מצב כתיבה?` | דילוג על הכתיבה כש-`DRY_RUN=true` |
| 9 | `יצירת שקילה (1069)` | `POST /api/record/1069` |
| 10 | `אימות יצירה` | מאמת שהתקבל מזהה רשומה |
| 11 | `לעדכן את הליווי?` | רק אם נוצרה שקילה **וגם** נמצא ליווי |
| 12 | `עדכון תהליך ליווי (1051)` | `PUT` · `pcfCurrentWeight` + `pcfLastWeighDate` |
| 13 | `אימות עדכון הליווי` | |
| 14 | `סיכום הריצה` | סיכום בעברית + אזהרות |

---

## 3. מיפוי השדות

**מ-Tally (זיהוי לפי `key`, עם נפילה לתווית ואז לסוג השדה):**

| שדה | key | לאן |
|---|---|---|
| פרטים אישיים | `question_l7aNEB` | שם הרשומה (רק אם הלקוח לא זוהה) |
| טלפון | `question_v46Rev` | זיהוי הלקוח + `pcfPhoneEntered` |
| מייל | `question_RJWz9d` | זיהוי משני |
| Date | `question_KL2X5A` | `pcfWeighDate` (תאריך) |
| Time | `question_LXx87j` | `pcfWeighDate` (שעה) |
| משקל | `question_pGqgyq` | `pcfWeight` |
| תחושה | `question_1M0jdQ` | `pcfFeeling` |

**נכתב ל-1069:**
`name` = «שם הלקוח — DD/MM/YYYY» · `pcfWeight` · `pcfWeighDate` · `pcfFeeling` · `pcfPhoneEntered` ·
`pcfaccountid` · `pcfFollowupProcess` · `ownerid` (מהליווי) · `pcfCreatedByPersonOrSystem = 1` (מערכת) ·
`pcfWeightDelta` = משקל קודם − נוכחי · `pcfChangeFromStart` = משקל התחלתי − נוכחי · `pcfWeekIndex` = שבועות מתחילת הליווי + 1

**נכתב ל-1051 (רק כשנמצא ליווי פעיל):** `pcfCurrentWeight` · `pcfLastWeighDate`
`pcfweightcount` (סך שקילות) מתעדכן לבד ע"י אוטומציה פנימית של Fireberry — **ה-workflow לא נוגע בו.**

---

## 4. החלטות והתנהגות בקצוות

| מצב | התנהגות |
|---|---|
| לא נמצא לקוח לפי טלפון/מייל | **נוצרת שקילה ללא שיוך** (בלי לקוח, בלי ליווי, בלי משימה) — לפי הכרעת אליאל 20/08 |
| נמצא לקוח אך אין ליווי **פעיל** (`pcfStatus = 2`) | **נוצרת שקילה ללא שיוך לליווי**, עם אזהרה בסיכום |
| כמה ליוויים פעילים | נבחר לפי `pcfStartDate` יורד, ואם שווה — לפי `createdon` יורד |
| כשל טכני בחיפוש (timeout/HTTP) | מדווח כ**כשל**, לא כ"לא נמצא" |
| הטופס שוכתב וה-`key` השתנה | זיהוי נופל לתווית ואז לסוג השדה, **ומדפיס אזהרה מפורשת בסיכום** |
| טלפון בפורמט בינלאומי (`+972…`) | מנורמל ל-`05XXXXXXXX` — כך שמור ב-CRM |
| אין תאריך בטופס | נלקח תאריך היום לפי `Asia/Jerusalem` |

⚠️ **picklist מ-`/api/query` חוזר כתווית ומ-`/api/record` כמספר** — הקוד מקבל את שני המצבים (`'2'` או `'ליווי בתהליך'`).

---

## 5. הבדלים מול תרחיש ה-Make הישן

| נושא | Make | WF-27 |
|---|---|---|
| לקוח לא מזוהה | התרחיש נשבר | נוצרת שקילה ללא שיוך |
| שעת השקילה | נמחקה (תאריך בלבד) | נשמרת (`Date` + `Time` מהטופס) |
| דלתות כשאין ליווי | נכתב `0` | לא נכתב כלל (במקום 0 שקרי) |
| שם הרשומה | לפעמים יצא מחרוזת תאריך שבורה | תמיד «שם — תאריך» |
| טופס ששוכתב | שובר בשקט | ממשיך + אזהרה |

---

## 6. בדיקות שבוצעו (20/08/2026)

| # | תרחיש | תוצאה |
|---|---|---|
| 1 | לקוח לא קיים · DRY | ✅ מזוהה כלא-משויך |
| 2 | מיכאל אוקס · DRY | ✅ לקוח + ליווי + דלתות נכונות |
| 3 | טופס ששוכתב + טלפון `+972` · DRY | ✅ זוהה נכון + 6 אזהרות |
| 4 | בלי טלפון ובלי מייל · DRY | ✅ ללא שיוך, בלי קריסה |
| 5 | **ריצה חיה · מיכאל אוקס** | ✅ נוצרה `975ff7c5-cc4e-4386-a4b4-fda3a892bc56` · הליווי עודכן · **בלי הזזת timezone** |
| 6 | **ריצה חיה · לקוח לא קיים** | ✅ נוצרה `449fa697-7b2c-44a8-bd37-0faf8c19d7f9` ללא שיוך |

**שחזור אחרי הבדיקה:** רשומת הליווי `f97075a4…` הוחזרה לערכיה המקוריים (`pcfCurrentWeight = 79.6` · `pcfLastWeighDate = 08/07/2026`).

### 🔴 שתי רשומות בדיקה מחכות למחיקה ידנית
סוכן אוטונומי אינו מוחק רשומות. **על אליאל למחוק ידנית** מאובייקט 1069:
- `975ff7c5-cc4e-4386-a4b4-fda3a892bc56` — «מיכאל אוקס — 20/08/2026»
- `449fa697-7b2c-44a8-bd37-0faf8c19d7f9` — «סער וינברג — 20/08/2026»

לינק עמוק: `https://app.fireberry.com/app/record/1069/<id>`

---

## 7. פתוח / לא נבנה (בכוונה)

1. **אין הגנת אידמפוטנטיות.** שליחה כפולה של אותה הגשה תיצור שתי שקילות. אם Tally ישלח retry — יהיו כפילויות. הפתרון (אם יידרש): שמירת `submissionId` בשדה על 1069 ובדיקה לפניה.
2. **אין אימות חתימה של Tally** — הוובהוק פתוח לכל מי שיודע את הכתובת.
3. **אין התראה** על שקילה שלא זוהתה — לפי הכרעת אליאל. אם ירצו בעתיד: לסנן ב-1069 לפי `pcfaccountid` ריק.
4. **תרחיש ה-Make הישן** — לוודא שהוא כבוי אצל מייק (הוא כבר מסומן "לא פעיל").
