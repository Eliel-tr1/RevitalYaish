# CLAUDE.md — רויטל יעיש · הטמעת CRM (ויטרו)

> קלוד קוד קורא את הקובץ הזה אוטומטית. הריפו הזה הוא **ייצוא מלא** של פרויקט Claude "Revital Yaish- Michael" נכון ל-**19/09/2026**:
> כל 199 מסמכי הידע, קובץ ה-xlsx, `template_snapshot.json` והוראות הפרויקט — ללא סינון.

## חובה לקרוא לפני כל עבודה
1. `PROJECT_INSTRUCTIONS.md` — הוראות הפרויקט המלאות (זהות Sensei, כללי תקשורת, איסורים, **פרטי גישה ומפתחות API**). הן חלות גם כאן.
2. `README.md` — שער הכניסה ומפת המסמכים (עודכן לאחרונה 03/08; מסמכים מאוחרים יותר נמצאים ב-`claude/`).
3. `claude/ROITAL_PROJECT_CONTEXT.md` — מקור האמת למטרות, היקף ומתודולוגיה.
4. המצב העדכני: המסמכים האחרונים בתיקיית `claude/` לפי תאריך בשם הקובץ — `SIKUM_2026-09-14_סשן_מלא.md`, `AVCHUN_2026-09-19_ניטור_ממצא_קריטי.md`, `MONITOR_state.json`, ה-CHANGELOG של 16–17/09.
5. `claude/GISHA_מפתחות_ומזהים.md` — מפתחות ומזהים של כל המערכות.

## מבנה הריפו
- שורש — מסמכי הבסיס (00–09, HANDOFF, PLAYBOOK, `template_snapshot.json`).
- `claude/` — כל מה שנכתב בסשנים: `CHANGELOG_*` (שינויים בחשבון), `AVCHUN_*` (אבחונים), `BACKUP_*` (גיבויי JSON לפני שינוי), `TOCHNIT_*` (תוכניות), `MONITOR_*` (סקריפט הניטור והמצב שלו), `WF*` (מפרטי תהליכי n8n), `SESSION_*`/`SIKUM_*` (סיכומי סשנים).
- `_files/` — קבצים מצורפים (xlsx אובייקטים ושדות).

## כללי עבודה בקלוד קוד
- תקשורת עם מייק/אליאל — **עברית בלבד**, ישיר.
- כל שינוי בחשבון Fireberry של רויטל → קובץ `claude/CHANGELOG_<תאריך>_<נושא>.md` בעברית, וגיבוי `claude/BACKUP_...json` **לפני** שינוי הרסני.
- **לא נוגעים ב-Make.com**, לא משכפלים חשבון Fireberry דרך API, לא מוחקים שדות/אובייקטים בלי אישור מפורש לכל פעולה.
- משימות האב בקליקאפ: https://app.clickup.com/t/25528216/86c8zamc2
- בסיום כל סשן: commit + push של המסמכים החדשים, כדי שהריפו יישאר מקור האמת.

## מה לא נכלל בייצוא
- היסטוריית הצ'אטים עצמה (השיחות) — אין גישה אליה. מה שנשמר מהשיחות הוא מה שנכתב למסמכי הפרויקט (סיכומים, changelog, handoff).
- זיכרון הפרויקט (project memory) — לא נגיש מסשן זה.
