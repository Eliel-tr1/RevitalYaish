# 06 · מפרט ה-Workflows ל-n8n

> **עודכן 03/08/2026 — סקירת 26 שיחות הפרויקט.**

> ~~מפרט 15 ה-Workflows~~ ⛔ מיושן (הוחלף 03/08/2026) — המסמך מכיל היום **25+ תהליכים** על שלוש פלטפורמות (n8n · Make · client-side JS). ראה **טבלת סטטוס ה-Workflows** להלן.
> 🔴 **אזהרת סטטוס:** 15 המפרטים המקוריים נכתבו **לפני** שהתקבלה גישה ל-n8n ו**לא אומתו מול מופע חי**. אין להתייחס אליהם כמותקנים. [chat_26]

## יומן שינויים — 03/08/2026

| # | מה שונה | מקור |
|---|---|---|
| 1 | נוספה **טבלת סטטוס Workflows** יחידה (פלטפורמה · נבנה · הועלה · פעיל · DRY_RUN · חוסמים) | סקירה רוחבית |
| 2 | נוסף פרק **קונבנציות n8n מחייבות** (Config · DRY_RUN · `WF16ErrHandler01` · credential `Revital Yaish new CRM` · `MAX_PER_RUN` · בדיקת שלילה) | chat_03, chat_16 |
| 3 | נוסף פרק **תשתית n8n** (host, IPv4, Docker, מפתח SSH, Public API) + סימון שחוסם "אין SSH" היה **כשל העברת ידע** | chat_03, chat_17, chat_21 |
| 4 | נוסף **קריטריון בחירת פלטפורמה** — פיתוח חדש = n8n · תחזוקת קיים = Make ידני של מייק | chat_12, chat_21, chat_26, chat_27 |
| 5 | **WF-01** — שער החתימה עובר מ-`pcfProductType` ל-**`categorycode`** (101 חוסם) | chat_12 |
| 6 | **WF-01/WF-02** — הפלטפורמה בפועל = **client-side JS בווידג'ט פיירברי**, לא n8n. `WF-P1`/`WF-P2` בוטלו ונמחקו | chat_12 |
| 7 | **WF-02** — שלושת שדות הזיהוי נשלפים מהרשומה (אין הקלדה); `installment_dates[]` מחליף "מרווח בין חיובים"; טיפול ב-`whatsapp_group_id = null` | chat_11 |
| 8 | 🔴 **WF-03** — המפרט קורא `pcfGroupid` בעוד הכתיבה עוברת ל-`pcfGreenApiId` ⇒ **כשל שקט**. סומן כחוסם + ⚠️ סתירה פתוחה | chat_14, chat_15, chat_20, chat_22 |
| 9 | נוסף מפרט מלא **`Boost - Join WhatsApp Group`** (11 nodes, N1–N11) — המימוש בפועל של WF-03 | chat_20 |
| 10 | נוסף מפרט **WF-06B · שליחת תפריטים מתוזמנת** (9 שלבים, Cron כפול, CloudChat 3 קריאות) | chat_13 |
| 11 | **WF-06** — תלות מפורשת בשיתוף ציבורי של 14 קובצי הדרייב; ⚠️ סתירת דגל שליחה `pcfSentDate` מול `pcfMenuMsgSent` | chat_14, chat_22 |
| 12 | **WF-07** — `pcfCreatedByPersonOrSystem` על 1069 = **`1`**, לא `2`; ⚠️ סתירת יום השקילה (ראשון מול חמישי) | chat_08, chat_22 |
| 13 | **WF-08** — מתועד כ-n8n אך **מומש ב-Make.com** ("Meetings Reminders"). תוקן | chat_10 |
| 14 | נוסף מפרט מלא **WF-16 · מסלול מת** (N1–N13, קונפיג, 6 בדיקות קבלה, וריאנט B) | chat_16 |
| 15 | נוסף מפרט מלא **WF-17 · מכירת ליווי לאחר בוסט** — `e30cNECSoBNJDQcf`, 19 nodes, כבוי, `DRY_RUN=true` | chat_03 |
| 16 | נוספו 3 מפרטי בוסט-וובינר: **6.6a** (7 nodes) · **6.6** (10 nodes) · **6.7** (8 nodes), סדר הרצה C→A→B | chat_17 |
| 17 | נוסף מפרט **`Boost Cohort — Daily Status Sync`** (7 nodes, node 7 `disabled:true`) כולל ביטויי `cur` ו-`desired` verbatim | chat_21 |
| 18 | נוספו **WF-BOOST-A** (backfill ~55 מחזורים עד 26.12.2027) ו-**WF-BOOST-B** (יצירת קבוצה אחת ביום) | chat_15 |
| 19 | **`pcfSignatureStatus`** — אין ולא יהיה ערך `4`. כל לוגיקה שבודקת `4` שבורה. תוקן לאורך המסמך | chat_18 |
| 20 | נוסף **פרוטוקול הרצה ראשונה** (שלב 0 אימות חיבורים · נקודת עצירה לאישור מייק · הרצת בדיקה מול המספר של מייק) | chat_16, chat_20 |
| 21 | נוספה החלטת **`POST /api/v3/query`** לכל workflow חדש — מותנה באימות בחשבון החי | chat_17, chat_20 |
| 22 | נוסף פרק **⚠️ סתירות פתוחות** מרוכז, עם בעלים ותאריכים | סקירה רוחבית |
| 23 | נוספה אזהרת **רוטציית 5 טוקנים** — ערכי מפתחות לעולם לא נרשמים במסמך | chat_03, chat_13, chat_20, chat_21 |
| 24 | 22 מחזורי בוסט ⟵ ⚠️ סתירה: **74 מחזורים** אומתו מול ה-API ב-02/08 | chat_03, chat_21 |

---

## קריטריון בחירת פלטפורמה (מחייב)

> **פיתוח חדש = n8n. תחזוקת קיים = Make, ידנית, ע"י מייק בלבד. קלוד לעולם לא כותב ל-Make דרך API.**
> **לוגיקת מסך (UI בפיירברי) = client-side JS בווידג'ט HTML אחד.** לא יוצאים למערכת חיצונית בשביל חיפוש מוצרים, בדיקת חתימה או בניית קישור. [chat_12 — מייק]
> **ערוצי הודעות:** **CloudChat = שליחת הודעות ללקוחות** · **GreenAPI = ניהול קבוצות בלבד** (`getContacts`, `getGroupData`, יצירה, נעילה, `addGroupParticipant`). [chat_13]

---

## קונבנציות n8n מחייבות (כל workflow חדש)

| # | הכלל | מקור |
|---|---|---|
| 1 | **בלוק `Config`** — Set node יחיד בראש ה-workflow. **אפס ערכים קשיחים בשום צומת אחר** | chat_03, chat_17 |
| 2 | **`DRY_RUN` — ברירת מחדל `true`.** אין הרצה כותבת לפני שהדוח היבש נבדק ידנית מול ה-CRM | chat_16 |
| 3 | **מטפל שגיאות `WF16ErrHandler01`** מוגדר כ-Error Workflow · `Save failed executions = on` | chat_03 |
| 4 | **credential `Revital Yaish new CRM`** (Header Auth, header name `tokenid`). **אסור לשתול טוקן בתוך node** | chat_03 |
| 5 | **תקרת `MAX_PER_RUN`** — 25 ב-WF-16. **אם רשימת ה-DRY-RUN ארוכה מהתקרה — לא הופכים ל-`false`** עד הכרעת מייק | chat_16 |
| 6 | **בדיקת שלילה חובה** — לקוחה שכן קנתה **לא** מופיעה בדוח. מופיעה → הלוגיקה שבורה, **עצור הכול** | chat_16 |
| 7 | `settings.timezone = "Asia/Jerusalem"` בכל workflow | chat_21 |
| 8 | **צמתי כתיבה מגיעים `disabled: true`** — מנגנון dry-run מובנה בקובץ | chat_17, chat_21 |
| 9 | **בלי Code node כשאפשר** — Set/Filter נפרדים, כדי שמייק יראה ערכים בעיניים | chat_21 |
| 10 | **`n8n import:workflow` לא מייבא credentials** — חיבור ה-credential ידני בממשק, תמיד | chat_21 |
| 11 | **גיבוי לפני כל import דורסני:** `docker exec -u node <c> n8n export:workflow --backup --output=/tmp/n8n-backup-$(date +%Y%m%d-%H%M)/` | chat_16, chat_17 |
| 12 | **חישוב תאריכים תמיד ב-n8n**, לעולם לא בנוסחת Fireberry. דפוס: תאריך → מספר `YYYYMMDD` | chat_16, chat_21 |
| 13 | **לא להדפיס טוקנים ל-stdout. לעולם.** אם נבחר GreenAPI ישיר — הטוקן נכנס ל-URL של הצומת ⇒ ל-export ⇒ **אסור לשלוח את ה-JSON בצ'אט/וואטסאפ** | chat_16, chat_17 |

### פרוטוקול הרצה ראשונה (מחייב, לפני כל בנייה)
| שלב | תוכן |
|---|---|
| **0 · אימות חיבורים** | SSH + זיהוי קונטיינר n8n וגרסה · MCP `fireberry-crm` מגיב · `getStateInstance` = `authorized`. 🛑 **כישלון באחד = עצירה** |
| **1 · Discovery (קריאה בלבד)** | שליפת סכימות ודאטה. 🛑 **נקודת עצירה לאישור מייק** לפני כתיבה כלשהי |
| **5 · בדיקה** | ריצה מול **המספר של מייק בלבד** + קבוצת בדיקה. ה-workflow נשאר **כבוי** |

---

## תשתית n8n

| פרט | ערך |
|---|---|
| Host | `srv964196.hstgr.cloud` |
| IPv4 | `72.60.34.206` |
| Port | `22` · User `root` |
| הרצה | n8n ב-**Docker** |
| מפתח SSH | קיים, בשם **`claude-code-n8n-dedupe`** (החלק הציבורי נמסר — אינו סוד, לא דורש רוטציה) |
| Public API | **`https://n8n.srv964196.hstgr.cloud` — דלוק** |
| Skill | `n8n-hostinger` |
| המלצה | משתמש `n8nops` בקבוצת `docker` במקום עבודה כ-root |

⚠️ **ה-API הציבורי של n8n לא מאפשר הרצה ידנית** של workflow. אימות נעשה בסימולציה של הלוגיקה מול הדאטה החי. [chat_03]
⚠️ **SSH לפורט 22 חסום מסביבת קלוד (צ'אט).** הנתיב שעובד: Public API, או Claude Code. [chat_03, chat_21]
🟢 **הבהרה חשובה:** החוסם *"אין גישת SSH"* שנרשם ב-chat_13, chat_15, chat_16 ו-chat_20 היה **כשל בהעברת ידע — לא היעדר אישורים.** הפרטים היו קיימים כל הזמן. אין לרשום אותו שוב כחוסם.

### 🔐 רוטציית טוקנים
**5 מפתחות נחשפו בצ'אטים ומחכים לרוטציה:** Fireberry `tokenid` · Make API · Tofsy `api_key` · GreenAPI `apiTokenInstance` · CloudChat API key. בנוסף `pcfPowerlinkToken` באובייקט 1036.
⚠️ **נחשף בצ'אט — לרוטציה.** **ערכי מפתחות לעולם לא נרשמים במסמך הזה.**
- **תזמון:** הוכרע שהרוטציה תבוצע **רק אחרי סיום הטסטים** — היא שוברת את כל חיבורי Make עד שמעדכנים אותם. [chat_19, chat_22]
- **אחרי הרוטציה — שלושה מקומות לעדכן:** החיבורים ב-Make ("Revital - New") · ה-credential ב-n8n · השדה `pcfPowerlinkToken` ב-1036.

---

## טבלת סטטוס ה-Workflows (מקור אמת יחיד)

| WF | שם | פלטפורמה | נבנה? | הועלה לשרת? | פעיל? | DRY_RUN | חוסמים |
|---|---|---|---|---|---|---|---|
| **WF-01** | שער חתימה לפני סליקה | **client-JS** (ווידג'ט פיירברי) | ✅ | לא רלוונטי | ✅ במצב סטטי | — | מפתחות Grow Light API · הכרעת `categorycode` 102 |
| **WF-02** | קליטת תשלום שאינו אשראי | client-JS → **Make** | 🟡 כפתור נבנה, תרחיש לא | ❌ | ❌ | — | בניית תרחיש Make ידנית (מייק) · טיפול ב-`whatsapp_group_id = null` |
| **WF-03** | צירוף לקבוצת WhatsApp (מפרט) | n8n | 🟡 מפרט בלבד | ❌ | ❌ | — | 🔴 **קורא `pcfGroupid` במקום `pcfGreenApiId`** · השדה ריק ב-8 הרשומות · credential GreenAPI |
| **—** | `Boost - Join WhatsApp Group` (המימוש של WF-03) | n8n · **11 nodes** | ❌ (פרומפט מוכן) | ❌ | ❌ | — | `idInstance` לא אומת · גורל אובייקט 1005 · טבלת התאמה קבוצה↔מחזור לאישור מייק |
| **WF-04** | קבוצה מלאה (90%/100%) | n8n | ❌ | ❌ | ❌ | — | `pcfCapacity` ריק ב-6 מתוך 8 קבוצות |
| **WF-05** | הודעת Welcome | n8n + CloudChat | ❌ | ❌ | ❌ | — | תבניות סטטיות צריכות לשבת במקום שהצוות עורך |
| **WF-06** | תפריט — משימה + כפתור שליחה | n8n | ❌ | ❌ | ❌ | — | ⚠️ סתירת דגל שליחה · שיתוף ציבורי של 14 קובצי דרייב |
| **WF-06B** | שליחת תפריטים **מתוזמנת** | n8n + CloudChat · 9 שלבים | ❌ (פרומפט v4 סופי) | ❌ | ❌ | — | חיבור פלואו `menu_sending` ידני · חלון 24ש' · מי כותב `pcfScheduledSendAt` |
| **WF-07** | טופס סטטוס שבועי | n8n | ❌ | ❌ | ❌ | — | ⚠️ **יום השקילה — ראשון מול חמישי** |
| **WF-08** | תזכורת פגישות יומית | **Make.com** ⛔ לא n8n | ✅ נבנה ("Meetings Reminders") | — | ❌ לא הופעל ולא תוזמן | — | `flow_ns` מטל · אימות שהשדות המקושרים חוזרים מלאים |
| **WF-09** | תזכורות וובינר | n8n | ❌ | ❌ | ❌ | — | `ZOOM_LINK` |
| **WF-10** | הזמנה לוובינר | n8n | ⤳ הוחלף ב-**6.6 / 6.6a** | — | — | — | ראה 6.6 |
| **WF-11** | פולו-אפ לפגישה | n8n | ❌ | ❌ | ❌ | — | נתוני נוכחות בוובינר |
| **WF-12** | ריטיינר / חידוש | n8n + client-JS | ❌ | ❌ | ❌ | — | מפתחות Grow |
| **WF-13** | משימה שבועיים לפני סיום ליווי | n8n | ❌ | ❌ | ❌ | — | — |
| **WF-14** | כתיבה כפולה לשני חשבונות | n8n | ❌ | ❌ | ❌ | — | טוקן CRM ישן |
| **WF-15** | החלפת תזכורות הוובינר הישנות | n8n | ❌ | ❌ | ❌ | — | תלוי ב-6.6/6.6a |
| **WF-16** | **מסלול מת — סיום בוסט ללא המרה** | n8n · N1–N13 | ✅ נבנה, **עודכן 02/08** | ✅ | ❌ **כבוי** | ✅ `true` | תרחיש התשלום של הבוסט טרם נבנה ⇒ יחזיר אפס · ⚠️ `prioritycode` 4/5 |
| **WF-17** | **מכירת ליווי לאחר בוסט** | n8n · id **`e30cNECSoBNJDQcf`** · **19 nodes** | ✅ | ✅ | ❌ **כבוי** | ✅ `true` | 🔴 **שדה דגל "נשלח" על אובייקט 4 לא קיים** · `sub_flow_ns` של קלאודצ'אט |
| **6.6a** | `Boost · אירוע וובינר — יצירה אוטומטית` | n8n · **7 nodes** · יומי 08:30 | ✅ JSON | ❌ | ❌ | צמתי כתיבה `disabled` | `ZOOM_LINK` · אישור שעת הוובינר |
| **6.6** | `Boost · הזמנה לוובינר ביום 3` | n8n · **10 nodes** · יומי 09:00 | ✅ JSON | ❌ | ❌ | צמתי כתיבה `disabled` | ערוץ שליחה · אישור נוסח (רויטל) · **חובה להדליק את שני צמתי הכתיבה יחד** |
| **6.7** | `Boost · אינדיקטור בריאות למחזור` | n8n · **8 nodes** · יומי 08:00 | ✅ JSON | ❌ | ❌ | צמתי כתיבה `disabled` | GUID של עודד · `ALERT_WEBHOOK` |
| **—** | `Boost Cohort — Daily Status Sync` | n8n · **7 nodes** · יומי 00:01 | ✅ JSON (`/home/claude/boost_cohort_status_sync.n8n.json`) | ❌ | ❌ `active:false` | **node 7 `disabled:true`** | כיבוי האוטומציה הפנימית ב-Fireberry (אחרת שתי מערכות נלחמות על `pcfStatus`) |
| **WF-BOOST-A** | Backfill רשומות מחזורי בוסט עד 26.12.2027 | n8n · חד-פעמי | ❌ (brief v2 מוכן) | ❌ | ❌ | ✅ `DRY_RUN` | `system_name` של lookup 1007→1068 (⚠️ לא נמצא) |
| **WF-BOOST-B** | יצירת קבוצת WhatsApp + כתיבת מזהה ל-1007 | n8n · יומי, **קבוצה אחת ביום** | ❌ | ❌ | ❌ | — | credential GreenAPI · `allowParticipantsAddMembers` לא אומת · URL קבוע לתמונת פרופיל |
| ~~WF-P1 / WF-P2~~ | ~~pay load / pay checkout~~ | ~~n8n~~ | ⛔ **בוטלו ונמחקו** (chat_12) | — | — | — | מייק פסל את n8n בנתיב הסליקה |

---

## תבנית קריאה ל-Fireberry (חוזרת בכל workflow)

```
# שאילתה — Legacy
POST https://api.fireberry.com/api/query
headers: { "tokenid": "<TOKEN>", "Content-Type": "application/json" }
body: { "objecttype": <N>, "page_size": 50, "page_number": 1,
        "fields": "field1,field2,..." }     # לעולם לא "*"
# התשובה: data.Data[]  ← מערך

# שאילתה — v3 (מאומץ בכל workflow חדש, מותנה באימות)
POST https://api.fireberry.com/api/v3/query
body: { "objectType": <N>, "fields": [{"name":"..."}],
        "filter": [{"type":"AND","conditions":[...]}],
        "pageNumber": 1, "pageSize": 500 }
# התשובה: data[] שטוח · מזהה הרשומה ב-_id · isLastPage

# יצירה
POST https://api.fireberry.com/api/record/<objectType>
# עדכון
PUT  https://api.fireberry.com/api/record/<objectType>/<recordId>
```

⚠️ **סינון picklist — לפי הערך המספרי, לא לפי התווית.** `pcfStatus = 2`, לא `"ליווי בתהליך"`.
⚠️ **picklist חוזר בשאילתה כתווית בעברית ונכתב כמספר.** אל תשווה `pcfStatus === 2` על התשובה — סמוך על הפילטר בשרת, או מפה שני המצבים.
⚠️ **lookup מחזיר שני וריאנטים:** `pcfX` (GUID) ו-`pcfXname` (תצוגה). **תמיד להתאים לפי ה-GUID.** התאמה לפי name מחזירה אפס **בשקט**.
⚠️ **`"fields": "*"` לא עובד.** שדה אחד לא קיים → **כל השאילתה נופלת ב-400**.
⚠️ **"אפס תוצאות בשקט" היא התקלה המסוכנת ביותר ב-Fireberry** — אין הבחנה בין "אין נתונים" ל"הסינון לא נתמך".
⚠️ **ווידג'ט HTML בתוך פיירברי לא צריך טוקן כלל** — `fetch('/api/query')` בנתיב יחסי עובד בסשן של המשתמשת. [chat_12]
⚠️ **`/api/query` מחזיר picklist כתווית · `/api/record` כמספר.** לעולם לא לסנן על תוצאות שאילתה — לאסוף מזהים, לשלוף רשומה מלאה, לסנן שם. [chat_12]

**אופרטורים:** `eq` · `ne` · `lt` · `gt` · `le` · `ge` · `eq-in` · `not-in` · `between` · `is-null` · `is-not-null` · `start-with`
**לינק עמוק:** `https://app.fireberry.com/app/record/{objectType}/{recordId}`

⚠️ **סתירה פתוחה — rate limit:** **100 בקשות/דקה לארגון** [chat_20, chat_21] מול **צינון 10–15 דקות אחרי ~350 קריאות מטא-דאטה** [chat_26]. ייתכן ששתי מגבלות נפרדות. **בעלים: מייק** — לאמת בשטח.

---

## WF-01 · שער חתימה לפני סליקה
**פלטפורמה בפועל: client-side JS בווידג'ט HTML בכרטיס "תהליך מכירה".** ⛔ לא n8n. [chat_12 — מייק: "הכל ישירות בקוד ב-JS, בלי לצאת החוצה למערכת חיצונית."]
**טריגר:** פתיחת מסך הסליקה מתוך כרטיס תהליך המכירה.

| שלב | פעולה |
|---|---|
| 1 | קריאת תהליך המכירה → `pcfSignatureStatus`, `pcfcfDocLink` |
| 2 | שליפת קטלוג המוצרים — **רק פעילים שיש להם `pcfpaymenturl`** |
| 3 | **IF** `categorycode = 101` ("תוכנית ליווי") → **דורש חתימה** ~~IF `pcfProductType = היכרות/שימור` → דלג~~ ⛔ מיושן (הוחלף 03/08/2026) |
| 4 | **IF** `pcfSignatureStatus ≠ 2 (נחתם)` **או** `pcfcfDocLink` ריק → **חסום.** מסך החסימה מפנה לכפתור החתימה הנפרד בכרטיס |
| 5 | אחרת → דף סליקה |

**שדות:** `4.pcfSignatureStatus` (**1=ממתין לחתימה · 2=נחתם · 3=בוטל**) · `4.pcfcfDocLink` · `14.pcfpaymenturl` · `14.categorycode` (101=תוכנית ליווי · 102=תוכנית המשך)
✅ **נסגר 26/07/2026:** הדרישה לערך `4` ב-`pcfSignatureStatus` **בוטלה.** ערך `1` עבר רילייבל ל"ממתין לחתימה".
🔴 **אזהרה מ-chat_18:** **אין ולא יהיה ערך `4`.** כל לוגיקת n8n/JS שבודקת `pcfSignatureStatus = 4` **שבורה**.
⚠️ **סתירה פתוחה — מקור אמת אחד לשער החתימה:** המימוש עובד לפי `categorycode` [chat_12, 27/07]; המפרט המקורי לפי `pcfProductType`. **מוצר יחליק בפער בין שני השדות. בעלים: מייק.**
⛔ **פתוח:** האם קטגוריה **102 ("תוכנית המשך") דורשת חתימה?** נשאל 3 פעמים ולא נענה. **בעלים: מייק.**

**מגבלות Grow/משולם (chat_12):** `pay.grow.link` **לא ניתן להטמעה** (`frame-ancestors`) · `secure.meshulam.co.il` **כן** · מילוי מראש + מזהה נסתר `cField1` אפשריים **רק** דרך `createPaymentProcess` **server-side** — `x-api-key` לעולם לא בצד לקוח.

---

## WF-02 · טופס קליטת תשלום שאינו אשראי
**פלטפורמה: כפתור client-JS בכרטיס תהליך המכירה → webhook → תרחיש Make.**
**טריגר:** לחיצה על כפתור "תשלום ידני" מתוך רשומת תהליך מכירה (אובייקט 4).

**קלט:** ~~לקוחה · תהליך מכירה · מוצר~~ ⛔ מיושן (הוחלף 03/08/2026) — **שלושת השדות נשלפים אוטומטית מהרשומה שממנה נפתח הכפתור. אין הקלדה — פחות טעויות שיוך.** [chat_11, החלטה 2]
**קלט שנשאר ידני:** סכום כולל · אמצעי תשלום (העברה/מזומן/צ'ק) · מספר תשלומים · **`installment_dates: string[]`** — מערך תאריכים מוכן מראש, ניתן לעריכה פר-תשלום (ברירת מחדל חודשית). ~~מרווח בין חיובים~~ ⛔ מיושן (הוחלף 03/08/2026) [chat_11, החלטה 3]

**מבנה ה-payload:** `source`, `opportunity_id`, `account_id`, `account_name`, `product_id`, `product_name`, `product_type`, `owner_id`, `whatsapp_group_id`, `signature_status`, `deal_amount`, `total_entered`, `payments[]`, `installment_dates[]`

| שלב | פעולה |
|---|---|
| 1 | צור **הזמנה** (13) — לקוחה, מכירה, מוצר, סכום |
| 2 | צור **תשלום** (1039) — אחד לכל איבר ב-`payments`: סכום, אמצעי, מספר תשלומים, סטטוס |
| 3 | צור **תיעוד תשלום** (1018) — אחד לכל תאריך ב-`installment_dates`: `pcfPaymentDate`, `pcfPaymentType`, `pcfPayedIncludingVAT` = סכום ÷ מספר תשלומים, `pcfPaymetStatus = 3` |
| 4 | `pcfCreatedByPersonOrSystem = 2` (מערכת) בכל הרשומות |
| 5 | חיבור לאוטומציות הפוסט-רכישה הקיימות — **אותו מסלול בדיוק כמו אשראי** |

🚫 **חוק:** הנציג לעולם לא יוצר הזמנה ידנית.
🔴 **דרישה מפורשת:** `whatsapp_group_id` **יכול להגיע `null`** (מאז ההחלטה שהפכה את `pcfWhatsAppToRef` מחסימה לאזהרה). **התרחיש חייב לדלג על שיוך הקבוצה או לפתוח משימה לנציגה — ולא להיכשל.** [chat_11]
✅ **דפוס אבטחה לחיקוי:** בכפתור התשלום הידני **לא נחשף שום טוקן** — כל הקריאות דרך `/api/record/...` בסשן של המשתמשת. **זה התקן לכל ווידג'ט עתידי בפיירברי.**

---

## WF-03 · צירוף לקבוצת WhatsApp
**טריגר:** אוטומציית Fireberry על **שינוי סטטוס הזמנה → "נסגרה"** כאשר המוצר = בוסט → webhook ל-n8n. [chat_20, החלטה 1]

### 🔴 חוסם קריטי — כשל שקט
**המפרט קורא `1068.pcfGroupid`, אבל הכתיבה בפועל עוברת ל-`1068.pcfGreenApiId` ו-`pcfGroupid` נשאר ריק במפורש.**
**התוצאה: WF-03 יקרא שדה ריק וייכשל בשקט — בלי שגיאה, פשוט לא יצרף אף לקוחה לקבוצה.** [chat_15]

> ⚠️ **סתירה פתוחה — `pcfGroupid` מול `pcfGreenApiId` ב-1068**
> **עמדה א' [chat_20, 26/07/2026]:** מייק אישר מפורשות ("הראשון") — **`pcfGreenApiId` הוא הקנוני**, `pcfGroupid` לא בשימוש: לא לכתוב, לא לקרוא, לא למחוק.
> **עמדה ב' [chat_22]:** מייק אמר **"לא יודע — צריך לבדוק"**, ומפרט WF-03 עדיין מפנה ל-`pcfGroupid`.
> **בעלים: מייק.**
> 🔎 **בדיקת אימות בת 5 דקות:** לפתוח **2–3 מתוך 8 הרשומות** באובייקט 1068 ולראות **באיזה שדה יושב הפורמט `1203630xxxxx@g.us`**. זה מכריע את הכל.
> 🛑 **עד ההכרעה — WF-03 חסום. אין להפעיל את WF-BOOST-B לפני שהמפרט מעודכן.**

**חוסם נוסף:** `pcfGreenApiId` **ריק בכל 8 הרשומות** ואין דרך למלא אותו בלי `idInstance` + `apiTokenInstance` של GreenAPI. [chat_14]

**ענף בוסט** (המוצר = בוסט):
1. מצא מחזור בוסט פעיל: `1007` where `pcfStatus = 1` **וגם** `pcfStartSaleDate ≤ היום < pcfEndSaleDate`
2. **0 תוצאות** → לא לצרף, לפתוח משימה לעודד + התראה. **>1** → קח את `pcfStartDate` הקרוב ביותר. **לעולם לא לנחש.**
3. קרא `1068.pcfGreenApiId` דרך lookup מ-1007 ~~`1007.pcfEventLink`~~ ⛔ מיושן (הוחלף 03/08/2026) — `pcfEventLink` יוצא מהתמונה, מקור האמת עבר ל-1068
4. GreenAPI → `addGroupParticipant`
5. `addParticipant: false` → שליחת `pcfGroupLink` בהודעה פרטית + סימון "צורפה בקישור". **אסור כישלון שקט**
6. עדכן `4.pcfCourse` = GUID המחזור · `1051.pcfBoostLinked` = GUID המחזור (אם קיים ליווי)

**ענף מסלול** (`= פרימיום`):
1. קרא `4.pcfWhatsAppToRef` (הקבוצה שהנציג בחר) → `1068`
2. קרא **`1068.pcfGreenApiId`** ~~`1068.pcfGroupid`~~ ⛔ מיושן (הוחלף 03/08/2026)
3. צרף לקבוצה
4. עדכן `1051.pcfActualGroup` + `1.pcfWhatsAppReferans`
5. `1051.ownerid` = מנהלת הקבוצה (`1068.ownerid`) — **קבוצה↔מנטורית חד-חד-ערכי**

### מימוש n8n — `Boost - Join WhatsApp Group` (11 nodes)

| Node | תפקיד |
|---|---|
| **N1** | Webhook POST — טלפון, `accountid`, `opportunityid`, `productid`. 🛑 להחזיר **Production URL** |
| **N2** | Code — נרמול טלפון: הסרת תווים לא-ספרתיים · `0` מוביל → `972` · אין קידומת → הוסף `972` · פלט `chatId = "<digits>@c.us"` · **לא 12–13 ספרות → שגיאה מפורשת** |
| **N3** | HTTP — `POST https://api.fireberry.com/api/v3/query`, objectType **1007**, filter AND: `pcfStatus eq 1` + `pcfStartSaleDate le today` + `pcfEndSaleDate gt today`, orderBy `pcfStartDate asc`, pageSize 5 (`gt` = לא כולל יום הסיום) |
| **N4** | IF — 0 מחזורים → משימה לעודד + עצירה. 1+ → הראשון (המוקדם) |
| **N5** | HTTP — קריאת רשומת **1068** לפי GUID מה-lookup → `pcfGreenApiId` + `pcfGroupLink`. ריק → משימה לעודד + עצירה |
| **N6** | HTTP — בדיקת כפילות על **1005** (לקוחה + מחזור). קיים → **עצירה שקטה** |
| **N7** | HTTP — `addGroupParticipant` · `groupId` (`@g.us`) + `participantChatId` (`@c.us`) |
| **N8** | IF `addParticipant` — true → N9 · false → `sendMessage` בפרטי עם `pcfGroupLink` + סימון "צורפה בקישור" → N9 |
| **N9** | HTTP — יצירת רשומת **1005**: לקוחה · מחזור · תהליך מכירה · מוצר · סטטוס נרשם · `pcfCreatedByPersonOrSystem = 2` |
| **N10** | HTTP — `4.pcfCourse` = GUID המחזור · שאילתה על 1051 לפי הלקוחה → `1051.pcfBoostLinked`. לא קיים → **דלג בלי שגיאה** |
| **N11** | Error Trigger + `continueOnFail` על HTTP חיצוני → משימה ב-Fireberry · השהיה קצרה בין קריאות (rate limit) |

**מגבלות GreenAPI שאומתו (chat_20):**
- ❌ **אין צירוף דרך קישור הזמנה** — סגור סופית. הקישור הוא לצירוף עצמי בלבד
- ❌ **אי אפשר לשלוח הזמנה לקבוצה דרך API** — רק מהמכשיר. ה-fallback היחיד: שליחת הקישור כהודעת טקסט
- ⚠️ `getGroupData` מחזיר 200 עם `groupInviteLink` **ריק** אם המספר המחובר אינו אדמין
- ⚠️ `addParticipant: false` הוא תרחיש **צפוי בפועל** — הגדרות פרטיות. ב-50–120 נשים למחזור זה יקרה
- גילוי קבוצות: `getContacts` → פילטר `id` שמסתיים ב-`@g.us` (אנשי קשר = `@c.us`). **אין endpoint "תביא את כל הקבוצות"**
- קריאה ראשונה חובה: `getStateInstance` → חייב `authorized`
- **idInstance:** `710701674604` ⚠️ **לא אומת** (מייק מסר "שם Instance", לא idInstance) · `apiUrl` לא ודאי — לנסות `https://api.green-api.com` וגם `https://7107.api.greenapi.com`
- **המספר המחובר הוא אדמין בכל קבוצות הבוסט** (אישור מייק)
- ⚠️ **`apiTokenInstance` נחשף בצ'אט — לרוטציה**

⚠️ **סתירה פתוחה — גורל אובייקט 1005 (הרשמה לבוסט):**
**[chat_20, 26/07 18:23–19:15Z]** 1005 הוא אובייקט ההרשמה, **יש להפעילו** (הוא "מנוטרל"), N6/N9 כותבים אליו · **[chat_25]** מייק ענה "תשאיר מחזור בוסט" — **דו-משמעי** · **[chat_27, 22-23/07]** 1005 **הופל** לטובת one-to-many. בנוסף `4.pcfBoostLinked` הוא lookup ל-1005 ולכן **זומבי**.
**בעלים: מייק.** 🛑 **להקפיא כל בנייה שנשענת על 1005 עד הכרעה חד-משמעית.**
⚠️ **מפת השדות של 1005 לא קיימת באף מסמך תיעוד** — דורשת discovery בזמן ריצה.

---

## WF-04 · קבוצה מלאה (אופציונלי)
**טריגר:** תזמון יומי / אחרי WF-03.
1. לכל `1068` — חשב תפוסה מול קיבולת
2. **IF ≥ 90%** → התראה לעודד (WhatsApp/משימה)
3. **IF = 100%** → צור קבוצה חדשה ב-GreenAPI · הוסף עודד + מנטורית כמנהלים · צור רשומת `1068` · עדכן את שדה הקבוצה במוצר

> שחר: *"הכי רחוק שאני מוכן ללכת זה התראה ב-90%. אני לא בונה על אף אחד."*

⚠️ **חוסם:** `pcfCapacity` / `pcfCurrentCount` **ריקים ב-6 מתוך 8 הקבוצות** — בלי קיבולת אין מה לחשב.

---

## WF-05 · הודעת Welcome
**טריגר:** סטטוס הזמנה → שולמה (אחרי WF-03).
1. Query Product → `pcfProductType`, `name`
2. **Switch לפי מוצר** → בחר תבנית הודעה
3. **CloudChat** → שלח בפרטי ~~GreenAPI/בוט~~ ⛔ מיושן (הוחלף 03/08/2026) — GreenAPI ירד מהתמונה לשליחת הודעות
4. סמן `1051.pcfWelcomeSent` = now ← **מונע כפל שליחה**

**דינמי:** שם הלקוחה · שם הקבוצה · שם המנטורית · לינק לקבוצה
**סטטי:** חוברת · הקלטות — **חייבים לשבת במקום שהצוות יכול לערוך** (אובייקט תבניות או Data Store), **לא hardcoded**.

---

## WF-06 · תפריט — משימה + שליחה בכפתור
**חלק א' — טריגר: יצירת תהליך ליווי**
→ צור משימה למנטורית (`1051.ownerid`): "בחרי תפריט ללקוחה X", יעד = +יומיים

**חלק ב' — טריגר: כפתור "שלח תפריט" במסך הליווי**
1. Query `1059` (הקצאת תפריט) לפי `pcfFollowupProcess`
2. קרא `1059.pcfMenu` → `1060` → `name` + `pcfMenuUrl`
3. **IF** הקישור ריק → החזר שגיאה "העלי קודם את התפריט המותאם"
4. CloudChat → פלואו `menu_sending` + **פרמטרים דינמיים: `diet_menu_name` + `diet_menu_link`**
5. עדכן `1059.pcfSentDate` + `pcfMenuMsgSent` + `pcfMenuStatus = 2`

**חלק ג' — סטטוס חריג**
תזמון יומי: ליווי פעיל שעברו >2 ימים מהרכישה ואין `pcfSentDate` → **דגל לדשבורד**

🔴 **תלות חוסמת — שיתוף דרייב:** `1060.pcfMenuUrl` מכיל **לינקי דרייב גולמיים** `https://drive.google.com/file/d/{id}/view`. **כל קובץ חייב הרשאת "כל מי שיש לו הקישור – צופה", אחרת השליחה מצליחה והלקוחה מקבלת שגיאת הרשאה.** 14 קובצי התפריטים משותפים היום ברמת התיקייה בלבד, בבעלות `revitalfh@gmail.com`. [chat_14]

⚠️ **סתירה פתוחה — דגל השליחה:** דגל מניעת הכפל מתפצל בין **`1059.pcfSentDate`** (date, לדוחות) ל-**`1059.pcfMenuMsgSent`** (dateTime, חותמת מדויקת). **לא הוכרע מי המקור הקנוני ⇒ סיכון כפל שליחה.** [chat_22]
**בעלים: מייק.** 🔎 **ההכרעה = בדיקת 5 רשומות 1059 ב-UI.** 🛑 **WF-06 חסום עד ההכרעה.**

---

## WF-06B · שליחת תפריטים מתוזמנת (חדש)
**סטטוס:** ❌ לא נבנה. פרומפט v4 סופי ומוכן להרצה. [chat_13, 27/07/2026]
**ערוץ:** **CloudChat** — n8n **לא בונה את נוסח ההודעה**. הנוסח והתבנית חיים בפלואו `menu_sending`; n8n רק מזין שדות ויורה פלואו.

| שלב | תוכן |
|---|---|
| **0** | curl אימות: האם `/api/query` תומך בפרמטר `query`. **דיווח לפני המשך** |
| **1** | Set node `CONFIG`: `CC_BASE`, `CC_FLOW_NS=f201689s4391449`, `CC_FIELD_NAME=diet_menu_name`, `CC_FIELD_LINK=diet_menu_link`. **מפתחות רק ב-n8n Credentials (Header Auth), אפס hardcoded** |
| **2** | Schedule Trigger · `Asia/Jerusalem` · **שתי חוקיות Cron:** `0,30 8-19 * * 0-4` ו-`0 20 * * 0-4` (0=ראשון · שישי ושבת מוחרגים · 08:00–20:00 כל חצי שעה) |
| **3** | `POST /api/query` על **1059**, `page_size: 200`, שדות מפורשים |
| **4** | Code "סינון מועמדים": `pcfMenuStatus = 5` (מוכן לשליחה) + `pcfScheduledSendAt` בעבר + `pcfMenuMsgSent` **ריק** + `pcfMenu` **לא ריק**. מערך ריק → **סיום שקט** |
| **5** | העשרה מ-**1060** ומ-**1** לפי GUID. קטלוג התפריטים נמשך פעם אחת למפה בזיכרון |
| **6** | **Split In Batches = 1**, סדרתי, בלי מקביליות — כדי שכל שליחה תיחתם מיד |
| **7** | CloudChat, שלוש קריאות: **7א** `GET /subscriber/get-info-by-user-id?user_id={טלפון 972}` (fallback `/subscribers`) + בדיקת `allow_send_message` · **7ב** `PUT /subscriber/set-user-fields-by-name` · **7ג** `POST /subscriber/send-sub-flow`. **אימות `status: ok` בגוף, לא רק HTTP 200** |
| **8** | **PUT חתימה** ל-1059: `{"pcfMenuStatus":2, "pcfMenuMsgSent":"<ISO>", "pcfSentDate":"<YYYY-MM-DD>"}`. **כישלון → עצור את ה-workflow והתרע** — רשומה שנשלחה ולא נחתמה תישלח שוב |
| **9** | `continueOnFail` ברמת node · **הודעת סיכום שגיאות אחת בסוף ריצה** עם מזהי רשומות + סיבה + שלב (7א-לא-קיימת / 7א-מחוץ-לחלון / 7ב / 7ג / 8) · "Save failed executions" פעיל |

**נרמול טלפון:** הסר כל תו שאינו ספרה → מתחיל ב-0 → החלף ל-`972` → כבר 972 → השאר. **פחות מ-11 ספרות = לא שולחים**, סימון שגיאה, המשך לבא בתור.

### 🔴 גוצ'ה קריטית — חלון 24 השעות
`send-sub-flow` ללקוחה **מחוץ לחלון 24 השעות לא יגיע** אלא אם הצומת הראשון בפלואו הוא **תבנית מאושרת**. הקריאה מחזירה `ok`, הרשומה נחתמת כ"נשלח" ב-Fireberry, **והלקוחה לא רואה כלום.** תפריט נשלח יומיים אחרי רכישה ⇒ **רוב הלקוחות מחוץ לחלון.**
**מיטיגציה:** בדיקת `allow_send_message` ב-7א. `false` → **לא לשלוח ולא לחתום**, לסמן "מחוץ לחלון 24 שעות". חתימה שם = שקר ברשומה.
⛔ **תבנית מטא ייעודית לתפריט — בוטלה בהחלטת מייק.** קלוד התריע שזה יוצר תרחיש כשל שקט. פתרון חלופי שהוצע: תבנית מאושרת קיימת בתחילת `menu_sending` (למשל `send_form_after_sign_template_v2`).

### CloudChat — פרטי חשבון ו-endpoints מאומתים
| פרט | ערך |
|---|---|
| team id | **205651** · bot id **f201689** |
| Base URL | `https://console.thecloud.chat/api` · אימות `Authorization: Bearer <key>` |
| מלאי | **351** שדות משתמש · **139** תת-פלואוז · **77** תבניות WhatsApp |
| שדות שנוצרו | `diet_menu_name` = `f201689v15923499` · `diet_menu_link` = `f201689v15923501` |
| פלואו יעד | `menu_sending` = **`f201689s4391449`** — 🔴 **טרם חובר לשדות (ידני בממשק)** |
| דפוס אב לחיקוי | `document_link_after_sign` (`f201689v15852771`) · `send_form_after_sign_flow` (`f201689s4376835`) |

⛔ **מגבלת API קריטית:** **אין endpoint לעריכת תוכן פלואו.** קיימים רק `subflows` (רשימה) ו-`delete-sub-flow`. **כל בנייה/עריכה של פלואו היא ידנית בממשק.**
⚠️ **מפתח CloudChat נחשף בצ'אט — לרוטציה.**

**חוסמים ייחודיים:** מי כותב את `pcfScheduledSendAt`? היום **אין תהליך שממלא אותו** ⇒ ה-workflow ימצא **אפס רשומות**. · האם למלא רטרואקטיבית בהקצאות ישנות? · לאן שולחים את הודעת סיכום השגיאות?

---

## WF-07 · טופס סטטוס שבועי
**טריגר:** Cron — **כל יום ראשון 09:00** (Asia/Jerusalem)

> ⚠️ **סתירה פתוחה — יום השקילה.**
> **עמדה א' [chat_08, ClickUp משימה 13.1 + התרחיש בפועל]:** הועבר במפורש מחמישי ל-**ראשון 09:00**, וכך התרחיש רץ.
> **עמדה ב' [chat_08, chat_10]:** **המצגת שהוצגה ללקוחה אומרת חמישי**, וגם ציר הזמן של סער וינברג מציב את כל 6 השקילות בימי חמישי.
> **הסתירה מופיעה בארבעה נכסים:** מסמך זה · `04_LIVE_MAKE.md` · המצגת · תוכנית דאטת ההדגמה.
> **בעלים: מייק (בתיאום רויטל). חובה הכרעה לפני פגישת הלקוחה.**

| שלב | פעולה |
|---|---|
| 1 | Query `1051` where `pcfStatus = 2` AND `pcfEstimatedEndDate ≥ today` |
| 2 | לכל אחת → קרא טלפון מ-`1` |
| 3 | CloudChat → הודעה + לינק לטופס עם `process_id` מוזרק ב-query string |
| 4 | סמן שנשלח (מונע כפל) |

**קליטת התשובה (webhook):**
1. `parseNumber` על המשקל
2. **קרא `1051.pcfCurrentWeight` לפני העדכון** (צריך את הערך הקודם לדלתא)
3. צור `1069` — משקל · הערות לקוחה · **הערות מנטורית** (`pcfMentorNotes` ✅) · תאריך · קישורים: `pcfFollowupProcess`, `pcfaccountid`, מכירה, **הזמנה** (`pcfcrmorderid` ✅), מנטורית
4. חשב `שינוי ממשקל התחלה` = `pcfInitialWeight − משקל חדש` (**חיובי = ירידה**)
5. עדכן `1051.pcfCurrentWeight`, `pcfLastWeighDate`, `pcfWeighCount`

🔴 **תיקון:** ~~`pcfCreatedByPersonOrSystem = 2` (מערכת)~~ ⛔ מיושן (הוחלף 03/08/2026) — **על אובייקט 1069 הערך `2` = אנושי.** כשהיעד הוא 1069 יש לכתוב **`1`**. [chat_22]

**פולו-אפ:** Cron ראשון 20:00 — מי שלא ענתה → הודעה שנייה.

✅ **נסגר 26/07/2026:** `pcfMentorNotes` ו-`pcfcrmorderid` נוצרו על `1069`.
ℹ️ **1069 (טופס שקילה) = אוטומציה 11** בחשבון החי. [chat_25]

---

## WF-08 · תזכורות פגישת מכירה
> 🔴 **תיקון פלטפורמה:** ~~טריגר: Cron כל 15 דקות ב-n8n~~ ⛔ מיושן (הוחלף 03/08/2026).
> **WF-08 מומש בפועל ב-Make.com**, לא ב-n8n — תרחיש **"Meetings Reminders - חיפוש פגישות למחר"**, נבנה 27/07/2026 ע"י מייק.
> **המפרט כאן הופך למפרט התייחסות בלבד.** ראה `04_LIVE_MAKE.md`.

**סטטוס:** ✅ נבנה · ❌ **לא הופעל ולא תוזמן** (נשאר במצב Run once ידני) · תזמון מיועד **Daily 08:30 Asia/Jerusalem** — לא נשמר בבלופרינט, הגדרה ידנית.

**מבנה התרחיש (Make):** HTTP `POST /api/v3/query` על אובייקט **6** → Iterator → SetVariables → parseNumber → CreateJSON → CloudChat New Subscriber → Get Contact Info → **Send Flow** → PUT סימון `pcfZoomReminderDay` → Sleep 2s (הגנת rate limit).

| תזמון | תנאי | פעולה |
|---|---|---|
| יום לפני | `pcfZoomReminderDay` ריק | שלח + בקש אישור הגעה · סמן |
| באותו יום | **רק אם סטטוס אישור ≠ אישרה** | שלח · סמן |
| 30 דק' לפני | `pcfZoomReminder30` ריק | שלח · סמן |

**החלטות מפתח (chat_10):** שדה הלקוחה = **`pcfClient`** (לא `objectid`) · **שליפה מקושרת בקריאה אחת:** `pcfClient_accountname`, `pcfClient_telephone1`, `pcfClient_emailaddress1` · סינון `statuscode in [3, 7]` · **פורמט זמן UTC מפורש:** `formatDate(parseDate(...); "HH:mm"; "UTC")` · חוזה `data_params` לקלאודצ'אט: `param1`=שם · `param2`=שעה HH:mm · `param3`=לינק זום · `param4`=תאריך DD/MM/YYYY.

✅ **נסגר 26/07/2026:** `pcfArrivalConfirmation` נוצר על `6` (1=אישרה · 2=לא אישרה · 3=לא ענתה) — **שונה מהגעה בפועל**, ולא `statuscode` ערך 7. **מייק אישר את ההחלטה.** [chat_22]
❌ **לא אומת:** שהערכים המקושרים באמת חוזרים מלאים — פגישת הבדיקה נוצרה בלי `pcfClient` וכל השדות המקושרים חזרו `null`. **זה "רגע האמת" הפתוח.**
⚠️ מתייחס לפגישות עם רויטל. צפייה בזום נמדדת בנפרד ב-`1051.pcfWhatchedZoom`.
**קיים:** `pcfZoomLink`, `pcfZoomMsgSent`, `pcfZoomReminderDay`, `pcfZoomReminder30`.
🔴 **נכס מזוהם:** `Meetings_Reminders_FIXED_blueprint.json` מכיל את טוקן Fireberry מוטמע **בשני מודולים (1 ו-6)**. ⚠️ **נחשף בצ'אט — לרוטציה.**

---

## WF-09 · תזכורות וובינר
**טריגר:** Cron — יום רביעי
- **בוקר האירוע** · **שעה לפני** · **5 דקות לפני**
- נמענים: כל מי שבמחזור הבוסט הפעיל
- לינק: `1032.pcfEventLink` (האירוע) או לינק הזום הסטטי ~~`1007.pcfEventLink`~~ ⛔ מיושן (הוחלף 03/08/2026) — ריק ב-22/74 המחזורים
- שדה סימון נפרד לכל תזכורת

---

## WF-10 · הזמנה לוובינר
> ⤳ **הוחלף במימוש בפועל ע"י `Boost · הזמנה לוובינר ביום 3 (6.6)` + `6.6a`.** ראה להלן.

**טריגר:** Cron יומי
1. מצא מחזורי בוסט ש-`today = pcfStartDate + 3 ימים` ~~+ 3 ימים מהרכישה~~ ⛔ מיושן — **3 ימים מתחילת המחזור, לא מהרכישה**
2. לכל הליוויים המקושרים (`1051.pcfBoostLinked`) → הודעה **פרטית** עם הזמנה
3. צור רשומת `1030` (הרשמה לאירוע) מקושרת ל-`1032` ← **הבסיס לדשבורד הוובינר**
4. סמן שנשלח

🔴 **`1030` ו-`1032` ריקים לחלוטין.** בלי יצירת הרשומות אין דשבורד וובינר בכלל.
⚠️ **תזמון:** מחזור נפתח ביום ראשון → יום 3 = **יום רביעי** ⇒ ההזמנה יוצאת **בבוקר יום הוובינר עצמו**.

---

## WF-11 · פולו-אפ לפגישה
**טריגר:** Cron — יום חמישי 12:00 (למחרת הוובינר)
1. מצא מי השתתפה בוובינר ולא קבעה פגישה
2. הודעת פולו-אפ **+ מתנה/בונוס טיזר מרויטל**
3. אם יש נתוני נוכחות → הודעות נפרדות לנכחה / לא נכחה

---

## WF-12 · ריטיינר / חידוש
**טריגר:** כפתור "מכירה" במסך הליווי → דף סליקה משולם → **ללא חתימה**

**אחרי התשלום:**
| שלב | פעולה |
|---|---|
| 1 | צור הזמנה + תשלום + תיעודי תשלום (כמו WF-02) |
| 2 | **IF** מוצר = שימור **AND** קיים ליווי קודם (פעיל או לא) → |
| 3 | `1051.pcfEstimatedEndDate = 2099-12-31` ← **זקיף "פתוח"** |
| 4 | `1051.pcfCurrentProgram = 3 (מתקדמים)` או `4 (המשך)` |
| 5 | `1051.pcfStatus` **נשאר 2 (ליווי בתהליך)** — מעבר תוכנית ≠ שינוי סטטוס |
| 6 | עדכן `pcfContinuationStatus` = `4` (בחרה להמשיך) |

✅ **נסגר 26/07/2026:** `pcfContinuationStatus` נוצר על `1051` — `1` בוצעה שיחה · `2` פולו-אפ · `3` בחרה לא להמשיך · `4` בחרה להמשיך.
**מבצע: תמיד המנטורית המשויכת** — אין צורך בשדה "מי ביצע".
🐞 **שדה זומבי:** `pcfsystemfield102` בתווית "תוכנית נוכחית (לא בשימוש)" — **אל תיגע ואל תקרא ממנו.** השדה הנכון: `pcfCurrentProgram`.

> למה 2099: *"אני רוצה משהו שאומר 'לא מילאנו' — תאריך לא הגיוני. ואז אוטומציות הבקרה ימשיכו בשוטף, הן לא יעצרו."*

---

## WF-13 · משימה שבועיים לפני סיום ליווי
**טריגר:** Cron יומי
1. Query `1051` where `pcfStatus = 2` AND `pcfEstimatedEndDate = today + 14`
2. **סנן החוצה** `pcfEstimatedEndDate = 2099-12-31` (ריטיינרים)
3. צור משימה למנטורית: "הצעת המשך ל-X"
4. סמן (מונע כפל)

---

## WF-14 · כתיבה כפולה לשני חשבונות Fireberry
**טריגר:** תהליך קליטת הליד המרכזי

1. משתנה גלובלי `WRITE_TO_OLD_CRM` = true/false
2. **IF true** → כתוב גם ל-CRM הישן (טוקן נפרד)
3. תמיד → כתוב ל-CRM החדש
4. אותו דפוס לעדכוני הצ'אטבוט חזרה

**בעת המעבר:** להפוך ל-false. **בלי לגעת בלוגיקה.**
**שיפורים לשלב בדרך:** טיפול בכפילויות · פתיחת רישומים שיווקיים · UTM.
⛔ **פתוח:** האם תרחיש `Main Leads CRM` עובר לבנייה מחדש ב-n8n? המיפוי ב-chat_05 הוא בסיס מוכן לספק. ⚠️ **סתירה במספר התרחיש: `9339860` [chat_05] מול `6600296` [chat_04]** — לאמת בממשק Make ולקבע מספר אחד. **בעלים: מייק.**

---

## WF-15 · תזכורות וובינר — החלפת הגרסה הישנה
בונים ב-n8n ומכבים את `Webinar Reminders - Old CRM`.
**`Webinar Reminders (New CRM)` ב-Make (27 מודולים) — מעולם לא רץ. משמש כמפרט התנהגות בלבד. לסמן deprecated.**

---

## WF-16 · מסלול מת — סיום בוסט ללא המרה
**סטטוס:** ✅ נבנה · ✅ בשרת · ❌ כבוי · `DRY_RUN=true` · **עודכן 02/08/2026**.
**שם ב-n8n:** `WF-16 · מסלול מת — סיום בוסט ללא המרה`
**Settings:** Timezone `Asia/Jerusalem` · Error Workflow `WF16ErrHandler01` · Save failed executions = on
**Trigger:** Schedule · Cron `0 10 * * *`
**מקור:** ClickUp `86cawzkk4` — "🟡 6.8 · מסלול מת — סיימה בוסט ולא המירה" (parent `86cawzg61`)

### קונפיג (`Set: Config`)
```
DRY_RUN = true
GRACE_DAYS = 3
LOOKBACK_DAYS = 45        (רשת ביטחון לימים שה-workflow לא רץ)
MAX_PER_RUN = 25
FOLLOWUP_DAYS = 2
EXCLUDED_PRODUCT_IDS = [<GUID בוסט>, <GUID הרצאת אורח>]
FALLBACK_OWNER_ID = <GUID משתמש>
FIREBERRY_BASE = https://api.fireberry.com
TZ = Asia/Jerusalem
```

### שרשרת ה-nodes
| Node | פעולה |
|---|---|
| N1 | Schedule Trigger `0 10 * * *` |
| N2 | Set — `Config` |
| N3 | HTTP — `Query Cohorts` · `POST /api/v3/query` · `objectType: 1007` · שדות `customobject1007id, name, pcfEndDate, pcfStatus` · פילטר `pcfEndDate between [today-LOOKBACK_DAYS, today-GRACE_DAYS]` |
| N4 | Code — `Eligible Cohorts` — מסנן החוצה "התבטל" **לפי התווית בעברית** (picklist חוזר כתווית). בונה Map `cohortId → {name, endDate}` |
| N5 | HTTP — `Query Coaching Candidates` · `objectType: 1051` · שדות `customobject1051id, name, pcfaccountid, pcfSale, pcfBoostLinked, ownerid` · פילטר AND: `pcfCurrentProgram eq 1` + `pcfStatus eq-in [1,2]` + `pcfBoostLinked is-not-null` · פגינציה עד `isLastPage` |
| N6 | Code — `Join & Filter` — התאמה **לפי GUID** של `pcfBoostLinked` מול ה-Map |
| N7 | IF — `Any Candidates?` → אפס = NoOp וסיום שקט (המצב הצפוי ברוב הימים) |
| N8 | HTTP — `Query Opportunities` · `objectType: 4` · צ'אנקים של 50 `accountid` · שדות `opportunityid, accountid, ownerid, pcfProduct, statuscode` · פילטר `statuscode eq-in [10, 11]` |
| N9 | Code — **`Conversion Guard`** — עסקה אחת לפחות עם `pcfProduct` שאינו ב-`EXCLUDED_PRODUCT_IDS` → **המירה, זרוק החוצה**. אחרת: `repOwnerId` = `ownerid` של עסקת הבוסט → fallback `1051.ownerid`; `opportunityId` = `1051.pcfSale`. **אין `repOwnerId` → דלג ורשום ל-Warnings (לא ליצור משימה יתומה)** |
| N10 | IF — `DRY_RUN?` → true: `Code: Report` (שם לקוחה · מחזור · תאריך סיום · נציג · מזהה ליווי · לינק עמוק) **אפס כתיבות** |
| N11 | Limit — `MAX_PER_RUN` = 25 |
| N12 | Loop Over Items (batch size 1) |
| N12a | HTTP — **`Create Task` (קודם!)** `POST /api/record/10` · Retry 3 × השהיה 2 שניות |
| N12b | HTTP — **`Update Coaching Status` (רק אחרי)** `PUT /api/record/1051/{{coachingId}}` → `{"pcfStatus": 8, "pcfStatusUpdateTime": "<ISO now>"}` |
| N13 | Code — `Summary`: נסרקו / הומרו והוחרגו / משימות נוצרו / נכשלו / Warnings |

🔴 **סדר קריטי ולא הפיך:** יצירת המשימה (N12a) **קודם**, עדכון `pcfStatus=8` (N12b) **אחר כך**. `pcfStatus=8` הוא מפתח האידמפוטנטיות — ברגע שנכתב, הרשומה נושרת מהשאילתה לנצח. סטטוס-קודם + כשל במשימה = **כשל שקט**. משימה-קודם + כשל בסטטוס = משימה כפולה — מעצבן, גלוי, הפיך. **בחירה מודעת בכפילות גלויה על פני אובדן שקט.**

### גוף יצירת המשימה
```json
{
  "subject": "מסלול מת — סיימה בוסט ולא המירה: {{שם הלקוחה}}",
  "description": "לקוחה: {{שם}}\nסיימה מחזור בוסט: {{cohortName}} (סיום {{cohortEndDate}})\nלא נרשמה למסלול ליווי.\n\nפעולה נדרשת: שיחת פולואפ.\nלאחר השיחה — עדכון סטטוס תהליך המכירה ידנית.\n\nתהליך ליווי: https://app.fireberry.com/app/record/1051/{{coachingId}}\nתהליך מכירה: https://app.fireberry.com/app/record/4/{{opportunityId}}\nלקוחה: https://app.fireberry.com/app/record/1/{{accountId}}",
  "ownerid": "{{repOwnerId}}",
  "pcfClient": "{{accountId}}",
  "pcfSale": "{{opportunityId}}",
  "objectid": "{{accountId}}",
  "scheduledend": "{{מחר 10:00 ISO}}",
  "reminderdate": "{{מחר 10:00 ISO}}",
  "prioritycode": 5,
  "tasktypecode": 5,
  "statuscode": 1,
  "pcfCreatedByPersonOrSystem": 2
}
```

> ⚠️ **סתירה פתוחה — שלושה פרטים בין שתי גרסאות הבריף:**
> | נושא | גרסה 1 (19:50) | גרסה 2 (20:17) |
> |---|---|---|
> | `prioritycode` | **4** (בינונית) | **5** (גבוהה) |
> | נוסח `subject` | `פולו-אפ בוסט — {שם} לא המירה` | `מסלול מת — סיימה בוסט ולא המירה: {שם}` |
> | החרגות מוצר | `BOOST_PRODUCT_ID` **יחיד** | `EXCLUDED_PRODUCT_IDS` — **שניים** (בוסט + הרצאת אורח) |
> **גרסה 2 מאוחרת ומדויקת יותר**, אך `prioritycode` והנוסח **טעונים הכרעת מייק/רויטל.** [chat_16, 26/07/2026]

### עדכון 02/08/2026
✅ **WF-16 עודכן ותואם:** לקוחה עם **תהליך מכירה פתוח שמצביע על עסקת בוסט כבר לא תסומן "לא המירה"**. [chat_03]

### החלטות נעולות
- **אפס שדות דגל חדשים** — האידמפוטנטיות נשענת רק על `pcfStatus=8`
- **לא נוגעים ב-`4.statuscode`** — סגירת העסקה = החלטת נציג, ידנית, אחרי השיחה. *(טעון אישור מייק)*
- **הקישור לרשומת הליווי = לינק עמוק ב-`description`** — לאובייקט 10 אין lookup ל-1051
- **לא מסננים מחזורים לפי `pcfStatus`** — סינון **לפי תאריך בלבד**, ורק `5`=התבטל מוחרג. הסינון לפי תאריך **מחסן** את ה-workflow מבאג הסטטוסים במקום לרשת אותו
- **רימרקטינג מחוץ לטווח לחלוטין** — אפס הודעות יוצאות ללקוחה (בקשת מייק המפורשת)
- **`GRACE_DAYS = 3`** — 🟡 **פתוח, הכרעת רויטל.** ערך נמוך מדי יסמן "מתה" לקוחה באמצע תהליך מכירה פעיל

### 6 בדיקות קבלה
1. DRY_RUN — כל שורה מאומתת ידנית ב-CRM
2. **בדיקת שלילה — הכי חשובה:** לקוחה שכן קנתה מסלול **לא** מופיעה בדוח. מופיעה → `Conversion Guard` שבור, **עצור הכול**
3. הרצה חיה על פריט אחד (`MAX_PER_RUN=1`, `DRY_RUN=false`)
4. אידמפוטנטיות — הרצה חוזרת מיידית לא מייצרת משימה שנייה
5. ריצה ריקה — סיום שקט בלי שגיאה
6. כשל מכוון — נטרול יצירת המשימה → הסטטוס **לא** מתעדכן

### וריאנט B — אם אין רשומות `1051` עם `pcfBoostLinked`
מחליפים עוגן מ-`1051` ל-**תהליך מכירה (`4`)**:
1. Query `4` where `pcfCourse` = מזהה המחזור AND `pcfProduct` = GUID הבוסט AND `statuscode = 11`
2. לכל אחת — Query `4` נוסף לפי `accountid`: יש `statuscode=11` על מוצר שאינו בוסט? → skip
3. **צור רק את המשימה**
4. עדכן `1051` **רק אם קיימת** רשומת ליווי. אין רשומה → **לא יוצרים אחת**
> **אל תבנה את וריאנט B ואת המסלול הראשי במקביל. אחד. לפי מה שהדאטה אמרה.**

🔴 **החוסם האמיתי:** **תרחיש התשלום של הבוסט טרם נבנה.** מי שיוצר את רשומת הליווי עם `pcfBoostLinked` ו-`pcfCurrentProgram=1` — זה התרחיש הזה. בלעדיו **WF-16 ירוץ ויחזיר אפס.** `pcfCurrentProgram` מתעדכן ל-`2` בתרחיש התשלום שנמצא **בהקפאה (אשכול A)**.
💡 **פוטנציאל עסקי:** זו הקבוצה הכי גדולה במערכת — **50–120 נשים בשבוע** שנחשפו למותג, שילמו כסף, ולא המירו.
🔴 **סיכון הריצה הראשונה:** עלולה לתפוס backlog של כל מי שאי פעם סיימה בוסט ולא המירה. **תקדים: 525 אירועים תקועים בתורי וובהוקים ב-Make.** DRY_RUN → מייק קורא → רשימה > 25 → **לא הופכים ל-false** עד הכרעה.

**change-log לאליאל:** `[1051] pcfStatus | קריאה + כתיבת ערך 8 בלבד | ללא שינוי מבני — לא נוצרו/נמחקו שדות`

---

## WF-17 · מכירת ליווי לאחר בוסט
**סטטוס:** ✅ בנוי ועומד ב-n8n · **id `e30cNECSoBNJDQcf`** · **19 נודים** · ❌ **כבוי** · `DRY_RUN=true`. [chat_03, 02/08/2026]
**קונבנציות שאומצו מ-WF-16:** בלוק `Config` · `DRY_RUN` · מטפל שגיאות **`WF16ErrHandler01`** · קרדנציאל **`Revital Yaish new CRM`**.

### ההכרעות שננעלו
| נושא | הכרעה |
|---|---|
| טריגר | **רביעי 20:00**, Asia/Jerusalem |
| מחזור יעד | `pcfEndDate` = **הראשון הקרוב** (היום + 4) |
| מקור הסינון | **תהליך מכירה (4)** לפי `pcfCourse` — לא דרך תהליך ליווי |
| פסילה | כל לקוחה עם תהליך ליווי שאינו "בוסט" |
| מוצר חדש | **תוכנית ליווי 12 שבועות** · GUID **`d9e842a7-f284-4596-bb61-4d2a350eebb5`** · **3,601.69 ₪ לפני מע"מ** |
| נציג | אותו `ownerid` כמו בעסקת הבוסט |
| פולואפ | חמישי 10:00 → `pcfFollowUpTime` |
| שאלון | `pcfFormLink` + `pcfFormFilled` מתהליך הבוסט |
| שם הרשומה | `מכירת ליווי לאחר בוסט - {שם לקוחה}` |
| אב | `pcfParentSale` = מזהה תהליך הבוסט |
| הוכחת רכישת בוסט | סטטוס **"עסקה נסגרה בהצלחה"** (`statuscode = 11`) |

### ארכיטקטורה ומנגנוני הגנה
- **4 שערי סינון עצמאיים**, כל אחד עוצר בנפרד. **כל כשל טכני → skip מפורש, לא יצירה.** *"ספק לא מייצר רשומות."*
- **הגנת כפילויות בעלות אפס:** התהליך החדש נושא את **אותו `pcfCourse`**, לכן חוזר באותה שאילתה — מזהים מי כבר יש לו "בן" דרך **`pcfParentSale`**. ריצה כפולה / ידנית / השלמה — **לא ייצרו כפילות.**
- **כלל השאלון:** **קיום `pcfFormLink` גובר על התווית.** יש לינק → "מילאה שאלון" = כן. אי-התאמה מדווחת ברשימה נפרדת בדוח הריצה, **אך רשומת המקור לא מתוקנת בשקט.**

### דפוס קלאודצ'אט מאומת (הועתק מ-WF-06B שרץ חי)
| שלב | קריאה |
|---|---|
| 7א | `GET /subscriber/get-info-by-user-id?user_id={טלפון 972}` (fallback `/subscribers`) |
| 7ב | `PUT /subscriber/set-user-fields-by-name` |
| 7ג | `POST /subscriber/send-sub-flow` |
> **שתי הגנות חובה בכל WF שמדבר עם קלאודצ'אט:** (א) **בדיקת חלון 24 השעות**; (ב) **אימות שקלאודצ'אט קלט את השדות** — הוא מחזיר `status: ok` גם כשהוא **מתעלם בשקט** משדה שלא קיים.

### ⛔ פתוח — שכבת קלאודצ'אט (WF-17 שלב ב')
1. **`sub_flow_ns` של התבנית** — ⚠️ בסיכום 27/07 נרשם שה-ns של הבוסט והליווי השתבשו. **אין לסמוך על ns ישן בלי אימות מול טל.**
2. **מיפוי `data_param1/2/3/4`** — זמין: שם לקוחה, שם מנטורית, מחזור בוסט, קבוצת וואטסאפ, שם מוצר.
3. 🔴 **שדה דגל "נשלח" על תהליך מכירה (אובייקט 4) — לא קיים וחייב להיווצר.** בלעדיו **כל הרצה תשלח שוב לאותה לקוחה. חוסם אמיתי — אין הפעלה בלעדיו.**

> ⚠️ **סתירה פתוחה — תזמון השליחה.**
> **עמדה א' (הנוכחית במימוש):** טריגר יחיד **רביעי 20:00**.
> **עמדה ב' (המלצת תכן שהועלתה ולא הוכרעה):** **לא לשלוח ברביעי 20:00 — זה זמן הוובינר.** ליצור את הרשומות ברביעי בערב, ולשלוח **בסריקה נפרדת בחמישי בבוקר** לפי "תאריך לפולואפ = היום". מנתק את שתי השכבות — אם קלאודצ'אט נופל, הרשומות עדיין נוצרו.
> **בעלים: מייק.** [chat_03, 02/08/2026]

### ✅ אומת מול ה-API החי (02/08/2026)
1. **כל 74 מחזורי הבוסט מסתיימים ביום ראשון.** ⚠️ **סתירה פתוחה: 74 מחזורים [chat_03, 02/08] מול 22 מחזורים [chat_20, chat_21, 26/07]** בכל שאר המסמכים. **בעלים: מייק** — כנראה גדל בעקבות backfill, לאמת.
2. **176 תהליכי מכירה בחשבון** — מתוכם **רק 2** משויכים למחזור בוסט, ו-**175 מתוך 176 בסטטוס "ליד חדש"**. זה מאגר לידים, לא לקוחות משלמות.
   > 🔴 **סיכון מתועד:** מי שיעשה backfill ל-`pcfCourse` **בלי מסנן תשלום** — יפתח **175 תהליכי מכירה במכה.**
3. **אין בחשבון אף עסקה בסטטוס "עסקה נסגרה בהצלחה"** → הפלואו יחזיר **אפס** עד שענף התשלום יעלה. **אפס ≠ תקלה.**
4. **מוצר "מסלול ליווי 12 חודשים" לא קיים.** נסגר על **12 שבועות**.
5. 🐛 **באג דאטה לתיקון ידני:** מחזור **"בוסט · אוגוסט 02" נושא תאריך התחלה 09.08** — כפילות של "בוסט · אוגוסט 09" שמסומן "התבטל". כל עוד זה כך, **מחזור 02.08 לא מתפקד.**
6. 🐛 **באג דאטה נוסף:** לעסקה של **מיכאל אוקס** יש `pcfFormLink` אבל מסומן שלא מולא שאלון.

**הערה מסחרית (לרויטל, לא טכנית):** קיבוע הבוסט להוביל אוטומטית ל-12 שבועות **הופך את 12 למוצר ברירת המחדל.** 12 שבועות = 4,250 ₪ מול 3,850 ₪ ל-8 שבועות.

---

## Boost · אירוע וובינר — יצירה אוטומטית (6.6a)
**n8n · 7 nodes · כל יום 08:30** · ✅ JSON נבנה, ❌ לא הועלה · צמתי כתיבה `disabled: true`
**מקור:** ClickUp `86cawzkfn`

```
כל יום ב-08:30 → קונפיגורציה → שליפת מחזורי בוסט (1007)
   → מחזורים ביום 3 → שליפת אירוע → מה חסר → יצירת אירוע וובינר [disabled]
```
- **"מחזורים ביום 3":** `pcfStartDate + 3 ימים = היום` (שעון ישראל), **מדלג על סטטוס `5` (התבטל)**. אין מחזור מתאים → נעצר בשקט. תקין ברוב ימות השבוע.
- **"מה חסר":** מדלג על מחזור שכבר יש לו אירוע (`pcfExternalSoftwareID1`). **אידמפוטנטי.**
- **גוף יצירת אירוע (objectType 1032):** `name` = `וובינר בוסט — <שם המחזור>` · `pcfEventType = 1` · `pcfStatus = 1` · `pcfStartDate` = יום 3 בשעה מהקונפיגורציה · `pcfEndDate` = +90 דק' · `pcfEventLink` = לינק הזום · `pcfProduct` + `ownerid` + `pcfRegistrationQuota` מהמחזור · **`pcfExternalSoftwareID1` = מזהה המחזור** (מפתח האידמפוטנטיות — אין lookup בין 1032 ל-1007) · `pcfCreatedByPersonOrSystem = 2`

---

## Boost · הזמנה לוובינר ביום 3 (6.6)
**n8n · 10 nodes · כל יום 09:00** · ✅ JSON נבנה, ❌ לא הועלה · צמתי כתיבה `disabled: true`

```
כל יום ב-09:00 → קונפיגורציה → שליפת מחזורי בוסט → מחזורים ביום 3
   → שליפת אירוע הוובינר → שליפת ליוויים במחזור → שליפת הרשמות קיימות
   → מי צריכה לקבל הזמנה → שליחת הודעה בפרטי [disabled] → יצירת הרשמה לאירוע [disabled]
```
- **שליפת ליוויים:** objectType `1051` מסונן `pcfBoostLinked eq-in [מזהי המחזורים]`, כולל השדה המקושר **`pcfaccountid_telephone1`**
- **"מי צריכה לקבל הזמנה" (Code):** מדלג על סטטוס ליווי **`5`** (בתהליך ביטול) ו-**`6`** (בוטל) · מדלג על מי שכבר יש לה הרשמה לאותו אירוע · **נרמול טלפון** → `972XXXXXXXXX@c.us` · **זורק שגיאה** אם יש מחזור ביום 3 **בלי אירוע** (סימן ש-6.6a לא רץ) · **זורק שגיאה** אם יש מחזור ביום 3 עם **אפס ליוויים מקושרים** (50–60 נשים במחזור, אפס = תקלה)
- **גוף יצירת הרשמה (objectType 1030):** `pcfEventId` · `pcfAccountid` · `pcfSale` · `pcfProductId` · `pcfStatus = 1` (נרשם) · `pcfWebinarLink` · `pcfCreatedByPersonOrSystem = 2`
- **דגל "נשלחה הזמנה" = רשומת ההרשמה (1030) עצמה.** אין שדה דגל חדש

🔴 **אזהרת תפעול קריטית:** **חובה להדליק את שני צמתי הכתיבה ביחד.** הדלקת "יצירת הרשמה לאירוע" **בלי** "שליחת הודעה בפרטי" יוצרת רשומות 1030 בלי שאף אחת קיבלה הזמנה — **והדדופ יחסום שליחה לנצח.**
⚠️ **כשל שליחה באמצע ריצה:** הצומת עוצר. מי שקיבלה — נרשמה; השאר לא יקבלו וגם **לא ינסו שוב מחר** (המחזור כבר לא ביום 3). execution אדום. `continue on error` **לא נכנס לגרסה הזו** — נדחה בכוונה.

**נוסח ההודעה (טיוטה — טעון אישור רויטל/טל):**
```
היי [שם] 💜
הערב ([תאריך]) מחכה לך המפגש עם רויטל.
במפגש הזה לומדים לעומק את השיטה — וזה בדיוק מה שהופך את הירידה לתוצאה שנשארת לאורך זמן.

מצטרפות כאן: [לינק]

שווה להיות שם. נתראה 🙌
```

**Payload של צומת השליחה (נייטרלי לערוץ):**
```json
{ "type": "boost_webinar_invite", "phone": "972501234567",
  "chatId": "972501234567@c.us", "message": "...",
  "customerName": "...", "accountId": "...", "cohortId": "...", "eventId": "..." }
```
🔴 **בבחירה ב-GreenAPI ישיר הטוקן נכנס ל-URL של הצומת ⇒ ל-export של ה-workflow ⇒ אסור לשלוח את ה-JSON בצ'אט או בוואטסאפ.** ההמלצה: **webhook של הבוט של טל.**

---

## Boost · אינדיקטור בריאות למחזור (6.7)
**n8n · 8 nodes · כל יום 08:00** · ✅ JSON נבנה, ❌ לא הועלה · צמתי כתיבה `disabled: true`
**מקור:** ClickUp `86cawzkh4`

```
כל יום ב-08:00 → קונפיגורציה → שליפת מחזורי בוסט → שליפת אירועי וובינר
   → שליפת משימות בקרה קיימות → בדיקת בריאות → יצירת משימת בקרה [disabled] → התראה לעודד [disabled]
```

**בדיקות על מחזור שנפתח בעוד יומיים:**
| בדיקה | חומרה |
|---|---|
| `pcfEventLink` ריק / הסטטוס אינו "פתוח להרשמה" | 🔴 |
| אין מוצר מקושר | 🔴 |
| אין אחראי מחזור | 🟠 |
| אין מכסת נרשמים | 🟠 |
| תאריך סיום ריק או אינו תחילה+7 | 🟠 |
| תאריך סיום מכירה אינו תאריך פתיחת המחזור | 🟠 |

**בדיקות רוחב (תוספת מעבר לניסוח המשימה):**
| בדיקה | חומרה | למה |
|---|---|---|
| מחזור שכבר עבר את יום 3 ואין לו רשומת אירוע | 🔴 | תופס כשל של 6.6a/6.6 |
| שני מחזורים עם אותו תאריך פתיחה | 🟠 | כפילות ידנית |
| **אין אף מחזור שנפתח ב-14 הימים הקרובים** | 🔴 | **הכשל שהבדיקה המקורית מפספסת** — "48 שעות לפני" עוברת בהצלחה כשאין בכלל מחזור |

- **דדופ:** משימה פתוחה עם `pcfExternalSoftwareID1 = BOOST-HEALTH-<מזהה מחזור>` → לא נפתחת שנייה. סוגרים בלי לתקן → **נפתחת מחדש למחרת. בכוונה.**
- **גוף משימה (objectType 10):** `subject` עם 🔴/🟠 · `description` עם רשימת התקלות + לינק עמוק · `ownerid` = עודד · `prioritycode` = `6` לאדום / `5` לכתום · `statuscode = 1` · `scheduledend` = היום

> העיקרון שהוגדר במשימה: *"את עובדת עם בן אדם טיפש ולא אחראי... כל תהליך חייב לעבוד גם כשמישהו שכח."*
> ב-26.07 שני התנאים נכשלו בפועל: **22 מחזורים בלי לינק קבוצה, 21 עם סטטוס הפוך. אף אחד לא ידע.**

### 🔴 סדר הרצה מחייב לשלושת ה-workflows: **C → A → B** (6.7 → 6.6a → 6.6)
6.7 קורא-בלבד ויגיד כמה מחזורים שבורים היום. 6.6 רק אחרי ש-6.6a יצר אירוע אמיתי.

### טבלת קונפיגורציה (הצומת `קונפיגורציה` — אין ערכים קשיחים בשום מקום אחר)
| Workflow | מפתח | ערך |
|---|---|---|
| 6.6a, 6.6 | `ZOOM_LINK` | לינק הזום של הוובינר — **חסר** |
| 6.6a | `WEBINAR_TIME` | ברירת מחדל `20:00` — לאשר מול רויטל |
| 6.6a | `WEBINAR_DURATION_MIN` | `90` |
| 6.6 | `WEBINAR_OFFSET_DAYS` | `3` (שינוי ל-`2` = הזמנה יום מראש) |
| 6.6 | `MESSAGE_TEMPLATE` | טיוטה — **טעונה אישור רויטל/טל** |
| 6.6 | `SEND_WEBHOOK` | ריק — תלוי בהכרעת ערוץ |
| 6.7 | `TASK_OWNER_ID` | GUID של עודד ב-Fireberry (אובייקט `9`) — **חסר** |
| 6.7 | `ALERT_WEBHOOK` + `ALERT_PHONE` | ערוץ ההתראה + הטלפון של עודד — **חסר** |
| 6.7 | `LOOKAHEAD_DAYS` / `HORIZON_DAYS` | `2` / `14` |

**תלות חוסמת:** כל שלושת המפרטים מותנים בהצלחת **3 קריאות האימות ל-`/api/v3/query`** בחשבון החי. **אם v3 לא נענה — שכתוב ל-Legacy.**
⚠️ **מלכודת n8n:** **IF+Merge נשבר בשקט** — זו הסיבה לפיצול ל-3 workflows ליניאריים במקום אחד מותנה.

---

## Boost Cohort — Daily Status Sync
**n8n · 7 nodes · כל יום 00:01** · ✅ JSON נבנה (`/home/claude/boost_cohort_status_sync.n8n.json`) · ❌ לא הועלה · `active: false` · **node 7 `disabled: true`**
**מקור:** ClickUp `86cawzk99` (משימה 6.2)

| # | שם ה-node (עברית, מדויק) | type | הערות |
|:--:|---|---|---|
| 1 | `כל יום ב-00:01` | `n8n-nodes-base.scheduleTrigger` v1.2 | `triggerAtHour:0`, `triggerAtMinute:1` |
| 2 | `שליפת מחזורי בוסט` | `n8n-nodes-base.httpRequest` v4.2 | POST `/api/query` · `retryOnFail` · `maxTries 3` · `timeout 30000` |
| 3 | `פיצול לרשומות` | `n8n-nodes-base.splitOut` v1 | `fieldToSplitOut: "data.Data"` |
| 4 | `נרמול נתונים` | `n8n-nodes-base.set` v3.4 | 7 שדות: `id, name, cur, today, d_startsale, d_boundary, d_end` |
| 5 | `חישוב סטטוס יעד` | `n8n-nodes-base.set` v3.4 | שדה יחיד `desired`, `includeOtherFields: true` |
| 6 | `רק מה שהשתנה` | `n8n-nodes-base.filter` v2.2 | `desired > 0` AND `desired ≠ cur` |
| 7 | `עדכון סטטוס ב-Fireberry` | `n8n-nodes-base.httpRequest` v4.2 | **`disabled: true`** · batching 10/1000ms |

**הגדרות:** `settings.timezone = "Asia/Jerusalem"` · `executionOrder: v1` · `executionTimeout: 300` · `active: false` · מזהי nodes `a1000000-0000-4000-8000-00000000000{1..7}`

**ביטוי המיפוי של `cur` (node 4)** — מכסה גם מספר וגם תווית עברית:
```
{{ ({'1':1,'פתוח להרשמה':1,'3':3,'קורס פעיל':3,'4':4,'הסתיים':4,'5':5,'התבטל':5,'6':6,'ממתין לפתיחה':6})[String($json.pcfStatus).trim()] ?? 0 }}
```

**ביטוי `desired` (node 5):**
```
{{ $json.cur === 5 ? 5 : (($json.d_startsale === 0 || $json.d_boundary === 0 || $json.d_end === 0) ? 0 : ($json.today < $json.d_startsale ? 6 : ($json.today < $json.d_boundary ? 1 : ($json.today < $json.d_end ? 3 : 4)))) }}
```

**טבלת ההחלטה:**
| תנאי | סטטוס יעד | ערך |
|---|---|:--:|
| `cur = 5` (התבטל) | **לא נוגעים. אף פעם** | 5 (נחסם) |
| חסר אחד מ-3 התאריכים | מדלגים | 0 (נחסם) |
| `today < d_startsale` | ממתין לפתיחה | **6** |
| `today < d_boundary` | פתוח להרשמה | **1** |
| `today < d_end` | קורס פעיל | **3** |
| אחרת | הסתיים | **4** |

**Endpoints:**
```
POST https://api.fireberry.com/api/query
{"objecttype":1007,"page_size":500,"page_number":1,
 "fields":"customobject1007id,name,pcfStatus,pcfStartSaleDate,pcfEndSaleDate,pcfStartDate,pcfEndDate"}

PUT https://api.fireberry.com/api/record/1007/{customobject1007id}
{ "pcfStatus": <desired>, "pcfStatusUpdateTime": "<ISO>" }
```

**החלטות נעולות:** 4 חלונות סטטוס (לא 3 — נוסף ערך 6) · **גבול-המכירה = `pcfEndSaleDate`** · ביום סיום המכירה עצמו כבר "קורס פעיל" · ביום סיום הבוסט עצמו כבר "הסתיים" · **תיקון רטרואקטיבי מאושר** (~21 רשומות בריצה הראשונה) · **dry-run כפוי דרך node 7 מנוטרל** · **מבצע: מייק.**

**ערכי `1007.pcfStatus` — מלא ומאומת:** `1`=פתוח להרשמה · `3`=קורס פעיל · `4`=הסתיים · `5`=התבטל · `6`=ממתין לפתיחה. ⚠️ **אין ערך "סגור". ערך 2 לא קיים.** (ClickUp טעה בזה.)

**אינווריאנט מבני — בדיקת הבריאות הזולה ביותר:**
> **בכל רגע נתון: בדיוק מחזור אחד "פתוח להרשמה", ובדיוק אחד "קורס פעיל".**
> אם אחרי ריצה יש שניים פתוחים או אפס — יש תאריך שגוי באחת מהרשומות.

**דאטה חיה שאומתה (26.07.2026, מחזור `בוסט · אוגוסט 02` GUID `ec34c207-237c-408d-8b30-6b6637dd1ff5`):** `pcfStartSaleDate` 26/07 · `pcfEndSaleDate` **02/08** · `pcfStartDate` **02/08** · `pcfEndDate` 09/08.
🔑 **`pcfEndSaleDate` = `pcfStartDate` — אותו יום בדיוק. אין יום ביניים.**

🔴 **חוסם תפעולי:** **כיבוי האוטומציה הפנימית ב-Fireberry + שינוי שמה ל-`לא פעיל — <שם קודם>`** (מייק). אחרת **שתי מערכות נלחמות על `pcfStatus`**. מגבלת האוטומציות הפנימיות ב-Fireberry: **50, כמעט מוצתה** — הכיבוי משחרר תא.
🟡 **טעון אימות בהרצה:** `data.Data` נגזר מתיעוד ולא מהרצה חיה · אם `cur` יוצא `0` בכל השורות — התווית מ-Fireberry שונה מהמיפוי.
✅ **סטטוס באג הסטטוסים:** ב-27/07 בבוקר **נצפה בפיד של מחזור בוסט מעבר תקין "ממתין לפתיחה ← נפתח להרשמה" שבוצע ע"י אוטומציית n8n** — **הבאג כנראה כבר טופל. טעון אימות.** אין למחוק את אזהרת הסינון-לפי-תאריך עד שיאומת. [chat_19]

---

## WF-BOOST-A · Backfill רשומות מחזורי בוסט
**n8n · הרצה חד-פעמית** · ❌ לא נבנה (brief v2 מוכן: `CC_BRIEF_boost_cycles_whatsapp_groups_n8n_v2.md`)

- יצירת **~55 רשומות** מחזור בוסט עד **26.12.2027**
- **אידמפוטנטי**, עם `DRY_RUN` לאישור מייק לפני כתיבה
- **פורמט שם הרשומה: `בוסט · <חודש> <DD>`** (למשל `בוסט · אוגוסט 02`) — **לא** `בוסט - {תאריך}` כפי שביקש ClickUp. **לאמת מול רשומה קיימת אחת לפני יצירת 55.**
- `pcfEndSaleDate = pcfStartDate` (הוכרע ע"י מייק)

**למה לא פותחים 75 קבוצות במכה (הדחייה של המשימה המקורית):**
1. יצירת קבוצות בכמות ברצף מול מספר טלפון אחד = **דגל אדום של WhatsApp** → סיכון חסימת המספר של רויטל/הבוט
2. קבוצה שנפתחת היום עבור מחזור של דצמבר 2027 יושבת ריקה 18 חודש

🔴 **חוסם — `system_name` של שדה ה-lookup 1007→1068:** מייק טוען שהשדה קיים; קלוד **לא מצא אותו** לא ב-`03_LIVE_FIREBERRY.md` (36 שדות) ולא ב-`template_snapshot.json`. שני ה-lookups היחידים ב-1007 הם **עצמיים** (`customobject1007id`, `pcfparentcoursenum`). ה-lookups ל-1068 שכן קיימים יושבים על **אובייקטים אחרים**: `pcfWhatsAppToRef` (על 4) ו-`pcfWhatsAppReferans` (על 1).
**בלי ה-`system_name` המדויק — WF-BOOST-B כותב לשדה לא קיים, מקבל 400, וכל השאילתה נופלת.**
**בעלים: מייק** — אימות בן 10 שניות: `GET https://api.fireberry.com/metadata/records/1007/fields`, לחפש `fieldObjectType` שמצביע ל-**1068**. **🛑 שלב 0.1 חוסם: אם לא נמצא — עוצרים ומדווחים, לא מנחשים.**

---

## WF-BOOST-B · יצירת קבוצת WhatsApp + כתיבת המזהה
**n8n · מתוזמן יומי, קבוצה אחת ביום לכל היותר** · ❌ לא נבנה

- נפתחת **~4 שבועות לפני תחילת המכירה** של כל מחזור
- כותב את המזהה ל-**`1068.pcfGreenApiId`**. **`pcfGroupid` נשאר ריק במפורש, בלי שכפול ערך** [chat_15, הכרעת מייק]
- **בונוס:** סוגר אגב אורחא את **`pcfEventLink` הריק ב-22 המחזורים הקיימים**

🛑 **תנאי מקדים להפעלה: עדכון מפרט WF-03 לקריאה מ-`pcfGreenApiId`.** אחרת הצירוף לקבוצה נשאר שבור בשקט.
⚠️ **`allowParticipantsAddMembers` (חסימת צירוף חברים ע"י משתתפים) — לא אומת שנתמך ב-GreenAPI.** ה-endpoint לנעילת קבוצה הוא **beta** עם שני פרמטרים מתועדים בלבד. אם לא נתמך — ההגדרה נשארת **ידנית בכל קבוצה** (75 פעולות ידניות). **לבדוק מול התיעוד הרשמי, לא להמציא פרמטר. בעלים: מייק/עודד/טל.**
⚠️ **חוסם:** URL ציבורי **קבוע** לתמונת פרופיל הקבוצה — הקישור שהיה ב-ClickUp פג תוקף תוך דקות.

---

## ⛔ WF-P1 / WF-P2 — בוטלו
`WF-P1 pay load` ו-`WF-P2 pay checkout` **נבנו ונמחקו באותה שיחה** [chat_12, 27/07/2026].
**החלטה: מסך הסליקה = client-side JS בווידג'ט HTML אחד בפיירברי. אין n8n בנתיב הזה.**
> מייק: *"חיפוש המוצרים, החתימה ובניית הקישור — הכל ישירות בקוד ב-JS, בלי לצאת החוצה למערכת חיצונית."*

---

## ⚠️ סתירות פתוחות — ריכוז

| # | הסתירה | עמדה א' | עמדה ב' | בעלים | חומרה |
|---|---|---|---|---|---|
| 1 | **`pcfGroupid` מול `pcfGreenApiId` ב-1068** (WF-03) | `pcfGreenApiId` קנוני — אישור מייק "הראשון" [chat_20, 26/07] | מייק: **"לא יודע — צריך לבדוק"**; המפרט עדיין מפנה ל-`pcfGroupid` [chat_22] | **מייק** | 🔴 חוסם — כשל שקט |
| 2 | **יום השקילה** (WF-07) | ראשון 09:00 — ClickUp 13.1 + התרחיש בפועל [chat_08] | חמישי — המצגת ללקוחה + ציר הזמן של סער [chat_08, chat_10] | **מייק/רויטל** | 🔴 מופיע ב-4 נכסים |
| 3 | **גורל אובייקט 1005** | הפעל, הוא אובייקט ההרשמה [chat_20] | הופל לטובת one-to-many [chat_27] / "תשאיר מחזור בוסט" — דו-משמעי [chat_25] | **מייק** | 🔴 מקפיא כל בנייה שנשענת עליו |
| 4 | **דגל שליחת התפריט** (WF-06) | `pcfSentDate` | `pcfMenuMsgSent` [chat_22] | **מייק** | 🔴 סיכון כפל שליחה |
| 5 | **`prioritycode` ב-WF-16** | `4` בינונית (גרסה 19:50) | `5` גבוהה (גרסה 20:17) [chat_16] | **מייק/רויטל** | 🟡 |
| 6 | **`/api/query` מול `/api/v3/query`** | Legacy — מבנה `data.Data[]` [chat_16 v1, chat_21] | v3 — `data[]` שטוח, `_id`, `isLastPage` [chat_17, chat_20] | **Claude Code (שלב 0)** | 🟡 להכריע בשטח |
| 7 | **rate limit של Fireberry** | 100 בקשות/דקה לארגון [chat_20, chat_21] | צינון 10–15 דק' אחרי ~350 קריאות מטא-דאטה [chat_26] | **מייק** | 🟡 ייתכן ששתי מגבלות נפרדות |
| 8 | **תזמון שליחת WF-17** | רביעי 20:00 (המימוש) | יצירה ברביעי + שליחה בסריקה נפרדת בחמישי בבוקר [chat_03] | **מייק** | 🟡 |
| 9 | **מספר מחזורי הבוסט** | 22 [chat_20, chat_21, 26/07] | **74** — אומת מול ה-API [chat_03, 02/08] | **מייק** | 🟡 |
| 10 | **קיום lookup 1007→1068** | מייק: "יש שדה מקושר" | לא נמצא ב-36 השדות ולא ב-snapshot [chat_15] | **מייק** | 🔴 חוסם WF-BOOST-A/B |
| 11 | **מספר תרחיש `Main Leads CRM`** | `9339860` [chat_05] | `6600296` [chat_04] | **מייק** | 🟡 |
| 12 | **שער החתימה — מקור אמת** | `categorycode` (המימוש) [chat_12] | `pcfProductType` (המפרט) | **מייק** | 🟡 מוצר יחליק בפער |

---

## שדות חדשים שצריך ליצור ב-Fireberry

| אובייקט | שדה | סוג | ערכים | בשביל |
|---|---|---|---|---|
| ✅ `4` תהליך מכירה | `pcfSignatureStatus` | picklist | **בוטל** — במקום ערך `4`, ערך `1` עבר רילייבל ל"ממתין לחתימה". 🔴 **אין ערך 4. כל לוגיקה שבודקת 4 שבורה** | WF-01 |
| 🔴 `4` תהליך מכירה | **שדה דגל "נשלח" — חסר וחייב להיווצר** | — | בלעדיו כל הרצה של WF-17 תשלח שוב לאותה לקוחה | **WF-17 — חוסם** |
| ✅ `6` פגישה | `pcfArrivalConfirmation` | picklist | 1 אישרה · 2 לא אישרה · 3 לא ענתה | WF-08 |
| ✅ `1051` תהליך ליווי | `pcfContinuationStatus` | picklist | 1 בוצעה שיחה · 2 פולו-אפ · 3 בחרה לא להמשיך · 4 בחרה להמשיך | WF-12 |
| ✅ `1059` הקצאת תפריט | `pcfMenuStatus` ערך **5** = "מוכן לשליחה" | picklist | ⚠️ **אין לשנות תווית של ערך קיים** — יש אינטגרציות תלויות | WF-06B |
| ✅ `1059` הקצאת תפריט | `pcfScheduledSendAt` | dateTime | תזמון שליחת תפריט | WF-06B |
| ✅ `1069` טופס שקילה | `pcfMentorNotes` | textArea | — | WF-07 |
| ✅ `1069` טופס שקילה | `pcfcrmorderid` | lookup → 13 | — | WF-07 |
| ✅ `1068` קבוצת WhatsApp | `pcfCapacity` · `pcfCurrentCount` · `pcfAvailableSlots` | number | קיימים. `pcfAvailableSlots` = **נוסחה**. ⚠️ **הנתונים ריקים** — קיבולת חסרה ב-6 מתוך 8 קבוצות | WF-04 |
| ⚠️ `1007` מחזור בוסט | lookup → `1068` | lookup | **לא נמצא בתיעוד. ⚠️ סתירה פתוחה #10** | WF-BOOST-A/B |
| ✅ CloudChat | `diet_menu_name` (`f201689v15923499`) · `diet_menu_link` (`f201689v15923501`) | text | נוצרו ואומתו בחשבון החי | WF-06B |

> ✅ **השדות שסומנו ✅ נוצרו ב-26–27/07/2026.** ראה `HANDOFF_שלב2_שדות_2026-07-26.md`.
> ⚠️ שם שדה חדש **חייב** להתחיל ב-`pcf`. `system_name` לעולם לא משתנה אחרי יצירה.
> 🚫 **מחיקות ע"י סוכן אוטונומי אסורות לחלוטין** — גם כשהמשימה מבקשת ניקוי. מחיקה ידנית בלבד, אחרי אישור פר-שדה מאליאל.

---

## מפת קודי אובייקטים (מאוחדת)
`1`=לקוח · `4`=תהליך מכירה · `6`=פגישה · `9`=משתמש · `10`=משימה · `13`=הזמנה · `14`=מוצר · `1002`=רישום שיווקי · `1005`=הרשמה לבוסט (⚠️ מנוטרל, גורל פתוח) · `1007`=מחזור בוסט · `1018`=תיעוד תשלום · `1030`=הרשמה לאירוע · `1032`=אירוע · `1039`=תשלום · `1041`=מפגש · `1051`=תהליך ליווי · `1057`=חג ומועד · `1059`=הקצאת תפריט · `1060`=קטלוג תפריט · `1068`=קבוצת WhatsApp (**אוטומציה 9**) · `1069`=טופס שקילה (**אוטומציה 11**)

---

## 🛑 גבולות — לא לחצות
- 🚫 **לא לגעת ב-Make דרך Claude Code / API.** אף תרחיש, אף וובהוק. **כל תיקון ב-Make הוא ידני של מייק.**
- 🚫 **לא ליצור ולא למחוק שדות ב-Fireberry** בלי אישור מפורש פר-שדה.
- 🚫 **לא ליצור רשומות בדיקה בחשבון החי** בלי אישור ממייק.
- 🚫 **לא לסגור אוטומטית את תהליך המכירה** (`4.statuscode`).
- 🚫 **לא לנחש התנהגות של Fireberry** — עוצרים על הכשלון הראשון, מדווחים endpoint + body + השגיאה המדויקת.
- 🚫 **לא מדפיסים טוקנים ל-stdout. לעולם.**
- 🔴 **אסור להדליק תרחיש כלשהו לפני בדיקת תוכן התור שלו.** תקדים: **525 אירועים תקועים** ב-Make (`My Zoom Webinars` 292 · `Revital 6.7.2026` 179 · `Create Payments Records` 42 · `Welcome To Premium` 6 · `Notifications System` 5; **471 מתוכם מהעולם הישן**). הדלקה עיוורת **תשפוך מאות רשומות למערכת.**
