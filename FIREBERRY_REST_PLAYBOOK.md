# FIREBERRY REST PLAYBOOK — פרויקט רויטל יעיש

> **עודכן 03/08/2026 — סקירת 26 שיחות הפרויקט.** גרסה זו מאחדת ומחליפה את שתי הגרסאות הקודמות של המסמך (141 שורות ו-132 שורות, שתיהן מ-04/06/2026). מסמך זה הוא הגרסה היחידה התקפה.

## Change log

| # | מה שונה | מקור |
|---|---|---|
| 1 | איחוד שתי הגרסאות הכפולות של הפלייבוק למסמך יחיד; הוספת באנר "גרסה יחידה תקפה" | סקירת 26 השיחות |
| 2 | ⚠️ **סתירה פתוחה** — פרק 2א חדש: השוואת `POST /api/query` (Legacy) מול `POST /api/v3/query` זו לצד זו, כולל מבני תשובה שונים | chat_16, chat_17, chat_20, chat_21 |
| 3 | תיעוד ה-gotcha של עטיפה כפולה ב-v3: `{ data: { data: [...] } }` → `{{2.data.data}}` / `{{4.data.data}}` ב-Make | chat_19 |
| 4 | תיעוד ההחלטה העומדת: workflow חדש = `/api/v3/query`, מותנה באימות חי; מפרטים קיימים שנכתבו מול `/api/query` — לבדוק מחדש | chat_17 |
| 5 | ⚠️ **סתירה פתוחה** — פרק 2ב: שתי מגבלות קצב נפרדות (100/דקה לארגון מול צינון 10–15 דק' אחרי ~350 קריאות מטא-דאטה) | chat_20, chat_21, chat_26 |
| 6 | תיקון האימות: header `tokenid` **בלי `Bearer`**, בסיס `https://api.fireberry.com`, region eu2 | chat_15, chat_16, chat_21 |
| 7 | הוספת `GET /metadata/records/{objecttype}/fields` כדרך הריצה לאמת קיום שדה במקום להסתמך על הסנאפשוט | chat_15 |
| 8 | הבהרה מבנית: **חובה / readonly / ברירת מחדל / maxlength אינם נחשפים ב-API כלל** — מייק או ה-UI בלבד; + טבלת ספירות שדות חובה | chat_22 |
| 9 | חיזוק גוצ'ת `"fields":"*"` — נכשלת בשקט, אנמרציה מפורשת חובה, שדה לא מוכר אחד מפיל את כל השאילתה ב-400 | chat_04, chat_12, chat_13, chat_21, chat_24 |
| 10 | פער picklist קריא/כתיב: סינון וכתיבה במספר, תשובה חוזרת בתווית עברית; חובה לתמוך בשלוש הצורות | chat_08, chat_12, chat_13, chat_21 |
| 11 | כלל ברזל ל-lookup: התאמה לפי GUID בלבד; התאמה לפי שם תצוגה מחזירה אפס תוצאות בשקט | chat_08, chat_13 |
| 12 | גוצ'ת ה-403: השוואת שדה מזהה לערך שאינו GUID מפילה את השרת ומחזירה 403 (לא 400); סיבה שנייה — טוקן שגוי | chat_27 |
| 13 | הוספת `/api/record/{objecttype}/{id}` כנתיב in-session בלי טוקן — הדפוס לכפתורים ולווידג'טים בפיירברי | chat_11, chat_12 |
| 14 | חותמות זמן נשמרות כ**שעה מקומית עם סיומת `Z`** — לא UTC אמיתי | סקירת השיחות |
| 15 | כל שדות ה-text הם `maxlength: 200` כברירת מחדל — ערך ארוך יותר מפיל את המודול | chat_04 |
| 16 | ⛔ **אין להפעיל שכפול חשבון פיירברי דרך API** — פעולה בתשלום וידנית | כלל פרויקט |
| 17 | ⛔ אוטומציות פנימיות בפיירברי — תקרה קשיחה של 50, כמעט מוצתה; טריגר "בזמן יחסי" חסום ברישיון הנוכחי → תזמון עובר ל-Make/n8n | סקירת השיחות |
| 18 | הוספת רשימת האופרטורים הנתמכים המלאה, פגינציה (`pageSize` עד 500, ברירת מחדל 50, `isLastPage`), timeout 30 שניות, `408`, ולינק עמוק | chat_16 |
| 19 | הוספת דפוס שדות מקושרים `<lookupfield>_<targetfield>` — מאומת על 1051, תחבירית בלבד על 6 | chat_10, chat_17 |
| 20 | הוספת שאילתת אובייקט 6 המלאה (18 שדות) כדוגמה מוכנה + קודים מאומתים (`statuscode` 3, `activitytypecode` 301, `pcfMeetingPurpose` 2) | chat_10 |
| 21 | הוספת `POST /api/record/{ot}` (יצירה) ו-`PUT /api/record/{ot}/{id}` (עדכון) כדפוסי כתיבה כלליים | chat_10, chat_16, chat_21 |
| 22 | כלל בטיחות: **אפס תוצאות בשקט = התקלה המסוכנת ביותר** — כל שאילתה חדשה נבדקת קודם על דאטה שידוע שקיימת | chat_17, chat_21 |
| 23 | כלל תפעולי: כתיבה בכמות דורשת אישור מייק פר-בלוק; מחיקות ע"י סוכן אוטונומי אסורות לחלוטין | chat_08, chat_18 |
| 24 | דפוס תיוג רשומות הדגמה `pcfExternalSoftwareID1 = DEMO-2607` + שאילתת ניקוי | chat_08 |
| 25 | אזהרה: lookups ב-`template_snapshot.json` עלולים להצביע על שמות אובייקט ישנים — אימות ב-UI לפני הסתמכות | chat_07 |
| 26 | הוספת טבלת `systemfieldtypeid` (GUID→סוג) — הפתרון לכך שאין endpoint שמחזיר סוג שדה | chat_26, chat_27 |
| 27 | הוספת מפת קודי האובייקטים המאוחדת + אזהרת בטיחות: פגישה = **6**, לא 1041 | chat_11, chat_25 |
| 28 | פרק חדש "מתי UI ומתי API" — שלוש גוצ'ות UI (חיפוש prefix, יצירת lookup, אין API ללייאאוט) | chat_18 |
| 29 | פרק חדש "ייבוא CSV" — UTF-8 עם BOM, מסך מיפוי, `ownerid` נקלט לפי שם תצוגה | chat_14 |
| 30 | פרק חדש "פרוטוקול הרצה ראשונה" — `DRY_RUN=true`, `MAX_PER_RUN`, בדיקת שלילה חובה | chat_16 |
| 31 | הוספת דפוס "רשומת הגדרות יחידה" — אובייקט 1036, `name = "1"` | chat_05, chat_27 |
| 32 | הערת אבטחה: ערכי טוקן לעולם לא נכתבים במסמך; ה-`tokenid` של פיירברי ⚠️ נחשף בצ'אט — לרוטציה | chat_03, chat_10, chat_19, chat_25, chat_27 |

---

> **מה זה:** ספר החוקים הטכני לעבודה מול Fireberry דרך ה-REST API.
> כל פרומפט/סשן Claude Code חדש בפרויקט הזה — **קרא את הקובץ הזה קודם.**
> רוב מה שכתוב כאן אומת בפועל מול החשבון החי של רויטל. כל מה שלא אומת — **מסומן במפורש.**
> מיקום: `roital-yaish/FIREBERRY_REST_PLAYBOOK.md`.

> ⚠️ **אזהרת אמינות מקורות:** `template_snapshot.json` הוא ground truth מבני, אבל **שדות lookup בו עלולים להצביע על שמות אובייקט ישנים.** לפני שמסתמכים על יעד של lookup — לאמת ב-UI או ב-metadata החי. (chat_07)

---

## 0. למה REST ולא MCP

ה-MCP של Fireberry **לא חושף** עדכון מטא-דאטה של שדה קיים (שם תצוגה / picklist / default). הוא יודע רק: יצירה (שדה/אובייקט/רשומה), עדכון דאטה של רשומה, וקריאה. בנוסף, ה-MCP **מקצץ פלט ב-~10K טוקנים** — הוא החזיר ~118 שדות ב-Account במקום 130. **לרשימות מלאות ולעדכון שדות — תמיד REST, לא MCP.**

עדכון שדות קיימים נעשה דרך ה-REST API ישירות (HTTP), עם ה-TokenID של רויטל שכבר מוגדר.

---

## 1. כללי הזהב (לא משתנים)

1. **לא מוחקים שדות.** בשום מצב. מחיקה רק באישור מפורש פר-שדה ממייק/אליאל. **מחיקות ע"י סוכן אוטונומי אסורות לחלוטין** — גם כשהמשימה מבקשת "ניקוי". (chat_18)
2. **שם המערכת (`system_name`, עמודה E) לא משתנה לעולם.** אינטגרציות Make קשורות אליו. משנים רק שם תצוגה (label). **`system_name` גם לא ניתן לשינוי אחרי יצירה** — טעות בשם = שדה חדש. (chat_18)
3. **לא נוגעים ב-Make דרך Claude Code / API.** פיתוח חדש = n8n. תחזוקת קיים = Make, ידנית, ע"י מייק בלבד.
4. **לא ממציאים התנהגות.** אם endpoint/body לא עובד — עוצרים על הכשלון הראשון, מדווחים endpoint+body+שגיאה מדויקת, ולא מנחשים את השדה הבא.
5. **חשבון לקוח חי.** עובדים שדה-אחד-בכל-פעם עם אימות חוזר. לא מבצעים יצירה/מחיקה של רשומות דאטה לצורך בדיקה בלי אישור מפורש ממייק.
6. **שדה בסטטוס "בדיקה" / קבוצת "לא בשימוש" — לא נוגעים.**
7. **שדה נוסחה** = רילייבל בלבד בשלב הזה (לא הופכים לנוסחה — Make יחשב). **שדה שיקוף** (`[שיקוף]` / סוג "שיקוף") = לא נוגעים, לא פיזי.
8. **כתיבה בכמות דורשת אישור מייק פר-בלוק.** כתיבות מרובות נחסמו בסשן 27/07 ודרשו אישור מפורש. (chat_08)
9. ⛔ **אין להפעיל שכפול (clone) של חשבון פיירברי דרך API.** השכפול הוא פעולה **בתשלום וידנית** — לעולם לא דרך API.

### שני כללי כתיבה שאסור לפספס

- **picklist נכתב כמספר, לעולם לא כתווית עברית.** `pcfStatus = 2`, לא `"ליווי בתהליך"`.
- **lookup נכתב כ-GUID, לעולם לא כשם תצוגה.**

---

## 2. אימות ובסיס

**בסיס:** `https://api.fireberry.com`
**Region:** eu2.
**אימות:** header בשם **`tokenid`** — **ללא prefix של `Bearer`.** זו טעות נפוצה שמחזירה 401.

```
POST https://api.fireberry.com/api/v3/query
tokenid: <TOKEN>
Content-Type: application/json
```

> 🔴 **הטוקן של פיירברי ⚠️ נחשף בצ'אט — לרוטציה.** ערך הטוקן **לעולם לא נרשם במסמך הזה ולא בשום מסמך אחר.** מקומות שליפה: Fireberry ← הגדרות ← API ← Token ID · משתנה סביבה `FIREBERRY_TOKEN_ID` ב-MCP config · Header Auth ב-n8n · חיבורי Make. אחרי רוטציה חובה לעדכן שלושה מקומות: החיבורים ב-Make, ה-credential ב-n8n, והשדה `pcfPowerlinkToken` ב-1036. תזמון הרוטציה — חלון מתואם, לא "ניצחון מהיר".

**לינק עמוק לרשומה:** `https://app.fireberry.com/app/record/{objectType}/{recordId}`

---

## 2א. ⚠️ סתירה פתוחה — שני נתיבי שאילתה (בעלים: מייק)

בפרויקט קיימים **שני נתיבי שאילתה שונים מהותית**, ששניהם שימשו באותו שבוע. **אין להניח איזה תקף — צריך אימות חי.**

| | **Legacy — `POST /api/query`** | **v3 — `POST /api/v3/query`** |
|---|---|---|
| מקור | chat_12, chat_13, chat_21 | chat_10, chat_16, chat_17, chat_19, chat_20 |
| שם פרמטר האובייקט | `objecttype` (lowercase) | `objectType` (camelCase) |
| `fields` | מחרוזת CSV, **בלי רווחים** | מערך / רשימה מובנית |
| סינון | **אין סינון צד-שרת אמין** — הפתרון שנבחר: למשוך הכל ולסנן באוטומציה. פרמטר `query` מועבר **כמחרוזת**, ותמיכתו **לא אומתה** | `filter` מובנה עם `eq` / `in` / `eq-in` ועוד |
| שדות מקושרים | ❌ אין | ✅ `pcfaccountid_telephone1`, `pcfaccountid_accountname` |
| פגינציה | `page_size` / `page_number` | `pageSize` (עד **500**, ברירת מחדל 50) / `pageNumber` + `isLastPage` |
| מבנה תשובה | `data.Data[]` (D גדולה). ⚠️ נגזר מתיעוד, **לא אומת בהרצה חיה** | `data[]` שטוח, מזהה הרשומה ב-**`_id`** |
| מבנה בפועל ב-Make | עטיפות `__IMTLENGTH__` / `__IMTINDEX__` | 🔴 **עטיפה כפולה:** `{ data: { data: [...] } }` |
| מגבלת קצב מדווחת | ~350 קריאות רצופות → חסימה 10–15 דק' | 100 בקשות/דקה לארגון |

### 🔴 ה-gotcha הקריטי של v3 — עטיפה כפולה

מבנה התשובה הוא `{ data: { data: [ ... ] } }`. לכן **נתיב האיטרטור ב-Make הוא `{{2.data.data}}` / `{{4.data.data}}`** — לא `{{2.data}}`. זו הייתה סיבת התקלה שתוקנה ב-chat_19. כל מודול Make שקורא ל-v3 חייב את הנתיב הכפול.

### ההחלטה העומדת

> **כל workflow חדש נבנה מול `POST /api/v3/query` — מותנה באימות חי בחשבון של רויטל.**
> Legacy `/api/query` נשאר **fallback בלבד**.
> ⚠️ **כל מפרט קיים שנכתב מול `/api/query` חייב בדיקה מחדש לפני שימוש חוזר** — במיוחד מפרטים שמניחים `data.Data[]`.

**שלוש קריאות האימות שחייבות לעבור לפני שסוגרים את הסתירה:**
1. `/api/v3/query` חוזר 200 על אובייקט ידוע.
2. סינון `eq-in` על GUID של שדה lookup מחזיר את הרשומות הנכונות.
3. שדה מקושר (`pcfaccountid_telephone1`) חוזר עם ערך מלא כשה-lookup מאוכלס.

**סטטוס:** לא נסגר. **בעלים: מייק.**

---

## 2ב. ⚠️ סתירה פתוחה — מגבלות קצב (בעלים: מייק)

שתי מגבלות דווחו, ככל הנראה **שתי מגבלות נפרדות ולא סתירה** — אבל אף אחת לא אומתה רשמית:

| מגבלה | מקור | הקשר |
|---|---|---|
| **100 בקשות לדקה לארגון** | chat_20, chat_21 | קריאות תפעוליות (query / record) |
| **צינון 10–15 דקות אחרי ~350 קריאות מטא-דאטה** | chat_26 | סריקות סכימה רחבות (`/metadata/...`) |

**מסקנה תפעולית:** כל סריקת סכימה רחבה חייבת batching ו-caching **מראש**. לא סורקים 839 שדות בלולאה נאיבית.
**סטטוס:** לא הוכרע איזו מגבלה חלה על מה. **בעלים: מייק.**

**עוד מגבלות שאומתו:** timeout של 30 שניות; קוד `408` = "תפשט" — פחות שדות, עמוד קטן יותר.

---

## 3. ה-ENDPOINTS

### קריאת מטא-דאטה (GET)

```
GET /metadata/records/{objectType}/fields          ← כל השדות של האובייקט
GET /metadata/records/{objectType}/fields/{fieldName}   ← שדה בודד
GET /metadata/records/{objectType}/fields/{fieldName}/values  ← ערכי picklist
```

- **זו הדרך בזמן ריצה לאמת ששדה קיים** — במקום לסמוך על `template_snapshot.json`. חיפוש שורה עם `fieldObjectType` שמצביע לאובייקט היעד נותן גם את יעד ה-lookup. (דוגמה שאומתה: 1007 → 1068.)
- objectType = מספר האובייקט (ראה פרק 6).
- **שים לב:** GET על endpoint ה-PUT (`/api/v2/system-field/...`) מחזיר 405 — הוא PUT-only. קריאה תמיד מ-`/metadata/records/`.

> 🔴 **מגבלה מבנית — חובה לדעת:**
> **`mandatory` / `readonly` / `default` / `maxlength` אינם נחשפים ב-API כלל.**
> הם מגיעים **אך ורק** מה-xlsx או מה-UI. כשזה משנה — **שואלים את מייק.**
> ספירות שדות חובה שנספרו ידנית: `1051`=11 · `1`=11 · `4`=9 · `14`=12 · `1039`=12 · `1032`=10 · `1030`=9.

> 🔴 **מגבלה נוספת: ל-Fireberry אין endpoint שמחזיר סוג שדה.** הפתרון התקף — מילון GUID→סוג שנבנה מ-`template_snapshot.json` (פרק 7).

### שאילתה (POST)

```
POST /api/v3/query      ← המומלץ (ראה פרק 2א)
POST /api/query         ← Legacy / fallback
```

### כתיבה (POST / PUT)

```
POST /api/record/{objectType}            ← יצירת רשומה
PUT  /api/record/{objectType}/{recordId} ← עדכון רשומה
```

דוגמאות שאומתו:
- `PUT https://api.fireberry.com/api/record/6/{activityid}` — עדכון פגישה.
- `PUT https://api.fireberry.com/api/record/1007/{customobject1007id}` עם `{ pcfStatus: <n>, pcfStatusUpdateTime: <ISO> }`.

### עדכון מטא-דאטה של שדה קיים (PUT)

```
PUT /api/v2/system-field/{objectType}/{type}/{fieldName}
```
- `{type}` = מחרוזת הסוג ב-URL (ראה פרק 4).
- ה-body תלוי-סוג.
- תשובת הצלחה: HTTP 200 + `success:true`, והשרת מחזיר את אובייקט השדה המלא.

---

## 3א. גוצ'ות שאילתה — קרא לפני שאתה כותב שאילתה

### 🔴 1. `"fields": "*"` נכשל — ובשקט

**אומת פעמיים (chat_13, chat_21, ואושרר ב-chat_24).** חובה למנות שדות מפורשות, מופרדים בפסיק, **בלי רווחים**:

```json
"fields":"customobject1007id,name,pcfStatus,pcfStartSaleDate,pcfEndSaleDate,pcfStartDate,pcfEndDate"
```

**ובנוסף:** **שם שדה אחד לא מוכר מפיל את כל השאילתה ב-HTTP 400** — Fireberry לא מדלג על השדה הבעייתי, הוא מחזיר שגיאה גנרית ומפיל את המודול כולו. לכן הדפוס הבטוח: **שדות ליבה → מזהים → `GET /api/record` לכל רשומה** לשדות שלא אומתו.

> ⚠️ **חריג:** במודול Make של Fireberry, **`fields` ריק = כל השדות** — תקין ועובד. זו התנהגות **שונה** מ-`"fields":"*"` בשאילתת REST ישירה.

### 🔴 2. פער picklist קריא/כתיב — שקט ורצחני

- **שאילתה מחזירה תווית עברית** (`statuscode` → `"פעיל"`).
- **`/api/record` מחזיר מספר** (`1`).
- **כתיבה וסינון צד-שרת דורשים מספר.**
- **אין** שדה `statuscodename` נפרד ב-`/api/query`.

**הכלל:** כל בדיקת picklist חייבת לקבל **שלוש צורות** — מספר / מחרוזת-מספר / תווית. דפוס מומלץ: פונקציית `pick()` שמנרמלת את שלושתן.
**כלל נוסף:** **לעולם לא לסנן על תוצאות שאילתה** בהנחה שהערך מספרי.

### 🔴 3. כלל הברזל של lookup — GUID בלבד

שדה lookup מחזיר שני וריאנטים: `pcfX` (GUID) ו-`pcfXname` (תצוגה).
**תמיד להתאים לפי GUID.** התאמה לפי `name` מחזירה **אפס תוצאות בשקט, בלי שגיאה.**

### 🔴 4. אפס תוצאות בשקט = התקלה המסוכנת ביותר ב-Fireberry

אין הבחנה בין "אין נתונים" ל"הסינון לא נתמך".
**הכלל:** כל שאילתה חדשה נבדקת **קודם על דאטה שידוע שקיימת.** אפס תוצאות = **עצור ודווח**, לא להמשיך.

### 🔴 5. גוצ'ת ה-403

השוואת שדה מזהה (`customobject1036id`, `customobject1039id`) לערך **שאינו GUID** (למשל `1`) **מפילה את שרת פיירברי ומחזירה `[403] RunTimeError, Origin: Fireberry`** — לא 400. השגיאה מטעה ונראית כבעיית הרשאות.

**תיקון:** להשוות לפי `name` (בתבנית תמיד יש רשומה בשם `"1"`), או להדביק GUID מלא. fallback: `00000000-0000-0000-0000-000000000000`.
**סיבה שנייה ל-403 באותו הקשר:** טוקן לא תקין ב-`pcfPowerlinkToken`.

### 6. חותמות זמן

Fireberry שומר **שעה מקומית עם סיומת `Z`** — **לא UTC אמיתי.** אל תמיר. אל תניח offset. השוואות תאריכים בין Fireberry למערכת חיצונית חייבות לקחת את זה בחשבון.

### 7. אורך טקסט

**כל שדות ה-text ב-Fireberry הם `maxlength: 200` כברירת מחדל.** ערך ארוך יותר **מפיל את כל המודול**, לא מקצץ.

### 8. אופרטורים נתמכים

`eq` · `ne` · `lt` · `gt` · `le` · `ge` · `eq-in` · `not-in` · `between` · `is-null` · `is-not-null` · `start-with`

**סמנטיקה שאומתה:** `gt` על תאריך סיום **לא כולל** את יום הסיום. הערך המילולי `"today"` נתמך.
דוגמת AND שאומתה: `pcfStatus eq 1` + `pcfStartSaleDate le today` + `pcfEndSaleDate gt today`, עם `orderBy pcfStartDate asc`.

---

## 3ב. שדות מקושרים (linked fields) — v3 בלבד

דפוס: **`<lookupfield>_<targetfield>`** — שולף שדה מהרשומה המקושרת בלי קריאה שנייה.

| אובייקט | דוגמאות | סטטוס אימות |
|---|---|---|
| 1051 (תהליך ליווי) | `pcfaccountid_accountname`, `pcfaccountid_telephone1` | ✅ מאומת |
| 6 (פגישה) | `pcfClient_accountname`, `pcfClient_telephone1`, `pcfClient_emailaddress1`, `ownerid_fullname` | ⚠️ **תחבירית בלבד** — לא החזיר `400 invalid fields`, אך לא אומת שהערכים חוזרים מלאים כשה-lookup מאוכלס |

**שאילתת אובייקט 6 מלאה (verbatim, מוכנה לשימוש) — 18 שדות:**
```
activityid, subject, scheduledstart, scheduledend, statuscode, activitytypecode,
pcfMeetingPurpose, location, pcfZoomLink, pcfArrivalConfirmation, pcfZoomReminderDay,
pcfClient, pcfClient_accountname, pcfClient_telephone1, pcfClient_emailaddress1,
ownerid, ownerid_fullname, pcfSale
```

**קודים מאומתים באובייקט 6:** `statuscode` **3** = "עתידה להתקיים" · `activitytypecode` **301** = "שיחת מכירה" · `pcfMeetingPurpose` **2** = "עבודה" · `_id` = מפתח הרשומה (`activityid`).

**דפוס שאילתה מוכן — סריקת משתמשים:** אובייקט **9**, `fields = timezone,locale,statuscode`.

**דפוס "רשומת הגדרות יחידה":** Query על אובייקט **1036** עם `name = "1"` — מקור ברירות המחדל הגלובליות בתבנית ויטרו (כולל `pcfPowerlinkToken`).

**תקן כתיבה:** `pcfCreatedByPersonOrSystem = 2` = "נוצר ע"י מערכת" — הערך שכל אוטומציה כותבת ביצירת רשומה. **חריג: באובייקט 1069 הסמנטיקה הפוכה.**

**תיוג רשומות זמניות:** כל רשומת הדגמה מקבלת `pcfExternalSoftwareID1 = DEMO-2607`, ושאילתת הניקוי מסננת על אותו ערך. בלי תיוג — אין ניקוי בטוח.

---

## 3ג. קריאות מתוך ווידג'ט / כפתור בפיירברי — בלי טוקן

ווידג'ט HTML או כפתור בפיירברי קורא ל-API **בנתיב יחסי, בסשן של המשתמשת המחוברת, בלי טוקן כלל:**

```js
fetch(`/api/record/${objectType}/${recordId}`)
fetch('/api/query', { method: 'POST', body: ... })
```

**זה תקן הפרויקט לכל כפתור/ווידג'ט עתידי בפיירברי.** בכפתור התשלום הידני לא נחשף שום טוקן — הארכיטקטורה מכוונת.
**כלל ארכיטקטוני נלווה:** לוגיקת מסך (חיפוש מוצרים, חתימה, בניית קישור) = **client-side JS בתוך פיירברי**. אורקסטרציה ואינטגרציות = n8n/Make.

**זכור את ההבדל:** `/api/query` מחזיר picklist כתווית עברית, `/api/record` מחזיר אותו כמספר.

---

## 4. צורות ה-BODY לפי סוג שדה ⭐ (עדכון מטא-דאטה)

**כלל-על:** ל-PUT **תמיד חייבים לכלול `label`**, גם כשמעדכנים רק default. שליחת `defaultValue` בלי `label` → 400 "Missing property 'label'".

| סוג שדה | `{type}` ב-URL | body לרילייבל | הערות |
|---|---|---|---|
| text | `text` | `{"label":"<יעד>"}` | אומת ✅ |
| emailAddress | `text`* | `{"label":"<יעד>"}` | אומת ✅ — זהה ל-text. *(נשלח דרך משפחת text) |
| lookup | `lookup` | `{"label":"<יעד>","relatedObjectType":<N>}` | אומת ✅ — **חובה** `relatedObjectType`, אחרת 400. ⚠️ ראה כלל הזהב למטה |
| number | `number` | `{"label":"<יעד>"}` | (צפוי כמו text — לאמת בשימוש ראשון) |
| dateTime | `datetime` | `{"label":"<יעד>"}` | endpoint קיים; defaultValue תומך בערכים יחסיים (Now/Today/±ימים) |
| picklist | `picklist` | _(ראה פרק 5)_ | ⛔ ברירת מחדל: ידני ב-UI |

### ⚠️ כלל הזהב ל-lookup ב-PUT

ב-PUT על lookup חייבים `relatedObjectType` — אבל **אך ורק את הערך שקראת מה-`fieldObjectType` של אותו שדה ב-metadata.** לעולם לא ערך מנוחש. ערך שגוי **משנה את יעד הקישור** = נזק מבני שאינטגרציות Make מסתמכות עליו, לא רילייבל הפיך. אחרי PUT — קרא חזרה ואמת ש-`relatedObjectType` שחזר זהה למקור. אם לא — עצור ודווח.

### ברירת מחדל (defaultValue)

```
PUT .../system-field/{objectType}/{type}/{fieldName}
body: {"label":"<label קיים>","defaultValue":"<ערך>"}
```
אומת שהכתיבה מתקבלת (200/success). ⚠️ **מגבלה:** ל-Fireberry **אין endpoint שמחזיר default בקריאה** (text). לכן default הוא תמיד **"נשלח, התקבל, לא אומת בקריאה"** — לא כשל, מגבלת פלטפורמה. אימות אפשרי רק התנהגותית (יצירת רשומה) — **לא עושים את זה על חשבון חי בלי אישור.** מסמנים בקובץ "בוצע (לא אומת)".

---

## 4א. תהליך העבודה לכל שדה

1. קרא מטא-דאטה של השדה (GET) → ודא סוג, label נוכחי, ול-lookup את `fieldObjectType`.
2. בנה body לפי הסוג (פרק 4).
3. PUT.
4. **קרא חזרה ואמת** שה-label נתפס (ול-lookup שגם היעד נשמר).
5. אם 200 ואומת → עדכן בקובץ: עמודה **N**=בוצע, עמודה **P**="רילייבל בוצע דרך REST, אומת". עמודה A לא נוגעים.
6. אם כשל → עצור, דווח endpoint+body+שגיאה, אל תמשיך.

**מיפוי סטטוס פעולה (עמודה H) → מה עושים:**
- שמירה → no-op
- רילייבל → PUT label (פרק 4)
- עריכה → default (פרק 4) ו/או picklist (פרק 5); נוסחה=רילייבל בלבד
- יצירה → MCP `field_create` או REST (שדה חדש, חייב להתחיל ב-pcf)
- בדיקה → לא נוגעים

---

## 5. ערכי PICKLIST — אומת ✅ → המסקנה: ידני ב-UI

עריכת ערכי picklist דרך REST **אפשרית טכנית אבל "מלוכלכת" — לכן ברירת המחדל היא ידני ב-UI.**

**הממצא (אומת בטסט הפיך על pcfGender):**
- קריאת ערכים: `GET /metadata/records/{objectType}/fields/{fieldName}/values` — מחזיר `name`+`value` בלבד, **לא `color`**.
- כתיבת ערכים: `PUT /api/v2/system-field/{objectType}/picklist/{fieldName}` — body: `{"label":"...","options":[{"order","color","label","value"}]}`.
- ⚠️ **ה-gotcha הקריטי:** ה-API **דורש `color` בכתיבה אבל לא מחזיר אותו בקריאה.** לכן כל שכתוב `options` כופה קביעת צבעים מחדש → **הצבעים הקיימים נשרפים.** עריכת picklist אינה הפיכה-מושלמת.
- ה-PUT ל-picklist **אינו partial-update**: `{"label","defaultValue"}` בלי `options` → 400 "Missing property 'options'". כלומר אי אפשר לגעת ב-default של picklist בלי לשכתב את כל הערכים (ולשרוף צבעים).
- ולידציה קודמת למוטציה: body שגוי מחזיר 400 בלי לשנות דאטה (בטוח לנסות).

**הכלל לכל הפרויקט:**
- ✅ REST ל-**labels** (רילייבל) ול-**default של שדות text** — מצוין.
- ⛔ **ערכי picklist + defaults של picklist → ידני ב-UI.** לשדות קריטיים — תמיד ידני.
- לפני נגיעה: **קרא את הערכים הקיימים. אם הם כבר תואמים את האפיון — אל תיגע.** ברוב המקרים בתבנית הם כבר תואמים.

---

## 6. מספרי אובייקטים (objectType) — המפה המאוחדת

| קוד | אובייקט | קוד | אובייקט |
|---|---|---|---|
| **1** | לקוח (Account) | **1018** | תיעוד תשלום |
| **4** | תהליך מכירה | **1030** | הרשמה לאירוע |
| **5** | קריאות שירות | **1032** | אירוע |
| **6** | **פגישה (Activity)** | **1036** | הגדרות (רשומת `name="1"`) |
| **9** | משתמש מערכת (CrmUser) | **1039** | תשלום |
| **10** | משימה | **1041** | מפגש (25 שדות, **לא בשימוש**) |
| **13** | הזמנה | **1051** | תהליך ליווי |
| **14** | מוצר (Product) | **1057** | חג ומועד |
| **100** | שיחות טלפון | **1059** | הקצאת תפריט |
| **1002** | רישום שיווקי | **1060** | קטלוג תפריט |
| **1007** | מחזור בוסט | **1068** | קבוצת WhatsApp |
| **1005** | ⚠️ **גורלו שנוי במחלוקת** — ראה `09_OPEN_QUESTIONS.md`. אין לבנות עליו | **1069** | טופס שקילה |

> 🔴 **אזהרת בטיחות:** **פגישה = 6, לא 1041.**
> `PUT /api/v2/system-field/1041/...` יערוך את **"מפגש"** (אובייקט לא בשימוש) במקום את "פגישה". טעות שקטה ויקרה.

> דרך מהירה לאמת מספר אובייקט: `client.api.metadata.getObjects()` או Object Studio ב-UI.

---

## 7. מילון `systemfieldtypeid` (GUID → סוג שדה)

**למה זה קיים:** ל-Fireberry **אין endpoint שמחזיר סוג שדה.** הפתרון המאושר — מילון GUID→type שנבנה מ-`template_snapshot.json` ופיענח את כל 839 השדות.

| GUID | סוג |
|---|---|
| `a1e7ed6f-5083-477b-b44c-9943a6181359` | טקסט (maxlength 200) |
| `a8fcdf65-91bc-46fd-82f6-1234758345a1` | Lookup (עם `fieldobjecttype`) |
| `b4919f2e-2996-48e4-a03c-ba39fb64386c` | רשימת בחירה (picklist) |
| `6a34bfe3-fece-4da1-9136-a7b1e5ae3319` | מספר |
| `83bf530c-e04c-462b-9ffc-a46f750fc072` | תאריך |
| `ce972d02-5013-46d4-9d1d-f09df1ac346a` | תאריך + שעה |
| `80108f9d-1e75-40fa-9fa9-02be4ddc1da1` | רב-ערכי / קודים |

---

## 8. מתי UI ומתי API

| מקרה | המלצה | נימוק |
|---|---|---|
| רילייבל שדה קיים | **API** | מהיר, בטוח, הפיך |
| default של שדה text | **API** | עובד (לא אומת בקריאה) |
| ערכי picklist / default של picklist | **UI** | REST שורף צבעים; אין partial update |
| **יצירת שדה lookup** | **UI** | אין `relatedObjectType` לנחש — היעד נקבע בבחירת האריח (G3) |
| בניית מסכים / טאבים / לייאאוט | **UI בלבד** | **אין API ללייאאוט.** שדה שנוצר ולא נגרר ידנית — **לא קיים לאף משתמש** (G7) |
| Make scenarios | **UI, מייק בלבד** | כלל פרויקט |
| שכפול חשבון | **ידני, בתשלום** | ⛔ לעולם לא דרך API |

**גוצ'ת UI נוספת (G1):** החיפוש ברשימת השדות ב-UI מחפש **רק prefix ורק לפי שם מערכת** — חיפוש "סטטוס" לא ימצא `pcfStatus`. חפש `pcf`.

> ⛔ **אוטומציות פנימיות של פיירברי — תקרה קשיחה של 50, וכמעט מוצתה.**
> בנוסף, טריגר **"בזמן יחסי"** (relative-time) **חסום ברישיון הנוכחי של רויטל.**
> **מסקנה:** כל תזמון מבוסס-זמן חייב לרוץ ב-Make או ב-n8n, לא באוטומציה פנימית של פיירברי.

---

## 9. ייבוא CSV — נתיב שאינו REST

1. **קידוד: UTF-8 עם BOM.** בלי BOM — עברית נשברת.
2. מסך **מיפוי עמודות** ידני בכל ייבוא.
3. **`ownerid` נקלט כ-lookup לפי שם תצוגה** (למשל "ויטרו ראשי") — זהו **חריג** לכלל "lookup = GUID בלבד", ותקף רק בייבוא CSV.

---

## 10. פרוטוקול הרצה ראשונה (חובה בחשבון חי)

1. **`DRY_RUN = true` כברירת מחדל.**
2. **תקרת `MAX_PER_RUN`** (25 ב-WF-16).
3. אם רשימת ה-DRY-RUN ארוכה מהתקרה — **לא הופכים ל-false** עד הכרעת מייק.
4. **בדיקת שלילה חובה:** לקוחה שכן קנתה — לא מופיעה בדוח.
5. ⛔ **אסור להדליק תרחיש כלשהו לפני בדיקת תוכן התור שלו.** תקדים: **525 אירועים תקועים** בתורי Make (471 מהם מהעולם הישן) — הדלקה עיוורת תשפוך מאות רשומות למערכת.

---

## 11. מה לא עושים דרך API בכלל

- **בניית מסכים / טאבים** — אין API לפריסת מסך. ידני ב-UI על ידי מייק. (כל "קבוצה" בקובץ = טאב; מבנה אופקי; רק "במסך?=כן" מוצג; "אדמין בלבד"=טאב נסתר.)
- **Make scenarios** — ידני, מייק בלבד. קלוד לעולם לא כותב ל-Make דרך API.
- **שכפול חשבון פיירברי** — פעולה בתשלום וידנית.
- **מחיקות** — לא ע"י סוכן אוטונומי, בשום מצב.

---

## 12. תיעוד וסטטוס פרויקט

- **source of truth לשדות:** `roital-yaish/roital_objects_fields.xlsx` (14 גיליונות; 16 עמודות; עמודה A של מייק לא נוגעים; אנחנו מסמנים N+P).
- **ground truth מבני:** `template_snapshot.json` — ⚠️ עם הסתייגות ה-lookups (ראה אזהרה בראש המסמך).
- **change-log:** שורה לכל שדה — `[<אובייקט>] <system_name> | <פעולה> | <תוצאה>`. עברית, לאליאל.
- **סטטוס לקוחות (Account):** **סגור** בכל מה שבטוח/אוטומטי — 6 רילייבלים ✅ אומתו; ערכי 3 ה-picklists אומתו תקינים (לא נדרש שינוי). נשאר ידני ב-UI: default ל-`billingcountry` (נשלח, לא אומת) + 3 defaults של picklist + בניית טאבים.
- **פתוח לתיעוד:** 130 שדות חי מול 129 אפיון ב-Account — `numberofemployees` ("לא בשימוש") חסר באפיון, להוסיף. שאלות לאליאל: Q3 (`ownerid` + Make), Q6 (`idnumber`→"ת.ז"), Q4 (`pcfwhatsapptemplate`), Q5 (`lastactiondate`), Q7 (`pcfType`).
- **מצב החשבון החי:** נכון ל-26/07/2026 — **clean state תפעולי**, למעט שני שינויים מ-22–23/07 (עדכון `pcfPowerlinkToken` ב-1036, ותיקון קריטריון חיפוש ב-Make). כל מי שממשיך מתחיל משם.

---

## 13. סתירות פתוחות שהמסמך הזה לא מכריע

| # | הסתירה | בעלים | היכן מתועד במלואו |
|---|---|---|---|
| 1 | `/api/query` מול `/api/v3/query` — איזה נתיב תקף לאיזה שימוש | **מייק** | פרק 2א |
| 2 | מגבלות קצב — 100/דקה מול צינון 350 קריאות מטא-דאטה | **מייק** | פרק 2ב |
| 3 | גורל אובייקט 1005 — שלוש עמדות סותרות | **מייק** | `09_OPEN_QUESTIONS.md` |
| 4 | `pcfGroupid` מול `pcfGreenApiId` ב-1068 | **מייק** | `05_GAP_CHECKLIST.md` |

**כלל:** אין להכריע אף אחת מהן בשקט בתוך מפרט או workflow. מי שנתקל — עוצר, מסמן, ומעביר למייק.
