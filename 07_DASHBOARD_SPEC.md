# 07 · מפרט לוחות בקרה

> **עודכן 03/08/2026 — סקירת 26 שיחות הפרויקט.**

> **החלטה:** הדשבורדים נבנים **מחוץ ל-Fireberry** — אפליקציית web שמושכת מה-API.
> *"בפיירברי צריך להגדיר כל מדד ומדד — זה לוקח יום שלם, וגם אז שינוי פרמטר אחד זה עוד יום."*
> **למדל לפי פרויקט הדשבורד הקיים של ויטרו** — לקחת משם את המנגנון והלוגיקה.

> **פלטפורמה:** **אתר הלקוחה — לא Fireberry.** הוצג ללקוחה כשלב 3 (~שבועיים), *"מתקדם בהרבה · מתעדכן לבד"*.
> **מסחרי:** **אפסייל נפרד, מתומחר בנפרד מהפרויקט.**
> **מעקב ביצוע:** ClickUp **שלב 15 · לוחות בקרה**, 7 משימות.

---

## 🔴 חוסמים ותלויות — לפני כל בנייה

| # | חוסם | מצב | בעלים |
|---|---|---|---|
| 1 | **גישת SFTP לדומיין של הלקוחה** | 🔴 **לא זז.** בלי SFTP אין דיפלוי לדומיין שלהם | **עודד** |
| 2 | **אין דאטה אמיתית במערכת** | 🔴 0 הזמנות · 0 תשלומים · 0 אירועי וובינר (`1030`/`1032` ריקים) · 2 רשומות שקילה בלבד. **אי אפשר לאמת שום דשבורד מול מספרים חיים עד שהסליקה תעלה לאוויר** | מייק |
| 3 | אין UTM-ים במקורות הפרסום | 🟠 דשבורדי המקור יישארו ריקים עד שהפרסום ייתייג ב-UTM. העבודה בצד הפרסום (משימה 5.5), **לא ב-CRM** | רויטל / שיווק |
| 4 | תמחור האפסייל | ⚠️ **סתירה פתוחה** — ראה למטה | מייק / פנימי |

> ⚠️ **סתירה פתוחה — תמחור האפסייל.**
> **עמדה א':** הדשבורדים הוצגו ללקוחה בתמחור **18,000 ₪** כשלב 3 (מקור: chat_08, 27–28/07/2026).
> **עמדה ב':** תמחור האפסייל נרשם כ**החלטה פתוחה, בעלות פנימית** (מקור: chat_22, 26/07/2026).
> **בעלים: מייק.** עד להכרעה — לא לצטט מספר ללקוחה.

**כלל בדיקות מחייב:** *"לא נעשה טסטים על הדומיין שלהם."* כל בדיקה רצה קודם על **`a2.co.il`**; מעבר לדומיין הלקוחה רק אחרי אישור ורק דרך ה-SFTP של עודד.

---

## דרישות מערכת

| דרישה | פירוט |
|---|---|
| מקור נתונים | Fireberry REST API, **סנכרון אוטומטי יומי** |
| רענון ידני | **כפתור סנכרון מיידי** שמושך הכול עכשיו |
| אימות | **סיסמה גלובלית אחת.** לא לפי משתמשים, אין הרשאות פר-משתמש |
| אירוח | קודם `a2.co.il` לבדיקות → אחר כך הדומיין שלהם דרך **SFTP מעודד** 🔴 חסום |
| בדיקות | *"לא נעשה טסטים על הדומיין שלהם"* |
| מסחרי | **אפסייל נפרד.** לתמחר בנפרד מהפרויקט |

> 🔐 **אבטחה:** טוקן ה-API של Fireberry ⚠️ **נחשף בצ'אט — לרוטציה.** אפליקציית הדשבורד חייבת להחזיק את הטוקן בצד השרת בלבד, לעולם לא ב-JS של הדפדפן. אחרי הרוטציה יש לעדכן גם את הקונפיג של הדשבורד.

---

## דשבורד 1 · מכירות

| מדד | חישוב | חיתוכים |
|---|---|---|
| כמות מכירות | `COUNT(Opportunity)` where `statuscode = עסקה נסגרה` | בוסט / מסלולים · נציג · מקור · מוצר · יום/שבוע/חודש |
| סכום מכירות | `SUM(pcfincomingcashflow)` על עסקאות סגורות | אותם חיתוכים |
| שווי עסקה ממוצע | `סכום ÷ כמות` | אותם חיתוכים |
| יחס המרה ליד→מכירה | `עסקאות סגורות ÷ סה"כ לידים` בטווח | **נציג · מקור/קמפיין · מוצר** |
| המרת בוסט→מסלול | `רכשו מסלול ÷ רכשו בוסט` | מחזור · מקור |
| **לא המירו אחרי בוסט** | `COUNT(1051)` where `pcfStatus = 8` ("סיום בוסט — לא המירה") | מחזור · מקור · מנטורית |
| **פאנל המרה בוסט→ליווי** | נרשמו לבוסט → סיימו בוסט → `pcfStatus = 8` מול רכשו מסלול | מחזור |

**שלושת חיתוכי החובה:** נציג המכירות · הקמפיין/מקור ההגעה · המוצר.
**מקורות:** `4` Opportunity · `14` Product · `1002` רישום שיווקי · `13` הזמנה · `1051` תהליך ליווי
⚠️ **חוסם:** 0 הזמנות ו-0 תשלומים במערכת. הדשבורד יהיה ריק עד שהסליקה תזרום.
> 💡 `1051.pcfStatus = 8` היא **הקבוצה הגדולה במערכת** — 50–120 נשים בשבוע — ואף מדד לא נשען עליה היום. מקור: chat_23.

---

## דשבורד 2 · שיווק

| מדד | חישוב | חיתוכים |
|---|---|---|
| כמות לידים | `COUNT(1002)` where `pcfMarketingRecordType = ליד חדש` | יום · שבוע · חודש |
| לידים לפי מקור | `COUNT` GROUP BY UTM Source | פילוח עוגה |
| **הקמפיין המוצלח ביותר לפי הכנסות** | `SUM(מחזור) GROUP BY UTM Campaign` ORDER DESC | טווח תאריכים |
| ביצועים לפי מקור | הכנסות ÷ לידים לכל מקור | — |
| לידים לפי סטטוס טיפול | `COUNT(Opportunity) GROUP BY statuscode` | תמונת עומס למכירות |
| **CAC לפי קמפיין** | `הוצאה יומית ÷ לידים` ו-`הוצאה ÷ מכירות`, GROUP BY קמפיין | טווח תאריכים |
| **הרשמות לבוסט** | `COUNT(1005)` — נפח צפוי 50–120 לשבוע | ❓ **מותנה בהכרעת גורל 1005** (ראה סתירה חוצת-מסמכים) |

**מקורות:** `1002` · `1043` UTM Source · `67` Campaign · `1003` AdSet · `1028` Ad · `1048` נתוני קמפיין יומיים
⚠️ **חוסם:** אין UTM-ים במקורות של הלקוחה. צריך להוסיף לפני שהמדדים ישמעו משהו. **זה לא באג ולא בעיית CRM** — זו עבודה בצד הפרסום.

> ⚠️ **מלכודת דיווח — קריטית.** יש **שני שדות UTM Source**: `pcfUtmSource` (picklist, מסומן "לא בשימוש") ו-`pcfUTMSsource` (lookup→`1043`). כתיבה לשדה הראשון מכניסה את הנתון למקום ש**אף דשבורד לא קורא — בלי שגיאה.** כל workflow שכותב UTM חייב לכתוב ל-`pcfUTMSsource`. מקור: chat_22.

> 📊 **CAC:** אובייקט `1048` (נתוני קמפיין יומיים) **כבר קיים**. חסר בו **רק נתון ההוצאה היומית** כדי להפיק CAC לפי קמפיין. זו הדרישה היחידה החסרה. מקור: chat_26.

---

## דשבורד 3 · וובינר

> 🔴 **תלות מוחלטת:** הדשבורד תלוי ב-workflows A ו-B. אובייקטים **`1030` ו-`1032` ריקים לחלוטין (0 רשומות)** — בלי רשומות אין לדשבורד שום דאטה. מקור: chat_17.

| מדד | חישוב | חיתוכים |
|---|---|---|
| כמה נרשמו | `COUNT(1030)` per `1032` | כללי + לכל וובינר |
| כמה הגיעו | `COUNT` where נכחה = true | כללי + לכל וובינר |
| **ממוצע נוכחות** | `הגיעו ÷ נרשמו` | כללי + לכל וובינר |
| משך נוכחות ממוצע | `AVG(משך)` | 🔬 תלוי בנתוני זום |
| 🚩 **בריאות מחזורים** | ראה למטה | דגל חריגה |

**ווידג'ט "בריאות מחזורים" (workflow C · 6.7):**

| רמה | תנאי |
|---|---|
| 🔴 | מחזור שנפתח בעוד **יומיים** עם `pcfEventLink` ריק |
| 🔴 | מחזור שנפתח בעוד יומיים בסטטוס שאינו "פתוח להרשמה" |
| 🔴 | מחזור שנפתח בעוד יומיים **בלי מוצר מקושר** |
| 🟠 | **אין אף מחזור שנפתח ב-14 הימים הקרובים** (בדיקת רוחב) |

**מקורות:** `1032` אירוע · `1030` הרשמה לאירוע · `1007` מחזור בוסט
**פילטרים:** טווח תאריכים ידני · וובינר ספציפי
⚠️ **חוסם:** `1032` ו-`1030` **ריקים לחלוטין (0 רשומות)**. צריך WF-10 שייצר רשומות.
🔬 נתוני נוכחות תלויים במחקר זום — אופציונלי.

---

## דשבורד 4 · ליווי

| מדד | חישוב | חיתוכים |
|---|---|---|
| לקוחות בליווי כרגע | `COUNT(1051)` where `pcfStatus = 2` | **מנטורית · מוצר · תוכנית נוכחית** |
| לפי מנטורית | GROUP BY `ownerid` | איזון עומסים |
| לפי מוצר | GROUP BY `pcfProduct` | |
| **לקראת סיום (אפסייל)** | `pcfEstimatedEndDate` בין today ל-today+14, **מסנן `2099-12-31`** | מנטורית |
| 🚩 **צריכות תפריט וטרם קיבלו** | ליווי פעיל · `today − תאריך רכישה > 2 ימים` · אין `1059.pcfSentDate` | **סטטוס חריג** |
| סה"כ ליוויים אי-פעם | `COUNT(1051)` ללא פילטר | *"יכול להיות 1000 ליוויים אבל 100 פעילים"* |
| **% המשך לריטיינר** | `סטטוס המשך = בחרה להמשיך ÷ סה"כ שהגיעו לסיום` | **מנטורית × מוצר** |
| לקוחות לפי סטטוס | GROUP BY `pcfStatus` (8 ערכים) | פילוח עוגה |
| שביעות רצון | GROUP BY `pcfSatisfication` (1–4) | |
| **תפריטים × לקוחות** | `COUNT(1059) GROUP BY pcfMenu` | + מדדי הצלחה לפי תפריט |
| **תפריטים מותאמים של התזונאית** | `COUNT(1059)` where `pcfMenuType = 2` (מותאם) + `pcfDietitian` | **פר דיאטנית · פר חודש** |
| **תפריטים שנכשלו בשליחה** | מתוך הודעת סיכום השגיאות של WF-06B | לפי קטגוריית כשל |
| 🚩 **לקוחה בסיכון** | ראה למטה | דגל חריגה |

**קטגוריות כשל לשליחת תפריט (מ-WF-06B):** `7א-לא-קיימת` · `7א-מחוץ-לחלון` · `7ב` · `7ג` · `8`. מקור: chat_13.

### 🚩 חלון "לקוחה בסיכון"

**מה זה:** זיהוי אוטומטי של לקוחה ש**לא ירדה 3 שבועות ברצף או עלתה במשקל**, והקפצת התראה למנטורית — **לפני** שהיא נוטשת.

| פרמטר | ערך |
|---|---|
| מקור נתונים | `1069` טופס שקילה (סדרת משקל שבועית) |
| תנאי א' | `שינוי משקל ≥ 0` בשלוש שקילות שבועיות רצופות |
| תנאי ב' | `משקל נוכחי > משקל השקילה הקודמת` (עלייה) — התראה מיידית |
| פלט | דגל בדשבורד + התראה למנטורית המשויכת |
| עלות | כמעט אפס — הדאטה כבר נאספת |
| ערך | ישיר על שיעור הנטישה, **המדד הכואב ביותר בעסק** |

⚠️ **תלוי בהכרעת יום השקילה** (ראה סתירה פתוחה למטה) — בלי יום קבוע אין "3 שבועות ברצף" מוגדר.

**מקורות:** `1051` · `1068` · `1059` · `1060` · `14` · `1069`
> תובנת שחר: *"אני יכול לראות במוצר מסוים מעריכים יותר, או מנטורית מסוימת מעריכה יותר, או מנטורית מסוימת במוצר מסוים."*

> 📌 **ClickUp:** בלוק התפריטים (6 משימות) נותב לשלב 12 · תפריטים, **ושני מדדי התפריטים נותבו לשלב 15 · לוחות בקרה.** המקור (chat_02, 03/08/2026) **לא נוקב בשמותיהם**; המועמדים לפי התיעוד: **"תפריטים מותאמים של התזונאית"** ו-**"תפריטים שנכשלו בשליחה"**. ❓ לאמת מול ClickUp לפני שמתחילים לבנות.

---

## לוח בקרה · תשלום לדיאטניות

> מקור: chat_07. הבסיס הוא אובייקט **`1059` הקצאת תפריט**. התשלום עצמו **מחוץ ל-CRM** — ה-CRM רק סופר ומדווח.

### תצוגות

| תצוגה | מסנן |
|---|---|
| **ממתין לתשלום** | `pcfDietitianPaid = 1` |
| **שולם החודש** | `pcfDietitianPaidDate` בתוך החודש הנוכחי |

### ווידג'טים

| ווידג'ט | תיאור |
|---|---|
| סה"כ ממתין | סכום כל מה שטרם שולם |
| תשלום פר דיאטנית | פילוח לפי `pcfDietitian` |
| יחס שולם / ממתין | תמונת מצב תזרימית |
| כמות תפריטים החודש | נפח עבודה |

### נוסחאות פר דיאטנית

| מדד | נוסחה |
|---|---|
| כמות תפריטים אישיים | `COUNT` רשומות עם `pcfMenuType = 2` |
| כמה שולם | `SUM(pcfDietitianFee)` WHERE `pcfDietitianPaid = 2` |
| **כמה חייבים עכשיו** | `SUM(pcfDietitianFee)` WHERE `pcfDietitianPaid = 1` ← **המספר המרכזי** |

### ❓ תלויות היתכנות — לבדוק לפני עיצוב התצוגות

1. **האם תצוגת Fireberry נותנת סכום פר-קבוצה בכותרת הקבוצה?** אם כן — לא צריך לוח בקרה נפרד לזה.
2. **האם `roll-up` על כרטיס הדיאטנית (איש קשר) אפשרי?** יש בפרויקט היסטוריה של מגבלות roll-up ונוסחאות ב-Fireberry.
3. **מתי תפריט "נספר" לתשלום** — ביצירה ("בהכנה") או רק בשליחה? **המלצת Claude: רק בשליחה.** (פריט B37 ב-`09_OPEN_QUESTIONS.md`, בעלים: מייק)
4. **`pcfDietitian` — Contact (2) או CrmUser (9)?** ההכרעה טרם ניתנה ומשפיעה על מבנה הדוחות. (B48, בעלים: מייק)

---

## דשבורד 5 · עדכוני סטטוס / תוצאות

**הדשבורד החשוב ביותר עסקית.**

| מדד | חישוב |
|---|---|
| **מגמת ירידה לפי שבוע** | ממוצע `שינוי ממשקל התחלה` GROUP BY מספר שבוע (1, 2, 3, …) |
| ירידה ממוצעת כוללת | `AVG(pcfInitialWeight − pcfCurrentWeight)` |
| הצלחה לפי מסלול | אותו חישוב GROUP BY מוצר |
| הצלחה לפי מנטורית | GROUP BY `ownerid` |
| הצלחה לפי תפריט | GROUP BY `1059.pcfMenu` |
| **דריל-דאון ללקוחה בודדת** | עקומת המשקל של לקוחה אחת: התחלה → שבוע 1 → 2 → 3 … |
| 🚩 **לקוחה בסיכון** | לא ירדה 3 שבועות ברצף **או עלתה** → התראה למנטורית (ראה דשבורד 4) |

**פילטרים:** פעילות / לא פעילות · סוג ליווי · מנטורית · מוצר · תפריט · טווח תאריכים
**ויזואל:** גרף. *"אני רוצה לראות משהו גרפי ויזואלי בעיניים."*

> ⚠️ **סתירה פתוחה — יום השקילה השבועי.** הסדרה השבועית של הדשבורד הזה נשענת עליו ישירות.
> **עמדה א':** **ראשון 09:00** — האפיון המקורי, משימה 13.1 ב-ClickUp, והתרחיש כפי שרץ בפועל (chat_10).
> **עמדה ב':** **חמישי** — המצגת שהוצגה ללקוחה וציר הזמן של סער וינברג, שבו כל 6 השקילות בימי חמישי (chat_08, 27–28/07/2026).
> **בעלים: רויטל / מייק.** משפיע במקביל על `06_N8N_WORKFLOW_SPECS`, `04_LIVE_MAKE`, המצגת, תוכנית דאטת ההדגמה — **ועל הגדרת "3 שבועות ברצף" בחלון "לקוחה בסיכון".**

> **התובנה העסקית שמצדיקה את הדשבורד:**
> *"אני יכול לראות אם יש קפיצת מדרגה בשבוע השביעי — יכול להיות שזה השלב שבו הגוף מגיב."*

> **הדריל-דאון:**
> *"אם אני מנטורית ובא לי להסתכל על ירדן שכרגע בליווי — במקום ממוצע כללי, אני מסתכל עליה: משקל התחלה, שבוע ראשון, שני, שלישי — ואני יכול לדבר אליה ולהתייעץ עליה."*

**מקורות:** `1069` טופס שקילה · `1051` · `1059`
⚠️ **חוסם:** 2 רשומות שקילה בלבד (דאטת בדיקה).

---

## מיפוי מדד → מקור נתונים

| מקור | type | משמש בדשבורדים |
|---|---|---|
| Opportunity | 4 | 1, 2 |
| רישום שיווקי | 1002 | 2 |
| UTM (Source/Campaign/AdSet/Ad) | 1043/67/1003/1028 | 1, 2 |
| נתוני קמפיין יומיים | 1048 | 2 (CAC) |
| הזמנה · תשלום · תיעוד תשלום | 13/1039/1018 | 1 |
| אירוע · הרשמה לאירוע | 1032/1030 | 3 |
| מחזור בוסט | 1007 | 1, 3 |
| הרשמה לבוסט ❓ | 1005 | 2 — **מותנה בהכרעת גורל 1005** |
| תהליך ליווי | 1051 | 1, 4, 5 |
| טופס שקילה | 1069 | 4 (לקוחה בסיכון), 5 |
| קטלוג/הקצאת תפריט | 1060/1059 | 4, 5 + תשלום לדיאטניות |
| קבוצת WhatsApp | 1068 | 4 |
| מוצר | 14 | 1, 4, 5 |

---

## מה שהאפיון המקורי ביקש — ומה קרה לזה

האפיון (עמ' 31–32) הגדיר **18 מדדים ב-3 לוחות** בתוך Fireberry. **הוחלף** ב-5 הדשבורדים החיצוניים לעיל. המיפוי:

| מדד באפיון המקורי | מה קרה |
|---|---|
| כמות לידים · לידים לפי מקור · המרה לפי מקור · לידים לפי סטטוס | → דשבורד 2 |
| מכירות לפי מוצר · הכנסות חודשיות · שווי עסקה · תהליכים פתוחים · אחוזי ביטולים · מכירות לפי מקור | → דשבורד 1 |
| לקוחות לפי תוכנית · לפי מנטורית · לפי סטטוס · סיום החודש · אחוז השלמה · שיעור נטישה · שביעות רצון | → דשבורד 4 |
| שינוי משקל מדווח ממוצע | → דשבורד 5 (**הורחב משמעותית**) |

**מדדים חדשים שלא היו באפיון:** % המשך לריטיינר · צריכות תפריט וטרם קיבלו · תפריטים × לקוחות · הצלחה לפי תפריט · מגמת ירידה לפי שבוע · דריל-דאון ללקוחה · כל דשבורד הוובינר · **לקוחה בסיכון** · **תפריטים מותאמים של התזונאית** · **תפריטים שנכשלו בשליחה** · **בריאות מחזורים** · **CAC לפי קמפיין** · **לא המירו אחרי בוסט (`pcfStatus = 8`)** · **לוח תשלום לדיאטניות**.

---

## יומן שינויים — 03/08/2026

| # | מה שונה | מקור |
|---|---|---|
| 1 | נוסף בראש המסמך: פלטפורמה = אתר הלקוחה (לא Fireberry) · אפסייל בתמחור נפרד · הפניה ל-ClickUp שלב 15 (7 משימות) | chat_08 · chat_22 |
| 2 | נוסף פרק "חוסמים ותלויות" — SFTP מעודד 🔴 לא זז, ואין דאטה אמיתית לאימות | chat_08 · chat_17 · סקירת 03/08 |
| 3 | סומנה `⚠️ סתירה פתוחה` על תמחור האפסייל: 18,000 ₪ שהוצגו ללקוחה מול "החלטה פתוחה" | chat_08 מול chat_22 |
| 4 | הודגש כלל הבדיקות: `a2.co.il` קודם, לעולם לא על הדומיין של הלקוחה | מפרט קיים + chat_08 |
| 5 | הודגשו סיסמה גלובלית אחת · סנכרון יומי אוטומטי · כפתור סנכרון מיידי | מפרט קיים |
| 6 | נוסף מדד **לקוחה בסיכון** (3 שבועות ללא ירידה או עלייה במשקל) לדשבורדים 4 ו-5 | `09_OPEN_QUESTIONS.md` חלק ד' |
| 7 | הודגש דגל החריגה **"צריכות תפריט וטרם קיבלו"** — מעל יומיים מהרכישה בלי `1059.pcfSentDate` | מפרט קיים |
| 8 | פורט מדד **תפריטים מותאמים של התזונאית** — `pcfMenuType = 2` פר דיאטנית ופר חודש | chat_07 |
| 9 | נוספה הפניה לשני מדדי התפריטים שנותבו לשלב 15 ב-ClickUp (שמותיהם לא נקובים במקור — ❓ לאמת) | chat_02 |
| 10 | סומנה `⚠️ סתירה פתוחה` על יום השקילה (ראשון 09:00 מול חמישי) — משפיע על הסדרה השבועית ועל "3 שבועות ברצף" | chat_08 + chat_10 |
| 11 | נוסף פרק שלם **לוח בקרה · תשלום לדיאטניות** — 2 תצוגות, 4 ווידג'טים, 3 נוסחאות, 4 תלויות היתכנות | chat_07 |
| 12 | נוספה שורת תלות מוחלטת בראש דשבורד הוובינר (`1030`/`1032` ריקים) | chat_17 |
| 13 | נוסף ווידג'ט **בריאות מחזורים** עם 3 תנאי 🔴 ובדיקת רוחב 🟠 של 14 יום | chat_17 |
| 14 | נוסף מדד **תפריטים שנכשלו בשליחה** עם 5 קטגוריות הכשל של WF-06B | chat_13 |
| 15 | נוספה אזהרת **מלכודת הדיווח** `pcfUtmSource` מול `pcfUTMSsource` | chat_22 |
| 16 | הובהר ששדות ה-UTM ריקים **לא בגלל באג** — אין תיוג במקורות הפרסום | chat_22 |
| 17 | נוסף מדד **CAC לפי קמפיין** עם מקור `1048` והדרישה החסרה היחידה (הוצאה יומית) | chat_26 |
| 18 | נוסף `1005` כמקור נתונים פוטנציאלי להרשמות בוסט — ❓ **מותנה בהכרעת גורל 1005** | chat_20 |
| 19 | נוספו מדד **"לא המירו אחרי בוסט"** (`1051.pcfStatus = 8`) ופאנל המרה בוסט→ליווי | chat_23 |
| 20 | נוספה הערת אבטחה: הטוקן בצד שרת בלבד; ⚠️ **נחשף בצ'אט — לרוטציה**, ולעדכן את קונפיג הדשבורד אחריה | סקירת 03/08 |
